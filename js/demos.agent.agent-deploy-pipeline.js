/* INTRX registry - deterministic agent deploy pipeline */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
  id:'agent-deploy-pipeline',
  title:'Agent Deploy Pipeline',
  cat:'Agent & AI UI',
  rootClass:'d-agent-agent-deploy-pipeline',
  tags:['pipeline','logs','status'],
  libs:[],
  desc:'A deterministic four-stage deployment rail streams bounded logs, exercises an exact one-in-three TEST failure and automatically retries before production.',
  seen:"Seen on: Jeet Nirnejak's Deploy Pipeline component",
  hint:'press, Enter or Space to pause; → steps; End inspects retry; Home or R resets',
  html:`<div class="d-agent-agent-deploy-pipeline" role="region" aria-label="Agent deployment pipeline">
  <div class="d-agent-agent-deploy-pipeline-head" aria-hidden="true"><span>AGENT / DEPLOY</span><span class="d-agent-agent-deploy-pipeline-live"><i></i> STREAMING</span></div>
  <div class="d-agent-agent-deploy-pipeline-stage" role="application" tabindex="0" aria-keyshortcuts="ArrowRight Enter Space End Home R" aria-label="Deployment pipeline. Press Enter or Space to pause or resume automatic deployment. Press Right Arrow to pause and advance one deterministic event. Press End to inspect the TEST failure and retry. Press Home or R to reset.">
    <div class="d-agent-agent-deploy-pipeline-stage-head"><span aria-hidden="true">DEPLOY / PIPELINE</span><span class="d-agent-agent-deploy-pipeline-head-state" aria-hidden="true"><b class="d-agent-agent-deploy-pipeline-retry">RETRY 1/1</b><i class="d-agent-agent-deploy-pipeline-stage-tone">BUILD</i></span></div>
    <div class="d-agent-agent-deploy-pipeline-rail" role="list" aria-label="Deployment stages">
      <div class="d-agent-agent-deploy-pipeline-step" role="listitem" data-key="BUILD"><div class="d-agent-agent-deploy-pipeline-node-shell"><span class="d-agent-agent-deploy-pipeline-ring" aria-hidden="true"></span><span class="d-agent-agent-deploy-pipeline-node" aria-hidden="true"><i class="d-agent-agent-deploy-pipeline-check"></i></span></div><span class="d-agent-agent-deploy-pipeline-label" aria-hidden="true">BUILD</span></div>
      <div class="d-agent-agent-deploy-pipeline-connector" aria-hidden="true"><i class="d-agent-agent-deploy-pipeline-connector-fill"></i></div>
      <div class="d-agent-agent-deploy-pipeline-step" role="listitem" data-key="TEST"><div class="d-agent-agent-deploy-pipeline-node-shell"><span class="d-agent-agent-deploy-pipeline-ring" aria-hidden="true"></span><span class="d-agent-agent-deploy-pipeline-node" aria-hidden="true"><i class="d-agent-agent-deploy-pipeline-check"></i></span></div><span class="d-agent-agent-deploy-pipeline-label" aria-hidden="true">TEST</span></div>
      <div class="d-agent-agent-deploy-pipeline-connector" aria-hidden="true"><i class="d-agent-agent-deploy-pipeline-connector-fill"></i></div>
      <div class="d-agent-agent-deploy-pipeline-step" role="listitem" data-key="PREVIEW"><div class="d-agent-agent-deploy-pipeline-node-shell"><span class="d-agent-agent-deploy-pipeline-ring" aria-hidden="true"></span><span class="d-agent-agent-deploy-pipeline-node" aria-hidden="true"><i class="d-agent-agent-deploy-pipeline-check"></i></span></div><span class="d-agent-agent-deploy-pipeline-label" aria-hidden="true">PREVIEW</span></div>
      <div class="d-agent-agent-deploy-pipeline-connector" aria-hidden="true"><i class="d-agent-agent-deploy-pipeline-connector-fill"></i></div>
      <div class="d-agent-agent-deploy-pipeline-step" role="listitem" data-key="PROD"><div class="d-agent-agent-deploy-pipeline-node-shell"><span class="d-agent-agent-deploy-pipeline-ring" aria-hidden="true"></span><span class="d-agent-agent-deploy-pipeline-node" aria-hidden="true"><i class="d-agent-agent-deploy-pipeline-check"></i></span></div><span class="d-agent-agent-deploy-pipeline-label" aria-hidden="true">PROD</span></div>
    </div>
    <section class="d-agent-agent-deploy-pipeline-log" aria-label="Deployment log" aria-live="off">
      <div class="d-agent-agent-deploy-pipeline-log-head" aria-hidden="true"><span>EVENT LOG</span><span class="d-agent-agent-deploy-pipeline-run">RUN 01 / 06</span></div>
      <div class="d-agent-agent-deploy-pipeline-log-view"></div>
    </section>
  </div>
  <div class="d-agent-agent-deploy-pipeline-foot" aria-hidden="true"><span>6 RUN ORACLE</span><span>150–400MS LOGS</span></div>
  <span class="d-agent-agent-deploy-pipeline-status" aria-live="polite" aria-atomic="true">Run one build started</span>
</div>`,
  css:`
.d-agent-agent-deploy-pipeline{position:relative;width:100%;height:320px;box-sizing:border-box;overflow:hidden;contain:layout paint;container-type:inline-size;background:#0a0a0b;color:#ececef;font-family:'Roboto Mono','JetBrains Mono',ui-monospace,monospace;isolation:isolate;touch-action:pan-y}
.d-agent-agent-deploy-pipeline *{box-sizing:border-box}
.d-agent-agent-deploy-pipeline-head{position:absolute;z-index:2;top:15px;left:16px;right:16px;display:flex;align-items:center;justify-content:space-between;color:#5c5c66;font-size:10px;line-height:1;letter-spacing:.08em}
.d-agent-agent-deploy-pipeline-live{display:flex;align-items:center;gap:5px;color:#9b9ba3}
.d-agent-agent-deploy-pipeline-live i{width:5px;height:5px;border-radius:50%;background:#fa7319;box-shadow:0 0 7px rgba(250,115,25,.35)}
.d-agent-agent-deploy-pipeline-stage{position:absolute;top:39px;right:12px;bottom:25px;left:12px;min-width:0;padding:16px;border:1px solid #232327;border-radius:10px;overflow:hidden;background:#101012;outline:none;cursor:pointer;touch-action:pan-y}
.d-agent-agent-deploy-pipeline-stage:focus-visible{border-color:#fa7319;box-shadow:0 0 0 1px #fa7319}
.d-agent-agent-deploy-pipeline-stage-head{height:16px;display:flex;align-items:flex-start;justify-content:space-between;gap:8px;color:#9b9ba3;font-size:10px;line-height:1;letter-spacing:.08em;white-space:nowrap}
.d-agent-agent-deploy-pipeline-head-state{display:flex;align-items:center;justify-content:flex-end;gap:6px;min-width:0}
.d-agent-agent-deploy-pipeline-stage-tone{overflow:hidden;color:#fa7319;font-style:normal;text-overflow:ellipsis}
.d-agent-agent-deploy-pipeline-retry{display:none;height:16px;margin-top:-3px;padding:3px 6px 0;border:1px solid rgba(250,115,25,.44);border-radius:8px;color:#fa7319;background:rgba(250,115,25,.08);font-size:9px;line-height:9px;font-weight:500;letter-spacing:.05em}
.d-agent-agent-deploy-pipeline[data-retry="true"] .d-agent-agent-deploy-pipeline-retry{display:block}
.d-agent-agent-deploy-pipeline-rail{height:55px;margin-top:6px;display:grid;grid-template-columns:32px minmax(0,1fr) 32px minmax(0,1fr) 32px minmax(0,1fr) 32px;align-items:start}
.d-agent-agent-deploy-pipeline-step{height:55px;display:grid;grid-template-rows:32px 10px;row-gap:10px;justify-items:center;min-width:0}
.d-agent-agent-deploy-pipeline-node-shell{position:relative;width:32px;height:32px;transform:translateX(0) scale(1);transform-origin:50% 50%}
.d-agent-agent-deploy-pipeline-node{position:absolute;inset:0;display:grid;place-items:center;border:1px solid #2e2e34;border-radius:50%;background:#161619;color:#5c5c66;box-shadow:none}
.d-agent-agent-deploy-pipeline-node::before{content:'';width:5px;height:5px;border-radius:50%;background:currentColor}
.d-agent-agent-deploy-pipeline-ring{position:absolute;z-index:2;inset:0;display:none;border-radius:50%;background:conic-gradient(from 0deg,#fa7319 0 28%,rgba(250,115,25,.16) 28% 100%);-webkit-mask:radial-gradient(farthest-side,transparent calc(100% - 2px),#000 0);mask:radial-gradient(farthest-side,transparent calc(100% - 2px),#000 0);pointer-events:none;transform:rotate(0deg)}
.d-agent-agent-deploy-pipeline-step[data-state="active"] .d-agent-agent-deploy-pipeline-ring{display:block}
.d-agent-agent-deploy-pipeline-step[data-state="active"] .d-agent-agent-deploy-pipeline-node{border-color:rgba(250,115,25,.30);color:#fa7319;background:rgba(250,115,25,.06)}
.d-agent-agent-deploy-pipeline-step[data-state="success"] .d-agent-agent-deploy-pipeline-node{border-color:#b8b8b8;color:#b8b8b8;background:#101012}
.d-agent-agent-deploy-pipeline-step[data-state="error"] .d-agent-agent-deploy-pipeline-node{border-color:#ececef;border-style:dashed;color:#ececef;background:#161619}
.d-agent-agent-deploy-pipeline-step[data-state="error"] .d-agent-agent-deploy-pipeline-node::before,.d-agent-agent-deploy-pipeline-step[data-state="error"] .d-agent-agent-deploy-pipeline-node::after{content:'';position:absolute;width:10px;height:1px;border-radius:0;background:currentColor}
.d-agent-agent-deploy-pipeline-step[data-state="error"] .d-agent-agent-deploy-pipeline-node::before{transform:rotate(45deg)}
.d-agent-agent-deploy-pipeline-step[data-state="error"] .d-agent-agent-deploy-pipeline-node::after{transform:rotate(-45deg)}
.d-agent-agent-deploy-pipeline-check{display:none;width:9px;height:5px;border-left:1px solid currentColor;border-bottom:1px solid currentColor;font-style:normal;transform:translateY(-1px) rotate(-45deg)}
.d-agent-agent-deploy-pipeline-step[data-state="success"] .d-agent-agent-deploy-pipeline-node::before{display:none}
.d-agent-agent-deploy-pipeline-step[data-state="success"] .d-agent-agent-deploy-pipeline-check{display:block}
.d-agent-agent-deploy-pipeline-label{max-width:48px;color:#5c5c66;font-size:10px;line-height:10px;letter-spacing:.04em;white-space:nowrap;text-align:center}
.d-agent-agent-deploy-pipeline-step[data-state="active"] .d-agent-agent-deploy-pipeline-label{color:#ececef}
.d-agent-agent-deploy-pipeline-step[data-state="success"] .d-agent-agent-deploy-pipeline-label{color:#9b9ba3}
.d-agent-agent-deploy-pipeline-step[data-state="error"] .d-agent-agent-deploy-pipeline-label{color:#ececef}
.d-agent-agent-deploy-pipeline-connector{position:relative;height:2px;margin-top:15px;overflow:hidden;background:#232327}
.d-agent-agent-deploy-pipeline-connector-fill{position:absolute;inset:0;background:#fa7319;transform:scaleX(0);transform-origin:0 50%}
.d-agent-agent-deploy-pipeline-log{height:136px;margin-top:9px;overflow:hidden;border:1px solid #232327;border-radius:6px;background:#161619}
.d-agent-agent-deploy-pipeline-log-head{height:22px;padding:0 8px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #232327;color:#5c5c66;font-size:9px;line-height:1;letter-spacing:.08em}
.d-agent-agent-deploy-pipeline-run{color:#9b9ba3;font-variant-numeric:tabular-nums}
.d-agent-agent-deploy-pipeline-log-view{height:112px;overflow:hidden;padding:0 8px;scroll-behavior:auto}
.d-agent-agent-deploy-pipeline-log-row{height:14px;display:grid;grid-template-columns:56px minmax(0,1fr);column-gap:8px;align-items:center;min-width:0;color:#9b9ba3;font-size:10px;line-height:14px;white-space:nowrap;font-variant-numeric:tabular-nums}
.d-agent-agent-deploy-pipeline-log-time{overflow:hidden;color:#5c5c66;text-overflow:clip}
.d-agent-agent-deploy-pipeline-log-message{overflow:hidden;text-overflow:ellipsis}
.d-agent-agent-deploy-pipeline-log-row[data-tone="acc"] .d-agent-agent-deploy-pipeline-log-message{color:#fa7319}
.d-agent-agent-deploy-pipeline-log-row[data-tone="warn"] .d-agent-agent-deploy-pipeline-log-message{color:#b8b8b8}
.d-agent-agent-deploy-pipeline-log-row[data-tone="ok"] .d-agent-agent-deploy-pipeline-log-message{color:#b8b8b8}
.d-agent-agent-deploy-pipeline-log-row[data-tone="err"] .d-agent-agent-deploy-pipeline-log-message{color:#ececef}
.d-agent-agent-deploy-pipeline-foot{position:absolute;left:16px;right:16px;bottom:9px;display:flex;justify-content:space-between;color:#5c5c66;font-size:10px;line-height:1;letter-spacing:.08em}
.d-agent-agent-deploy-pipeline-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);clip-path:inset(50%);white-space:nowrap}
@container(max-width:340px){.d-agent-agent-deploy-pipeline-stage{right:10px;left:10px}.d-agent-agent-deploy-pipeline-head,.d-agent-agent-deploy-pipeline-foot{left:12px;right:12px}.d-agent-agent-deploy-pipeline-foot{font-size:9px}}
@media(prefers-reduced-motion:reduce){.d-agent-agent-deploy-pipeline *,.d-agent-agent-deploy-pipeline *::before,.d-agent-agent-deploy-pipeline *::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}
`,
  js:`const deployStage=root.querySelector('.d-agent-agent-deploy-pipeline-stage'),stageTone=root.querySelector('.d-agent-agent-deploy-pipeline-stage-tone'),runLabel=root.querySelector('.d-agent-agent-deploy-pipeline-run'),retryPill=root.querySelector('.d-agent-agent-deploy-pipeline-retry'),logView=root.querySelector('.d-agent-agent-deploy-pipeline-log-view'),statusElement=root.querySelector('.d-agent-agent-deploy-pipeline-status'),stepElements=[...root.querySelectorAll('.d-agent-agent-deploy-pipeline-step')],connectorElements=[...root.querySelectorAll('.d-agent-agent-deploy-pipeline-connector')],connectorFillElements=[...root.querySelectorAll('.d-agent-agent-deploy-pipeline-connector-fill')];
const seed=42,runCount=6,attemptKeys=['BUILD','TEST','TEST_RETRY','PREVIEW','PROD'],stageKeys=['BUILD','TEST','PREVIEW','PROD'],drawsPerAttempt=26,rawGapsPerAttempt=25,totalDraws=780,durationMinimum=2500,durationMaximum=4000,gapMinimum=150,gapMaximum=400,successPopDuration=300,failurePauseDuration=1500,shakeDuration=300,ringDuration=1200,productionHoldDuration=1500,logCap=8,maxDelta=50,clockBaseMs=(14*60*60+2*60+10)*1000;
const colors={bg0:'#0a0a0b',bg1:'#101012',bg2:'#161619',line0:'#232327',line1:'#2e2e34',txt0:'#ececef',txt1:'#9b9ba3',txt2:'#5c5c66',acc:'#fa7319',ok:'#b8b8b8',warn:'#b8b8b8',err:'#ececef'};
const expectedChecksums={schedule:'0c15af19',actual:'80e1e4c3',timeline:'82514750',failure:'57b93cf8',fixture:'0ebbec39',messages:'9f39f0cc',logs:'8a7a0db8',logFixture:'55c69243',pop:'628a3988',shake:'e9f9719c',glow:'474b3922',ring:'c3f81d63',motion:'12ce7312',extended:'26a36003'};
const messages={BUILD:['workspace graph resolved','lockfile verified','cache hit · dependencies','server entry compiled','client entry compiled','routes manifest emitted','assets fingerprinted','chunks minified','source maps written','server trace collected','output size 184 kB','artifact sealed','build manifest ready'],TEST:['vitest config loaded','18 suites collected','unit / parser','unit / scheduler','unit / registry','ui / keyboard','ui / reduced motion','ui / reconnect cleanup','a11y / roles','a11y / live regions','snapshot / 380px','snapshot / 320px','coverage 94.8%'],TEST_RETRY:['retry sandbox restored','18 suites recollected','unit / parser','unit / scheduler','unit / registry','ui / keyboard','ui / reduced motion','ui / reconnect cleanup','a11y / roles','a11y / live regions','snapshot / 380px','snapshot / 320px','coverage 94.8%'],PREVIEW:['edge sandbox allocated','runtime bundle uploaded','static assets uploaded','routes provisioned','environment injected','headers validated','cache warmed','health probe /','health probe /api','tls handshake verified','preview alias assigned','region iad1 ready','preview smoke passed'],PROD:['release policy validated','production bundle promoted','iad1 deployment started','sfo1 deployment started','fra1 deployment started','edge config synced','functions activated','traffic ramp 10%','traffic ramp 50%','traffic ramp 100%','health checks green','release alias switched','deployment stable']};
function clamp(value,minimum,maximum){return Math.max(minimum,Math.min(maximum,value))}
function checksumText(value){let hash=2166136261;for(const character of String(value))hash=Math.imul(hash^character.charCodeAt(0),16777619)>>>0;return hash.toString(16).padStart(8,'0')}
function mulberry32(nextSeed){return function(){let value=nextSeed+=0x6D2B79F5;value=Math.imul(value^(value>>>15),value|1);value^=value+Math.imul(value^(value>>>7),value|61);return((value^(value>>>14))>>>0)/4294967296}}
function cubicCoordinate(parameter,first,second){const inverse=1-parameter;return 3*inverse*inverse*parameter*first+3*inverse*parameter*parameter*second+parameter*parameter*parameter}
function cubicBezier(value,x1,y1,x2,y2){const bounded=clamp(value,0,1);if(bounded===0||bounded===1)return bounded;let low=0,high=1;for(let index=0;index<40;index++){const parameter=(low+high)/2;if(cubicCoordinate(parameter,x1,x2)<bounded)low=parameter;else high=parameter}return cubicCoordinate((low+high)/2,y1,y2)}
function popScale(milliseconds){const elapsed=clamp(milliseconds,0,successPopDuration);if(elapsed<=120)return 1+.25*cubicBezier(elapsed/120,.22,1,.36,1);return 1.25-.25*cubicBezier((elapsed-120)/180,.65,0,.35,1)}
function shakeOffset(milliseconds){if(milliseconds<=0||milliseconds>=shakeDuration)return 0;return 3*Math.sin(6*Math.PI*milliseconds/shakeDuration)}
function glowAlpha(milliseconds){return .18*Math.sin(Math.PI*clamp(milliseconds,0,productionHoldDuration)/productionHoldDuration)}
function ringAngle(milliseconds){return(clamp(milliseconds,0,Infinity)%ringDuration)*360/ringDuration}
function formatTimestamp(offset){let value=(clockBaseMs+offset)%86400000,hours=Math.floor(value/3600000);value-=hours*3600000;const minutes=Math.floor(value/60000);value-=minutes*60000;const seconds=Math.floor(value/1000),milliseconds=value-seconds*1000;return String(hours).padStart(2,'0')+':'+String(minutes).padStart(2,'0')+':'+String(seconds).padStart(2,'0')+'.'+String(milliseconds).padStart(3,'0')}
const random=mulberry32(seed),runs=[];for(let runIndex=0;runIndex<runCount;runIndex++){const attempts=[];for(let attemptIndex=0;attemptIndex<attemptKeys.length;attemptIndex++){const duration=durationMinimum+Math.floor(random()*(durationMaximum-durationMinimum+1)),rawGaps=[];for(let gapIndex=0;gapIndex<rawGapsPerAttempt;gapIndex++)rawGaps.push(gapMinimum+Math.floor(random()*(gapMaximum-gapMinimum+1)));const actualGaps=[];let total=0,rawCursor=0;while(total<duration){const remaining=duration-total;if(remaining<=gapMaximum){actualGaps.push(remaining);total+=remaining}else{const next=Math.min(rawGaps[rawCursor++],remaining-gapMinimum);actualGaps.push(next);total+=next}}attempts.push({run:runIndex,attemptIndex:attemptIndex,key:attemptKeys[attemptIndex],duration:duration,rawGaps:rawGaps,actualGaps:actualGaps,usedRawCount:rawCursor,lineCount:actualGaps.length})}runs.push({run:runIndex,fail:runIndex%3===2,attempts:attempts})}
const timelines=runs.map(function(run){const attempts=run.attempts,buildStart=0,buildDone=attempts[0].duration,testStart=buildDone+successPopDuration,testDone=testStart+attempts[1].duration,retryStart=run.fail?testDone+failurePauseDuration:null,retryDone=run.fail?retryStart+attempts[2].duration:null,previewStart=run.fail?retryDone+successPopDuration:testDone+successPopDuration,previewDone=previewStart+attempts[3].duration,prodStart=previewDone+successPopDuration,prodDone=prodStart+attempts[4].duration,holdEnd=prodDone+productionHoldDuration;return{run:run.run,fail:run.fail,runDuration:holdEnd,buildStart:buildStart,buildDone:buildDone,testStart:testStart,testDone:testDone,retryStart:retryStart,retryDone:retryDone,previewStart:previewStart,previewDone:previewDone,prodStart:prodStart,prodDone:prodDone,holdEnd:holdEnd}});
const canonicalLogs=[],logsByRun=[];runs.forEach(function(run){let offset=0,sequence=0;const runLogs=[],order=run.fail?[0,1,2,3,4]:[0,1,3,4];order.forEach(function(attemptIndex){const attempt=run.attempts[attemptIndex],startOffset=offset,startEntry={run:run.run,sequence:sequence++,attempt:attempt.key,kind:'start',tone:attemptIndex===2?'warn':'acc',offset:startOffset,timestamp:formatTimestamp(run.run*60000+startOffset),message:attemptIndex===2?'RETRY 1/1 · TEST':attempt.key+' / start',gapIndex:-1};runLogs.push(startEntry);canonicalLogs.push(startEntry);let gapTotal=0;attempt.actualGaps.forEach(function(gap,gapIndex){gapTotal+=gap;const final=gapIndex===attempt.actualGaps.length-1,failure=final&&run.fail&&attemptIndex===1,entry={run:run.run,sequence:sequence++,attempt:attempt.key,kind:final?(failure?'failure':'success'):'stream',tone:final?(failure?'err':'ok'):'txt1',offset:startOffset+gapTotal,timestamp:formatTimestamp(run.run*60000+startOffset+gapTotal),message:final?(failure?String.fromCharCode(10005)+' assertion failed · auth redirects to /login':String.fromCharCode(10003)+' done in '+(attempt.duration/1000).toFixed(1)+'s'):messages[attempt.key][gapIndex],gapIndex:gapIndex};runLogs.push(entry);canonicalLogs.push(entry)});if(attemptIndex===1&&run.fail)offset+=attempt.duration+failurePauseDuration;else if(attemptIndex===4)offset+=attempt.duration+productionHoldDuration;else offset+=attempt.duration+successPopDuration});logsByRun.push(runLogs)});
const scheduleSource=runs.flatMap(function(run){return run.attempts}).map(function(attempt){return[attempt.run,attempt.attemptIndex,attempt.key,attempt.duration,attempt.usedRawCount,attempt.lineCount,attempt.rawGaps.join('.')].join(',')}).join('|'),actualSource=runs.flatMap(function(run){return run.attempts}).map(function(attempt){return[attempt.run,attempt.attemptIndex,attempt.key,attempt.duration,attempt.actualGaps.join('.')].join(',')}).join('|'),timelineSource=timelines.map(function(item){return[item.run,item.fail?1:0,item.runDuration,item.buildStart,item.buildDone,item.testStart,item.testDone,item.retryStart===null?'-':item.retryStart,item.retryDone===null?'-':item.retryDone,item.previewStart,item.previewDone,item.prodStart,item.prodDone,item.holdEnd].join(',')}).join('|'),failureSource=runs.map(function(run){return run.run+','+(run.fail?1:0)}).join('|'),messageSource=Object.keys(messages).map(function(key){return key+','+messages[key].join('~')}).join('|'),logSource=canonicalLogs.map(function(entry){return[entry.run,entry.sequence,entry.attempt,entry.kind,entry.tone,entry.offset,entry.timestamp,entry.message].join(',')}).join('|');
const popAnchors=[0,30,60,90,120,165,210,255,300].map(function(ms){return{ms:ms,value:popScale(ms)}}),shakeAnchors=[0,25,75,125,175,225,275,300].map(function(ms){return{ms:ms,value:shakeOffset(ms)}}),glowAnchors=[0,375,750,1125,1500].map(function(ms){return{ms:ms,value:glowAlpha(ms)}}),ringAnchors=[0,300,600,900,1200].map(function(ms){return{ms:ms,value:ringAngle(ms)}});function motionSource(items){return items.map(function(item){return item.ms+','+item.value.toFixed(12)}).join('|')}
const actualChecksums={schedule:checksumText(scheduleSource),actual:checksumText(actualSource),timeline:checksumText(timelineSource),failure:checksumText(failureSource),messages:checksumText(messageSource),logs:checksumText(logSource),pop:checksumText(motionSource(popAnchors)),shake:checksumText(motionSource(shakeAnchors)),glow:checksumText(motionSource(glowAnchors)),ring:checksumText(motionSource(ringAnchors))};actualChecksums.fixture=checksumText([actualChecksums.schedule,actualChecksums.actual,actualChecksums.timeline,actualChecksums.failure].join('|'));actualChecksums.logFixture=checksumText([actualChecksums.messages,actualChecksums.logs].join('|'));actualChecksums.motion=checksumText([actualChecksums.pop,actualChecksums.shake,actualChecksums.glow,actualChecksums.ring].join('|'));actualChecksums.extended=checksumText([actualChecksums.fixture,actualChecksums.logFixture,actualChecksums.motion].join('|'));for(const key of Object.keys(expectedChecksums))if(actualChecksums[key]!==expectedChecksums[key])throw new Error('agent-deploy-pipeline deterministic fixture mismatch: '+key+' '+actualChecksums[key]);
const runTwo=runs[2],runTwoFailureLog=logsByRun[2].find(function(entry){return entry.kind==='failure'});if(totalDraws!==runCount*attemptKeys.length*drawsPerAttempt||runTwo.attempts[0].actualGaps.join('.')!=='243.207.326.291.173.392.338.315.300'||runTwo.attempts[1].actualGaps.join('.')!=='284.201.387.388.216.280.365.384.321.292.353.279'||runTwo.attempts[2].actualGaps.join('.')!=='370.229.370.253.235.283.360.305.367.308'||runTwo.attempts[3].actualGaps.join('.')!=='374.319.255.261.304.330.279.160.192.174.180.244.178'||runTwo.attempts[4].actualGaps.join('.')!=='307.210.270.219.215.330.280.272.298.248'||runTwoFailureLog.offset!==6635)throw new Error('agent-deploy-pipeline boundary oracle mismatch');
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,controller=new AbortController(),listener={signal:controller.signal},passiveListener={signal:controller.signal,passive:true};let runIndex=0,attemptIndex=0,phase='stage',source='initial',attemptElapsed=0,phaseElapsed=0,gapCursor=0,runElapsed=0,activeTime=0,userPaused=false,focused=false,visible=!('IntersectionObserver'in window),documentVisible=document.visibilityState!=='hidden',running=false,cleaned=false,frameId=0,lastWall=0,resizeObserver=null,intersectionObserver=null,connectionObserver=null,visibleLogs=[],retryVisible=false,lastRender={stageIndex:0,stageStates:[],connectorProgress:[0,0,0],ringAngle:0,popScale:1,shakeOffset:0,glowAlpha:0};
const counterNames=['rafRequests','rafCancels','schedulerFrames','schedulerStarts','schedulerStops','logAppends','logTrims','stageStarts','stageSuccesses','stageFailures','retryStarts','runCompletions','loopResets','pauseToggles','manualAdvances','fastForwards','resets','pointerDowns','ignoredPointers','keyPauses','keyAdvances','keyEnds','keyResets','focusEvents','blurEvents','visibilityChanges','intersectionChanges','resizeEvents','cleanupCalls'];const counters={};counterNames.forEach(function(name){counters[name]=0});
function activeAttempt(){return runs[runIndex].attempts[attemptIndex]}
function stageIndexForAttempt(index){return index===2?1:index>2?index-1:index}
function canonicalEntryForGap(nextGap){const key=activeAttempt().key;return logsByRun[runIndex].find(function(entry){return entry.attempt===key&&entry.gapIndex===nextGap})}
function canonicalStartEntry(index){const key=runs[runIndex].attempts[index].key;return logsByRun[runIndex].find(function(entry){return entry.attempt===key&&entry.gapIndex===-1})}
function appendLog(entry){if(!entry)return;visibleLogs.push(Object.assign({},entry));counters.logAppends++;if(visibleLogs.length>logCap){visibleLogs=visibleLogs.slice(-logCap);counters.logTrims++}}
function announce(text){statusElement.textContent=text}
function stageStates(){const current=stageIndexForAttempt(attemptIndex),states=stageKeys.map(function(){return'pending'});for(let index=0;index<current;index++)states[index]='success';if(phase==='stage')states[current]='active';else if(phase==='failure-pause')states[current]='error';else states[current]='success';return states}
function connectorProgress(){const current=stageIndexForAttempt(attemptIndex),progress=[0,0,0];for(let index=0;index<3;index++){if(index<current-1)progress[index]=1;else if(index===current-1){if(phase==='stage')progress[index]=clamp(attemptElapsed/activeAttempt().duration,0,1);else progress[index]=1}}return progress}
function render(nextSource){if(nextSource)source=nextSource;const states=stageStates(),current=stageIndexForAttempt(attemptIndex),attempt=activeAttempt(),angle=phase==='stage'?ringAngle(attemptElapsed):0,scale=phase==='success-pop'||phase==='deploy-hold'?popScale(phaseElapsed):1,shake=phase==='failure-pause'?shakeOffset(phaseElapsed):0,glow=phase==='deploy-hold'?glowAlpha(phaseElapsed):0,connectors=connectorProgress();root.dataset.phase=phase;root.dataset.stage=attempt.key;root.dataset.tone=phase==='failure-pause'?'err':phase==='deploy-hold'?'ok':attemptIndex===2?'warn':'acc';root.dataset.paused=String(userPaused||reduced);root.dataset.run=String(runIndex);root.dataset.retry=String(retryVisible);root.dataset.reduced=String(reduced);root.dataset.cleaned=String(cleaned);stageTone.textContent=(userPaused&&!reduced?'PAUSED · ':'')+(attemptIndex===2?'TEST RETRY':attempt.key);runLabel.textContent='RUN '+String(runIndex+1).padStart(2,'0')+' / 06';stepElements.forEach(function(element,index){element.dataset.state=states[index];element.setAttribute('aria-label',stageKeys[index][0]+stageKeys[index].slice(1).toLowerCase()+' '+(states[index]==='active'?'active':states[index]==='success'?'complete':states[index]==='error'?'failed':'pending'));if(index===current)element.setAttribute('aria-current','step');else element.removeAttribute('aria-current');const shell=element.querySelector('.d-agent-agent-deploy-pipeline-node-shell'),ring=element.querySelector('.d-agent-agent-deploy-pipeline-ring'),node=element.querySelector('.d-agent-agent-deploy-pipeline-node');shell.style.transform='translateX('+(index===current?shake:0).toFixed(6)+'px) scale('+(index===current?scale:1).toFixed(12)+')';ring.style.transform='rotate('+(index===current?angle:0).toFixed(6)+'deg)';node.style.boxShadow=index===3&&phase==='deploy-hold'?'0 0 0 1px rgba(250,115,25,'+(glow*.55).toFixed(6)+'),0 0 13px rgba(250,115,25,'+glow.toFixed(6)+')':'none'});connectorFillElements.forEach(function(element,index){element.style.transform='scaleX('+connectors[index].toFixed(12)+')'});retryPill.textContent='RETRY 1/1';logView.replaceChildren();visibleLogs.forEach(function(entry){const row=document.createElement('div'),time=document.createElement('span'),message=document.createElement('span');row.className='d-agent-agent-deploy-pipeline-log-row';row.dataset.tone=entry.tone;time.className='d-agent-agent-deploy-pipeline-log-time';message.className='d-agent-agent-deploy-pipeline-log-message';time.textContent=entry.timestamp.slice(3);message.textContent=entry.message.replace(String.fromCharCode(10005),'ERR').replace(String.fromCharCode(10003),'OK');row.appendChild(time);row.appendChild(message);logView.appendChild(row)});logView.scrollTop=logView.scrollHeight;lastRender={stageIndex:current,stageStates:states.slice(),connectorProgress:connectors.slice(),ringAngle:angle,popScale:scale,shakeOffset:shake,glowAlpha:glow};expose()}
function beginAttempt(nextAttempt,nextSource,carriedElapsed){attemptIndex=nextAttempt;attemptElapsed=Math.max(0,carriedElapsed||0);gapCursor=0;phase='stage';phaseElapsed=0;if(nextAttempt===2){retryVisible=true;counters.retryStarts++;announce('Run '+(runIndex+1)+' retrying TEST')}appendLog(canonicalStartEntry(nextAttempt));counters.stageStarts++;render(nextSource||'stage start')}
function finishStage(carriedElapsed){const attempt=activeAttempt(),failed=runs[runIndex].fail&&attemptIndex===1;carriedElapsed=Math.max(0,carriedElapsed||0);if(failed){phase='failure-pause';phaseElapsed=carriedElapsed;retryVisible=true;counters.stageFailures++;announce('Run '+(runIndex+1)+' TEST failed; retry scheduled');render('test failed');return}counters.stageSuccesses++;if(attemptIndex===2)retryVisible=false;if(attemptIndex===4){phase='deploy-hold';phaseElapsed=carriedElapsed;counters.runCompletions++;announce('Run '+(runIndex+1)+' production deployment complete');render('production hold');return}phase='success-pop';phaseElapsed=carriedElapsed;announce((attemptIndex===2?'TEST retry':attempt.key)+' complete');render('stage complete')}
function emitNextGap(deliberate){const attempt=activeAttempt();if(gapCursor>=attempt.actualGaps.length)return false;const entry=canonicalEntryForGap(gapCursor),targetElapsed=attempt.actualGaps.slice(0,gapCursor+1).reduce(function(total,gap){return total+gap},0);if(deliberate){const advance=Math.max(0,targetElapsed-attemptElapsed);runElapsed+=advance;activeTime+=advance;attemptElapsed=targetElapsed}else attemptElapsed=Math.max(attemptElapsed,targetElapsed);gapCursor++;appendLog(entry);if(gapCursor===attempt.actualGaps.length)finishStage(deliberate?0:Math.max(0,attemptElapsed-attempt.duration));else render('log event');return true}
function nextAfterSuccess(carriedElapsed){if(attemptIndex===0)beginAttempt(1,'test start',carriedElapsed);else if(attemptIndex===1||attemptIndex===2)beginAttempt(3,'preview start',carriedElapsed);else if(attemptIndex===3)beginAttempt(4,'production start',carriedElapsed)}
function nextRun(nextSource,carriedElapsed){carriedElapsed=Math.max(0,carriedElapsed||0);runIndex=(runIndex+1)%runCount;attemptIndex=0;phase='stage';source=nextSource||'loop reset';attemptElapsed=carriedElapsed;phaseElapsed=0;gapCursor=0;runElapsed=carriedElapsed;visibleLogs=[];retryVisible=false;counters.loopResets++;appendLog(canonicalStartEntry(0));counters.stageStarts++;announce('Run '+(runIndex+1)+' build started');render(source)}
function advanceOne(nextSource){if(cleaned)return;counters.manualAdvances++;if(phase==='stage')emitNextGap(true);else if(phase==='success-pop'){const advance=Math.max(0,successPopDuration-phaseElapsed);runElapsed+=advance;activeTime+=advance;phaseElapsed=successPopDuration;nextAfterSuccess()}else if(phase==='failure-pause'){const advance=Math.max(0,failurePauseDuration-phaseElapsed);runElapsed+=advance;activeTime+=advance;phaseElapsed=failurePauseDuration;beginAttempt(2,'retry start')}else if(phase==='deploy-hold'){const advance=Math.max(0,productionHoldDuration-phaseElapsed);runElapsed+=advance;activeTime+=advance;phaseElapsed=productionHoldDuration;nextRun('manual loop reset')}render(nextSource||'manual advance')}
function advanceRuntime(delta){if(phase==='stage'){attemptElapsed+=delta;runElapsed+=delta;const gaps=activeAttempt().actualGaps,nextAt=gaps.slice(0,gapCursor+1).reduce(function(total,gap){return total+gap},0);if(gapCursor<gaps.length&&attemptElapsed>=nextAt)emitNextGap(false)}else if(phase==='success-pop'){phaseElapsed+=delta;runElapsed+=delta;if(phaseElapsed>=successPopDuration)nextAfterSuccess(phaseElapsed-successPopDuration)}else if(phase==='failure-pause'){phaseElapsed+=delta;runElapsed+=delta;if(phaseElapsed>=failurePauseDuration)beginAttempt(2,'automatic retry',phaseElapsed-failurePauseDuration)}else if(phase==='deploy-hold'){phaseElapsed+=delta;runElapsed+=delta;if(phaseElapsed>=productionHoldDuration)nextRun('automatic loop reset',phaseElapsed-productionHoldDuration)}}
function materializeFailure(){if(cleaned)return;stopScheduler('fast-forward stop');runIndex=2;attemptIndex=1;phase='failure-pause';source='fast-forward failure';attemptElapsed=runTwo.attempts[1].duration;phaseElapsed=0;gapCursor=runTwo.attempts[1].actualGaps.length;runElapsed=timelines[2].testDone;activeTime=timelines[2].testDone;retryVisible=true;visibleLogs=logsByRun[2].filter(function(entry){return entry.offset<=timelines[2].testDone}).slice(-logCap).map(function(entry){return Object.assign({},entry)});userPaused=false;counters.fastForwards++;announce('Run 3 TEST failed; retry starts in 1.5 seconds');render('fast-forward failure');if(!reduced)startScheduler('fast-forward retry scheduler')}
function resetCounters(){counterNames.forEach(function(name){counters[name]=0})}
function resetDemo(){if(cleaned)return;const resetCount=counters.resets+1;if(frameId){cancelAnimationFrame(frameId);frameId=0}running=false;lastWall=0;runIndex=0;attemptIndex=0;phase='stage';source='reset';attemptElapsed=0;phaseElapsed=0;gapCursor=0;runElapsed=0;activeTime=0;userPaused=false;visibleLogs=[];retryVisible=false;resetCounters();counters.resets=resetCount;appendLog(canonicalStartEntry(0));counters.stageStarts++;announce('Deployment pipeline reset; run one build started');render('reset');if(!reduced)startScheduler('reset scheduler')}
function schedulerEligible(){return!cleaned&&!reduced&&!userPaused&&visible&&documentVisible}
function requestNextFrame(){if(frameId||!schedulerEligible())return;frameId=requestAnimationFrame(frame);counters.rafRequests++;expose()}
function startScheduler(nextSource){if(!schedulerEligible())return;if(!running){running=true;lastWall=0;counters.schedulerStarts++;if(nextSource)source=nextSource}requestNextFrame()}
function stopScheduler(nextSource){if(frameId){cancelAnimationFrame(frameId);frameId=0;counters.rafCancels++}if(running){running=false;counters.schedulerStops++}lastWall=0;if(nextSource)source=nextSource;expose()}
function togglePause(nextSource){if(cleaned||reduced){advanceOne(nextSource||'reduced deliberate advance');return}userPaused=!userPaused;counters.pauseToggles++;announce(userPaused?'Deployment paused':'Deployment resumed');if(userPaused)stopScheduler('user paused');else startScheduler('user resumed');render(nextSource||'pause toggle')}
function frame(now){frameId=0;if(!schedulerEligible()){running=false;lastWall=0;expose();return}const delta=lastWall?Math.min(maxDelta,Math.max(0,now-lastWall)):0;lastWall=now;activeTime+=delta;counters.schedulerFrames++;advanceRuntime(delta);render('frame');requestNextFrame()}
function copyAttempt(attempt){return{run:attempt.run,attemptIndex:attempt.attemptIndex,key:attempt.key,duration:attempt.duration,rawGaps:attempt.rawGaps.slice(),actualGaps:attempt.actualGaps.slice(),usedRawCount:attempt.usedRawCount,lineCount:attempt.lineCount}}
function copyLog(entry){return{run:entry.run,sequence:entry.sequence,attempt:entry.attempt,kind:entry.kind,tone:entry.tone,offset:entry.offset,timestamp:entry.timestamp,message:entry.message,gapIndex:entry.gapIndex}}
function deepFreeze(value){if(value&&typeof value==='object'&&!Object.isFrozen(value)){Object.freeze(value);Object.keys(value).forEach(function(key){deepFreeze(value[key])})}return value}
function inspect(){const rootRect=root.getBoundingClientRect(),stageRect=deployStage.getBoundingClientRect(),railRect=root.querySelector('.d-agent-agent-deploy-pipeline-rail').getBoundingClientRect(),logRect=root.querySelector('.d-agent-agent-deploy-pipeline-log').getBoundingClientRect(),viewRect=logView.getBoundingClientRect(),attempt=activeAttempt();return deepFreeze({
 config:{seed:seed,rng:'mulberry32',runCount:runCount,attemptKeys:attemptKeys.slice(),stageKeys:stageKeys.slice(),drawsPerAttempt:drawsPerAttempt,rawGapsPerAttempt:rawGapsPerAttempt,totalDraws:totalDraws,duration:{minimum:durationMinimum,maximum:durationMaximum,inclusive:true},gap:{minimum:gapMinimum,maximum:gapMaximum,inclusive:true,partition:'bounded exact sum'},timing:{successPop:successPopDuration,failurePause:failurePauseDuration,shake:shakeDuration,ring: ringDuration,productionHold:productionHoldDuration,maxDelta:maxDelta},logCap:logCap,clockBase:'14:02:10.000',failureRule:'runIndex % 3 === 2',colors:Object.assign({},colors)},
 fixtures:{runs:runs.map(function(run){return{run:run.run,fail:run.fail,attempts:run.attempts.map(copyAttempt)}}),timelines:timelines.map(function(item){return Object.assign({},item)}),failureFlags:runs.map(function(run){return run.fail}),messages:Object.keys(messages).reduce(function(result,key){result[key]=messages[key].slice();return result},{}),logs:canonicalLogs.map(copyLog),logsByRun:logsByRun.map(function(items){return items.map(copyLog)}),motion:{pop:popAnchors.map(function(item){return Object.assign({},item)}),shake:shakeAnchors.map(function(item){return Object.assign({},item)}),glow:glowAnchors.map(function(item){return Object.assign({},item)}),ring:ringAnchors.map(function(item){return Object.assign({},item)})},serializers:{schedule:scheduleSource,actual:actualSource,timeline:timelineSource,failure:failureSource,messages:messageSource,logs:logSource,pop:motionSource(popAnchors),shake:motionSource(shakeAnchors),glow:motionSource(glowAnchors),ring:motionSource(ringAnchors)}},
 state:{phase:phase,source:source,activeTime:activeTime,runIndex:runIndex,runNumber:runIndex+1,runElapsed:runElapsed,runTime:runElapsed,runDuration:timelines[runIndex].runDuration,attemptIndex:attemptIndex,attemptKey:attempt.key,stageIndex:stageIndexForAttempt(attemptIndex),attemptElapsed:attemptElapsed,attemptDuration:attempt.duration,gapCursor:gapCursor,nextGap:gapCursor<attempt.actualGaps.length?attempt.actualGaps[gapCursor]:null,phaseElapsed:phaseElapsed,phaseDuration:phase==='success-pop'?successPopDuration:phase==='failure-pause'?failurePauseDuration:phase==='deploy-hold'?productionHoldDuration:0,userPaused:userPaused},
 rail:{nodes:stageKeys.map(function(key,index){return{key:key,state:lastRender.stageStates[index],ariaLabel:stepElements[index].getAttribute('aria-label'),ariaCurrent:stepElements[index].getAttribute('aria-current')}}),connectors:lastRender.connectorProgress.slice(),ringAngle:lastRender.ringAngle,popScale:lastRender.popScale,shakeOffset:lastRender.shakeOffset},
 logs:{visible:visibleLogs.map(copyLog),visibleCount:visibleLogs.length,cap:logCap,domCount:logView.children.length,total:counters.logAppends,lastSequence:visibleLogs.length?visibleLogs[visibleLogs.length-1].sequence:null,scrollTop:logView.scrollTop,scrollHeight:logView.scrollHeight,clientHeight:logView.clientHeight,totalCanonical:canonicalLogs.length},
 retry:{visible:retryVisible,text:retryPill.textContent,elapsed:phase==='failure-pause'?phaseElapsed:0,remaining:phase==='failure-pause'?Math.max(0,failurePauseDuration-phaseElapsed):0},glow:{active:phase==='deploy-hold',alpha:lastRender.glowAlpha,duration:productionHoldDuration},
 interaction:{focused:focused,userPaused:userPaused,statusText:statusElement.textContent,keyboardShortcuts:deployStage.getAttribute('aria-keyshortcuts'),soleFocusable:true,primaryAction:reduced?'advance one event':'pause or resume',arrowAction:'pause and advance one event',endAction:'materialize run 3 TEST failure'},counters:Object.assign({},counters),
 lifecycle:{running:running,reduced:reduced,cleaned:cleaned,visible:visible,documentVisible:documentVisible,frameId:frameId,signalAborted:controller.signal.aborted,hasResizeObserver:!!resizeObserver,hasIntersectionObserver:!!intersectionObserver,hasConnectionObserver:!!connectionObserver},
 geometry:{rootWidth:rootRect.width,rootHeight:rootRect.height,rootScrollWidth:root.scrollWidth,rootScrollHeight:root.scrollHeight,stageWidth:stageRect.width,stageHeight:stageRect.height,railWidth:railRect.width,railHeight:railRect.height,logWidth:logRect.width,logHeight:logRect.height,logViewportHeight:viewRect.height,nodeSizes:stepElements.map(function(element){const rect=element.querySelector('.d-agent-agent-deploy-pipeline-node-shell').getBoundingClientRect();return{width:rect.width,height:rect.height}}),connectorHeights:connectorElements.map(function(element){return element.getBoundingClientRect().height})},checksums:Object.assign({},actualChecksums),data:Object.assign({},root.dataset)})}
function expose(){Object.assign(root.dataset,{phase:phase,stage:activeAttempt().key,tone:phase==='failure-pause'?'err':phase==='deploy-hold'?'ok':attemptIndex===2?'warn':'acc',paused:String(userPaused||reduced),run:String(runIndex),runElapsed:runElapsed.toFixed(3),attemptElapsed:attemptElapsed.toFixed(3),gapCursor:String(gapCursor),retry:String(retryVisible),activeTime:activeTime.toFixed(3),running:String(running),visible:String(visible),documentVisible:String(documentVisible),reduced:String(reduced),cleaned:String(cleaned),frameId:String(frameId),schedulerFrames:String(counters.schedulerFrames),rafRequests:String(counters.rafRequests),fixtureChecksum:actualChecksums.fixture,logFixtureChecksum:actualChecksums.logFixture,motionChecksum:actualChecksums.motion,extendedChecksum:actualChecksums.extended,source:source})}
deployStage.addEventListener('pointerdown',function(event){if(event.isPrimary===false||event.button!==undefined&&event.button!==0){counters.ignoredPointers++;return}counters.pointerDowns++;if(document.activeElement!==deployStage)deployStage.focus({preventScroll:true});togglePause('pointer action')},passiveListener);
deployStage.addEventListener('focus',function(){focused=true;counters.focusEvents++;render('focus')},listener);deployStage.addEventListener('blur',function(){focused=false;counters.blurEvents++;render('blur')},listener);
deployStage.addEventListener('keydown',function(event){const accepted=['ArrowRight','Enter',' ','End','Home','r','R'];if(accepted.indexOf(event.key)===-1)return;event.preventDefault();if(event.key==='End'){counters.keyEnds++;materializeFailure();return}if(event.key==='Home'||event.key==='r'||event.key==='R'){resetDemo();counters.keyResets++;expose();return}if(event.key==='ArrowRight'){counters.keyAdvances++;if(!reduced){userPaused=true;stopScheduler('keyboard step pause')}advanceOne('keyboard step');return}counters.keyPauses++;togglePause('keyboard primary action')},listener);
function onVisibility(){documentVisible=document.visibilityState!=='hidden';counters.visibilityChanges++;if(documentVisible)startScheduler('document visible');else stopScheduler('document hidden')}document.addEventListener('visibilitychange',onVisibility,listener);
function cleanup(){if(cleaned)return;counters.cleanupCalls++;cleaned=true;if(frameId){cancelAnimationFrame(frameId);frameId=0;counters.rafCancels++}running=false;controller.abort();if(resizeObserver){resizeObserver.disconnect();resizeObserver=null}if(intersectionObserver){intersectionObserver.disconnect();intersectionObserver=null}if(connectionObserver){connectionObserver.disconnect();connectionObserver=null}render('cleanup')}
if('ResizeObserver'in window){resizeObserver=new ResizeObserver(function(){if(cleaned)return;counters.resizeEvents++;render('resize')});resizeObserver.observe(deployStage)}
if('IntersectionObserver'in window){intersectionObserver=new IntersectionObserver(function(entries){if(!entries.length||cleaned)return;visible=entries[0].isIntersecting;counters.intersectionChanges++;if(visible)startScheduler('intersection visible');else stopScheduler('intersection hidden')},{threshold:.05});intersectionObserver.observe(root)}
if('MutationObserver'in window){connectionObserver=new MutationObserver(function(){if(!root.isConnected)cleanup()});connectionObserver.observe(document.documentElement,{childList:true,subtree:true})}
root.__agentDeployPipelineInspect=Object.freeze(inspect);root.__agentDeployPipelineReset=Object.freeze(resetDemo);appendLog(canonicalStartEntry(0));counters.stageStarts++;render(reduced?'reduced initial':'initial');if(!reduced)startScheduler('initial scheduler');`,
  prompt:`Build one self-contained responsive 320px Agent and AI UI deployment-pipeline card with no dependencies, assets, network, persistence, timers, Math.random, Web Animations API or CSS keyframes. Use a four-node BUILD, TEST, PREVIEW and PROD rail with exact 32px circles and two-pixel connectors. Stream deterministic timestamped ten-pixel monospace logs into an eight-row capped viewport. Generate six fixed seed-42 runs with five attempts per run, a fixed 780-draw budget, inclusive 2.5 to 4 second durations and exact bounded 150 to 400ms partitions. TEST must fail on runs three and six, pause for 1.5 seconds and pass an automatic RETRY 1/1 attempt. Animate all motion through one capped requestAnimationFrame loop: a 1.2-second conic-mask active ring, connector progress, a 300ms success pop, a three-cycle three-pixel failure shake, and a restrained 1.5-second PROD glow. Reduced motion makes zero animation-frame requests and advances only through deliberate input. Use one role application focus target, semantic listitems, truthful aria-current and labels, a non-live log, a hidden polite status for major deliberate events, lifecycle pausing with no catch-up, detach cleanup, deeply frozen deterministic fixtures and exact inspect and reset hooks. Keep chrome grayscale on #0a0a0b, use Roboto Mono with JetBrains Mono fallback, reserve #fa7319 for live or active state, and distinguish completion and failure with labels, solid or dashed lines, and CSS-drawn marks rather than extra hues or dingbats.`
});
