/* INTRX registry — deterministic topographic extrusion */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
  id:'map-topo-extrude',
  title:'Map Topo Extrude',
  cat:'Maps & Geo',
  rootClass:'d-maps-map-topo-extrude',
  tags:['terrain','noise','3d'],
  libs:[],
  desc:'A seeded heatmap reconstructs itself as a compact topographic model. Its 216 cells rise by elevation, reveal shaded bottom faces and a snow line, then flatten in the exact reverse order.',
  seen:'Seen on: Topographical Explorer by SHAHNAB AHMED',
  hint:'hover anywhere to raise the terrain; move over cells for elevation; use arrows, Enter or Space, Escape, or R',
  html:`<div class="d-maps-map-topo-extrude d-maps-map-topo-extrude-card" role="region" aria-label="Interactive topographic elevation map">
  <div class="d-maps-map-topo-extrude-head" aria-hidden="true"><span>MAP / TOPOGRAPHY</span><span>HOVER / ARROWS</span></div>
  <div class="d-maps-map-topo-extrude-wrapper">
    <div class="d-maps-map-topo-extrude-stage" role="application" tabindex="0" aria-keyshortcuts="ArrowLeft ArrowRight ArrowUp ArrowDown Home End Enter Space Escape R" aria-label="Topographic elevation inspector. Use arrow keys to inspect cells, Enter or Space to hold the terrain, Escape to flatten, and R to reset.">
      <div class="d-maps-map-topo-extrude-grid" role="grid" aria-label="18 by 12 deterministic elevation grid">
        <div class="d-maps-map-topo-extrude-crosshair" aria-hidden="true"><i class="d-maps-map-topo-extrude-crosshair--row"></i><i class="d-maps-map-topo-extrude-crosshair--column"></i></div>
      </div>
      <output class="d-maps-map-topo-extrude-elevation" aria-live="polite" aria-atomic="true" aria-label="Elevation readout">EL 1,204M</output>
    </div>
  </div>
  <div class="d-maps-map-topo-extrude-foot" aria-hidden="true"><span>18 × 12 / SEED 42</span><span>3 OCTAVES / 216 CELLS</span></div>
  <span class="d-maps-map-topo-extrude-status" aria-live="polite" aria-atomic="true">Terrain ready at elevation 1,204 meters</span>
</div>`,
  css:`
.d-maps-map-topo-extrude{position:relative;width:100%;height:320px;box-sizing:border-box;overflow:hidden;contain:layout paint;container-type:inline-size;background:#0a0a0b;color:#ececef;font-family:'JetBrains Mono',ui-monospace,monospace;isolation:isolate;touch-action:pan-y}
.d-maps-map-topo-extrude-head{position:absolute;z-index:20;top:13px;left:16px;right:16px;display:flex;align-items:center;justify-content:space-between;gap:12px;color:#9b9ba3;font-size:10px;line-height:1;letter-spacing:.08em;white-space:nowrap}
.d-maps-map-topo-extrude-wrapper{position:absolute;inset:40px 14px 40px;display:grid;place-items:center;perspective:900px;perspective-origin:50% 48%;pointer-events:none}
.d-maps-map-topo-extrude-stage{position:relative;width:100%;height:100%;display:grid;place-items:center;box-sizing:border-box;border:1px solid #232327;border-radius:10px;background:#101012;outline:none;transform-style:preserve-3d;pointer-events:auto;touch-action:none;cursor:crosshair}
.d-maps-map-topo-extrude-stage:focus-visible{box-shadow:inset 0 0 0 1px #fa7319}
.d-maps-map-topo-extrude-grid{position:relative;display:grid;grid-template-columns:repeat(18,minmax(0,1fr));grid-template-rows:repeat(12,minmax(0,1fr));gap:1px;width:min(100%,318px);aspect-ratio:18/12;box-sizing:border-box;background:#161619;outline:none;transform:rotateX(0deg) rotateZ(0deg);transform-origin:50% 50%;transform-style:preserve-3d;transition:transform 450ms ease-in-out;pointer-events:auto;touch-action:none;cursor:crosshair}
.d-maps-map-topo-extrude-row{display:contents}
.d-maps-map-topo-extrude-cell{position:relative;display:block;min-width:0;min-height:0;box-sizing:border-box;border:0;border-top:1px solid transparent;background:var(--cell-color);transform:translateZ(0);transform-style:preserve-3d;transition-property:transform;transition-duration:450ms;transition-timing-function:ease-in-out;transition-delay:var(--leave-delay);will-change:transform}
.d-maps-map-topo-extrude-cell::after{content:'';position:absolute;left:0;right:0;top:100%;height:var(--extrusion);background:linear-gradient(180deg,rgba(16,16,18,.94),rgba(5,5,6,.99));opacity:0;transform:rotateX(-90deg);transform-origin:50% 0;transition:opacity 450ms ease-in-out var(--leave-delay);pointer-events:none}
.d-maps-map-topo-extrude-cell--snow{border-top-color:#ffffff}
.d-maps-map-topo-extrude[data-active="true"] .d-maps-map-topo-extrude-grid{transform:rotateX(52deg) rotateZ(0deg);transition-duration:600ms}
.d-maps-map-topo-extrude[data-active="true"] .d-maps-map-topo-extrude-cell{transform:translateZ(var(--extrusion));transition-duration:600ms;transition-delay:var(--enter-delay)}
.d-maps-map-topo-extrude[data-active="true"] .d-maps-map-topo-extrude-cell::after{opacity:1;transition-duration:600ms;transition-delay:var(--enter-delay)}
.d-maps-map-topo-extrude-crosshair{position:absolute;z-index:300;inset:0;transform:translateZ(var(--cross-z));transform-style:preserve-3d;pointer-events:none}
.d-maps-map-topo-extrude-crosshair--row,.d-maps-map-topo-extrude-crosshair--column{position:absolute;display:block;background:#fa7319;box-shadow:0 0 5px rgba(250,115,25,.45)}
.d-maps-map-topo-extrude-crosshair--row,.d-maps-map-topo-extrude-crosshair--column,.d-maps-map-topo-extrude-elevation{opacity:0;transition:opacity 120ms linear}
.d-maps-map-topo-extrude[data-crosshair-visible="true"] .d-maps-map-topo-extrude-crosshair--row,.d-maps-map-topo-extrude[data-crosshair-visible="true"] .d-maps-map-topo-extrude-crosshair--column,.d-maps-map-topo-extrude[data-crosshair-visible="true"] .d-maps-map-topo-extrude-elevation{opacity:1}
.d-maps-map-topo-extrude-crosshair--row{left:0;right:0;top:var(--cross-y);height:1px;transform:translateY(-.5px)}
.d-maps-map-topo-extrude-crosshair--column{top:0;bottom:0;left:var(--cross-x);width:1px;transform:translateX(-.5px)}
.d-maps-map-topo-extrude-elevation{position:absolute;z-index:400;top:10px;right:10px;min-width:66px;padding:5px 6px;border:1px solid #fa7319;background:#0a0a0b;color:#ececef;font:10px/1 'JetBrains Mono',ui-monospace,monospace;letter-spacing:.04em;text-align:center;white-space:nowrap;pointer-events:none}
.d-maps-map-topo-extrude-foot{position:absolute;z-index:20;left:16px;right:16px;bottom:13px;display:flex;align-items:center;justify-content:space-between;gap:8px;color:#5c5c66;font-size:9px;line-height:1;letter-spacing:.045em;white-space:nowrap}
.d-maps-map-topo-extrude-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}
@container(max-width:340px){.d-maps-map-topo-extrude-head{left:12px;right:12px;font-size:9px}.d-maps-map-topo-extrude-wrapper{left:10px;right:10px}.d-maps-map-topo-extrude-foot{left:12px;right:12px;font-size:8px}}
@media(prefers-reduced-motion:reduce){.d-maps-map-topo-extrude *,.d-maps-map-topo-extrude *::before,.d-maps-map-topo-extrude *::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}
`,
  js:`const card=root,topoStage=root.querySelector('.d-maps-map-topo-extrude-stage'),grid=root.querySelector('.d-maps-map-topo-extrude-grid'),crosshair=root.querySelector('.d-maps-map-topo-extrude-crosshair'),crosshairRow=root.querySelector('.d-maps-map-topo-extrude-crosshair--row'),crosshairColumn=root.querySelector('.d-maps-map-topo-extrude-crosshair--column'),elevationChip=root.querySelector('.d-maps-map-topo-extrude-elevation'),status=root.querySelector('.d-maps-map-topo-extrude-status');
const columns=18,rows=12,cellCount=columns*rows,seed=42,periods=[12,6,3],weights=[1,.5,.25],weightTotal=1.75,anchorDimensions=periods.map(function(period){return[Math.ceil((columns-1)/period)+2,Math.ceil((rows-1)/period)+2]}),smoothstepFormula='f*f*(3-2*f)',alphaMin=.15,alphaMax=.85,gap=1,perspective=900,tiltX=52,tiltZ=0,enterDuration=600,leaveDuration=450,easing='ease-in-out',extrudeHeight=46,enterDelayScale=180,leaveDelayScale=180,snowFraction=.05,snowCount=Math.ceil(cellCount*snowFraction),elevationBase=320,elevationRange=2048,defaultIndex=117;
const colors={background:'#0a0a0b',panel:'#101012',bg2:'#161619',line0:'#232327',line1:'#2e2e34',txt0:'#ececef',txt1:'#9b9ba3',txt2:'#5c5c66',acc:'#fa7319',snow:'#ffffff'};
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,controller=new AbortController(),listener={signal:controller.signal},passiveListener={signal:controller.signal,passive:true};
function mulberry32(nextSeed){return function(){let value=nextSeed+=0x6D2B79F5;value=Math.imul(value^(value>>>15),value|1);value^=value+Math.imul(value^(value>>>7),value|61);return((value^(value>>>14))>>>0)/4294967296}}
function smoothstep(value){return value*value*(3-2*value)}
function lerp(first,second,amount){return first+(second-first)*amount}
function clamp(value,minimum,maximum){return Math.max(minimum,Math.min(maximum,value))}
function checksumText(value){let hash=2166136261;for(const character of String(value))hash=Math.imul(hash^character.charCodeAt(0),16777619)>>>0;return hash.toString(16).padStart(8,'0')}
function formatInteger(value){const digits=String(Math.round(value));let output='';for(let index=0;index<digits.length;index++){if(index>0&&(digits.length-index)%3===0)output+=',';output+=digits[index]}return output}
function elevationText(value){return'EL '+formatInteger(value)+'M'}
const random=mulberry32(seed),octaves=[],anchorsFlat=[];
for(let octaveIndex=0;octaveIndex<periods.length;octaveIndex++){const period=periods[octaveIndex],width=anchorDimensions[octaveIndex][0],height=anchorDimensions[octaveIndex][1],anchors=[];for(let row=0;row<height;row++)for(let column=0;column<width;column++){const value=random();anchors.push(value);anchorsFlat.push(value)}octaves.push({index:octaveIndex,period:period,weight:weights[octaveIndex],width:width,height:height,anchors:anchors})}
function octaveValue(octave,column,row){const x=column/octave.period,y=row/octave.period,x0=Math.floor(x),y0=Math.floor(y),xMix=smoothstep(x-x0),yMix=smoothstep(y-y0),topLeft=octave.anchors[y0*octave.width+x0],topRight=octave.anchors[y0*octave.width+x0+1],bottomLeft=octave.anchors[(y0+1)*octave.width+x0],bottomRight=octave.anchors[(y0+1)*octave.width+x0+1],top=lerp(topLeft,topRight,xMix),bottom=lerp(bottomLeft,bottomRight,xMix);return lerp(top,bottom,yMix)}
const cells=[];
for(let row=0;row<rows;row++)for(let column=0;column<columns;column++){const index=row*columns+column,octaveValues=octaves.map(function(octave){return octaveValue(octave,column,row)});let weighted=0;for(let octaveIndex=0;octaveIndex<octaveValues.length;octaveIndex++)weighted+=octaveValues[octaveIndex]*weights[octaveIndex];const value=weighted/weightTotal,elevation=Math.round(elevationBase+value*elevationRange),alpha=alphaMin+(alphaMax-alphaMin)*value,translateZ=value*extrudeHeight,enterDelay=value*enterDelayScale,leaveDelay=(1-value)*leaveDelayScale;cells.push({index:index,row:row,column:column,octaveValues:octaveValues,value:value,elevation:elevation,alpha:alpha,translateZ:translateZ,enterDelay:enterDelay,leaveDelay:leaveDelay,snow:false,snowRank:-1,element:null})}
const rankedCells=cells.slice().sort(function(first,second){return second.value-first.value||first.index-second.index}),snowIndices=rankedCells.slice(0,snowCount).map(function(cell,rank){cell.snow=true;cell.snowRank=rank;return cell.index});
const fragment=document.createDocumentFragment();
for(let row=0;row<rows;row++){const rowElement=document.createElement('div');rowElement.className='d-maps-map-topo-extrude-row';rowElement.setAttribute('role','row');for(let column=0;column<columns;column++){const cell=cells[row*columns+column],element=document.createElement('span');element.className='d-maps-map-topo-extrude-cell'+(cell.snow?' d-maps-map-topo-extrude-cell--snow':'');element.setAttribute('role','gridcell');element.setAttribute('aria-label','Row '+(row+1)+', column '+(column+1)+', elevation '+formatInteger(cell.elevation)+' meters');element.dataset.index=String(cell.index);element.dataset.row=String(row);element.dataset.column=String(column);element.dataset.value=cell.value.toFixed(9);element.dataset.elevation=String(cell.elevation);element.dataset.snow=String(cell.snow);element.style.setProperty('--extrusion',cell.translateZ.toFixed(9)+'px');element.style.setProperty('--enter-delay',cell.enterDelay.toFixed(9)+'ms');element.style.setProperty('--leave-delay',cell.leaveDelay.toFixed(9)+'ms');element.style.setProperty('--cell-color','rgba(250,115,25,'+cell.alpha.toFixed(9)+')');cell.element=element;rowElement.appendChild(element)}fragment.appendChild(rowElement)}
grid.insertBefore(fragment,crosshair);
const values=cells.map(function(cell){return cell.value}),anchorChecksum=checksumText(anchorsFlat.map(function(value){return value.toFixed(9)}).join(',')),valueChecksum=checksumText(values.map(function(value){return value.toFixed(9)}).join(',')),snowChecksum=checksumText(snowIndices.join(',')),fixtureChecksum=checksumText(cells.map(function(cell){return[cell.index,cell.row,cell.column,cell.value.toFixed(9),cell.elevation].join(',')}).join('|'));
let active=false,locked=false,hovered=false,focused=false,pointerOverTerrain=false,keyboardSelection=false,selectedIndex=defaultIndex,source='initial',cleaned=false,resizeObserver=null,connectionObserver=null;
let enterEvents=0,leaveEvents=0,pointerMoves=0,pointerDowns=0,pointerUps=0,terrainEnters=0,terrainLeaves=0,keyboardMoves=0,keyboardToggles=0,focusEvents=0,blurEvents=0,selectionChanges=0,resizeEvents=0,resets=0,cleanupCalls=0,rafRequests=0;
function selectedCell(){return cells[selectedIndex]}
function updateCrosshairGeometry(){const cell=selectedCell(),element=cell.element,centerX=element.offsetLeft+element.offsetWidth/2,centerY=element.offsetTop+element.offsetHeight/2,crossZ=cell.translateZ+1;grid.style.setProperty('--cross-x',centerX.toFixed(3)+'px');grid.style.setProperty('--cross-y',centerY.toFixed(3)+'px');grid.style.setProperty('--cross-z',crossZ.toFixed(3)+'px');crosshairRow.dataset.row=String(cell.row);crosshairRow.style.setProperty('--row',String(cell.row));crosshairColumn.dataset.column=String(cell.column);crosshairColumn.style.setProperty('--column',String(cell.column))}
function updateSelection(nextIndex,nextSource,mode){const bounded=clamp(Math.round(nextIndex),0,cellCount-1),previous=cells[selectedIndex];if(previous&&previous.element)previous.element.removeAttribute('aria-current');if(bounded!==selectedIndex)selectionChanges++;selectedIndex=bounded;const cell=selectedCell();cell.element.setAttribute('aria-current','true');elevationChip.value=elevationText(cell.elevation);elevationChip.textContent=elevationText(cell.elevation);updateCrosshairGeometry();source=nextSource||source;if(mode==='keyboard')keyboardSelection=true;status.textContent='Row '+(cell.row+1)+', column '+(cell.column+1)+', elevation '+formatInteger(cell.elevation)+' meters';expose(source)}
function crosshairVisible(){return active&&(pointerOverTerrain||keyboardSelection)}
function syncActive(nextSource){active=!!(hovered||focused||locked);source=nextSource||source;root.dataset.active=String(active);root.dataset.crosshairVisible=String(crosshairVisible());root.dataset.transitionMode=active?'enter':'leave';expose(source)}
function expose(nextSource){source=nextSource||source;const cell=selectedCell();Object.assign(root.dataset,{columns:String(columns),rows:String(rows),cellCount:String(cellCount),seed:String(seed),octaves:String(periods.length),active:String(active),locked:String(locked),hovered:String(hovered),focused:String(focused),pointerOverTerrain:String(pointerOverTerrain),keyboardSelection:String(keyboardSelection),crosshairVisible:String(crosshairVisible()),selectedIndex:String(selectedIndex),selectedRow:String(cell.row),selectedColumn:String(cell.column),selectedValue:cell.value.toFixed(9),elevation:String(cell.elevation),enterDelay:cell.enterDelay.toFixed(9),leaveDelay:cell.leaveDelay.toFixed(9),translateZ:cell.translateZ.toFixed(9),snowCount:String(snowCount),anchorCount:String(anchorsFlat.length),perspective:String(perspective),tiltX:String(tiltX),tiltZ:String(tiltZ),transitionMode:active?'enter':'leave',enterDuration:String(enterDuration),leaveDuration:String(leaveDuration),reduced:String(reduced),running:'false',frameId:'0',rafRequests:String(rafRequests),cleaned:String(cleaned),anchorChecksum:anchorChecksum,valueChecksum:valueChecksum,snowChecksum:snowChecksum,fixtureChecksum:fixtureChecksum,resets:String(resets),source:source})}
function setLocked(nextLocked,nextSource){locked=!!nextLocked;keyboardSelection=true;syncActive(nextSource);status.textContent=locked?'Terrain locked in raised view':'Terrain lock released'}
function resetDemo(){if(cleaned)return;const nextReset=resets+1;active=false;locked=false;hovered=false;focused=false;pointerOverTerrain=false;keyboardSelection=false;selectedIndex=defaultIndex;enterEvents=0;leaveEvents=0;pointerMoves=0;pointerDowns=0;pointerUps=0;terrainEnters=0;terrainLeaves=0;keyboardMoves=0;keyboardToggles=0;focusEvents=0;blurEvents=0;selectionChanges=0;resizeEvents=0;resets=nextReset;rafRequests=0;root.dataset.active='false';root.dataset.crosshairVisible='false';root.dataset.transitionMode='leave';updateSelection(defaultIndex,'reset','reset');status.textContent='Topographic field reset';expose('reset')}
function deepFreeze(value){if(value&&typeof value==='object'&&!Object.isFrozen(value)){Object.freeze(value);Object.keys(value).forEach(function(key){deepFreeze(value[key])})}return value}
function inspect(){const rootRect=root.getBoundingClientRect(),stageRect=topoStage.getBoundingClientRect(),gridRect=grid.getBoundingClientRect(),cellItems=cells.map(function(cell){return{index:cell.index,row:cell.row,column:cell.column,octaveValues:cell.octaveValues.slice(),value:cell.value,snow:cell.snow,snowRank:cell.snowRank,elevation:cell.elevation,alpha:cell.alpha,enterDelay:cell.enterDelay,leaveDelay:cell.leaveDelay,translateZ:cell.translateZ,dataset:Object.assign({},cell.element.dataset)}});return deepFreeze({config:{columns:columns,rows:rows,cellCount:cellCount,seed:seed,octaves:periods.length,periods:periods.slice(),weights:weights.slice(),weightTotal:weightTotal,anchorDimensions:anchorDimensions.map(function(pair){return pair.slice()}),anchorCount:anchorsFlat.length,smoothstep:smoothstepFormula,normalization:'weighted-sum/1.75-no-minmax',alphaMin:alphaMin,alphaMax:alphaMax,gap:gap,perspective:perspective,tiltX:tiltX,tiltZ:tiltZ,enterDuration:enterDuration,leaveDuration:leaveDuration,easing:easing,extrudeHeight:extrudeHeight,enterDelayScale:enterDelayScale,leaveDelayScale:leaveDelayScale,enterDelayFormula:'value*180',leaveDelayFormula:'(1-value)*180',snowFraction:snowFraction,snowCount:snowCount,snowTieBreak:'value-descending-index-ascending',elevationBase:elevationBase,elevationRange:elevationRange,defaultIndex:defaultIndex,colors:Object.assign({},colors)},fixtures:{octaves:octaves.map(function(octave){return{index:octave.index,period:octave.period,weight:octave.weight,width:octave.width,height:octave.height,anchors:octave.anchors.slice()}}),anchorsFlat:anchorsFlat.slice(),values:values.slice(),snowIndices:snowIndices.slice()},cells:{items:cellItems,snowIndices:snowIndices.slice(),snowCount:snowCount,minimumValue:Math.min.apply(null,values),maximumValue:Math.max.apply(null,values)},state:{active:active,locked:locked,hovered:hovered,focused:focused,pointerOverTerrain:pointerOverTerrain,keyboardSelection:keyboardSelection,crosshairVisible:crosshairVisible(),selectedIndex:selectedIndex,selectedRow:selectedCell().row,selectedColumn:selectedCell().column,value:selectedCell().value,elevation:selectedCell().elevation,source:source},interaction:{readout:elevationChip.textContent,selectedAriaLabel:selectedCell().element.getAttribute('aria-label'),crosshairRow:Number(crosshairRow.dataset.row),crosshairColumn:Number(crosshairColumn.dataset.column),crosshairX:grid.style.getPropertyValue('--cross-x'),crosshairY:grid.style.getPropertyValue('--cross-y'),crosshairZ:grid.style.getPropertyValue('--cross-z')},counters:{enterEvents:enterEvents,leaveEvents:leaveEvents,pointerMoves:pointerMoves,pointerDowns:pointerDowns,pointerUps:pointerUps,terrainEnters:terrainEnters,terrainLeaves:terrainLeaves,keyboardMoves:keyboardMoves,keyboardToggles:keyboardToggles,focusEvents:focusEvents,blurEvents:blurEvents,selectionChanges:selectionChanges,resizeEvents:resizeEvents,resets:resets,cleanupCalls:cleanupCalls,rafRequests:rafRequests},lifecycle:{reduced:reduced,cleaned:cleaned,running:false,frameId:0,rafRequests:rafRequests,signalAborted:controller.signal.aborted,hasResizeObserver:!!resizeObserver,hasConnectionObserver:!!connectionObserver},geometry:{rootWidth:rootRect.width,rootHeight:rootRect.height,rootScrollWidth:root.scrollWidth,rootScrollHeight:root.scrollHeight,stageWidth:stageRect.width,stageHeight:stageRect.height,gridWidth:gridRect.width,gridHeight:gridRect.height,gridClientWidth:grid.clientWidth,gridClientHeight:grid.clientHeight},checksums:{anchorChecksum:anchorChecksum,valueChecksum:valueChecksum,snowChecksum:snowChecksum,fixtureChecksum:fixtureChecksum,cellChecksum:fixtureChecksum},data:Object.assign({},root.dataset)})}
root.addEventListener('pointerenter',function(event){if(event.isPrimary===false)return;hovered=true;enterEvents++;syncActive('card pointer enter')},passiveListener);
root.addEventListener('pointermove',function(event){if(event.isPrimary===false)return;if(!hovered){hovered=true;syncActive('card pointer recovery')}},passiveListener);
root.addEventListener('pointerleave',function(event){if(event.isPrimary===false)return;hovered=false;pointerOverTerrain=false;if(!focused&&!locked)keyboardSelection=false;leaveEvents++;syncActive('card pointer leave');status.textContent=active?(locked?'Terrain held in raised view':'Keyboard terrain inspection active'):'Terrain flattened'},passiveListener);
root.addEventListener('focusin',function(){focused=true;focusEvents++;syncActive('card focus in')},listener);
root.addEventListener('focusout',function(event){if(root.contains(event.relatedTarget))return;focused=false;keyboardSelection=false;blurEvents++;syncActive('card focus out');status.textContent=active?(locked?'Terrain held in raised view':'Pointer terrain inspection active'):'Terrain flattened'},listener);
grid.addEventListener('pointerenter',function(event){if(event.isPrimary===false)return;pointerOverTerrain=true;terrainEnters++;const element=event.target.closest('.d-maps-map-topo-extrude-cell');if(element)updateSelection(Number(element.dataset.index),'terrain pointer enter','pointer');syncActive('terrain pointer enter')},passiveListener);
grid.addEventListener('pointermove',function(event){if(event.isPrimary===false)return;pointerOverTerrain=true;pointerMoves++;const element=event.target.closest('.d-maps-map-topo-extrude-cell');if(element)updateSelection(Number(element.dataset.index),'terrain pointer move','pointer');else syncActive('terrain pointer gap')},passiveListener);
grid.addEventListener('pointerleave',function(event){if(event.isPrimary===false)return;pointerOverTerrain=false;terrainLeaves++;syncActive('terrain pointer leave')},passiveListener);
grid.addEventListener('pointerdown',function(event){if(event.isPrimary===false)return;pointerDowns++;if(document.activeElement!==topoStage)topoStage.focus({preventScroll:true});const element=event.target.closest('.d-maps-map-topo-extrude-cell');if(element)updateSelection(Number(element.dataset.index),'terrain pointer down','pointer');if(event.pointerType==='touch'){locked=true;keyboardSelection=true;syncActive('touch terrain lock')}},passiveListener);
grid.addEventListener('pointerup',function(event){if(event.isPrimary===false)return;pointerUps++;const element=event.target.closest('.d-maps-map-topo-extrude-cell');if(element)updateSelection(Number(element.dataset.index),'terrain pointer up','pointer')},passiveListener);
topoStage.addEventListener('focus',function(){keyboardSelection=true;updateSelection(defaultIndex,'terrain focus','keyboard');syncActive('terrain focus')},listener);
topoStage.addEventListener('keydown',function(event){const accepted=['ArrowLeft','ArrowRight','ArrowUp','ArrowDown','Home','End','Enter',' ','Escape','r','R'];if(accepted.indexOf(event.key)===-1)return;event.preventDefault();focused=true;if(event.key==='r'||event.key==='R'){resetDemo();return}if(event.key==='Enter'||event.key===' '){keyboardToggles++;setLocked(!locked,'keyboard toggle');return}if(event.key==='Escape'){keyboardToggles++;locked=false;hovered=false;focused=false;pointerOverTerrain=false;keyboardSelection=false;syncActive('keyboard escape');status.textContent='Terrain flattened';return}const cell=selectedCell();let row=cell.row,column=cell.column;if(event.key==='ArrowLeft')column--;if(event.key==='ArrowRight')column++;if(event.key==='ArrowUp')row--;if(event.key==='ArrowDown')row++;if(event.key==='Home'){row=0;column=0}if(event.key==='End'){row=rows-1;column=columns-1}row=clamp(row,0,rows-1);column=clamp(column,0,columns-1);keyboardMoves++;updateSelection(row*columns+column,'keyboard','keyboard');syncActive('keyboard')},listener);
if('ResizeObserver'in window){resizeObserver=new ResizeObserver(function(){if(cleaned)return;resizeEvents++;updateCrosshairGeometry();expose('resize observer')});resizeObserver.observe(grid)}
function cleanup(){if(cleaned)return;cleanupCalls++;cleaned=true;controller.abort();if(resizeObserver)resizeObserver.disconnect();if(connectionObserver)connectionObserver.disconnect();active=false;root.dataset.active='false';root.dataset.crosshairVisible='false';expose('cleanup')}
if('MutationObserver'in window){connectionObserver=new MutationObserver(function(){if(!root.isConnected)cleanup()});connectionObserver.observe(document.documentElement,{childList:true,subtree:true})}
root.__mapTopoExtrudeInspect=inspect;root.__mapTopoExtrudeReset=resetDemo;
updateSelection(defaultIndex,'initial','initial');root.dataset.active='false';root.dataset.crosshairVisible='false';root.dataset.transitionMode='leave';expose('ready');`,
  prompt:`
Build a self-contained responsive 320px-tall Maps & Geo card with an 18-column by 12-row terrain, exactly 216 cells and exactly 1px grid gaps. Use the palette bg0 #0a0a0b, bg1 #101012, bg2 #161619, line0 #232327, line1 #2e2e34, txt0 #ececef, txt1 #9b9ba3, txt2 #5c5c66, acc #fa7319 and white #ffffff. The empty card is bg0, its bordered stage panel is bg1, and the grid ground beneath every cell is bg2. Every cell uses fixed acc RGB rgba(250,115,25,alpha), where alpha is exactly 0.15 + value × 0.70, spanning 15–85%; do not interpolate the RGB channels.

Reconstruct the terrain with one standard mulberry32 stream seeded at 42. The generator step is: seed += 0x6D2B79F5; t = imul(t XOR t unsigned-right-shift 15, t OR 1); t XOR-equals t + imul(t XOR t unsigned-right-shift 7, t OR 61); return unsigned(t XOR t unsigned-right-shift 14) / 4294967296. Build three value-noise octaves broad to fine with cell periods [12,6,3] and weights [1,0.5,0.25]. For period p, its anchor width is ceil((18 − 1) / p) + 2 and height is ceil((12 − 1) / p) + 2, producing [4×3,5×4,8×6] grids and 80 anchors total. Fill every grid row-major from the same uninterrupted random stream. At cell column c and row r, sample coordinates c/p and r/p; bilinearly interpolate four anchors after easing each fractional coordinate with f²(3 − 2f). Divide the weighted octave sum by 1.75 and do no global min-max normalization.

Sort cells by value descending with row-major index ascending as the explicit tie-break. Mark exactly ceil(216 × 0.05) = 11 cells, the first 11 ranked cells, with a white 1px top border as the snow line. Elevation is round(320 + value × 2048) meters. Initial and keyboard-focus selection is row-major index 117, row 6 and column 9 zero-based; its deterministic value is 0.4317248811816431, elevation 1,204, and readout must format exactly EL 1,204M with comma grouping.

Put the terrain in a perspective(900px) wrapper. It starts flat at rotateX(0deg) rotateZ(0deg). Pointer entry anywhere on the card or focus raises it to exactly rotateX(52deg) rotateZ(0deg) over 600ms ease-in-out. Each cell simultaneously translates on Z by value × 46px after an enter delay of value × 180ms, so peaks rise last. Give every cell a darker linear-gradient pseudo-side hinged from its bottom edge, with height equal to that extrusion. On leave, flatten over 450ms ease-in-out using reverse delay (1 − value) × 180ms, so peaks flatten first and valleys last.

While active, pointer targeting and arrow-key selection must use one selection path. Draw an exact 1px acc horizontal and vertical crosshair through the selected cell on a Z plane exactly 1px above that cell's value × 46px top plane. Keep the deterministic elevation chip outside the transformed and aria-hidden crosshair, as a legible non-3D overlay fixed 10px from the stage's top and right edges. Make the bordered stage the sole focusable element and support clamped arrow keys, Home, End, Enter or Space hold, Escape flatten and R reset. A touch on the terrain holds the raised state without adding another focus target. Preserve stable selection, crosshair and elevation under reduced motion while disabling every transition and making zero requestAnimationFrame calls.

Use an AbortController for listeners, ResizeObserver for responsive crosshair placement, MutationObserver detach cleanup, no timers, no animation-frame scheduler, deterministic reset, and frozen inspection/reset hooks. Expose the noise anchors, octave samples, all 216 cell values and geometry, snow ranks, forward and reverse delays, elevations, interaction state, event counters, lifecycle state, responsive bounds and FNV-1a fixture checksums. Keep the card free of horizontal or vertical overflow at both 380px and 320px widths.`
});
