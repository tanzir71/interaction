/* INTRX registry - published WebGL displaced image mesh */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'webgl-vertex-wave',title:'WebGL Vertex Wave',cat:'Image & WebGL',rootClass:'d-vertexwave',tags:['WebGL','vertex displacement','indexed mesh'],libs:[],desc:'A 40 by 28 indexed grid carries an image-like procedural surface. Its vertex shader combines two traveling waves and a pointer-centered impulse, while pointer tilt reveals the mesh depth and hover raises displacement amplitude.',seen:'Seen on: digital fashion editorials, album artwork, architecture portfolios, and immersive product launches',hint:'move to tilt and disturb the mesh; click to hold full amplitude; Escape releases it',
html:`<div class="d-vertexwave" role="group" aria-label="Interactive displaced WebGL image mesh"><div class="d-vertexwave-head"><span>INDEXED MESH / 40 × 28</span><span>AMPLITUDE <b>0.070</b> <i>1,189 VERTICES</i></span></div><div class="d-vertexwave-field"><canvas tabindex="0" role="button" aria-pressed="false" aria-label="Subdivided WebGL image mesh. Move to tilt and disturb it; activate to hold full wave amplitude."></canvas><div class="d-vertexwave-grid" aria-hidden="true"><span>X</span><i></i><span>Y</span></div><div class="d-vertexwave-depth" aria-hidden="true"><span>+Z</span><i></i><span>−Z</span></div><p class="d-vertexwave-fallback">WebGL unavailable - showing a flat reference plane.</p><span class="d-vertexwave-note" aria-hidden="true">VERTEX Z = WAVE A + WAVE B + POINTER IMPULSE</span></div><div class="d-vertexwave-foot"><p aria-live="polite">Mesh breathing at idle amplitude</p><button type="button">Release amplitude</button></div></div>`,
css:`.d-vertexwave{width:100%;min-height:510px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#ebf0ef;background:#080b0c;font-family:Inter,system-ui,sans-serif}.d-vertexwave-head{display:flex;justify-content:space-between;color:#687477;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.12em}.d-vertexwave-head b{color:#8ce6ca}.d-vertexwave-head i{margin-left:8px;color:#b5c4c0;font-style:normal}.d-vertexwave-field{position:relative;height:390px;margin-top:13px;overflow:hidden;border:1px solid #243235;background:radial-gradient(circle at 50% 45%,#163c3c,#0a1518 55%,#06090a)}.d-vertexwave canvas{display:block;width:100%;height:100%;outline:none;cursor:crosshair}.d-vertexwave canvas:focus-visible{box-shadow:inset 0 0 0 2px #8ce6ca}.d-vertexwave-grid{position:absolute;left:13px;top:13px;display:flex;align-items:center;gap:6px;color:rgba(196,224,216,.5);font:700 6px/1 ui-monospace,monospace;pointer-events:none}.d-vertexwave-grid i{width:42px;height:1px;background:linear-gradient(90deg,#7bd6be,rgba(123,214,190,.08))}.d-vertexwave-depth{position:absolute;right:13px;top:13px;display:flex;align-items:center;gap:6px;color:rgba(196,224,216,.44);font:700 6px/1 ui-monospace,monospace;pointer-events:none}.d-vertexwave-depth i{width:34px;height:3px;background:linear-gradient(90deg,#e7b861,#62c9b2)}.d-vertexwave-note{position:absolute;left:13px;bottom:11px;color:rgba(197,219,214,.42);font:650 6px/1 ui-monospace,monospace;letter-spacing:.08em;pointer-events:none}.d-vertexwave-fallback{display:none;position:absolute;inset:0;place-items:center;margin:0;color:#a8c3bd;font:650 9px/1 ui-monospace,monospace}.d-vertexwave.no-webgl .d-vertexwave-fallback{display:grid}.d-vertexwave.no-webgl canvas{opacity:0}.d-vertexwave-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-vertexwave-foot p{margin:0;color:#6f7d7a;font:650 9px/1.3 ui-monospace,monospace}.d-vertexwave-foot button{min-height:31px;padding:7px 11px;border:1px solid #30403d;border-radius:3px;color:#9dafaa;background:#111817;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-vertexwave-foot button:hover,.d-vertexwave-foot button:focus-visible{color:#fff;border-color:#66837c;outline:none}@media(max-width:550px){.d-vertexwave{padding-inline:6px}.d-vertexwave-note{display:none}}`,
js:`const canvas=root.querySelector('canvas');const amplitudeReadout=root.querySelector('.d-vertexwave-head b');const status=root.querySelector('.d-vertexwave-foot p');const release=root.querySelector('.d-vertexwave-foot button');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const gl=canvas.getContext('webgl',{alpha:false,antialias:true,depth:true});if(!gl){root.classList.add('no-webgl');status.textContent='WebGL unavailable';return}const vertexSource='attribute vec2 a_position;attribute vec2 a_uv;uniform float u_time;uniform float u_amplitude;uniform vec2 u_pointer;uniform vec2 u_tilt;uniform float u_aspect;varying vec2 v_uv;varying float v_height;varying float v_light;void main(){vec2 p=a_position;float waveA=sin(p.x*7.2+u_time*1.6)*cos(p.y*4.6-u_time*.8);float waveB=sin((p.x+p.y)*10.0-u_time*1.25)*.42;float distanceToPointer=length(p-u_pointer);float impulse=cos(distanceToPointer*18.0-u_time*2.1)*exp(-distanceToPointer*3.8);float z=(waveA*.58+waveB*.32+impulse*.62)*u_amplitude;float dx=(7.2*cos(p.x*7.2+u_time*1.6)*cos(p.y*4.6-u_time*.8)*.58-18.0*sin(distanceToPointer*18.0-u_time*2.1)*exp(-distanceToPointer*3.8)*.62)*u_amplitude;float dy=(-4.6*sin(p.x*7.2+u_time*1.6)*sin(p.y*4.6-u_time*.8)*.58-18.0*sin(distanceToPointer*18.0-u_time*2.1)*exp(-distanceToPointer*3.8)*.62)*u_amplitude;vec3 normal=normalize(vec3(-dx,-dy,1.0));v_light=.38+.62*max(dot(normal,normalize(vec3(-.35,.45,1.0))),0.0);float cx=cos(u_tilt.y),sx=sin(u_tilt.y),cy=cos(u_tilt.x),sy=sin(u_tilt.x);vec3 pos=vec3(p.x,p.y,z);pos.yz=mat2(cx,-sx,sx,cx)*pos.yz;pos.xz=mat2(cy,sy,-sy,cy)*pos.xz;float perspective=1.0/(1.18-pos.z*.72);gl_Position=vec4(pos.x*perspective*.88,pos.y*perspective*u_aspect*.88,pos.z*.25,1.0);v_uv=a_uv;v_height=z/u_amplitude;}';const fragmentSource='precision mediump float;varying vec2 v_uv;varying float v_height;varying float v_light;float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453);}void main(){vec2 p=v_uv;vec3 deep=vec3(.018,.075,.095),teal=vec3(.08,.58,.49),sand=vec3(.93,.62,.28),foam=vec3(.75,.95,.87);float bands=sin(p.y*15.0+sin(p.x*8.0)*2.4)*.5+.5;float islands=smoothstep(.48,.53,sin(p.x*11.0+p.y*4.0)+sin(p.y*17.0-p.x*3.0)*.45);vec3 color=mix(deep,teal,bands*.76);color=mix(color,sand,islands*.38);float glint=pow(max(0.0,sin((p.x-p.y)*48.0+v_height*5.0)),16.0);color=mix(color,foam,glint*.42);vec2 cell=abs(fract(p*vec2(40.0,28.0))-.5);float grid=smoothstep(.472,.5,max(cell.x,cell.y));color=mix(color,foam,grid*.13);color*=v_light;color+=max(v_height,0.0)*vec3(.16,.3,.24);gl_FragColor=vec4(color,1.0);}';function compile(type,source){const shader=gl.createShader(type);gl.shaderSource(shader,source);gl.compileShader(shader);if(!gl.getShaderParameter(shader,gl.COMPILE_STATUS))throw new Error(gl.getShaderInfoLog(shader));return shader}let program;try{program=gl.createProgram();gl.attachShader(program,compile(gl.VERTEX_SHADER,vertexSource));gl.attachShader(program,compile(gl.FRAGMENT_SHADER,fragmentSource));gl.linkProgram(program);if(!gl.getProgramParameter(program,gl.LINK_STATUS))throw new Error(gl.getProgramInfoLog(program))}catch(error){root.classList.add('no-webgl');status.textContent='WebGL shader unavailable';return}gl.useProgram(program);const columns=40,rows=28,vertices=[],indices=[];for(let y=0;y<=rows;y++){for(let x=0;x<=columns;x++){const u=x/columns,v=y/rows;vertices.push((u-.5)*1.72,(v-.5)*1.15,u,v)}}for(let y=0;y<rows;y++){for(let x=0;x<columns;x++){const a=y*(columns+1)+x,b=a+1,c=a+columns+1,d=c+1;indices.push(a,c,b,b,c,d)}}const vertexBuffer=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,vertexBuffer);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(vertices),gl.STATIC_DRAW);const indexBuffer=gl.createBuffer();gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,indexBuffer);gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,new Uint16Array(indices),gl.STATIC_DRAW);const position=gl.getAttribLocation(program,'a_position'),uvAttribute=gl.getAttribLocation(program,'a_uv');gl.enableVertexAttribArray(position);gl.vertexAttribPointer(position,2,gl.FLOAT,false,16,0);gl.enableVertexAttribArray(uvAttribute);gl.vertexAttribPointer(uvAttribute,2,gl.FLOAT,false,16,8);const timeUniform=gl.getUniformLocation(program,'u_time'),amplitudeUniform=gl.getUniformLocation(program,'u_amplitude'),pointerUniform=gl.getUniformLocation(program,'u_pointer'),tiltUniform=gl.getUniformLocation(program,'u_tilt'),aspectUniform=gl.getUniformLocation(program,'u_aspect');gl.enable(gl.DEPTH_TEST);let hovered=false,focused=false,latched=false,visible=true,running=false,last=0,time=0,amplitude=.07,pointer={x:0,y:0},targetPointer={x:0,y:0},tilt={x:0,y:0},targetTilt={x:0,y:0};
function targetAmplitude(){return hovered||focused||latched?.18:.07}function resize(){const rect=canvas.getBoundingClientRect(),dpr=Math.min(2,window.devicePixelRatio||1),width=Math.max(1,Math.round(rect.width*dpr)),height=Math.max(1,Math.round(rect.height*dpr));if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height;gl.viewport(0,0,width,height)}draw()}function draw(){gl.clearColor(.025,.04,.045,1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.uniform1f(timeUniform,time);gl.uniform1f(amplitudeUniform,amplitude);gl.uniform2f(pointerUniform,pointer.x,pointer.y);gl.uniform2f(tiltUniform,tilt.x,tilt.y);gl.uniform1f(aspectUniform,canvas.width/canvas.height);gl.drawElements(gl.TRIANGLES,indices.length,gl.UNSIGNED_SHORT,0);amplitudeReadout.textContent=amplitude.toFixed(3)}function start(){if(reduce||!visible||running)return;running=true;last=performance.now();requestAnimationFrame(frame)}function frame(now){if(!visible){running=false;return}const dt=Math.min(60,now-last);last=now;time+=dt/1000;const alpha=1-Math.exp(-dt*.009);amplitude+=(targetAmplitude()-amplitude)*alpha;pointer.x+=(targetPointer.x-pointer.x)*alpha;pointer.y+=(targetPointer.y-pointer.y)*alpha;tilt.x+=(targetTilt.x-tilt.x)*alpha;tilt.y+=(targetTilt.y-tilt.y)*alpha;draw();requestAnimationFrame(frame)}function aim(event){const rect=canvas.getBoundingClientRect(),u=Math.max(0,Math.min(1,(event.clientX-rect.left)/rect.width)),v=Math.max(0,Math.min(1,1-(event.clientY-rect.top)/rect.height));targetPointer.x=(u-.5)*1.72;targetPointer.y=(v-.5)*1.15;targetTilt.x=(u-.5)*.34;targetTilt.y=(v-.5)*-.28;start()}function sync(announce){canvas.setAttribute('aria-pressed',latched?'true':'false');if(announce)status.textContent=latched?'Full wave amplitude held':'Amplitude released to hover control';if(reduce){amplitude=targetAmplitude();pointer={...targetPointer};tilt={...targetTilt};time=.55;draw()}else start()}canvas.addEventListener('pointerenter',function(event){hovered=true;status.textContent='Mesh amplitude rising';aim(event);sync(false)});canvas.addEventListener('pointermove',aim);canvas.addEventListener('pointerleave',function(){hovered=false;targetTilt={x:0,y:0};status.textContent=latched?'Wave remains held':'Mesh returning to idle amplitude';sync(false)});canvas.addEventListener('focus',function(){focused=true;sync(false)});canvas.addEventListener('blur',function(){focused=false;sync(false)});canvas.addEventListener('click',function(event){latched=!latched;aim(event);sync(true)});canvas.addEventListener('keydown',function(event){if(event.key==='Enter'||event.key===' '){event.preventDefault();latched=!latched;sync(true)}else if(event.key==='Escape'){latched=false;hovered=false;targetTilt={x:0,y:0};sync(true);canvas.blur()}});release.addEventListener('click',function(){latched=false;hovered=false;focused=false;targetTilt={x:0,y:0};sync(true);release.focus()});if('ResizeObserver'in window)new ResizeObserver(resize).observe(canvas);if('IntersectionObserver'in window)new IntersectionObserver(function(entries){visible=entries[0].isIntersecting;if(visible)start();else running=false},{threshold:.05}).observe(root);resize();if(reduce){amplitude=.11;time=.55;draw();status.textContent='Static displaced mesh - reduced motion'}else start();`,
prompt:`Build a self-contained WebGL 1.0 displaced image mesh without libraries. In JavaScript create a 40×28 grid: 41×29 vertices containing 2D position plus UV, and six Uint16 indices per cell. Render it with drawElements, depth testing, and separate interleaved vertex/index buffers.

Displace z in the vertex shader, not the fragment shader. Combine two directional sine waves with a pointer-centered damped cosine impulse. Estimate x/y slope, form a normal, and pass simple directional lighting plus normalized height to the fragment shader. Apply small pointer-controlled x/y rotations and manual perspective. The fragment shader should make an image-like teal/sand topographic surface from UV, with sparse highlights and a subtle 40×28 cell overlay.

Idle amplitude is .07; hover/focus/latch target .18. Ease amplitude, pointer, and tilt with alpha=1-exp(-dt*.009). Click or Enter/Space holds full amplitude; Escape/Release clears it. Animate only while visible, with IntersectionObserver and a running guard. Resize DPR-aware with ResizeObserver. Handle WebGL/shader failure visibly. Under prefers-reduced-motion, draw a static representative displaced frame and update only on input. Synchronize amplitude, aria-pressed, and live status; include visible focus, scoped CSS, and no dependencies.`});
