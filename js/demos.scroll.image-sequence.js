/* INTRX registry — published procedural scroll image sequence */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
id:'scroll-image-sequence',
title:'Scroll Image Sequence',
cat:'Scroll',
rootClass:'d-sequence',
tags:['canvas','frame sequence','scroll'],
libs:[],
desc:'A product-style canvas scene advances through 48 discrete, procedurally rendered frames as the local scroll position changes. The result has the precise, authored cadence of an image sequence without downloading a video or sprite sheet.',
seen:'Seen on: product launches, hardware detail pages, automotive reveals, and cinematic feature stories',
hint:'scroll inside the stage — each position resolves to an exact frame',
html:`<div class="d-sequence" role="group" aria-label="Scroll-controlled procedural image sequence"><div class="d-sequence-head"><span>ORBIT STUDY / FRAME SEQUENCE</span><span><b>01</b> / 48</span></div><div class="d-sequence-progress" role="progressbar" aria-label="Sequence progress" aria-valuemin="1" aria-valuemax="48" aria-valuenow="1" aria-valuetext="Frame 1 of 48"><i aria-hidden="true"></i></div><div class="d-sequence-scroll" tabindex="0" aria-label="Scrollable product frame sequence"><div class="d-sequence-track"><div class="d-sequence-sticky"><canvas aria-label="A procedural metallic object rotating and opening as you scroll"></canvas><div class="d-sequence-copy" aria-hidden="true"><p>PRECISION / 04</p><strong>Form follows<br>the frame.</strong></div><div class="d-sequence-axis" aria-hidden="true"><span>000°</span><i></i><span>270°</span></div></div></div></div><div class="d-sequence-foot"><p class="d-sequence-status" aria-live="polite">Frame 1 of 48 — profile</p><button class="d-sequence-reset" type="button">Rewind</button></div></div>`,
css:`.d-sequence{--sequence-progress:0;width:100%;min-height:520px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#edf0f5;background:#0a0a0b;font-family:Roboto Mono,JetBrains Mono,monospace}.d-sequence-head{display:flex;justify-content:space-between;color:#707784;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.13em}.d-sequence-head b{color:#e9edf3}.d-sequence-progress{height:2px;margin-top:13px;background:#242933}.d-sequence-progress i{display:block;width:calc(var(--sequence-progress)*100%);height:100%;background:#ff5f32;box-shadow:0 0 8px rgba(255,95,50,.45)}.d-sequence-scroll{height:390px;margin-top:12px;overflow-y:auto;overscroll-behavior:contain;scrollbar-width:thin;scrollbar-color:#414856 #11141a;outline:none;border:1px solid #20252e;background:#0c0f14}.d-sequence-scroll:focus-visible{box-shadow:inset 0 0 0 2px #aeb7c5}.d-sequence-track{height:1780px;position:relative}.d-sequence-sticky{position:sticky;top:0;height:388px;overflow:hidden;background:radial-gradient(circle at 55% 48%,#202733 0,#10151c 38%,#090b0f 76%)}.d-sequence canvas{display:block;width:100%;height:100%}.d-sequence-copy{position:absolute;z-index:2;left:clamp(20px,5vw,52px);top:54px;pointer-events:none}.d-sequence-copy p{margin:0 0 12px;color:#ff7048;font:700 8px/1 ui-monospace,monospace;letter-spacing:.16em}.d-sequence-copy strong{display:block;color:#f3f5f7;font:500 clamp(27px,4.5vw,44px)/.96 Georgia,serif;letter-spacing:-.035em}.d-sequence-axis{position:absolute;z-index:2;right:19px;top:55px;bottom:28px;display:flex;flex-direction:column;align-items:center;color:#6c7480;font:650 7px/1 ui-monospace,monospace;letter-spacing:.08em;pointer-events:none}.d-sequence-axis i{width:1px;flex:1;margin:8px 0;background:linear-gradient(#505866,transparent 48%,#505866)}.d-sequence-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-sequence-status{margin:0;color:#7e8693;font:650 9px/1.3 ui-monospace,monospace}.d-sequence-reset{min-height:31px;padding:7px 12px;border:1px solid #343b47;border-radius:3px;color:#adb4bf;background:#141820;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-sequence-reset:hover,.d-sequence-reset:focus-visible{color:#fff;border-color:#737e8e;outline:none}@media(max-width:520px){.d-sequence{padding-inline:6px}.d-sequence-copy{left:18px}.d-sequence-axis{display:none}}@media(prefers-reduced-motion:reduce){.d-sequence-scroll{scroll-behavior:auto}}`,
js:`const scroller=root.querySelector('.d-sequence-scroll');const canvas=root.querySelector('canvas');const ctx=canvas.getContext('2d');const frameReadout=root.querySelector('.d-sequence-head b');const progressbar=root.querySelector('.d-sequence-progress');const status=root.querySelector('.d-sequence-status');const resetButton=root.querySelector('.d-sequence-reset');const FRAME_COUNT=48;let frame=-1,queued=false,width=0,height=0,dpr=1;
function clamp(value,min,max){return Math.max(min,Math.min(max,value))}function phaseLabel(progress){if(progress<.22)return'profile';if(progress<.48)return'opening';if(progress<.74)return'core exposed';if(progress<.96)return'orbit complete';return'locked'}
function sizeCanvas(){const rect=canvas.getBoundingClientRect();dpr=Math.min(2,window.devicePixelRatio||1);width=Math.max(1,Math.round(rect.width));height=Math.max(1,Math.round(rect.height));const nextWidth=Math.round(width*dpr),nextHeight=Math.round(height*dpr);if(canvas.width!==nextWidth||canvas.height!==nextHeight){canvas.width=nextWidth;canvas.height=nextHeight}drawFrame(frame<0?0:frame)}
function roundedRect(x,y,w,h,r){const radius=Math.min(r,w/2,h/2);ctx.beginPath();ctx.moveTo(x+radius,y);ctx.arcTo(x+w,y,x+w,y+h,radius);ctx.arcTo(x+w,y+h,x,y+h,radius);ctx.arcTo(x,y+h,x,y,radius);ctx.arcTo(x,y,x+w,y,radius);ctx.closePath()}
function drawFrame(index){if(!width||!height)return;const progress=index/(FRAME_COUNT-1),ease=.5-Math.cos(progress*Math.PI)*.5;ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,width,height);const glow=ctx.createRadialGradient(width*.57,height*.51,5,width*.57,height*.51,Math.max(width,height)*.48);glow.addColorStop(0,'rgba(103,130,162,'+(.2+.14*Math.sin(progress*Math.PI))+')');glow.addColorStop(.45,'rgba(26,33,44,.34)');glow.addColorStop(1,'rgba(4,6,9,0)');ctx.fillStyle=glow;ctx.fillRect(0,0,width,height);
ctx.save();ctx.globalAlpha=.22;ctx.strokeStyle='#516070';ctx.lineWidth=.7;for(let ring=1;ring<=4;ring++){ctx.beginPath();ctx.ellipse(width*.57,height*.55,ring*width*.105,ring*height*.075,0,0,Math.PI*2);ctx.stroke()}ctx.restore();
const cx=width*(.62-.04*Math.sin(progress*Math.PI*2)),cy=height*(.57-.035*Math.sin(progress*Math.PI)),scale=Math.min(width/720,height/390);ctx.save();ctx.translate(cx,cy);ctx.rotate(-.28+ease*1.52);ctx.scale(scale,scale);const open=Math.sin(progress*Math.PI)*78;ctx.shadowColor='rgba(0,0,0,.75)';ctx.shadowBlur=32;ctx.shadowOffsetY=18;const body=ctx.createLinearGradient(-130,-80,145,110);body.addColorStop(0,'#151a21');body.addColorStop(.24,'#77818b');body.addColorStop(.46,'#20262e');body.addColorStop(.72,'#a6afb7');body.addColorStop(1,'#222831');roundedRect(-142,-72,284,144,56);ctx.fillStyle=body;ctx.fill();ctx.shadowColor='transparent';ctx.lineWidth=1.5;ctx.strokeStyle='rgba(225,233,239,.45)';ctx.stroke();
ctx.save();ctx.translate(0,-open);const lid=ctx.createLinearGradient(-120,-48,120,45);lid.addColorStop(0,'#aab2ba');lid.addColorStop(.25,'#292f37');lid.addColorStop(.58,'#777f87');lid.addColorStop(1,'#171c23');roundedRect(-132,-52,264,78,35);ctx.fillStyle=lid;ctx.fill();ctx.strokeStyle='rgba(242,245,247,.38)';ctx.stroke();ctx.fillStyle='rgba(255,255,255,.16)';roundedRect(-95,-38,150,3,2);ctx.fill();ctx.restore();
ctx.save();ctx.globalAlpha=clamp(open/55,0,1);const coreGlow=ctx.createRadialGradient(0,-20,2,0,-20,70);coreGlow.addColorStop(0,'rgba(255,119,70,.9)');coreGlow.addColorStop(.3,'rgba(255,78,35,.3)');coreGlow.addColorStop(1,'rgba(255,78,35,0)');ctx.fillStyle=coreGlow;ctx.fillRect(-85,-105,170,170);ctx.fillStyle='#ff6840';ctx.beginPath();ctx.arc(0,-18,14+4*Math.sin(progress*Math.PI*6),0,Math.PI*2);ctx.fill();ctx.strokeStyle='rgba(255,188,158,.8)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,-18,29,0,Math.PI*2);ctx.stroke();ctx.restore();
ctx.fillStyle='rgba(6,8,11,.82)';roundedRect(-104,16,208,36,18);ctx.fill();for(let slot=-2;slot<=2;slot++){ctx.fillStyle=slot===Math.round((progress-.5)*4)?'#ff7048':'#3c444e';ctx.beginPath();ctx.arc(slot*31,34,4,0,Math.PI*2);ctx.fill()}ctx.restore();
ctx.save();ctx.translate(width*.57,height*.55);ctx.rotate(progress*Math.PI*1.5);ctx.strokeStyle='rgba(255,112,72,.65)';ctx.lineWidth=1;ctx.beginPath();ctx.arc(0,0,Math.min(width,height)*.36,-.2,.3);ctx.stroke();ctx.fillStyle='#ff7048';ctx.beginPath();ctx.arc(Math.cos(.3)*Math.min(width,height)*.36,Math.sin(.3)*Math.min(width,height)*.36,3,0,Math.PI*2);ctx.fill();ctx.restore();ctx.fillStyle='rgba(222,228,235,.45)';ctx.font='700 8px ui-monospace, monospace';ctx.fillText('PROCEDURAL FRAME '+String(index+1).padStart(2,'0'),18,height-18)}
function update(){queued=false;const range=Math.max(1,scroller.scrollHeight-scroller.clientHeight),progress=clamp(scroller.scrollTop/range,0,1),nextFrame=Math.round(progress*(FRAME_COUNT-1));root.style.setProperty('--sequence-progress',progress.toFixed(4));if(nextFrame!==frame){frame=nextFrame;drawFrame(frame)}const shown=String(frame+1).padStart(2,'0');frameReadout.textContent=shown;progressbar.setAttribute('aria-valuenow',String(frame+1));progressbar.setAttribute('aria-valuetext','Frame '+(frame+1)+' of '+FRAME_COUNT);status.textContent='Frame '+(frame+1)+' of '+FRAME_COUNT+' — '+phaseLabel(progress)}function requestUpdate(){if(queued)return;queued=true;requestAnimationFrame(update)}scroller.addEventListener('scroll',requestUpdate,{passive:true});resetButton.addEventListener('click',function(){scroller.scrollTo({top:0,behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});requestUpdate();scroller.focus()});if('ResizeObserver'in window){new ResizeObserver(sizeCanvas).observe(canvas)}else{window.addEventListener('resize',sizeCanvas)}sizeCanvas();update();`,
prompt:`Build a self-contained Apple-style scroll image sequence using a procedural Canvas 2D scene instead of external image or video assets. Use exactly 48 discrete frames. Normalize a focusable local scroller as scrollTop/max(1,scrollHeight-clientHeight), then select round(progress*(frameCount-1)). Redraw only when the selected frame changes and coalesce passive scroll events through requestAnimationFrame.

The scene should read as authored product imagery: a metallic capsule rotates, its lid separates, an illuminated core is exposed, orbit guides advance, and frame telemetry remains crisp. Make the canvas DPR-aware and redraw on resize. Synchronize a progress rail, zero-padded frame counter, progressbar aria-valuenow/aria-valuetext, and a restrained aria-live phase label.

Keep native keyboard and touch scrolling authoritative. Rewind scrolls to frame one and restores focus, using auto instead of smooth when prefers-reduced-motion is enabled. Scope all styles, provide visible focus, contain overscroll, and use no external libraries or network media.`
});
