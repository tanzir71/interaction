/* INTRX registry - semantic knowledge orbit */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
  id:'agent-knowledge-orbit',
  title:'Knowledge Orbit',
  cat:'Agent & AI UI',
  rootClass:'d-agent-agent-knowledge-orbit',
  tags:['knowledge graph','semantic orbit','FLIP','agent UI'],
  libs:[],
  desc:'A query concept anchors three semantic-distance rings while related ideas orbit, cluster on inspection, and swap into the center for an endless drill-down.',
  seen:'Seen in knowledge-recall tools, research canvases, semantic search products, and AI memory interfaces.',
  hint:'hover or focus a concept to inspect its relations; activate it to promote it into the center',
  html:`<div class="d-agent-agent-knowledge-orbit" role="region" aria-label="Semantic knowledge orbit">
  <div class="d-agent-agent-knowledge-orbit-head" aria-hidden="true"><span>AGENT / KNOWLEDGE</span><span><i></i> SEMANTIC MAP</span></div>
  <div class="d-agent-agent-knowledge-orbit-stage">
    <div class="d-agent-agent-knowledge-orbit-canvas">
      <svg class="d-agent-agent-knowledge-orbit-svg" aria-hidden="true"><g class="d-agent-agent-knowledge-orbit-rings"></g><g class="d-agent-agent-knowledge-orbit-relations"></g></svg>
      <div class="d-agent-agent-knowledge-orbit-center-wrap"><div class="d-agent-agent-knowledge-orbit-center" role="status" aria-live="polite"><i aria-hidden="true"></i><span>design systems</span></div></div>
      <div class="d-agent-agent-knowledge-orbit-nodes" role="group" aria-label="Related concepts"></div>
    </div>
  </div>
  <div class="d-agent-agent-knowledge-orbit-foot" aria-hidden="true"><span>HOVER / RELATE</span><span>SELECT / DRILL DOWN</span></div>
  <span class="d-agent-agent-knowledge-orbit-status" aria-live="polite" aria-atomic="true">Design systems knowledge map ready</span>
</div>`,
  css:`.d-agent-agent-knowledge-orbit{position:relative;width:100%;height:320px;box-sizing:border-box;overflow:hidden;contain:layout paint;container-type:inline-size;background:#0a0a0b;color:#ececef;font-family:'Roboto Mono','JetBrains Mono',ui-monospace,monospace;isolation:isolate}
.d-agent-agent-knowledge-orbit *{box-sizing:border-box}
.d-agent-agent-knowledge-orbit-head{position:absolute;top:15px;right:16px;left:16px;display:flex;align-items:center;justify-content:space-between;color:#5c5c66;font:600 8px/1 'Roboto Mono','JetBrains Mono',ui-monospace,monospace;letter-spacing:.12em}
.d-agent-agent-knowledge-orbit-head span:last-child{display:flex;align-items:center;gap:6px;color:#9b9ba3}.d-agent-agent-knowledge-orbit-head i{width:5px;height:5px;border-radius:50%;background:#fa7319;box-shadow:0 0 7px rgba(250,115,25,.35)}
.d-agent-agent-knowledge-orbit-stage{position:absolute;top:39px;right:12px;bottom:25px;left:12px;padding:16px;overflow:hidden;border:1px solid #232327;border-radius:10px;background:#101012}
.d-agent-agent-knowledge-orbit-canvas{position:relative;width:100%;height:100%;overflow:hidden;border-radius:6px;background:radial-gradient(circle at 50% 50%,rgba(250,115,25,.035),transparent 38%),#0a0a0b}
.d-agent-agent-knowledge-orbit-svg,.d-agent-agent-knowledge-orbit-nodes{position:absolute;inset:0;width:100%;height:100%}.d-agent-agent-knowledge-orbit-svg{pointer-events:none}.d-agent-agent-knowledge-orbit-rings circle{fill:none;stroke:#232327;stroke-width:1;vector-effect:non-scaling-stroke}.d-agent-agent-knowledge-orbit-relations line{stroke:rgba(250,115,25,.4);stroke-width:1;stroke-linecap:round;vector-effect:non-scaling-stroke}
.d-agent-agent-knowledge-orbit-center-wrap{position:absolute;inset:0;display:grid;place-items:center;pointer-events:none}.d-agent-agent-knowledge-orbit-center{display:flex;align-items:center;gap:7px;min-height:27px;max-width:126px;padding:7px 10px;border:1px solid #fa7319;border-radius:999px;background:#161619;color:#ececef;font:600 10px/1 'Roboto Mono','JetBrains Mono',ui-monospace,monospace;white-space:nowrap;box-shadow:0 0 14px rgba(250,115,25,.08);transform-origin:50% 50%}.d-agent-agent-knowledge-orbit-center i{width:6px;height:6px;flex:none;border-radius:50%;background:#fa7319;box-shadow:0 0 7px rgba(250,115,25,.4)}
.d-agent-agent-knowledge-orbit-nodes{pointer-events:none}.d-agent-agent-knowledge-orbit-node{position:absolute;width:72px;height:20px;padding:0;border:0;background:transparent;color:#9b9ba3;font:500 9px/1 'Roboto Mono','JetBrains Mono',ui-monospace,monospace;cursor:pointer;outline:none;pointer-events:auto;transform-origin:50% 50%}.d-agent-agent-knowledge-orbit-node>span{display:flex;width:100%;height:100%;align-items:center;gap:5px;white-space:nowrap}.d-agent-agent-knowledge-orbit-node[data-side="left"]>span{flex-direction:row-reverse}.d-agent-agent-knowledge-orbit-node-dot{width:10px;height:10px;flex:none;border:1px solid #5c5c66;border-radius:50%;background:#101012}.d-agent-agent-knowledge-orbit-node-label{overflow:hidden;text-overflow:clip}.d-agent-agent-knowledge-orbit-node:hover,.d-agent-agent-knowledge-orbit-node:focus-visible{color:#ececef}.d-agent-agent-knowledge-orbit-node:hover .d-agent-agent-knowledge-orbit-node-dot,.d-agent-agent-knowledge-orbit-node:focus-visible .d-agent-agent-knowledge-orbit-node-dot{border-color:#fa7319;box-shadow:0 0 0 2px rgba(250,115,25,.18),0 0 7px rgba(250,115,25,.3)}.d-agent-agent-knowledge-orbit-node:focus-visible{border-radius:5px;box-shadow:0 0 0 1px #fa7319}
.d-agent-agent-knowledge-orbit-foot{position:absolute;right:16px;bottom:9px;left:16px;display:flex;justify-content:space-between;color:#5c5c66;font:600 8px/1 'Roboto Mono','JetBrains Mono',ui-monospace,monospace;letter-spacing:.1em}.d-agent-agent-knowledge-orbit-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);clip-path:inset(50%);white-space:nowrap}
@container(max-width:340px){.d-agent-agent-knowledge-orbit-stage{right:10px;left:10px}.d-agent-agent-knowledge-orbit-head,.d-agent-agent-knowledge-orbit-foot{right:12px;left:12px}.d-agent-agent-knowledge-orbit-node{font-size:8px}}
@media(prefers-reduced-motion:reduce){.d-agent-agent-knowledge-orbit *,.d-agent-agent-knowledge-orbit *::before,.d-agent-agent-knowledge-orbit *::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}
`,
  js:`const canvas=root.querySelector('.d-agent-agent-knowledge-orbit-canvas'),svg=root.querySelector('.d-agent-agent-knowledge-orbit-svg'),ringLayer=root.querySelector('.d-agent-agent-knowledge-orbit-rings'),relationLayer=root.querySelector('.d-agent-agent-knowledge-orbit-relations'),nodesLayer=root.querySelector('.d-agent-agent-knowledge-orbit-nodes'),centerChip=root.querySelector('.d-agent-agent-knowledge-orbit-center'),centerLabel=centerChip.querySelector('span'),statusElement=root.querySelector('.d-agent-agent-knowledge-orbit-status');
const concepts=[{id:'design-systems',label:'design systems',neighbors:['components','tokens','patterns']},{id:'components',label:'components',neighbors:['patterns','tokens','docs']},{id:'tokens',label:'tokens',neighbors:['color','type','themes']},{id:'type',label:'type',neighbors:['tokens','layout','a11y']},{id:'color',label:'color',neighbors:['tokens','themes']},{id:'patterns',label:'patterns',neighbors:['components','motion','layout']},{id:'a11y',label:'a11y',neighbors:['components','type','motion']},{id:'motion',label:'motion',neighbors:['patterns','a11y']},{id:'layout',label:'layout',neighbors:['type','patterns']},{id:'themes',label:'themes',neighbors:['tokens','color','components']},{id:'tooling',label:'tooling',neighbors:['docs','components','tokens']},{id:'docs',label:'docs',neighbors:['tooling','components','rules']},{id:'rules',label:'rules',neighbors:['docs','a11y','patterns']}],byId=Object.fromEntries(concepts.map(function(item){return[item.id,item]})),radii=[48,68,90],periods=[40000,70000,110000],reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,controller=new AbortController(),listener={signal:controller.signal};
const orbitIds=concepts.slice(1).map(function(item){return item.id}),slots=orbitIds.map(function(id,index){const ring=Math.floor(index/4),position=index%4,offset=ring===0?0:Math.PI/4;return{index:index,ring:ring,conceptId:id,baseAngle:offset+position*Math.PI/2,phase:index*1.61803398875,pull:0,x:0,y:0}});let centerId='design-systems',clock=0,lastWall=0,frameId=0,running=false,visible=!('IntersectionObserver'in window),documentVisible=document.visibilityState!=='hidden',cleaned=false,hoveredId=null,focusedId=null,relationId=null,relationStart=0,promotion=null,resizeObserver=null,intersectionObserver=null,connectionObserver=null;
function clamp(value,min,max){return Math.max(min,Math.min(max,value))}function cubicCoordinate(parameter,first,second){const inverse=1-parameter;return 3*inverse*inverse*parameter*first+3*inverse*parameter*parameter*second+parameter*parameter*parameter}function cubicBezier(value,x1,y1,x2,y2){const bounded=clamp(value,0,1);if(bounded===0||bounded===1)return bounded;let low=0,high=1;for(let index=0;index<32;index++){const parameter=(low+high)/2;if(cubicCoordinate(parameter,x1,x2)<bounded)low=parameter;else high=parameter}return cubicCoordinate((low+high)/2,y1,y2)}
function activeId(){return focusedId||hoveredId}function activeNeighbors(){const id=activeId(),concept=id&&byId[id];return new Set(concept?concept.neighbors:[])}function layout(){const width=canvas.clientWidth||322,height=canvas.clientHeight||222;return{width:width,height:height,cx:width/2,cy:height/2,scale:Math.min(1,(Math.min(width,height)-22)/180)}}
function slotPosition(slot,current){const radius=(radii[slot.ring]+4*Math.sin(clock*Math.PI*2/(9000+slot.ring*1700)+slot.phase)-slot.pull)*current.scale,angle=slot.baseAngle+clock*Math.PI*2/periods[slot.ring];return{x:current.cx+Math.cos(angle)*radius,y:current.cy+Math.sin(angle)*radius,angle:angle,radius:radius}}
function createNodes(){slots.forEach(function(slot){const button=document.createElement('button'),content=document.createElement('span'),dot=document.createElement('i'),label=document.createElement('span');button.type='button';button.className='d-agent-agent-knowledge-orbit-node';button.dataset.slot=String(slot.index);dot.className='d-agent-agent-knowledge-orbit-node-dot';dot.setAttribute('aria-hidden','true');label.className='d-agent-agent-knowledge-orbit-node-label';content.appendChild(dot);content.appendChild(label);button.appendChild(content);nodesLayer.appendChild(button)})}
function syncRings(current){if(!ringLayer.children.length)radii.forEach(function(){const circle=document.createElementNS(svg.namespaceURI,'circle');ringLayer.appendChild(circle)});[...ringLayer.children].forEach(function(circle,index){circle.setAttribute('cx',current.cx.toFixed(3));circle.setAttribute('cy',current.cy.toFixed(3));circle.setAttribute('r',(radii[index]*current.scale).toFixed(3))});svg.setAttribute('viewBox','0 0 '+current.width+' '+current.height)}
function drawLine(from,to,progress){const line=document.createElementNS(svg.namespaceURI,'line'),length=Math.hypot(to.x-from.x,to.y-from.y);line.setAttribute('x1',from.x.toFixed(3));line.setAttribute('y1',from.y.toFixed(3));line.setAttribute('x2',to.x.toFixed(3));line.setAttribute('y2',to.y.toFixed(3));line.setAttribute('stroke-dasharray',length.toFixed(3));line.setAttribute('stroke-dashoffset',(length*(1-progress)).toFixed(3));relationLayer.appendChild(line)}
function render(){const current=layout(),selected=activeId(),neighbors=activeNeighbors(),relationProgress=selected?(reduced?1:cubicBezier(clamp((clock-relationStart)/250,0,1),.22,1,.36,1)):0,promotionProgress=promotion?(reduced?1:cubicBezier(clamp(promotion.elapsed/400,0,1),.42,0,.58,1)):1;syncRings(current);centerLabel.textContent=byId[centerId].label;centerChip.style.transform=promotion?'translate('+(promotion.centerDx*(1-promotionProgress)).toFixed(3)+'px,'+(promotion.centerDy*(1-promotionProgress)).toFixed(3)+'px) scale('+(0.82+.18*promotionProgress).toFixed(6)+')':'none';centerChip.style.opacity=promotion?(0.35+0.65*promotionProgress).toFixed(3):'1';const positions={};slots.forEach(function(slot){const point=slotPosition(slot,current),button=nodesLayer.children[slot.index],concept=byId[slot.conceptId],side=Math.cos(point.angle)<0?'left':'right',left=side==='left'?point.x-67:point.x-5;slot.x=point.x;slot.y=point.y;positions[slot.conceptId]=point;button.dataset.conceptId=slot.conceptId;button.dataset.side=side;button.style.left=clamp(left,0,current.width-72).toFixed(3)+'px';button.style.top=clamp(point.y-10,0,current.height-20).toFixed(3)+'px';button.style.opacity=selected?(slot.conceptId===selected?'1':neighbors.has(slot.conceptId)?'.74':'.3'):'1';button.style.transform=promotion&&promotion.slotIndex===slot.index?'translate('+(promotion.nodeDx*(1-promotionProgress)).toFixed(3)+'px,'+(promotion.nodeDy*(1-promotionProgress)).toFixed(3)+'px) scale('+(1.16-.16*promotionProgress).toFixed(6)+')':'none';button.querySelector('.d-agent-agent-knowledge-orbit-node-label').textContent=concept.label;button.querySelector('.d-agent-agent-knowledge-orbit-node-label').style.opacity=promotion?(0.35+0.65*promotionProgress).toFixed(3):'1';button.setAttribute('aria-label','Promote '+concept.label+' to the center')});relationLayer.replaceChildren();if(selected&&positions[selected]&&!promotion){drawLine(positions[selected],{x:current.cx,y:current.cy},relationProgress);neighbors.forEach(function(id){if(positions[id])drawLine(positions[selected],positions[id],relationProgress)})}}
function setRelation(id){const next=id||null;if(relationId!==next){relationId=next;relationStart=clock}if(reduced)pullTargets(1);render();start()}
function pullTargets(delta){const selected=activeId(),neighbors=activeNeighbors();slots.forEach(function(slot){const target=slot.conceptId===selected?20:neighbors.has(slot.conceptId)?10:0;if(reduced)slot.pull=target;else slot.pull+=(target-slot.pull)*delta})}
function eligible(){return!cleaned&&!reduced&&visible&&documentVisible}function requestFrame(){if(!frameId&&eligible())frameId=requestAnimationFrame(tick)}function start(){if(!eligible())return;running=true;requestFrame()}function stop(){if(frameId){cancelAnimationFrame(frameId);frameId=0}running=false;lastWall=0}
function finishPromotion(){if(!promotion)return;promotion=null;statusElement.textContent=byId[centerId].label+' is now the center concept';render()}
function tick(now){frameId=0;if(!eligible()){running=false;lastWall=0;return}const delta=lastWall?Math.min(50,Math.max(0,now-lastWall)):0;lastWall=now;clock+=delta;const alpha=1-Math.pow(.85,delta/(1000/60));pullTargets(alpha);if(promotion){promotion.elapsed+=delta;if(promotion.elapsed>=400)finishPromotion()}render();requestFrame()}
function promote(slotIndex){if(cleaned||promotion)return;const slot=slots[slotIndex],current=layout(),clickedPoint=slotPosition(slot,current),oldCenter=centerId,newCenter=slot.conceptId;centerId=newCenter;slot.conceptId=oldCenter;hoveredId=null;focusedId=null;relationId=null;slot.pull=0;const orbitPoint=slotPosition(slot,current);if(reduced){statusElement.textContent=byId[centerId].label+' is now the center concept';render();return}promotion={slotIndex:slotIndex,elapsed:0,nodeDx:current.cx-orbitPoint.x,nodeDy:current.cy-orbitPoint.y,centerDx:clickedPoint.x-current.cx,centerDy:clickedPoint.y-current.cy};statusElement.textContent='Promoting '+byId[centerId].label+' to the center';render();start()}
nodesLayer.addEventListener('pointerover',function(event){const button=event.target.closest&&event.target.closest('.d-agent-agent-knowledge-orbit-node');if(!button)return;hoveredId=button.dataset.conceptId;setRelation(activeId())},listener);nodesLayer.addEventListener('pointerout',function(event){const button=event.target.closest&&event.target.closest('.d-agent-agent-knowledge-orbit-node');if(!button)return;const related=event.relatedTarget&&event.relatedTarget.closest&&event.relatedTarget.closest('.d-agent-agent-knowledge-orbit-node');if(related===button)return;hoveredId=null;setRelation(activeId())},listener);nodesLayer.addEventListener('focusin',function(event){const button=event.target.closest&&event.target.closest('.d-agent-agent-knowledge-orbit-node');if(!button)return;focusedId=button.dataset.conceptId;setRelation(activeId())},listener);nodesLayer.addEventListener('focusout',function(event){const button=event.target.closest&&event.target.closest('.d-agent-agent-knowledge-orbit-node');if(!button)return;focusedId=null;setRelation(activeId())},listener);nodesLayer.addEventListener('click',function(event){const button=event.target.closest&&event.target.closest('.d-agent-agent-knowledge-orbit-node');if(button)promote(Number(button.dataset.slot))},listener);
function onVisibility(){documentVisible=document.visibilityState!=='hidden';lastWall=0;if(documentVisible)start();else stop()}document.addEventListener('visibilitychange',onVisibility,listener);if('ResizeObserver'in window){resizeObserver=new ResizeObserver(function(){if(!cleaned)render()});resizeObserver.observe(canvas)}if('IntersectionObserver'in window){intersectionObserver=new IntersectionObserver(function(entries){if(!entries.length||cleaned)return;visible=entries[0].isIntersecting;lastWall=0;if(visible)start();else stop()},{threshold:.05});intersectionObserver.observe(root)}function cleanup(){if(cleaned)return;cleaned=true;stop();controller.abort();if(resizeObserver)resizeObserver.disconnect();if(intersectionObserver)intersectionObserver.disconnect();if(connectionObserver)connectionObserver.disconnect()}if('MutationObserver'in window){connectionObserver=new MutationObserver(function(){if(!root.isConnected)cleanup()});connectionObserver.observe(document.documentElement,{childList:true,subtree:true})}
createNodes();pullTargets(1);render();start();`,
  prompt:`Build a self-contained responsive semantic knowledge orbit. Keep one center query chip and twelve deterministic concept buttons distributed four per ring across radii 48, 68 and 90 pixels. Rotate the rings at 40, 70 and 110 seconds per revolution and add a unique sine-based radial wobble of plus or minus four pixels. Hover or focus a concept to ease it twenty pixels inward, pull its precomputed neighbors ten pixels inward, dim unrelated concepts to thirty percent, and draw one-pixel accent relation lines to the center and neighbors over 250 milliseconds. Activating any concept swaps it with the center through a 400 millisecond ease-in-out FLIP and refades labels. Use keyboard-accessible native buttons, polite promotion announcements, visibility lifecycle cleanup, and a complete static reduced-motion state with no animation-frame requests. Keep chrome grayscale on #0a0a0b, use Roboto Mono with JetBrains Mono fallback, and reserve #fa7319 for the current query, relations, hover, and focus.`
});
