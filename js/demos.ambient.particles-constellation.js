/* INTRX registry - seeded canvas constellation with pointer force modes */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'particles-constellation',title:'Particles Constellation',cat:'Ambient',rootClass:'d-constellation-demo',tags:['canvas','particles','proximity'],libs:[],desc:'A deterministic particle field drifts across a DPR-aware canvas and draws links only below an 86px threshold. Pointer force falls off with distance and can switch between attraction and repulsion without changing the shared simulation clock.',seen:'Seen on: technology homepages, network products, creative studios, AI tools, and ambient hero backgrounds',hint:'inspect the seeded frame, move through the field in attract mode, switch to repel, pause, and reset the exact constellation',
html:`<div class="d-constellation-demo"><header><span>AMBIENT / PROXIMITY NETWORK</span><span class="d-constellation-readout">RUNNING / ATTRACT / 28 NODES</span></header><div class="d-constellation-stage"><canvas aria-label="Interactive particle constellation"></canvas><div class="d-constellation-hud"><span>LINK &lt; 86PX</span><span>FORCE R 120PX</span><b>SEEDED / 1907</b></div><p class="d-constellation-spec">28 PARTICLES / WRAP EDGES / DISTANCE ALPHA / ONE RAF</p></div><footer><button class="d-constellation-inspect" type="button">Inspect seed</button><button class="d-constellation-mode" type="button">Mode repel</button><button class="d-constellation-toggle" type="button">Pause field</button><button class="d-constellation-reset" type="button">Reset seed</button><span class="d-constellation-status" aria-live="polite">Constellation running in attract mode</span></footer></div>`,
css:`.d-constellation-demo{width:100%;min-height:555px;box-sizing:border-box;padding:20px clamp(5px,4vw,28px);color:#dfe9e8;background:#172326;font-family:Roboto Mono,JetBrains Mono,monospace}.d-constellation-demo>header{display:flex;justify-content:space-between;color:#617579;font:700 8px/1 ui-monospace,monospace;letter-spacing:.14em}.d-constellation-readout{color:#68c4c0}.d-constellation-stage{position:relative;height:430px;margin-top:13px;overflow:hidden;border:1px solid #30494e;background:radial-gradient(circle at 50% 44%,rgba(68,145,145,.11),transparent 38%),#1b2a2e}.d-constellation-stage canvas{display:block;width:100%;height:100%;cursor:crosshair}.d-constellation-hud{position:absolute;left:16px;right:16px;top:15px;display:flex;gap:20px;color:#60767a;font:700 6px/1 ui-monospace,monospace;pointer-events:none}.d-constellation-hud b{margin-left:auto;color:#68c4c0}.d-constellation-spec{position:absolute;left:11px;bottom:9px;margin:0;color:#617579;font:700 6px/1 ui-monospace,monospace;letter-spacing:.1em;pointer-events:none}.d-constellation-demo footer{display:flex;justify-content:center;gap:5px;margin-top:10px;flex-wrap:wrap}.d-constellation-demo footer button{min-height:32px;padding:7px 8px;border:1px solid #334b50;color:#8ba0a3;background:#203136;font:700 7px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-constellation-demo footer button:hover,.d-constellation-demo footer button:focus-visible{color:#132325;background:#68c4c0;outline:none}.d-constellation-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}@media(max-width:540px){.d-constellation-demo{padding-inline:4px}.d-constellation-hud{gap:8px}}`,
js:`const canvas=root.querySelector('canvas'),ctx=canvas.getContext('2d'),readout=root.querySelector('.d-constellation-readout'),status=root.querySelector('.d-constellation-status'),inspect=root.querySelector('.d-constellation-inspect'),modeButton=root.querySelector('.d-constellation-mode'),toggleButton=root.querySelector('.d-constellation-toggle'),resetButton=root.querySelector('.d-constellation-reset');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;let particles=[],pointer={x:0,y:0,active:false},mode='attract',running=false,frames=0,links=0,toggles=0,raf=0,w=0,h=0;function randomFactory(seed){return()=>((seed=Math.imul(seed,1664525)+1013904223>>>0)/4294967296)}function seed(){const random=randomFactory(1907);particles=Array.from({length:28},()=>({x:random()*w,y:random()*h,vx:(random()-.5)*.38,vy:(random()-.5)*.38,r:1.2+random()*1.8}))}function resize(){const rect=canvas.getBoundingClientRect(),dpr=Math.min(devicePixelRatio||1,2);w=rect.width;h=rect.height;canvas.width=Math.round(w*dpr);canvas.height=Math.round(h*dpr);ctx.setTransform(dpr,0,0,dpr,0,0)}function checksum(){return particles.reduce((sum,p)=>(sum+p.x*3+p.y*5),0).toFixed(2)}function draw(source){ctx.clearRect(0,0,w,h);links=0;ctx.lineWidth=.7;for(let i=0;i<particles.length;i++)for(let j=i+1;j<particles.length;j++){const a=particles[i],b=particles[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<86){links++;ctx.strokeStyle='rgba(104,196,192,'+((1-d/86)*.42).toFixed(3)+')';ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}particles.forEach(p=>{ctx.fillStyle='#86d4cf';ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()});if(pointer.active){ctx.strokeStyle='rgba(104,196,192,.25)';ctx.beginPath();ctx.arc(pointer.x,pointer.y,18,0,Math.PI*2);ctx.stroke()}root.dataset.running=String(running);root.dataset.mode=mode;root.dataset.frames=String(frames);root.dataset.nodes=String(particles.length);root.dataset.links=String(links);root.dataset.pointer=pointer.active?pointer.x.toFixed(1)+','+pointer.y.toFixed(1):'none';root.dataset.checksum=checksum();root.dataset.toggles=String(toggles);root.dataset.source=source;readout.textContent=(running?'RUNNING':'PAUSED')+' / '+mode.toUpperCase()+' / '+particles.length+' NODES'}function step(){if(!running)return;particles.forEach(p=>{if(pointer.active){const dx=pointer.x-p.x,dy=pointer.y-p.y,d=Math.hypot(dx,dy)||1;if(d<120){const force=(1-d/120)*.012*(mode==='attract'?1:-1);p.vx+=dx/d*force;p.vy+=dy/d*force}}p.vx*=.995;p.vy*=.995;p.x=(p.x+p.vx+w)%w;p.y=(p.y+p.vy+h)%h});frames++;draw('frame');raf=requestAnimationFrame(step)}function setRunning(next,source){cancelAnimationFrame(raf);running=next&&!reduced;toggles++;draw(source);toggleButton.textContent=running?'Pause field':'Run field';if(running)raf=requestAnimationFrame(step)}canvas.addEventListener('pointermove',event=>{const rect=canvas.getBoundingClientRect();pointer={x:event.clientX-rect.left,y:event.clientY-rect.top,active:true};draw('pointer')});canvas.addEventListener('pointerleave',()=>{pointer.active=false;draw('leave')});inspect.addEventListener('click',()=>{cancelAnimationFrame(raf);running=false;frames=0;pointer.active=false;seed();draw('inspect');toggleButton.textContent='Run field';status.textContent='Seeded constellation frozen for inspection'});modeButton.addEventListener('click',()=>{mode=mode==='attract'?'repel':'attract';modeButton.textContent=mode==='attract'?'Mode repel':'Mode attract';draw('mode');status.textContent='Constellation mode '+mode});toggleButton.addEventListener('click',()=>{setRunning(!running,'toggle');status.textContent=running?'Constellation running':'Constellation paused'});resetButton.addEventListener('click',()=>{cancelAnimationFrame(raf);mode='attract';pointer.active=false;frames=0;toggles++;seed();running=false;modeButton.textContent='Mode repel';toggleButton.textContent='Run field';draw('reset');status.textContent='Constellation reset to seed 1907'});resize();seed();draw('initial');if(!reduced)setRunning(true,'auto');else status.textContent='Reduced motion: seeded static constellation';`,
prompt:`Build a DPR-aware canvas constellation with 28 particles from deterministic seed 1907. Wrap edges, draw links under 86px with alpha=(1-distance/86)*.42, and apply pointer force inside 120px with distance falloff and switchable attract/repel sign. Use one RAF.

Provide frozen seed inspection, pause/resume, reset, reduced motion/live status, and expose running/mode/frames/nodes/links/pointer/checksum/toggles/source data attributes.`});
