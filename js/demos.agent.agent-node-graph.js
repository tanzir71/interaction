/* INTRX registry - deterministic executable agent node graph */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
  id:'agent-node-graph',
  title:'Agent Node Graph',
  cat:'Agent & AI UI',
  rootClass:'d-agent-agent-node-graph',
  tags:['agents','nodes','dag','drag'],
  libs:[],
  desc:'A draggable executable DAG sends deterministic agent activity through live cubic connectors and supports cycle-safe port-to-port composition.',
  seen:'Seen on: acpx Agentic Graphs by Onur Solmaz and connector UI explorations by eminimnim',
  hint:'drag nodes · connect ports · → steps',
  html:`<div class="d-agent-agent-node-graph" role="region" aria-label="Executable agent node graph">
  <div class="d-agent-agent-node-graph-head" aria-hidden="true"><span>AGENT / NODE GRAPH</span><span class="d-agent-agent-node-graph-live"><i></i> EXECUTION</span></div>
  <div class="d-agent-agent-node-graph-stage" role="application" tabindex="0" aria-keyshortcuts="ArrowUp ArrowDown ArrowLeft ArrowRight Shift+ArrowUp Shift+ArrowDown Shift+ArrowLeft Shift+ArrowRight Enter Space C Escape End Home R" aria-label="Executable agent graph. Drag nodes or drag from an output port to an input port. Up and Down select nodes. Shift plus an arrow moves the selected node. C starts a keyboard connection, Up and Down choose a target, and Enter connects. Enter or Space pauses or resumes. Right Arrow pauses and steps. End creates the deterministic test connection. Home or R resets.">
    <div class="d-agent-agent-node-graph-canvas">
      <svg class="d-agent-agent-node-graph-edges" role="img" aria-label="Directed acyclic graph with five nodes and five connections" preserveAspectRatio="none"><defs class="d-agent-agent-node-graph-defs"></defs><g class="d-agent-agent-node-graph-edge-layer"></g><g class="d-agent-agent-node-graph-tail-layer"></g><path class="d-agent-agent-node-graph-preview" hidden></path></svg>
      <div class="d-agent-agent-node-graph-nodes" role="list" aria-label="Executable agent nodes"></div>
    </div>
  </div>
  <div class="d-agent-agent-node-graph-foot" aria-hidden="true"><span>DRAG NODES / CONNECT PORTS</span><span class="d-agent-agent-node-graph-mode">IDLE</span></div>
  <span class="d-agent-agent-node-graph-status" aria-live="polite" aria-atomic="true">Agent graph ready</span>
</div>`,
  css:`
.d-agent-agent-node-graph{position:relative;width:100%;height:320px;box-sizing:border-box;overflow:hidden;contain:layout paint;container-type:inline-size;background:#0a0a0b;color:#ececef;font-family:'JetBrains Mono',ui-monospace,SFMono-Regular,Consolas,monospace;isolation:isolate}
.d-agent-agent-node-graph *{box-sizing:border-box}
.d-agent-agent-node-graph-head{position:absolute;z-index:4;top:15px;left:16px;right:16px;display:flex;align-items:center;justify-content:space-between;color:#5c5c66;font-size:10px;line-height:1;letter-spacing:.08em}
.d-agent-agent-node-graph-live{display:flex;align-items:center;gap:5px;color:#9b9ba3}
.d-agent-agent-node-graph-live i{width:5px;height:5px;border-radius:50%;background:#a78bfa;box-shadow:0 0 7px rgba(167,139,250,.35)}
.d-agent-agent-node-graph-stage{position:absolute;top:39px;right:12px;bottom:25px;left:12px;padding:16px;overflow:hidden;border:1px solid #232327;border-radius:10px;background:#101012;outline:none;touch-action:pan-y}
.d-agent-agent-node-graph-stage:focus-visible{border-color:#a78bfa;box-shadow:0 0 0 1px #a78bfa}
.d-agent-agent-node-graph-canvas{position:relative;width:100%;height:100%;overflow:visible;border-radius:6px;background-color:#0a0a0b;background-image:radial-gradient(circle,rgba(46,46,52,.65) 0 1px,transparent 1.2px);background-size:18px 18px}
.d-agent-agent-node-graph-edges,.d-agent-agent-node-graph-nodes{position:absolute;inset:0;width:100%;height:100%;overflow:visible}
.d-agent-agent-node-graph-edges{z-index:1;pointer-events:none}
.d-agent-agent-node-graph-edge{fill:none;stroke:#2e2e34;stroke-width:1.5;stroke-linecap:round;vector-effect:non-scaling-stroke}
.d-agent-agent-node-graph-edge-new{stroke:#a78bfa}
.d-agent-agent-node-graph-tail{fill:none;stroke-width:2;stroke-linecap:round;vector-effect:non-scaling-stroke}
.d-agent-agent-node-graph-pulse{r:2;fill:#a78bfa;filter:drop-shadow(0 0 4px rgba(167,139,250,.65))}
.d-agent-agent-node-graph-tail[hidden],.d-agent-agent-node-graph-pulse[hidden]{display:none}
.d-agent-agent-node-graph-preview{fill:none;stroke:#a78bfa;stroke-width:1.5;stroke-linecap:round;stroke-dasharray:5 5;vector-effect:non-scaling-stroke;opacity:.8}
.d-agent-agent-node-graph-nodes{z-index:2;pointer-events:none}
.d-agent-agent-node-graph-node{position:absolute;height:42px;pointer-events:auto;touch-action:none;cursor:grab;transform-origin:50% 50%}
.d-agent-agent-node-graph-node[data-dragging="true"]{cursor:grabbing;z-index:3}
.d-agent-agent-node-graph-node-card{position:absolute;inset:0;display:flex;align-items:center;padding:0 7px;overflow:hidden;border:1px solid #232327;border-radius:8px;background:#101012;transform-origin:50% 50%}
.d-agent-agent-node-graph-node[data-selected="true"] .d-agent-agent-node-graph-node-card,.d-agent-agent-node-graph-node[data-dragging="true"] .d-agent-agent-node-graph-node-card{border-color:#2e2e34}
.d-agent-agent-node-graph-node-head{min-width:0;display:flex;align-items:center;gap:5px}
.d-agent-agent-node-graph-status-dot{width:6px;height:6px;flex:none;border-radius:50%;background:#5c5c66;transform-origin:50% 50%}
.d-agent-agent-node-graph-node[data-status="active"] .d-agent-agent-node-graph-status-dot{background:#a78bfa}
.d-agent-agent-node-graph-node[data-status="success"] .d-agent-agent-node-graph-status-dot{background:#4ade80;box-shadow:0 0 7px rgba(74,222,128,.3)}
.d-agent-agent-node-graph-node-label{min-width:0;overflow:hidden;color:#9b9ba3;font-size:9px;line-height:1;letter-spacing:.05em;white-space:nowrap;text-overflow:clip}
.d-agent-agent-node-graph-node[data-status="active"] .d-agent-agent-node-graph-node-label{color:#ececef}
.d-agent-agent-node-graph-port{position:absolute;z-index:5;top:50%;width:20px;height:20px;margin-top:-10px;pointer-events:auto;cursor:crosshair}
.d-agent-agent-node-graph-port-input{left:-10px}.d-agent-agent-node-graph-port-output{right:-10px}
.d-agent-agent-node-graph-port::before{content:'';position:absolute;top:7px;left:7px;width:6px;height:6px;border:1px solid #2e2e34;border-radius:50%;background:#101012}
.d-agent-agent-node-graph-port[data-hovered="true"]::before,.d-agent-agent-node-graph-port[data-valid="true"]::before{border-color:#a78bfa;box-shadow:0 0 0 2px rgba(167,139,250,.35),0 0 7px rgba(167,139,250,.35)}
.d-agent-agent-node-graph-port[data-invalid="true"]::before{border-color:#f87171}
.d-agent-agent-node-graph-foot{position:absolute;left:16px;right:16px;bottom:9px;display:flex;justify-content:space-between;color:#5c5c66;font-size:10px;line-height:1;letter-spacing:.08em}
.d-agent-agent-node-graph-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);clip-path:inset(50%);white-space:nowrap}
@container(max-width:340px){.d-agent-agent-node-graph-stage{right:10px;left:10px}.d-agent-agent-node-graph-head,.d-agent-agent-node-graph-foot{left:12px;right:12px}.d-agent-agent-node-graph-foot{font-size:8px}.d-agent-agent-node-graph-node-card{padding:0 4px}.d-agent-agent-node-graph-node-head{gap:4px}.d-agent-agent-node-graph-node-label{font-size:8px}}
@media(prefers-reduced-motion:reduce){.d-agent-agent-node-graph *,.d-agent-agent-node-graph *::before,.d-agent-agent-node-graph *::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}
`,
  js:`const graphStage=root.querySelector('.d-agent-agent-node-graph-stage'),canvas=root.querySelector('.d-agent-agent-node-graph-canvas'),svg=root.querySelector('.d-agent-agent-node-graph-edges'),defs=root.querySelector('.d-agent-agent-node-graph-defs'),edgeLayer=root.querySelector('.d-agent-agent-node-graph-edge-layer'),tailLayer=root.querySelector('.d-agent-agent-node-graph-tail-layer'),previewPath=root.querySelector('.d-agent-agent-node-graph-preview'),nodesLayer=root.querySelector('.d-agent-agent-node-graph-nodes'),modeElement=root.querySelector('.d-agent-agent-node-graph-mode'),statusElement=root.querySelector('.d-agent-agent-node-graph-status'),svgNamespace=svg.namespaceURI,instanceIndex=Math.max(0,[...document.querySelectorAll('.d-agent-agent-node-graph')].indexOf(root)),instancePrefix='agent-node-graph-'+instanceIndex;
const nodeTemplates=[{index:0,id:'root',label:'ROOT',nx:0,ny:.5},{index:1,id:'plan',label:'PLAN',nx:.5,ny:2/15},{index:2,id:'tools',label:'TOOLS',nx:.5,ny:13/15},{index:3,id:'write',label:'WRITE',nx:1,ny:2/15},{index:4,id:'check',label:'CHECK',nx:1,ny:13/15}],baseEdgeTemplates=[{index:0,id:'edge-0',from:'root',to:'plan',user:0},{index:1,id:'edge-1',from:'root',to:'tools',user:0},{index:2,id:'edge-2',from:'plan',to:'write',user:0},{index:3,id:'edge-3',from:'plan',to:'check',user:0},{index:4,id:'edge-4',from:'tools',to:'check',user:0}],deterministicEdge={index:5,id:'edge-5',from:'tools',to:'write',user:1};
const graphHeight=222,normalGraphWidth=322,narrowGraphWidth=266,normalNodeWidth=64,narrowNodeWidth=48,nodeHeight=42,widthBreakpoint=312,controlPull=60,edgeDuration=600,sinkDuration=300,okDuration=1000,cycleInterval=5000,morphDuration=200,liftDuration=160,tailFraction=.12,headDiameter=4,keyboardMove=4,maxDelta=50,maxEdges=10;
const colors={bg0:'#0a0a0b',bg1:'#101012',bg2:'#161619',line0:'#232327',line1:'#2e2e34',txt0:'#ececef',txt1:'#9b9ba3',txt2:'#5c5c66',acc:'#a78bfa',ok:'#4ade80',warn:'#fbbf24',err:'#f87171'};
const expectedChecksums={nodes:'ec90b5b8',edges:'e47b6751',normalGeometry:'de7f8969',narrowGeometry:'deb23f9f',plan:'19abba59',edgeEase:'c9ffec38',dotPulse:'7e0e816f',morphGap:'f991bf1f',lift:'2aad3cc0',fixture:'72d53fd5',motion:'d78249b3',extended:'cbcc3b80'};
function clamp(value,minimum,maximum){return Math.max(minimum,Math.min(maximum,value))}
function lerp(first,second,amount){return first+(second-first)*amount}
function checksumText(value){let hash=2166136261;for(const character of String(value))hash=Math.imul(hash^character.charCodeAt(0),16777619)>>>0;return hash.toString(16).padStart(8,'0')}
function cubicCoordinate(parameter,first,second){const inverse=1-parameter;return 3*inverse*inverse*parameter*first+3*inverse*parameter*parameter*second+parameter*parameter*parameter}
function cubicBezier(value,x1,y1,x2,y2){const bounded=clamp(value,0,1);if(bounded===0||bounded===1)return bounded;let low=0,high=1;for(let index=0;index<40;index++){const parameter=(low+high)/2;if(cubicCoordinate(parameter,x1,x2)<bounded)low=parameter;else high=parameter}return cubicCoordinate((low+high)/2,y1,y2)}
function edgeEase(milliseconds){return cubicBezier(clamp(milliseconds/edgeDuration,0,1),.65,0,.35,1)}
function softEase(milliseconds,duration){return cubicBezier(clamp(milliseconds/duration,0,1),.22,1,.36,1)}
function dotScale(milliseconds){return 1+.3*Math.pow(Math.sin(Math.PI*milliseconds/edgeDuration),2)}
function morphGap(milliseconds){return 5*(1-cubicBezier(clamp(milliseconds/morphDuration,0,1),.65,0,.35,1))}
function liftScale(milliseconds){return 1+.02*softEase(milliseconds,liftDuration)}
function motionSource(items){return items.map(function(item){return item.ms+','+item.value.toFixed(12)}).join('|')}
function fixtureLayout(width,height,itemWidth,itemHeight){return nodeTemplates.map(function(item){const x=item.nx*(width-itemWidth),y=item.ny*(height-itemHeight);return Object.assign({},item,{x:x,y:y,width:itemWidth,height:itemHeight,input:{x:x,y:y+itemHeight/2},output:{x:x+itemWidth,y:y+itemHeight/2}})})}
function fixtureGeometry(width,height,itemWidth,itemHeight){const fixtureNodes=fixtureLayout(width,height,itemWidth,itemHeight),byId=Object.fromEntries(fixtureNodes.map(function(item){return[item.id,item]})),nodeRows=fixtureNodes.map(function(item){return[item.id,item.x,item.y,item.width,item.height,item.input.x,item.input.y,item.output.x,item.output.y].map(function(value,index){return index?Number(value).toFixed(3):value}).join(',')}).join('|'),edgeRows=baseEdgeTemplates.map(function(edge){const start=byId[edge.from].output,end=byId[edge.to].input;return[edge.id,start.x,start.y,start.x+controlPull,start.y,end.x-controlPull,end.y,end.x,end.y].map(function(value,index){return index?Number(value).toFixed(3):value}).join(',')}).join('|');return{nodes:fixtureNodes,source:nodeRows+'||'+edgeRows,nodeRows:nodeRows,edgeRows:edgeRows}}
const normalGeometryFixture=fixtureGeometry(normalGraphWidth,graphHeight,normalNodeWidth,nodeHeight),narrowGeometryFixture=fixtureGeometry(narrowGraphWidth,graphHeight,narrowNodeWidth,nodeHeight);
const baseNodePlan=[['root',0,600,1600],['plan',600,1200,2200],['tools',600,1200,2200],['write',1200,1500,2500],['check',1200,1500,2500]],baseEdgePlan=[['edge-0',0,600],['edge-1',0,600],['edge-2',600,1200],['edge-3',600,1200],['edge-4',600,1200]],partialNodePlan=[['tools',0,600,1600],['write',600,900,1900],['check',600,900,1900]],partialEdgePlan=[['edge-4',0,600],['edge-5',0,600]];
const nodeSource=nodeTemplates.map(function(item){return[item.index,item.id,item.label,item.nx.toFixed(6),item.ny.toFixed(6)].join(',')}).join('|'),edgeSource=baseEdgeTemplates.map(function(item){return[item.index,item.id,item.from,item.to,item.user].join(',')}).join('|'),planSource=['base',baseNodePlan.map(function(item){return item.join(',')}).join(';'),baseEdgePlan.map(function(item){return item.join(',')}).join(';'),'reset,2500','next,5000','partial-tools',partialNodePlan.map(function(item){return item.join(',')}).join(';'),partialEdgePlan.map(function(item){return item.join(',')}).join(';'),'done,1900'].join('|');
const edgeEaseAnchors=[0,150,300,450,600].map(function(ms){return{ms:ms,value:edgeEase(ms)}}),dotPulseAnchors=[0,150,300,450,600].map(function(ms){return{ms:ms,value:dotScale(ms)}}),morphGapAnchors=[0,50,100,150,200].map(function(ms){return{ms:ms,value:morphGap(ms)}}),liftAnchors=[0,40,80,120,160].map(function(ms){return{ms:ms,value:liftScale(ms)}});
const actualChecksums={nodes:checksumText(nodeSource),edges:checksumText(edgeSource),normalGeometry:checksumText(normalGeometryFixture.source),narrowGeometry:checksumText(narrowGeometryFixture.source),plan:checksumText(planSource),edgeEase:checksumText(motionSource(edgeEaseAnchors)),dotPulse:checksumText(motionSource(dotPulseAnchors)),morphGap:checksumText(motionSource(morphGapAnchors)),lift:checksumText(motionSource(liftAnchors))};actualChecksums.fixture=checksumText([actualChecksums.nodes,actualChecksums.edges,actualChecksums.normalGeometry,actualChecksums.narrowGeometry,actualChecksums.plan].join('|'));actualChecksums.motion=checksumText([actualChecksums.edgeEase,actualChecksums.dotPulse,actualChecksums.morphGap,actualChecksums.lift].join('|'));actualChecksums.extended=checksumText([actualChecksums.fixture,actualChecksums.motion].join('|'));for(const key of Object.keys(expectedChecksums))if(actualChecksums[key]!==expectedChecksums[key])throw new Error('agent-node-graph deterministic fixture mismatch: '+key+' '+actualChecksums[key]);
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,controller=new AbortController(),listener={signal:controller.signal},passiveListener={signal:controller.signal,passive:true};
const counterNames=['rafRequests','rafCancels','schedulerFrames','schedulerStarts','schedulerStops','executionStarts','fullRuns','partialRuns','executionCompletions','executionInterruptions','nodeDrags','nodeMoves','dragCancels','connectionStarts','connectionCommits','connectionCancels','connectionRejects','edgeMorphs','pauseToggles','manualAdvances','resets','pointerDowns','pointerMoves','ignoredPointers','keyPauses','keyAdvances','keySelections','keyMoves','keyConnects','keyCancels','keyEnds','keyResets','focusEvents','blurEvents','visibilityChanges','intersectionChanges','resizeEvents','cleanupCalls'],counters={};counterNames.forEach(function(name){counters[name]=0});
let nodes=[],edges=[],phase='idle',source='initial',activeTime=0,nextAutoAt=0,userPaused=false,selectedNodeId='root',execution=null,edgeSerial=5,runSerial=0,drag=null,connection=null,hoveredPort=null,liftTween=null,focused=false,visible=!('IntersectionObserver'in window),documentVisible=document.visibilityState!=='hidden',running=false,cleaned=false,frameId=0,lastWall=0,resizeObserver=null,intersectionObserver=null,connectionObserver=null,lastNodes=[],lastEdges=[],lastPreview=null,lastLayout=null;
function cloneNode(item){return{index:item.index,id:item.id,label:item.label,nx:item.nx,ny:item.ny}}
function cloneEdge(item){return{index:item.index,id:item.id,from:item.from,to:item.to,user:item.user,createdAt:item.createdAt===undefined?null:item.createdAt,morphStart:item.morphStart===undefined?null:item.morphStart}}
function resetModel(){nodes=nodeTemplates.map(cloneNode);edges=baseEdgeTemplates.map(cloneEdge);phase='idle';source='reset';activeTime=0;nextAutoAt=0;userPaused=false;selectedNodeId='root';execution=null;edgeSerial=5;runSerial=0;drag=null;connection=null;hoveredPort=null;liftTween=null}
resetModel();source='initial';
function announce(text){statusElement.textContent=text}
function layout(){const width=canvas.clientWidth||normalGraphWidth,height=canvas.clientHeight||graphHeight,itemWidth=width>=widthBreakpoint?normalNodeWidth:narrowNodeWidth;const items=nodes.map(function(item){const x=item.nx*(width-itemWidth),y=item.ny*(height-nodeHeight);return Object.assign({},item,{x:x,y:y,width:itemWidth,height:nodeHeight,input:{x:x,y:y+nodeHeight/2},output:{x:x+itemWidth,y:y+nodeHeight/2}})});return{width:width,height:height,nodeWidth:itemWidth,nodeHeight:nodeHeight,items:items,byId:Object.fromEntries(items.map(function(item){return[item.id,item]}))}}
function controls(start,end){return{start:{x:start.x,y:start.y},control1:{x:start.x+controlPull,y:start.y},control2:{x:end.x-controlPull,y:end.y},end:{x:end.x,y:end.y}}}
function pathFor(points){return'M '+points.start.x.toFixed(3)+' '+points.start.y.toFixed(3)+' C '+points.control1.x.toFixed(3)+' '+points.control1.y.toFixed(3)+' '+points.control2.x.toFixed(3)+' '+points.control2.y.toFixed(3)+' '+points.end.x.toFixed(3)+' '+points.end.y.toFixed(3)}
function cubicPoint(points,parameter){const inverse=1-parameter,inverse2=inverse*inverse,parameter2=parameter*parameter;return{x:inverse2*inverse*points.start.x+3*inverse2*parameter*points.control1.x+3*inverse*parameter2*points.control2.x+parameter2*parameter*points.end.x,y:inverse2*inverse*points.start.y+3*inverse2*parameter*points.control1.y+3*inverse*parameter2*points.control2.y+parameter2*parameter*points.end.y}}
function tailPath(points,from,to){const rows=[];for(let index=0;index<=6;index++){const point=cubicPoint(points,lerp(from,to,index/6));rows.push((index?'L ':'M ')+point.x.toFixed(3)+' '+point.y.toFixed(3))}return rows.join(' ')}
function reaches(from,target){const stack=[from],seen=new Set();while(stack.length){const id=stack.pop();if(id===target)return true;if(seen.has(id))continue;seen.add(id);edges.forEach(function(edge){if(edge.from===id)stack.push(edge.to)})}return false}
function validateConnection(from,to){if(!from||!to||!nodes.some(function(item){return item.id===from})||!nodes.some(function(item){return item.id===to}))return{valid:false,reason:'no-target'};if(from===to)return{valid:false,reason:'self'};if(edges.some(function(edge){return edge.from===from&&edge.to===to}))return{valid:false,reason:'duplicate'};if(reaches(to,from))return{valid:false,reason:'cycle'};return{valid:true,reason:'valid'}}
function buildPlan(startId){const reachable=new Set([startId]),stack=[startId];while(stack.length){const id=stack.pop();edges.forEach(function(edge){if(edge.from===id&&!reachable.has(edge.to)){reachable.add(edge.to);stack.push(edge.to)}})}const orderedNodes=nodeTemplates.filter(function(item){return reachable.has(item.id)}),activeEdges=edges.filter(function(edge){return reachable.has(edge.from)&&reachable.has(edge.to)}).sort(function(a,b){return a.index-b.index}),indegree={};orderedNodes.forEach(function(item){indegree[item.id]=0});activeEdges.forEach(function(edge){indegree[edge.to]++});const queue=orderedNodes.filter(function(item){return indegree[item.id]===0}).sort(function(a,b){return a.index-b.index}),topological=[];while(queue.length){const node=queue.shift();topological.push(node);activeEdges.forEach(function(edge){if(edge.from===node.id){indegree[edge.to]--;if(indegree[edge.to]===0){queue.push(nodeTemplates.find(function(item){return item.id===edge.to}));queue.sort(function(a,b){return a.index-b.index})}}})}const levels={};levels[startId]=0;topological.forEach(function(node){if(levels[node.id]===undefined)levels[node.id]=0;activeEdges.forEach(function(edge){if(edge.from===node.id)levels[edge.to]=Math.max(levels[edge.to]===undefined?0:levels[edge.to],levels[node.id]+1)})});const edgeSchedule=activeEdges.map(function(edge){const arrive=levels[edge.to]*edgeDuration;return{id:edge.id,from:edge.from,to:edge.to,depart:arrive-edgeDuration,arrive:arrive}}),nodeSchedule=orderedNodes.map(function(node){const ignite=levels[node.id]*edgeDuration,outgoing=edgeSchedule.filter(function(edge){return edge.from===node.id}),finish=outgoing.length?Math.max.apply(null,outgoing.map(function(edge){return edge.arrive})):ignite+sinkDuration;return{id:node.id,level:levels[node.id],ignite:ignite,finish:finish,okEnd:finish+okDuration}}),done=Math.max.apply(null,nodeSchedule.map(function(item){return item.okEnd}));return{reachableNodes:orderedNodes.map(function(item){return item.id}),nodeSchedule:nodeSchedule,edgeSchedule:edgeSchedule,done:done}}
function startExecution(kind,startId,onset,nextSource,deliberate){if(execution)counters.executionInterruptions++;const plan=buildPlan(startId);execution={runId:'run-'+runSerial++,kind:kind,sourceNodeId:startId,start:onset,plan:plan};phase='executing';nextAutoAt=onset+cycleInterval;counters.executionStarts++;if(kind==='full')counters.fullRuns++;else counters.partialRuns++;source=nextSource||kind+' execution';if(deliberate)announce((kind==='full'?'Full':'Partial')+' graph execution started from '+startId.toUpperCase())}
function finishExecution(){if(!execution)return;execution=null;phase='idle';counters.executionCompletions++;source='execution complete'}
function executionElapsed(){return execution?Math.max(0,activeTime-execution.start):0}
function executionBoundaries(){if(!execution)return[];const values=[];execution.plan.edgeSchedule.forEach(function(item){values.push(item.depart,item.arrive)});execution.plan.nodeSchedule.forEach(function(item){values.push(item.ignite,item.finish,item.okEnd)});edges.forEach(function(edge){if(edge.morphStart!==null&&edge.morphStart!==undefined)values.push(edge.morphStart+ morphDuration-execution.start)});values.push(execution.plan.done);return[...new Set(values.filter(function(value){return value>=0}))].sort(function(a,b){return a-b})}
function nextBoundary(){if(!execution)return nextAutoAt>activeTime?nextAutoAt:null;const elapsed=executionElapsed();return executionBoundaries().find(function(value){return value>elapsed+1e-7})===undefined?null:executionBoundaries().find(function(value){return value>elapsed+1e-7})}
function settle(){edges.forEach(function(edge){if(edge.morphStart!==null&&edge.morphStart!==undefined&&activeTime-edge.morphStart>=morphDuration)edge.morphStart=null});if(execution&&executionElapsed()>=execution.plan.done)finishExecution();if(!execution&&activeTime>=nextAutoAt&&!userPaused&&!reduced)startExecution('full','root',nextAutoAt,'automatic execution',false)}
function advanceRuntime(delta){activeTime+=delta;if(liftTween){liftTween.elapsed+=delta;if(liftTween.elapsed>=liftDuration)liftTween=null}settle()}
function schedulerEligible(){return!cleaned&&!reduced&&!userPaused&&visible&&documentVisible}
function requestNextFrame(){if(frameId||!schedulerEligible())return;frameId=requestAnimationFrame(frame);counters.rafRequests++;expose()}
function startScheduler(nextSource){if(!schedulerEligible())return;if(!running){running=true;lastWall=0;counters.schedulerStarts++;if(nextSource)source=nextSource}requestNextFrame()}
function stopScheduler(nextSource){if(frameId){cancelAnimationFrame(frameId);frameId=0;counters.rafCancels++}if(running){running=false;counters.schedulerStops++}lastWall=0;if(nextSource)source=nextSource;expose()}
function frame(now){frameId=0;if(!schedulerEligible()){running=false;lastWall=0;expose();return}const delta=lastWall?Math.min(maxDelta,Math.max(0,now-lastWall)):0;lastWall=now;activeTime+=delta;counters.schedulerFrames++;if(liftTween){liftTween.elapsed+=delta;if(liftTween.elapsed>=liftDuration)liftTween=null}if(!execution&&activeTime>=nextAutoAt)startExecution('full','root',nextAutoAt,'automatic execution',false);settle();render('frame');requestNextFrame()}
function nodeStatus(id){if(!execution)return'idle';const row=execution.plan.nodeSchedule.find(function(item){return item.id===id});if(!row)return'idle';const elapsed=executionElapsed();if(elapsed<row.ignite)return'idle';if(elapsed<row.finish)return'active';if(elapsed<row.okEnd)return'success';return'idle'}
function nodePose(id){let scale=drag&&drag.nodeId===id?1.02:1;if(liftTween&&liftTween.nodeId===id){const value=liftScale(liftTween.elapsed);scale=liftTween.direction==='up'?value:2.02-value}const status=nodeStatus(id),row=execution&&execution.plan.nodeSchedule.find(function(item){return item.id===id}),elapsed=row?executionElapsed()-row.ignite:0,cycle=((elapsed%edgeDuration)+edgeDuration)%edgeDuration,dot=status==='active'?dotScale(cycle):1;return{scale:scale,dotScale:dot,dotGlow:status==='active'?.18+.22*Math.pow(Math.sin(Math.PI*cycle/edgeDuration),2):0}}
function findChild(parent,id){return[...parent.children].find(function(item){return item.dataset.id===id})||null}
function syncNodes(current){const wanted=new Set(current.items.map(function(item){return item.id}));[...nodesLayer.children].forEach(function(node){if(!wanted.has(node.dataset.id))node.remove()});lastNodes=current.items.map(function(item){let node=findChild(nodesLayer,item.id);if(!node){node=document.createElement('div');node.className='d-agent-agent-node-graph-node';node.dataset.id=item.id;node.id=instancePrefix+'-node-'+item.id;node.setAttribute('role','listitem');const card=document.createElement('div'),head=document.createElement('div'),dot=document.createElement('i'),label=document.createElement('span'),input=document.createElement('span'),output=document.createElement('span');card.className='d-agent-agent-node-graph-node-card';head.className='d-agent-agent-node-graph-node-head';dot.className='d-agent-agent-node-graph-status-dot';dot.setAttribute('aria-hidden','true');label.className='d-agent-agent-node-graph-node-label';label.textContent=item.label;input.className='d-agent-agent-node-graph-port d-agent-agent-node-graph-port-input';input.dataset.nodeId=item.id;input.dataset.direction='input';input.setAttribute('aria-hidden','true');output.className='d-agent-agent-node-graph-port d-agent-agent-node-graph-port-output';output.dataset.nodeId=item.id;output.dataset.direction='output';output.setAttribute('aria-hidden','true');head.appendChild(dot);head.appendChild(label);card.appendChild(head);node.appendChild(card);node.appendChild(input);node.appendChild(output);nodesLayer.appendChild(node)}const status=nodeStatus(item.id),pose=nodePose(item.id),aria=item.label+' node, '+(status==='success'?'finished':status);node.style.left=item.x.toFixed(3)+'px';node.style.top=item.y.toFixed(3)+'px';node.style.width=item.width.toFixed(3)+'px';node.style.height=item.height.toFixed(3)+'px';node.dataset.status=status;node.dataset.selected=String(item.id===selectedNodeId);node.dataset.dragging=String(!!drag&&drag.nodeId===item.id);node.setAttribute('aria-label',aria);if(item.id===selectedNodeId)node.setAttribute('aria-current','true');else node.removeAttribute('aria-current');node.querySelector('.d-agent-agent-node-graph-node-card').style.transform='scale('+pose.scale.toFixed(12)+')';const dot=node.querySelector('.d-agent-agent-node-graph-status-dot');dot.style.transform='scale('+pose.dotScale.toFixed(12)+')';dot.style.boxShadow=status==='active'?'0 0 7px rgba(167,139,250,'+pose.dotGlow.toFixed(6)+')':status==='success'?'0 0 7px rgba(74,222,128,.3)':'none';[...node.querySelectorAll('.d-agent-agent-node-graph-port')].forEach(function(port){const key=item.id+':'+port.dataset.direction,candidate=connection&&connection.targetNodeId===item.id&&port.dataset.direction==='input';port.dataset.hovered=String(hoveredPort===key);port.dataset.valid=String(!!candidate&&connection.valid);port.dataset.invalid=String(!!candidate&&!connection.valid)});return{index:item.index,id:item.id,label:item.label,nx:item.nx,ny:item.ny,x:item.x,y:item.y,width:item.width,height:item.height,status:status,pose:pose,ports:{input:{x:item.input.x,y:item.input.y,hovered:hoveredPort===item.id+':input',valid:!!connection&&connection.targetNodeId===item.id&&connection.valid},output:{x:item.output.x,y:item.output.y,hovered:hoveredPort===item.id+':output'}},ariaLabel:aria}});graphStage.setAttribute('aria-activedescendant',instancePrefix+'-node-'+selectedNodeId)}
function gradientId(id){return instancePrefix+'-'+id+'-tail'}
function syncEdges(current){const wanted=new Set(edges.map(function(item){return item.id}));[...edgeLayer.children].forEach(function(node){if(!wanted.has(node.dataset.id))node.remove()});[...tailLayer.children].forEach(function(node){if(!wanted.has(node.dataset.id))node.remove()});[...defs.children].forEach(function(node){if(!wanted.has(node.dataset.id))node.remove()});const elapsed=executionElapsed();lastEdges=edges.map(function(edge){const start=current.byId[edge.from].output,end=current.byId[edge.to].input,points=controls(start,end),path=pathFor(points),morphActive=edge.morphStart!==null&&edge.morphStart!==undefined,morphMs=morphActive?clamp(activeTime-edge.morphStart,0,morphDuration):morphDuration,morphProgress=morphActive?cubicBezier(morphMs/morphDuration,.65,0,.35,1):1,gap=morphActive?morphGap(morphMs):0,schedule=execution&&execution.plan.edgeSchedule.find(function(item){return item.id===edge.id}),pulseActive=!!schedule&&elapsed>=schedule.depart&&elapsed<schedule.arrive,raw=pulseActive?clamp((elapsed-schedule.depart)/edgeDuration,0,1):0,eased=pulseActive?edgeEase(raw*edgeDuration):0,head=pulseActive?cubicPoint(points,eased):null,tailStart=pulseActive?Math.max(0,eased-tailFraction):0,tail=pulseActive?tailPath(points,tailStart,eased):'';let group=findChild(edgeLayer,edge.id);if(!group){group=document.createElementNS(svgNamespace,'g');group.dataset.id=edge.id;group.classList.add('d-agent-agent-node-graph-edge-item');const base=document.createElementNS(svgNamespace,'path');base.classList.add('d-agent-agent-node-graph-edge');group.appendChild(base);edgeLayer.appendChild(group);const gradient=document.createElementNS(svgNamespace,'linearGradient');gradient.dataset.id=edge.id;gradient.id=gradientId(edge.id);gradient.setAttribute('gradientUnits','userSpaceOnUse');gradient.classList.add('d-agent-agent-node-graph-gradient');const stop0=document.createElementNS(svgNamespace,'stop'),stop1=document.createElementNS(svgNamespace,'stop');stop0.setAttribute('offset','0');stop0.setAttribute('stop-color','#a78bfa');stop0.setAttribute('stop-opacity','0');stop1.setAttribute('offset','1');stop1.setAttribute('stop-color','#a78bfa');gradient.appendChild(stop0);gradient.appendChild(stop1);defs.appendChild(gradient);const tailGroup=document.createElementNS(svgNamespace,'g');tailGroup.dataset.id=edge.id;tailGroup.classList.add('d-agent-agent-node-graph-tail-item');const tailNode=document.createElementNS(svgNamespace,'path'),pulseNode=document.createElementNS(svgNamespace,'circle');tailNode.classList.add('d-agent-agent-node-graph-tail');tailNode.setAttribute('stroke','url(#'+gradient.id+')');pulseNode.classList.add('d-agent-agent-node-graph-pulse');tailGroup.appendChild(tailNode);tailGroup.appendChild(pulseNode);tailLayer.appendChild(tailGroup)}const base=group.firstElementChild;base.setAttribute('d',path);base.setAttribute('stroke-dasharray',morphActive?'5 '+gap.toFixed(6):'none');base.classList.toggle('d-agent-agent-node-graph-edge-new',morphActive);const gradient=findChild(defs,edge.id),tailGroup=findChild(tailLayer,edge.id),tailNode=tailGroup.firstElementChild,pulseNode=tailGroup.lastElementChild;if(pulseActive){const tailPoint=cubicPoint(points,tailStart);gradient.setAttribute('x1',tailPoint.x.toFixed(3));gradient.setAttribute('y1',tailPoint.y.toFixed(3));gradient.setAttribute('x2',head.x.toFixed(3));gradient.setAttribute('y2',head.y.toFixed(3));tailNode.setAttribute('d',tail);tailNode.style.display='';pulseNode.setAttribute('cx',head.x.toFixed(3));pulseNode.setAttribute('cy',head.y.toFixed(3));pulseNode.style.display=''}else{tailNode.setAttribute('d','');tailNode.style.display='none';pulseNode.style.display='none'}return{index:edge.index,id:edge.id,from:edge.from,to:edge.to,user:edge.user,createdAt:edge.createdAt,path:path,points:points,morph:{active:morphActive,progress:morphProgress,gap:gap},pulse:{active:pulseActive,rawProgress:raw,easedProgress:eased,head:head,tailStart:tailStart,tailPath:tail}}});svg.setAttribute('viewBox','0 0 '+current.width+' '+current.height);svg.setAttribute('aria-label','Directed acyclic graph with five nodes and '+edges.length+' connections')}
function previewState(current){let mode='idle',visiblePreview=false,start=null,end=null,target=null,valid=false,reason='idle',pointerId=null,candidates=[],candidateIndex=-1;if(drag)mode='drag';if(connection){mode=connection.mode;visiblePreview=true;start=current.byId[connection.sourceNodeId].output;end=connection.end;target=connection.targetNodeId;valid=connection.valid;reason=connection.reason;pointerId=connection.pointerId===undefined?null:connection.pointerId;candidates=(connection.candidates||[]).slice();candidateIndex=connection.candidateIndex===undefined?-1:connection.candidateIndex}const points=start&&end?controls(start,end):null,path=points?pathFor(points):'';return{mode:mode,visible:visiblePreview,sourceNodeId:connection?connection.sourceNodeId:null,targetNodeId:target,candidateNodeId:target,valid:valid,reason:reason,pointerId:pointerId,start:start?{x:start.x,y:start.y}:null,end:end?{x:end.x,y:end.y}:null,control1:points?points.control1:null,control2:points?points.control2:null,path:path,keyboardCandidates:candidates,keyboardIndex:candidateIndex}}
function syncPreview(current){lastPreview=previewState(current);previewPath.hidden=!lastPreview.visible;if(lastPreview.visible)previewPath.setAttribute('d',lastPreview.path);else previewPath.setAttribute('d','')}
function modeText(){if(connection)return'CONNECT';if(drag)return'DRAG';if(userPaused&&!reduced)return'PAUSED';if(execution)return'RUN '+String(Number(execution.runId.split('-')[1])+1).padStart(2,'0');return'IDLE'}
function expose(){Object.assign(root.dataset,{phase:phase,activeTime:activeTime.toFixed(3),nextAutoAt:nextAutoAt.toFixed(3),nodeCount:String(nodes.length),edgeCount:String(edges.length),selected:selectedNodeId,preview:connection?connection.mode:'',execution:execution?execution.kind:'',reduced:String(reduced),paused:String(userPaused||reduced),running:String(running),visible:String(visible),documentVisible:String(documentVisible),cleaned:String(cleaned),frameId:String(frameId),fixtureChecksum:actualChecksums.fixture,motionChecksum:actualChecksums.motion,extendedChecksum:actualChecksums.extended,source:source})}
function render(nextSource){if(nextSource)source=nextSource;lastLayout=layout();syncNodes(lastLayout);syncEdges(lastLayout);syncPreview(lastLayout);modeElement.textContent=modeText();expose()}
function cancelGesture(reason,revert,announceChange){const captured=drag?drag.pointerId:connection&&connection.pointerId,hadDrag=!!drag,hadConnection=!!connection;if(drag&&revert){const node=nodes.find(function(item){return item.id===drag.nodeId});if(node){node.nx=drag.startNx;node.ny=drag.startNy}}drag=null;connection=null;if(hadDrag){counters.dragCancels++;liftTween=null}if(hadConnection)counters.connectionCancels++;if(captured!==null&&captured!==undefined&&graphStage.hasPointerCapture&&graphStage.hasPointerCapture(captured))graphStage.releasePointerCapture(captured);if(announceChange)announce(reason);render(reason)}
function startLift(nodeId,direction){if(reduced||userPaused||!visible||!documentVisible){liftTween=null;return}liftTween={nodeId:nodeId,direction:direction,elapsed:0}}
function localPoint(clientX,clientY){const rect=canvas.getBoundingClientRect();return{x:clamp(clientX-rect.left,0,rect.width),y:clamp(clientY-rect.top,0,rect.height)}}
function portAt(clientX,clientY){const target=document.elementFromPoint(clientX,clientY),port=target&&target.closest&&target.closest('.d-agent-agent-node-graph-port-input');if(!port||!root.contains(port)||port.dataset.direction!=='input')return null;return port.dataset.nodeId}
function updateConnectionTarget(clientX,clientY){if(!connection)return;const current=layout(),targetId=portAt(clientX,clientY),point=localPoint(clientX,clientY),validation=validateConnection(connection.sourceNodeId,targetId);connection.end=targetId?current.byId[targetId].input:point;connection.targetNodeId=targetId;connection.valid=validation.valid;connection.reason=validation.reason}
function commitEdge(from,to,nextSource){const validation=validateConnection(from,to);if(!validation.valid){counters.connectionRejects++;announce('Connection rejected: '+validation.reason);render('connection rejected');return false}const edge={index:edgeSerial,id:'edge-'+edgeSerial,from:from,to:to,user:1,createdAt:activeTime,morphStart:reduced?null:activeTime};edgeSerial++;edges.push(edge);counters.connectionCommits++;counters.edgeMorphs++;userPaused=false;startExecution('partial',from,activeTime,nextSource||'partial connection',true);announce('Connected '+from.toUpperCase()+' to '+to.toUpperCase()+'; partial execution started');render(nextSource||'connection committed');if(!reduced)startScheduler('connection scheduler');return true}
function startKeyboardConnection(){const current=layout(),candidates=nodeTemplates.map(function(item){return item.id}).filter(function(id){return validateConnection(selectedNodeId,id).valid});if(!candidates.length){counters.connectionRejects++;announce('No valid input ports for '+selectedNodeId.toUpperCase());render('keyboard connection unavailable');return}const target=candidates[0];connection={mode:'keyboard',sourceNodeId:selectedNodeId,targetNodeId:target,end:current.byId[target].input,valid:true,reason:'valid',candidates:candidates,candidateIndex:0,pointerId:null};counters.connectionStarts++;counters.keyConnects++;announce('Connecting from '+selectedNodeId.toUpperCase()+'; choose a target and press Enter');render('keyboard connection')}
function moveSelected(dx,dy){const current=layout(),item=nodes.find(function(node){return node.id===selectedNodeId}),maxX=current.width-current.nodeWidth,maxY=current.height-nodeHeight,x=clamp(item.nx*maxX+dx,0,maxX),y=clamp(item.ny*maxY+dy,0,maxY);item.nx=maxX?x/maxX:0;item.ny=maxY?y/maxY:0;counters.keyMoves++;counters.nodeMoves++;render('keyboard node move')}
function advanceOne(nextSource){if(cleaned)return;counters.manualAdvances++;if(!execution){if(activeTime<nextAutoAt)activeTime=nextAutoAt;startExecution('full','root',activeTime,nextSource||'manual execution',true)}else{const boundary=nextBoundary();if(boundary!==null)activeTime=execution.start+boundary;settle()}render(nextSource||'manual advance')}
function togglePause(){if(cleaned)return;if(reduced){advanceOne('reduced deliberate advance');return}userPaused=!userPaused;counters.pauseToggles++;announce(userPaused?'Graph execution paused':'Graph execution resumed');if(userPaused)stopScheduler('user paused');else startScheduler('user resumed');render('pause toggle')}
function materializeEnd(){if(cleaned)return;stopScheduler('end stop');nodes=nodeTemplates.map(cloneNode);edges=baseEdgeTemplates.map(cloneEdge);activeTime=0;nextAutoAt=5000;execution=null;edgeSerial=5;runSerial=0;drag=null;connection=null;hoveredPort=null;liftTween=null;selectedNodeId='tools';const edge=Object.assign({},deterministicEdge,{createdAt:0,morphStart:reduced?null:0});edges.push(edge);edgeSerial=6;userPaused=false;counters.edgeMorphs++;startExecution('partial','tools',0,'end oracle',true);announce('Deterministic TOOLS to WRITE connection created');render('end oracle');if(!reduced)startScheduler('end scheduler')}
function resetCounters(){counterNames.forEach(function(name){counters[name]=0})}
function resetDemo(){if(cleaned)return;const resets=counters.resets+1;if(frameId){cancelAnimationFrame(frameId);frameId=0}running=false;lastWall=0;resetModel();resetCounters();counters.resets=resets;announce('Agent graph reset');render(reduced?'reduced reset':'reset');if(!reduced)startScheduler('reset scheduler')}
function deepFreeze(value){if(value&&typeof value==='object'&&!Object.isFrozen(value)){Object.freeze(value);Object.keys(value).forEach(function(key){deepFreeze(value[key])})}return value}
function copyPlan(plan){return plan?{reachableNodes:plan.reachableNodes.slice(),nodeSchedule:plan.nodeSchedule.map(function(item){return Object.assign({},item)}),edgeSchedule:plan.edgeSchedule.map(function(item){return Object.assign({},item)}),done:plan.done}:null}
function inspect(){const rootRect=root.getBoundingClientRect(),stageRect=graphStage.getBoundingClientRect(),canvasRect=canvas.getBoundingClientRect(),elapsed=executionElapsed(),boundary=nextBoundary();return deepFreeze({
 config:{nodeCount:5,baseEdgeCount:5,maxEdges:maxEdges,graph:{normalWidth:normalGraphWidth,narrowWidth:narrowGraphWidth,height:graphHeight,widthBreakpoint:widthBreakpoint,normalNodeWidth:normalNodeWidth,narrowNodeWidth:narrowNodeWidth,nodeHeight:nodeHeight},controlPull:controlPull,edgeDuration:edgeDuration,sinkDuration:sinkDuration,okDuration:okDuration,cycleInterval:cycleInterval,morphDuration:morphDuration,liftDuration:liftDuration,tailFraction:tailFraction,headDiameter:headDiameter,keyboardMove:keyboardMove,maxDelta:maxDelta,colors:Object.assign({},colors)},
 fixtures:{nodes:nodeTemplates.map(cloneNode),baseEdges:baseEdgeTemplates.map(cloneEdge),deterministicEdge:Object.assign({},deterministicEdge),geometry:{normal:{width:normalGraphWidth,height:graphHeight,nodeWidth:normalNodeWidth,source:normalGeometryFixture.source},narrow:{width:narrowGraphWidth,height:graphHeight,nodeWidth:narrowNodeWidth,source:narrowGeometryFixture.source}},plans:{baseNodes:baseNodePlan.map(function(item){return item.slice()}),baseEdges:baseEdgePlan.map(function(item){return item.slice()}),partialNodes:partialNodePlan.map(function(item){return item.slice()}),partialEdges:partialEdgePlan.map(function(item){return item.slice()})},motion:{edgeEase:edgeEaseAnchors.map(function(item){return Object.assign({},item)}),dotPulse:dotPulseAnchors.map(function(item){return Object.assign({},item)}),morphGap:morphGapAnchors.map(function(item){return Object.assign({},item)}),lift:liftAnchors.map(function(item){return Object.assign({},item)})},serializers:{nodes:nodeSource,edges:edgeSource,normalGeometry:normalGeometryFixture.source,narrowGeometry:narrowGeometryFixture.source,plan:planSource,edgeEase:motionSource(edgeEaseAnchors),dotPulse:motionSource(dotPulseAnchors),morphGap:motionSource(morphGapAnchors),lift:motionSource(liftAnchors)}},
 state:{phase:phase,source:source,activeTime:activeTime,nextAutoAt:nextAutoAt,userPaused:userPaused,selectedNodeId:selectedNodeId,edgeSerial:edgeSerial,runSerial:runSerial,hoveredPort:hoveredPort,drag:drag?Object.assign({},drag):null},
 nodes:{items:lastNodes.map(function(item){return JSON.parse(JSON.stringify(item))}),count:nodes.length,domCount:nodesLayer.children.length,selectedId:selectedNodeId},
 edges:{items:lastEdges.map(function(item){return JSON.parse(JSON.stringify(item))}),count:edges.length,baseCount:edges.filter(function(item){return!item.user}).length,userCount:edges.filter(function(item){return!!item.user}).length,domCount:edgeLayer.children.length,max:maxEdges},
 preview:JSON.parse(JSON.stringify(lastPreview)),
 execution:{active:!!execution,kind:execution?execution.kind:null,runId:execution?execution.runId:null,sourceNodeId:execution?execution.sourceNodeId:null,start:execution?execution.start:null,elapsed:elapsed,done:execution?execution.plan.done:null,nextAutoAt:nextAutoAt,nextBoundary:boundary,nextBoundaryAt:execution&&boundary!==null?execution.start+boundary:boundary,reachableNodes:execution?execution.plan.reachableNodes.slice():[],nodeSchedule:execution?execution.plan.nodeSchedule.map(function(item){return Object.assign({},item)}):[],edgeSchedule:execution?execution.plan.edgeSchedule.map(function(item){return Object.assign({},item)}):[]},
 interaction:{focused:focused,userPaused:userPaused,selectedNodeId:selectedNodeId,dragging:!!drag,connecting:!!connection,statusText:statusElement.textContent,keyboardShortcuts:graphStage.getAttribute('aria-keyshortcuts'),primaryAction:reduced?'advance one boundary':'pause or resume',arrowAction:'pause and advance one boundary',endAction:'create TOOLS to WRITE edge and partial run'},
 counters:Object.assign({},counters),
 lifecycle:{running:running,reduced:reduced,cleaned:cleaned,visible:visible,documentVisible:documentVisible,frameId:frameId,signalAborted:controller.signal.aborted,hasResizeObserver:!!resizeObserver,hasIntersectionObserver:!!intersectionObserver,hasConnectionObserver:!!connectionObserver},
 geometry:{rootWidth:rootRect.width,rootHeight:rootRect.height,rootScrollWidth:root.scrollWidth,rootScrollHeight:root.scrollHeight,stageWidth:stageRect.width,stageHeight:stageRect.height,canvasWidth:canvasRect.width,canvasHeight:canvasRect.height,nodeWidth:lastLayout.nodeWidth,nodeHeight:lastLayout.nodeHeight,nodeRects:[...nodesLayer.children].map(function(node){const rect=node.getBoundingClientRect();return{id:node.dataset.id,left:rect.left,top:rect.top,width:rect.width,height:rect.height}})},
 checksums:Object.assign({},actualChecksums),data:Object.assign({},root.dataset)})}
function advanceHook(){if(cleaned)return;if(!reduced){userPaused=true;stopScheduler('hook advance pause')}advanceOne('hook advance')}
graphStage.addEventListener('pointerdown',function(event){if(event.isPrimary===false||event.button!==undefined&&event.button!==0){counters.ignoredPointers++;return}counters.pointerDowns++;if(document.activeElement!==graphStage)graphStage.focus({preventScroll:true});const port=event.target.closest&&event.target.closest('.d-agent-agent-node-graph-port'),node=event.target.closest&&event.target.closest('.d-agent-agent-node-graph-node');if(port){selectedNodeId=port.dataset.nodeId;if(port.dataset.direction!=='output'){counters.connectionRejects++;render('input port selected');return}const current=layout(),point=localPoint(event.clientX,event.clientY);connection={mode:'pointer',sourceNodeId:port.dataset.nodeId,targetNodeId:null,end:point,valid:false,reason:'no-target',candidates:[],candidateIndex:-1,pointerId:event.pointerId};counters.connectionStarts++;graphStage.setPointerCapture(event.pointerId);render('pointer connection start');return}if(node){const item=nodes.find(function(entry){return entry.id===node.dataset.id}),current=layout();selectedNodeId=item.id;drag={nodeId:item.id,pointerId:event.pointerId,startClientX:event.clientX,startClientY:event.clientY,startNx:item.nx,startNy:item.ny,startX:item.nx*(current.width-current.nodeWidth),startY:item.ny*(current.height-nodeHeight),width:current.width,height:current.height,nodeWidth:current.nodeWidth,moved:false};counters.nodeDrags++;startLift(item.id,'up');graphStage.setPointerCapture(event.pointerId);render('node drag start');return}togglePause()},passiveListener);
graphStage.addEventListener('pointermove',function(event){if(drag&&event.pointerId===drag.pointerId){counters.pointerMoves++;const item=nodes.find(function(entry){return entry.id===drag.nodeId}),maxX=drag.width-drag.nodeWidth,maxY=drag.height-nodeHeight,x=clamp(drag.startX+event.clientX-drag.startClientX,0,maxX),y=clamp(drag.startY+event.clientY-drag.startClientY,0,maxY);item.nx=maxX?x/maxX:0;item.ny=maxY?y/maxY:0;drag.moved=true;counters.nodeMoves++;render('node drag');return}if(connection&&connection.mode==='pointer'&&event.pointerId===connection.pointerId){counters.pointerMoves++;updateConnectionTarget(event.clientX,event.clientY);render('connection preview')}},passiveListener);
graphStage.addEventListener('pointerup',function(event){if(drag&&event.pointerId===drag.pointerId){const captured=drag.pointerId,nodeId=drag.nodeId;drag=null;startLift(nodeId,'down');if(graphStage.hasPointerCapture(captured))graphStage.releasePointerCapture(captured);announce('Node '+nodeId.toUpperCase()+' moved');render('node drag complete');return}if(connection&&connection.mode==='pointer'&&event.pointerId===connection.pointerId){updateConnectionTarget(event.clientX,event.clientY);const current=connection,captured=current.pointerId;connection=null;if(graphStage.hasPointerCapture(captured))graphStage.releasePointerCapture(captured);if(current.valid)commitEdge(current.sourceNodeId,current.targetNodeId,'pointer connection');else{counters.connectionRejects++;announce('Connection rejected: '+current.reason);render('connection rejected')}}},passiveListener);
graphStage.addEventListener('pointercancel',function(event){if(drag&&event.pointerId===drag.pointerId||connection&&event.pointerId===connection.pointerId)cancelGesture('Pointer gesture cancelled',true,true)},passiveListener);
graphStage.addEventListener('lostpointercapture',function(event){if(drag&&event.pointerId===drag.pointerId||connection&&event.pointerId===connection.pointerId)cancelGesture('Pointer capture lost',true,false)},passiveListener);
graphStage.addEventListener('pointerover',function(event){const port=event.target.closest&&event.target.closest('.d-agent-agent-node-graph-port');if(!port)return;hoveredPort=port.dataset.nodeId+':'+port.dataset.direction;render('port hover')},listener);
graphStage.addEventListener('pointerout',function(event){const port=event.target.closest&&event.target.closest('.d-agent-agent-node-graph-port');if(!port)return;const related=event.relatedTarget&&event.relatedTarget.closest&&event.relatedTarget.closest('.d-agent-agent-node-graph-port');if(related===port)return;hoveredPort=null;render('port leave')},listener);
graphStage.addEventListener('focus',function(){focused=true;counters.focusEvents++;render('focus')},listener);graphStage.addEventListener('blur',function(){focused=false;counters.blurEvents++;if(connection&&connection.mode==='keyboard')cancelGesture('Keyboard connection cancelled',false,false);render('blur')},listener);
graphStage.addEventListener('keydown',function(event){const arrow=['ArrowUp','ArrowDown','ArrowLeft','ArrowRight'].includes(event.key),accepted=arrow||['Enter',' ','c','C','Escape','End','Home','r','R'].includes(event.key);if(!accepted)return;event.preventDefault();if(connection&&connection.mode==='keyboard'){if(event.key==='Escape'){counters.keyCancels++;cancelGesture('Keyboard connection cancelled',false,true);return}if(event.key==='ArrowUp'||event.key==='ArrowDown'){const delta=event.key==='ArrowDown'?1:-1;connection.candidateIndex=(connection.candidateIndex+delta+connection.candidates.length)%connection.candidates.length;connection.targetNodeId=connection.candidates[connection.candidateIndex];connection.end=layout().byId[connection.targetNodeId].input;connection.valid=true;connection.reason='valid';counters.keySelections++;render('keyboard target');return}if(event.key==='Enter'){const from=connection.sourceNodeId,to=connection.targetNodeId;connection=null;commitEdge(from,to,'keyboard connection');return}return}if(event.shiftKey&&arrow){const delta=keyboardMove;if(event.key==='ArrowUp')moveSelected(0,-delta);if(event.key==='ArrowDown')moveSelected(0,delta);if(event.key==='ArrowLeft')moveSelected(-delta,0);if(event.key==='ArrowRight')moveSelected(delta,0);return}if(event.key==='ArrowUp'||event.key==='ArrowDown'||event.key==='ArrowLeft'){const currentIndex=nodeTemplates.findIndex(function(item){return item.id===selectedNodeId}),delta=event.key==='ArrowDown'?1:-1;selectedNodeId=nodeTemplates[(currentIndex+delta+nodeTemplates.length)%nodeTemplates.length].id;counters.keySelections++;render('keyboard selection');return}if(event.key==='c'||event.key==='C'){startKeyboardConnection();return}if(event.key==='Escape'){counters.keyCancels++;if(drag||connection)cancelGesture('Gesture cancelled',true,true);return}if(event.key==='End'){counters.keyEnds++;materializeEnd();return}if(event.key==='Home'||event.key==='r'||event.key==='R'){resetDemo();counters.keyResets++;expose();return}if(event.key==='ArrowRight'){counters.keyAdvances++;if(!reduced){userPaused=true;stopScheduler('keyboard step pause')}advanceOne('keyboard step');return}counters.keyPauses++;togglePause()},listener);
function onVisibility(){documentVisible=document.visibilityState!=='hidden';counters.visibilityChanges++;if(!documentVisible&&drag||!documentVisible&&connection)cancelGesture('Visibility gesture cancelled',true,false);if(documentVisible)startScheduler('document visible');else stopScheduler('document hidden');render(documentVisible?'document visible':'document hidden')}document.addEventListener('visibilitychange',onVisibility,listener);
function cleanup(){if(cleaned)return;counters.cleanupCalls++;if(drag||connection)cancelGesture('cleanup gesture',true,false);cleaned=true;if(frameId){cancelAnimationFrame(frameId);frameId=0;counters.rafCancels++}running=false;controller.abort();if(resizeObserver){resizeObserver.disconnect();resizeObserver=null}if(intersectionObserver){intersectionObserver.disconnect();intersectionObserver=null}if(connectionObserver){connectionObserver.disconnect();connectionObserver=null}render('cleanup')}
if('ResizeObserver'in window){resizeObserver=new ResizeObserver(function(){if(cleaned)return;counters.resizeEvents++;if(drag||connection)cancelGesture('Resize gesture cancelled',true,false);render('resize')});resizeObserver.observe(canvas)}
if('IntersectionObserver'in window){intersectionObserver=new IntersectionObserver(function(entries){if(!entries.length||cleaned)return;visible=entries[0].isIntersecting;counters.intersectionChanges++;if(!visible&&(drag||connection))cancelGesture('Intersection gesture cancelled',true,false);if(visible)startScheduler('intersection visible');else stopScheduler('intersection hidden');render(visible?'intersection visible':'intersection hidden')},{threshold:.05});intersectionObserver.observe(root)}
if('MutationObserver'in window){connectionObserver=new MutationObserver(function(){if(!root.isConnected)cleanup()});connectionObserver.observe(document.documentElement,{childList:true,subtree:true})}
root.__agentNodeGraphInspect=Object.freeze(inspect);root.__agentNodeGraphReset=Object.freeze(resetDemo);root.__agentNodeGraphAdvance=Object.freeze(advanceHook);render(reduced?'reduced initial':'initial');if(!reduced)startScheduler('initial scheduler');`,
  prompt:`Build one self-contained responsive 320px Agent and AI UI executable node graph with no dependencies, assets, network, persistence, timers, runtime randomness, Web Animations API or CSS keyframes. Use five bg1 #101012 nodes with eight-pixel radii, one-pixel line0 #232327 borders and six-pixel status dots in a preplaced DAG: ROOT to PLAN and TOOLS, PLAN to WRITE and CHECK, and TOOLS to CHECK. Place nodes from normalized coordinates so they remain in bounds while resizing. Use 64 by 42 pixel nodes in a 322 by 222 graph at 380px card width, and 48 by 42 nodes in a 266 by 222 graph at 320px. Draw every edge as a 1.5px line1 #2e2e34 cubic Bezier with controls exactly sixty pixels horizontally from its output and input ports.

Start deterministic execution waves every 5000ms. ROOT is active from 0 to 600ms, PLAN and TOOLS from 600 to 1200ms, and WRITE and CHECK from 1200 to 1500ms. Finished dots use success #4ade80 for exactly 1000ms, and all nodes are idle from 2500 to 5000ms. Move a four-pixel accent #a78bfa head and a twelve-percent gradient tail down every active edge for exactly 600ms with cubic-bezier(.65,0,.35,1). Use longest reachable predecessor levels and stable canonical node-order tie breaking so joins wait for all in-scope incoming pulses. Active dots pulse with scale one plus .3 times sine-squared over 600ms.

Nodes must drag with pointer capture, clamped normalized positions, a grab cursor, line1 lift border and inner-card scale to 1.02 without moving ports. Edges must recompute live. Dragging from an output port draws an accent five-pixel dashed preview. Resolve captured drops with elementFromPoint and accept only an input in the same root that is not self, duplicate or cycle-producing. A valid edge morphs its dash gap from five to zero over 200ms and immediately interrupts and starts a descendants-only partial cascade from its source, rebasing the next full run to 5000ms later. Pointercancel, lost capture, Escape, resize, visibility loss and detach must clear or revert gestures safely.

Use one focusable role application stage with semantic node listitems, live truthful node labels, visible focus and a polite atomic status. Up and Down select nodes; Shift plus arrows moves the selected node four CSS pixels; C begins a keyboard connection, Up and Down choose a valid target, Enter commits and Escape cancels. Enter or Space pauses or resumes normal motion, Right Arrow pauses and advances one deterministic boundary, End restores the base graph, adds TOOLS to WRITE and starts its exact partial cascade, and Home or R resets. Use exactly one capped requestAnimationFrame scheduler with a 50ms maximum delta, document and intersection pause without catch-up, ResizeObserver, AbortController listeners and MutationObserver detach cleanup. Reduced motion must request zero animation frames and advance only through deliberate stable boundaries with connection morphs instant. Expose deeply frozen inspect, reset and advance hooks named __agentNodeGraphInspect, __agentNodeGraphReset and __agentNodeGraphAdvance, deterministic fixtures, serializers, topology, paths, schedules, interaction, lifecycle, geometry and FNV-1a checksums.`
});
