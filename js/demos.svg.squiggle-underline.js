/* INTRX registry - published live hand-drawn SVG underlines */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'svg-squiggle-underline',title:'SVG Squiggle Underline',cat:'SVG & Line',rootClass:'d-squiggle',tags:['SVG','underline','live path'],libs:[],desc:'Three editorial phrases reveal measured hand-drawn SVG strokes on hover or focus. Once visible, each underline keeps a restrained two-frequency wobble generated in JavaScript, sharing one visibility-aware animation loop.',seen:'Seen on: editorial navigation, playful link lists, campaign calls-to-action, and culture-site headlines',hint:'hover or focus a phrase to draw it; click to hold its underline alive',
html:`<div class="d-squiggle" role="group" aria-label="Live hand-drawn SVG underline phrases"><div class="d-squiggle-head"><span>INK MOTION / TWO-FREQUENCY PATH</span><span>ACTIVE <b>0 / 3</b> <i>RESTING</i></span></div><div class="d-squiggle-list"><button class="d-squiggle-item" type="button" aria-pressed="false" style="--ink:#efc96f"><span>MAKE IT MATTER</span><svg viewBox="0 0 280 52" preserveAspectRatio="none" aria-hidden="true"><path></path></svg><small>STRATEGY / 01</small></button><button class="d-squiggle-item" type="button" aria-pressed="false" style="--ink:#83dfc2"><span>MOVE WITH INTENT</span><svg viewBox="0 0 280 52" preserveAspectRatio="none" aria-hidden="true"><path></path></svg><small>MOTION / 02</small></button><button class="d-squiggle-item" type="button" aria-pressed="false" style="--ink:#df829d"><span>LEAVE A TRACE</span><svg viewBox="0 0 280 52" preserveAspectRatio="none" aria-hidden="true"><path></path></svg><small>IDENTITY / 03</small></button></div><div class="d-squiggle-foot"><p aria-live="polite">Underline field waiting</p><button type="button">Clear underlines</button></div></div>`,
css:`.d-squiggle{width:100%;min-height:510px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#f0eee8;background:#0a0a0b;font-family:Roboto Mono,JetBrains Mono,monospace}.d-squiggle-head{display:flex;justify-content:space-between;color:#6e7476;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.11em}.d-squiggle-head b{color:#8ddfc2}.d-squiggle-head i{margin-left:8px;color:#bac1bf;font-style:normal}.d-squiggle-list{height:390px;margin-top:13px;display:grid;grid-template-rows:repeat(3,1fr);gap:6px}.d-squiggle-item{position:relative;display:grid;grid-template-columns:1fr auto;align-items:center;padding:13px 18px;border:1px solid #2d3436;color:#e7e7df;background:#101516;text-align:left;overflow:hidden;cursor:pointer}.d-squiggle-item:hover,.d-squiggle-item:focus-visible,.d-squiggle-item[aria-pressed="true"]{border-color:#66736f;background:#131a1a;outline:none}.d-squiggle-item>span{position:relative;z-index:2;font:700 clamp(20px,5vw,38px)/1 Georgia,serif;letter-spacing:-.03em}.d-squiggle-item small{align-self:start;color:#697270;font:650 6px/1 ui-monospace,monospace;letter-spacing:.1em}.d-squiggle-item svg{position:absolute;z-index:1;left:15px;right:15px;bottom:13%;width:calc(100% - 30px);height:38%;overflow:visible}.d-squiggle-item path{fill:none;stroke:var(--ink);stroke-width:3.2;stroke-linecap:round;stroke-linejoin:round;vector-effect:non-scaling-stroke;stroke-dasharray:var(--length);stroke-dashoffset:var(--length);transition:stroke-dashoffset .62s cubic-bezier(.2,.75,.2,1)}.d-squiggle-item.is-active path{stroke-dashoffset:0}.d-squiggle-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-squiggle-foot p{margin:0;color:#737b79;font:650 9px/1.3 ui-monospace,monospace}.d-squiggle-foot button{min-height:31px;padding:7px 11px;border:1px solid #39413f;border-radius:3px;color:#a5adaa;background:#141917;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-squiggle-foot button:hover,.d-squiggle-foot button:focus-visible{color:#fff;border-color:#707c78;outline:none}@media(prefers-reduced-motion:reduce){.d-squiggle-item path{transition:none}}@media(max-width:550px){.d-squiggle{padding-inline:6px}.d-squiggle-item{padding-inline:10px}.d-squiggle-item small{display:none}}`,
js:`const items=[...root.querySelectorAll('.d-squiggle-item')];const counter=root.querySelector('.d-squiggle-head b');const mode=root.querySelector('.d-squiggle-head i');const status=root.querySelector('.d-squiggle-foot p');const clear=root.querySelector('.d-squiggle-foot button');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;let visible=true,running=false,last=0,time=0;const state=items.map(function(item,index){return{item,path:item.querySelector('path'),index,hovered:false,focused:false,held:false}});
function active(entry){return entry.hovered||entry.focused||entry.held}function pathData(index,t){const points=[];for(let x=5;x<=275;x+=9){const y=27+Math.sin(x*.095+t*4.2+index*1.7)*2.8+Math.sin(x*.225-t*2.7+index)*1.35;points.push({x,y})}let d='M'+points[0].x+' '+points[0].y.toFixed(2);for(let i=1;i<points.length-1;i++){const midX=(points[i].x+points[i+1].x)/2,midY=(points[i].y+points[i+1].y)/2;d+='Q'+points[i].x+' '+points[i].y.toFixed(2)+' '+midX.toFixed(2)+' '+midY.toFixed(2)}const end=points[points.length-1];return d+'T'+end.x+' '+end.y.toFixed(2)}function draw(entry){entry.path.setAttribute('d',pathData(entry.index,time))}function update(){const count=state.filter(active).length;counter.textContent=count+' / 3';mode.textContent=count?'WOBBLING':'RESTING';state.forEach(function(entry){entry.item.classList.toggle('is-active',active(entry))});if(count)start()}function start(){if(reduce||!visible||running||!state.some(active))return;running=true;last=performance.now();requestAnimationFrame(frame)}function frame(now){if(!visible||!state.some(active)){running=false;return}const dt=Math.min(60,now-last);last=now;time+=dt/1000;state.forEach(function(entry){if(active(entry))draw(entry)});requestAnimationFrame(frame)}state.forEach(function(entry){draw(entry);const length=Math.ceil(entry.path.getTotalLength());entry.path.style.setProperty('--length',length);entry.item.addEventListener('pointerenter',function(){entry.hovered=true;status.textContent='Hand-drawn underline moving under '+entry.item.querySelector('span').textContent.toLowerCase();update()});entry.item.addEventListener('pointerleave',function(){entry.hovered=false;update()});entry.item.addEventListener('focus',function(){entry.focused=true;update()});entry.item.addEventListener('blur',function(){entry.focused=false;update()});entry.item.addEventListener('click',function(){entry.held=!entry.held;entry.item.setAttribute('aria-pressed',entry.held?'true':'false');status.textContent=entry.held?'Underline held live':'Underline released';update()})});clear.addEventListener('click',function(){state.forEach(function(entry){entry.held=entry.hovered=entry.focused=false;entry.item.setAttribute('aria-pressed','false')});status.textContent='All live underlines cleared';update();clear.focus()});if('IntersectionObserver'in window)new IntersectionObserver(function(entries){visible=entries[0].isIntersecting;if(visible)start();else running=false},{threshold:.05}).observe(root);update();`,
prompt:`Build a self-contained set of three live SVG squiggle underlines with no dependencies. For each path, sample x from 5 to 275 in 9-unit steps and compute y=27+sin(x*.095+t*4.2+phase)*2.8+sin(x*.225-t*2.7+phase)*1.35. Convert the samples into a smooth quadratic path using Q commands to successive midpoints and a final T endpoint.

Generate the initial path, measure its exact getTotalLength(), and use that value for dasharray/dashoffset. Hover or focus transitions dashoffset to zero. Once shown, regenerate only active paths each RAF so the line keeps a restrained two-frequency wobble. Click holds individual underlines; pointer exit/blur hides unheld ones; Clear releases all.

Use one shared RAF only while at least one underline is active and the component is visible; pause with IntersectionObserver. Under prefers-reduced-motion, retain draw/hide states but do not run the wobble loop. Synchronize active count, wobbling/resting state, aria-pressed holds, and live status. Include measured strokes, visible focus, scoped CSS, and no dependencies.`});
