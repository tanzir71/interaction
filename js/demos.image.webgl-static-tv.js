/* INTRX registry - published WebGL analog signal resolver */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'webgl-static-tv',title:'WebGL Static TV',cat:'Image & WebGL',rootClass:'d-statictv',tags:['WebGL','analog static','signal reveal'],libs:[],desc:'A fragment shader begins as an unstable analog broadcast: RF snow, sync tears, rolling bars, and separated chroma. Hover, focus, or touch tunes the signal until a procedural coastal transmission resolves beneath the noise.',seen:'Seen on: broadcast identities, music releases, experimental portfolios, and cinematic campaign sites',hint:'hover or focus to tune the signal; click to hold reception; Escape releases it',
html:`<div class="d-statictv" role="group" aria-label="Interactive analog signal tuner"><div class="d-statictv-head"><span>CHANNEL 07 / COASTAL RELAY</span><span>SIGNAL <b>00%</b> <i>NO CARRIER</i></span></div><div class="d-statictv-set"><div class="d-statictv-screen"><canvas tabindex="0" role="button" aria-pressed="false" aria-label="WebGL analog static. Hover or focus to resolve the broadcast image; activate to hold reception."></canvas><div class="d-statictv-glass" aria-hidden="true"></div><div class="d-statictv-osd" aria-hidden="true"><span>REC</span><span>CH 07</span></div><p class="d-statictv-fallback">WebGL unavailable - signal cannot be decoded.</p></div><div class="d-statictv-dial" aria-hidden="true"><span>TUNE</span><i></i><small>RF / 187.25 MHz</small></div></div><div class="d-statictv-foot"><p aria-live="polite">Searching for carrier signal</p><button type="button">Release signal</button></div></div>`,
css:`.d-statictv{width:100%;min-height:510px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#e9e6d8;background:#0c0d0d;font-family:Roboto Mono,JetBrains Mono,monospace}.d-statictv-head{display:flex;justify-content:space-between;color:#72736d;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.11em}.d-statictv-head b{color:#f2dc76}.d-statictv-head i{margin-left:8px;color:#b7b8af;font-style:normal}.d-statictv-set{display:grid;grid-template-columns:minmax(0,1fr) 74px;gap:13px;height:390px;margin-top:13px;padding:12px;border:1px solid #33342f;border-radius:9px;background:linear-gradient(145deg,#2d2e2a,#171816 52%,#0d0e0d);box-shadow:inset 0 1px rgba(255,255,255,.06),inset 0 -12px 30px rgba(0,0,0,.35)}.d-statictv-screen{position:relative;min-width:0;overflow:hidden;border:7px solid #090a09;border-radius:18px/24px;background:#111}.d-statictv canvas{display:block;width:100%;height:100%;outline:none;cursor:crosshair}.d-statictv canvas:focus-visible{box-shadow:inset 0 0 0 2px #ffe37d}.d-statictv-glass{position:absolute;inset:0;border-radius:11px/17px;background:radial-gradient(ellipse at center,transparent 55%,rgba(0,0,0,.34) 100%),linear-gradient(115deg,rgba(255,255,255,.055),transparent 28%);pointer-events:none}.d-statictv-osd{position:absolute;left:13px;right:13px;top:12px;display:flex;justify-content:space-between;color:rgba(255,241,196,.62);font:750 7px/1 ui-monospace,monospace;letter-spacing:.12em;pointer-events:none}.d-statictv-osd span:first-child::before{content:'';display:inline-block;width:5px;height:5px;margin-right:5px;border-radius:50%;background:#ef644f}.d-statictv-fallback{display:none;position:absolute;inset:0;place-items:center;margin:0;padding:20px;text-align:center;color:#bbb9ac;font:650 9px/1.5 ui-monospace,monospace}.d-statictv.no-webgl .d-statictv-fallback{display:grid}.d-statictv.no-webgl canvas{opacity:0}.d-statictv-dial{display:flex;flex-direction:column;align-items:center;padding-top:30px;color:#777970;font:700 7px/1 ui-monospace,monospace;letter-spacing:.1em}.d-statictv-dial i{position:relative;width:40px;height:40px;margin-top:12px;border:1px solid #484943;border-radius:50%;background:radial-gradient(circle at 38% 35%,#484943,#1b1c1a 58%,#0d0e0d);box-shadow:0 2px 5px #000}.d-statictv-dial i::after{content:'';position:absolute;left:19px;top:5px;width:2px;height:13px;background:#e0c868;transform-origin:1px 15px;transform:rotate(var(--dial, -65deg));transition:transform .12s linear}.d-statictv-dial small{margin-top:auto;margin-bottom:8px;color:#51534d;font:650 6px/1.4 ui-monospace,monospace;text-align:center}.d-statictv-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-statictv-foot p{margin:0;color:#787a73;font:650 9px/1.3 ui-monospace,monospace}.d-statictv-foot button{min-height:31px;padding:7px 11px;border:1px solid #3a3b36;border-radius:3px;color:#a4a69e;background:#151613;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-statictv-foot button:hover,.d-statictv-foot button:focus-visible{color:#fff;border-color:#75786e;outline:none}@media(max-width:550px){.d-statictv{padding-inline:6px}.d-statictv-set{grid-template-columns:minmax(0,1fr);padding:8px}.d-statictv-dial{display:none}}`,
js:`const canvas=root.querySelector('canvas');const signalReadout=root.querySelector('.d-statictv-head b');const carrierReadout=root.querySelector('.d-statictv-head i');const status=root.querySelector('.d-statictv-foot p');const release=root.querySelector('.d-statictv-foot button');const dial=root.querySelector('.d-statictv-dial');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const gl=canvas.getContext('webgl',{alpha:false,antialias:false});if(!gl){root.classList.add('no-webgl');status.textContent='WebGL unavailable';return}const vertexSource='attribute vec2 a_position;void main(){gl_Position=vec4(a_position,0.0,1.0);}';const fragmentSource='precision highp float;uniform vec2 u_resolution;uniform float u_time;uniform float u_resolve;float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453123);}float noise(vec2 p){vec2 i=floor(p),f=fract(p);f=f*f*(3.0-2.0*f);return mix(mix(hash(i),hash(i+vec2(1.0,0.0)),f.x),mix(hash(i+vec2(0.0,1.0)),hash(i+vec2(1.0)),f.x),f.y);}vec3 broadcast(vec2 uv){float aspect=u_resolution.x/u_resolution.y;vec2 p=vec2((uv.x-.5)*aspect,uv.y);vec3 sky=mix(vec3(.055,.13,.19),vec3(.77,.39,.22),smoothstep(.18,.9,uv.y));float sun=1.0-smoothstep(.105,.115,length(p-vec2(.34,.69)));sky=mix(sky,vec3(1.0,.71,.31),sun*.9);float ridge=.40+.055*sin(p.x*5.3)+.026*sin(p.x*13.0+1.2);float mountain=1.0-smoothstep(ridge-.008,ridge+.008,uv.y);vec3 color=mix(sky,vec3(.035,.075,.08),mountain);float water=1.0-smoothstep(.395,.405,uv.y);float shimmer=pow(max(0.0,sin(p.x*38.0+sin(uv.y*91.0)*2.0)),12.0)*smoothstep(.0,.38,uv.y);color=mix(color,vec3(.025,.14,.18)+vec3(.66,.3,.08)*shimmer,water);float tower=step(abs(p.x+.22),.018)*step(.405,uv.y)*step(uv.y,.7);float roof=step(abs(p.x+.22),(.73-uv.y)*.65)*step(.68,uv.y)*step(uv.y,.73);color=mix(color,vec3(.83,.72,.49),max(tower,roof));float beam=smoothstep(.045,.0,abs(uv.y-(.715+(p.x+.22)*.075)))*smoothstep(-.18,p.x,.3)*(1.0-smoothstep(.3,.58,p.x));color+=vec3(.95,.65,.25)*beam*.34;return color;}void main(){vec2 uv=gl_FragCoord.xy/u_resolution;float t=floor(u_time*24.0);float snow=hash(floor(gl_FragCoord.xy*.72)+vec2(t,t*17.0));float fine=hash(gl_FragCoord.xy+vec2(t*3.0,-t));float tearGate=step(.92,hash(vec2(floor(uv.y*18.0+t*.07),floor(t*.12))));float tear=(hash(vec2(floor(uv.y*140.0),t))-.5)*.12*tearGate*(1.0-u_resolve);float jitter=(hash(vec2(t,3.0))-.5)*.012*(1.0-u_resolve);vec2 sampleUV=vec2(fract(uv.x+tear+jitter),uv.y);float chroma=(1.0-u_resolve)*(.018+.018*sin(u_time*.7));vec3 image;image.r=broadcast(vec2(fract(sampleUV.x+chroma),sampleUV.y)).r;image.g=broadcast(sampleUV).g;image.b=broadcast(vec2(fract(sampleUV.x-chroma),sampleUV.y)).b;float staticField=smoothstep(.18,.82,snow*.78+fine*.34);vec3 staticColor=vec3(staticField);staticColor*=.62+.38*sin(gl_FragCoord.y*3.14159);float rolling=.72+.28*smoothstep(.0,.11,abs(fract(uv.y-u_time*.19)-.5));staticColor*=rolling;float lock=smoothstep(.08,.92,u_resolve);float dropout=step(lock*.74+.18,hash(vec2(floor(uv.y*230.0),t)));float residual=(1.0-lock)*staticField+dropout*(1.0-lock)*.42;vec3 color=mix(staticColor,image,lock);color=mix(color,vec3(staticField),residual*.32);float scan=.94+.06*sin(gl_FragCoord.y*3.14159);color*=scan;color+=(hash(gl_FragCoord.xy+u_time)-.5)*mix(.11,.018,lock);gl_FragColor=vec4(max(color,0.0),1.0);}';function compile(type,source){const shader=gl.createShader(type);gl.shaderSource(shader,source);gl.compileShader(shader);if(!gl.getShaderParameter(shader,gl.COMPILE_STATUS))throw new Error(gl.getShaderInfoLog(shader));return shader}let program;try{program=gl.createProgram();gl.attachShader(program,compile(gl.VERTEX_SHADER,vertexSource));gl.attachShader(program,compile(gl.FRAGMENT_SHADER,fragmentSource));gl.linkProgram(program);if(!gl.getProgramParameter(program,gl.LINK_STATUS))throw new Error(gl.getProgramInfoLog(program))}catch(error){root.classList.add('no-webgl');status.textContent='WebGL shader unavailable';return}gl.useProgram(program);const buffer=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buffer);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),gl.STATIC_DRAW);const position=gl.getAttribLocation(program,'a_position');gl.enableVertexAttribArray(position);gl.vertexAttribPointer(position,2,gl.FLOAT,false,0,0);const resolutionUniform=gl.getUniformLocation(program,'u_resolution'),timeUniform=gl.getUniformLocation(program,'u_time'),resolveUniform=gl.getUniformLocation(program,'u_resolve');let hovered=false,focused=false,latched=false,resolve=0,visible=true,running=false,last=0,time=0;
function target(){return hovered||focused||latched?1:0}function resize(){const rect=canvas.getBoundingClientRect(),dpr=Math.min(2,window.devicePixelRatio||1),width=Math.max(1,Math.round(rect.width*dpr)),height=Math.max(1,Math.round(rect.height*dpr));if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height;gl.viewport(0,0,width,height)}draw()}function draw(){gl.uniform2f(resolutionUniform,canvas.width,canvas.height);gl.uniform1f(timeUniform,time);gl.uniform1f(resolveUniform,resolve);gl.drawArrays(gl.TRIANGLES,0,3);const percent=Math.round(resolve*100);signalReadout.textContent=String(percent).padStart(2,'0')+'%';carrierReadout.textContent=percent>88?'LOCKED':percent>25?'TUNING':'NO CARRIER';dial.style.setProperty('--dial',(-65+resolve*130).toFixed(1)+'deg')}function start(){if(reduce||!visible||running)return;running=true;last=performance.now();requestAnimationFrame(frame)}function frame(now){if(!visible){running=false;return}const dt=Math.min(60,now-last);last=now;time+=dt/1000;resolve+=(target()-resolve)*(1-Math.exp(-dt*.0065));draw();requestAnimationFrame(frame)}function sync(announce){canvas.setAttribute('aria-pressed',latched?'true':'false');if(announce)status.textContent=latched?'Carrier signal held':'Signal hold released';if(reduce){resolve=target()||.72;time=.35;draw()}else start()}canvas.addEventListener('pointerenter',function(){hovered=true;status.textContent='Tuning carrier - image resolving';sync(false)});canvas.addEventListener('pointerleave',function(){hovered=false;status.textContent=latched?'Carrier remains locked':'Carrier fading into static';sync(false)});canvas.addEventListener('focus',function(){focused=true;status.textContent='Keyboard focus tuning carrier';sync(false)});canvas.addEventListener('blur',function(){focused=false;sync(false)});canvas.addEventListener('click',function(){latched=!latched;sync(true)});canvas.addEventListener('keydown',function(event){if(event.key==='Enter'||event.key===' '){event.preventDefault();latched=!latched;sync(true)}else if(event.key==='Escape'){latched=false;hovered=false;sync(true);canvas.blur()}});release.addEventListener('click',function(){latched=false;hovered=false;focused=false;sync(true);release.focus()});if('ResizeObserver'in window)new ResizeObserver(resize).observe(canvas);if('IntersectionObserver'in window)new IntersectionObserver(function(entries){visible=entries[0].isIntersecting;if(visible)start()},{threshold:.05}).observe(root);resize();if(reduce){resolve=.72;time=.35;draw();status.textContent='Static representative signal - reduced motion'}else start();`,
prompt:`Build a self-contained WebGL 1.0 analog television signal resolver without textures or libraries. Render a fullscreen triangle. The fragment shader should create animated RF snow from frame-quantized hashes, fine grain, scanlines, a rolling brightness bar, intermittent horizontal sync tears, frame jitter, and red/blue chroma offsets. Separately generate a recognizable procedural coastal broadcast with gradient sky, sun, mountain ridge, water shimmer, lighthouse, and beam.

Drive the transition with u_resolve. Use smoothstep(.08,.92,u_resolve) for signal lock: as it rises, reduce sync displacement and chroma separation, mix static into the image, and retain sparse dropout lines until nearly locked. Hover/focus target 1; idle target 0. Ease resolve with alpha=1-exp(-dt*.0065). Click or Enter/Space latches reception for touch and keyboard; Escape/Release clears it.

Animate only while visible with IntersectionObserver and a running guard. Resize DPR-aware with ResizeObserver. Handle WebGL and shader failure visibly. Under prefers-reduced-motion, draw a single representative partially resolved frame and update only on interaction. Synchronize signal percentage, carrier state, tuning dial, aria-pressed, and restrained live status. Include visible focus, scoped CSS, and no dependencies.`});
