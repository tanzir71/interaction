/* INTRX registry - gravity drop and throwable icon pile */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'gravity-drop-icons',title:'Gravity Drop Icons',cat:'Physics',rootClass:'d-gravity',tags:['gravity','collision','throw'],libs:[],desc:'Ten interface glyphs drop under gravity, bounce against the container, and resolve pairwise circle collisions into a loose floor pile. Any body can be grabbed and thrown using sampled pointer velocity.',seen:'Seen on: playful launch pages, icon showcases, creative tools, and physics-driven navigation',hint:'wait for the pile, then grab an icon and throw it back into the field',
html:`<div class="d-gravity"><header><span>DROP FIELD / 10 BODIES</span><span class="d-gravity-count">AIRBORNE 10</span></header><div class="d-gravity-stage" tabindex="0" role="application" aria-label="Ten throwable icons falling under gravity"><div class="d-gravity-body" data-symbol="✦" style="--tone:#d9ec66">✦</div><div class="d-gravity-body" data-symbol="⌁" style="--tone:#e79b72">⌁</div><div class="d-gravity-body" data-symbol="◇" style="--tone:#76bfd7">◇</div><div class="d-gravity-body" data-symbol="＋" style="--tone:#d893ce">＋</div><div class="d-gravity-body" data-symbol="≈" style="--tone:#7bcda4">≈</div><div class="d-gravity-body" data-symbol="○" style="--tone:#dec16f">○</div><div class="d-gravity-body" data-symbol="△" style="--tone:#96a7e3">△</div><div class="d-gravity-body" data-symbol="✣" style="--tone:#da8796">✣</div><div class="d-gravity-body" data-symbol="□" style="--tone:#8dc1aa">□</div><div class="d-gravity-body" data-symbol="∿" style="--tone:#c9a2da">∿</div><span class="d-gravity-floor" aria-hidden="true">COLLISION FLOOR / RESTITUTION .58</span><span class="d-gravity-guide">GRAB + THROW / SPACE SCATTERS</span></div><footer><button type="button" class="d-gravity-drop">Drop again</button><button type="button" class="d-gravity-scatter">Scatter</button><p class="d-gravity-status" aria-live="polite">Ten icons released into the field</p></footer></div>`,
css:`.d-gravity{width:100%;min-height:555px;box-sizing:border-box;padding:20px clamp(5px,4vw,28px);color:#eef0e9;background:#10120f;font-family:Inter,system-ui,sans-serif}.d-gravity>header{display:flex;justify-content:space-between;color:#70766d;font:700 8px/1 ui-monospace,monospace;letter-spacing:.14em}.d-gravity-count{color:#d9ec66}.d-gravity-stage{position:relative;height:430px;margin-top:13px;overflow:hidden;border:1px solid #31362e;background:linear-gradient(rgba(211,228,167,.035) 1px,transparent 1px),linear-gradient(90deg,rgba(211,228,167,.035) 1px,transparent 1px),radial-gradient(ellipse at 50% 0,#283024,#151813 70%);background-size:30px 30px,30px 30px,auto;outline:none;cursor:default;touch-action:none}.d-gravity-stage:focus-visible{box-shadow:inset 0 0 0 2px #d9ec66}.d-gravity-body{position:absolute;left:0;top:0;width:50px;height:50px;margin:-25px;display:grid;place-items:center;box-sizing:border-box;border:1px solid color-mix(in srgb,var(--tone),#30332c 52%);border-radius:50%;color:var(--tone);background:radial-gradient(circle at 35% 28%,#32372d,#191c17 72%);box-shadow:0 7px 12px rgba(0,0,0,.25),inset 0 0 10px color-mix(in srgb,var(--tone),transparent 88%);font:500 21px/1 Georgia,serif;cursor:grab;user-select:none;will-change:transform}.d-gravity-body.is-dragging{z-index:4;cursor:grabbing;box-shadow:0 0 0 2px var(--tone),0 13px 20px rgba(0,0,0,.35)}.d-gravity-floor{position:absolute;left:11px;right:11px;bottom:9px;padding-top:7px;border-top:1px solid #4b5248;color:#646b61;font:700 6px/1 ui-monospace,monospace;letter-spacing:.12em}.d-gravity-guide{position:absolute;left:11px;top:10px;color:#60675d;font:700 6px/1 ui-monospace,monospace;letter-spacing:.12em}.d-gravity footer{display:flex;justify-content:center;gap:6px;margin-top:10px}.d-gravity footer button{min-height:32px;padding:7px 11px;border:1px solid #373c33;color:#858c80;background:#171a15;font:700 7px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-gravity footer button:hover,.d-gravity footer button:focus-visible{color:#15170f;background:#d9ec66;outline:none}.d-gravity-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}@media(max-width:540px){.d-gravity{padding-inline:4px}.d-gravity-stage{height:400px}.d-gravity-body{width:42px;height:42px;margin:-21px}}@media(prefers-reduced-motion:reduce){.d-gravity-body{transition:transform .1s}}`,
js:`const field=root.querySelector('.d-gravity-stage');const elements=[...root.querySelectorAll('.d-gravity-body')];const countLabel=root.querySelector('.d-gravity-count');const dropButton=root.querySelector('.d-gravity-drop');const scatterButton=root.querySelector('.d-gravity-scatter');const status=root.querySelector('.d-gravity-status');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const radius=25;let bodies=[],drag=-1,running=false,visible=true,last=0,lastPointer={x:0,y:0,t:0};function size(){return{w:field.clientWidth,h:field.clientHeight,floor:field.clientHeight-30}}function reset(scatter){const d=size();bodies=elements.map((el,i)=>({el,x:d.w*(.1+(i%5)*.2)+(Math.random()-.5)*18,y:scatter?d.h*.25+Math.random()*80:-35-(i%4)*48,vx:scatter?(Math.random()-.5)*520:(Math.random()-.5)*45,vy:scatter?-260-Math.random()*220:0,held:false,sleep:0}));root.dataset.piled='0';status.textContent=scatter?'Icons scattered with random impulses':'Ten icons released into the field';draw();start()}function collide(){for(let i=0;i<bodies.length;i++)for(let j=i+1;j<bodies.length;j++){const a=bodies[i],b=bodies[j];if(a.held||b.held)continue;const dx=b.x-a.x,dy=b.y-a.y,d=Math.max(.001,Math.hypot(dx,dy)),minimum=radius*2;if(d>=minimum)continue;const nx=dx/d,ny=dy/d,overlap=minimum-d;a.x-=nx*overlap*.5;a.y-=ny*overlap*.5;b.x+=nx*overlap*.5;b.y+=ny*overlap*.5;const relative=(b.vx-a.vx)*nx+(b.vy-a.vy)*ny;if(relative<0){const impulse=-relative*.48;a.vx-=nx*impulse;a.vy-=ny*impulse;b.vx+=nx*impulse;b.vy+=ny*impulse}}}function draw(){const d=size();let piled=0;bodies.forEach(body=>{body.el.style.transform='translate('+body.x.toFixed(2)+'px,'+body.y.toFixed(2)+'px) rotate('+(body.x*.018+body.y*.011).toFixed(2)+'deg)';if(body.y>d.floor-radius-3&&Math.abs(body.vy)<35)piled++});root.dataset.piled=String(piled);countLabel.textContent=piled===bodies.length?'PILE SETTLED':'AIRBORNE '+String(bodies.length-piled).padStart(2,'0')}function frame(now){if(!visible){running=false;return}const dt=Math.min(.028,Math.max(.008,(now-last)/1000));last=now;const d=size();let active=drag>=0;bodies.forEach(body=>{if(body.held)return;if(reduce){body.y=d.floor-radius;body.vx=body.vy=0;return}body.vy+=980*dt;body.x+=body.vx*dt;body.y+=body.vy*dt;if(body.x<radius){body.x=radius;body.vx=Math.abs(body.vx)*.64}else if(body.x>d.w-radius){body.x=d.w-radius;body.vx=-Math.abs(body.vx)*.64}if(body.y>d.floor-radius){body.y=d.floor-radius;if(Math.abs(body.vy)>18)body.vy=-Math.abs(body.vy)*.58;else body.vy=0;body.vx*=Math.pow(.83,dt*60)}if(Math.abs(body.vx)+Math.abs(body.vy)>1)active=true});for(let i=0;i<3;i++)collide();draw();if(active)requestAnimationFrame(frame);else running=false}function start(){if(!running){running=true;last=performance.now();requestAnimationFrame(frame)}}function local(event){const box=field.getBoundingClientRect();return{x:event.clientX-box.left,y:event.clientY-box.top}}elements.forEach((element,index)=>element.addEventListener('pointerdown',event=>{if(event.button!==0)return;event.stopPropagation();drag=index;const body=bodies[index],p=local(event);body.held=true;body.x=p.x;body.y=p.y;body.vx=body.vy=0;element.classList.add('is-dragging');element.setPointerCapture(event.pointerId);lastPointer={x:p.x,y:p.y,t:performance.now()};start()}));field.addEventListener('pointermove',event=>{if(drag<0)return;const body=bodies[drag],p=local(event),now=performance.now(),dt=Math.max(8,now-lastPointer.t);body.vx=(p.x-lastPointer.x)/dt*1000;body.vy=(p.y-lastPointer.y)/dt*1000;body.x=p.x;body.y=p.y;lastPointer={x:p.x,y:p.y,t:now};draw()});function release(event){if(drag<0)return;const body=bodies[drag];body.held=false;body.vx=Math.max(-900,Math.min(900,body.vx));body.vy=Math.max(-900,Math.min(900,body.vy));body.el.classList.remove('is-dragging');if(body.el.hasPointerCapture(event.pointerId))body.el.releasePointerCapture(event.pointerId);status.textContent=body.el.dataset.symbol+' thrown at '+Math.round(Math.hypot(body.vx,body.vy))+' pixels per second';drag=-1;start()}field.addEventListener('pointerup',release);field.addEventListener('pointercancel',release);field.addEventListener('keydown',event=>{if(event.key===' '||event.key==='Enter'){event.preventDefault();reset(true)}else if(event.key==='Escape'){event.preventDefault();reset(false)}});dropButton.addEventListener('click',()=>reset(false));scatterButton.addEventListener('click',()=>reset(true));if('ResizeObserver'in window)new ResizeObserver(()=>{const d=size();bodies.forEach(body=>{body.x=Math.min(body.x,d.w-radius);body.y=Math.min(body.y,d.floor-radius)});draw()}).observe(field);if('IntersectionObserver'in window)new IntersectionObserver(entries=>{visible=entries[0].isIntersecting;if(visible)start();else running=false},{threshold:.05}).observe(root);reset(false);`,
prompt:`Build a self-contained ten-body gravity icon pile with circle collision physics. Each body stores x/y, velocity, held state, and radius 25. Per frame clamp dt 0.008–0.028s, add gravity 980px/s², integrate, collide with side walls at restitution .64 and floor at .58, and apply floor friction pow(.83,dt*60).

Resolve every body pair three times per frame: separate overlap equally along the collision normal, then if relative normal velocity is closing, apply an equal/opposite impulse at restitution .48. Pointer down grabs one body, pointer move samples velocity in px/s, and release clamps throw velocity to ±900. Drop again starts bodies above the field; Scatter gives random horizontal and upward impulses.

Stop near rest, pause offscreen, place bodies on floor under reduced motion, resize safely, expose piled count, synchronize airborne/settled label and live throw status, provide keyboard Space/Enter scatter and Escape drop, visible focus, scoped CSS, and no dependencies.`});
