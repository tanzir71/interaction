/* INTRX registry — deterministic street-route drawing */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
  id:'map-route-draw',
  title:'Map Route Draw',
  cat:'Maps & Geo',
  rootClass:'d-maps-map-route-draw',
  tags:['svg','route','wayfinding'],
  libs:[],
  desc:'A courier marker draws a deterministic route across a seeded Manhattan street graph, easing through every turn, leaving fading passes, and accepting accessible click or keyboard reroutes.',
  seen:'Seen on: delivery and ride-app onboarding maps, dispatch dashboards, and compact wayfinding interfaces',
  hint:'click near an intersection to reroute; use arrows then Enter or Space from the focused map; press R to replay',
  html:`<div class="d-maps-map-route-draw" role="region" aria-label="Interactive animated street route">
  <div class="d-maps-map-route-draw-head" aria-hidden="true"><span>MAP / ROUTE</span><span>SEED 42 · LIVE</span></div>
  <div class="d-maps-map-route-draw-stage" role="application" tabindex="0" aria-keyshortcuts="ArrowLeft ArrowRight ArrowUp ArrowDown Enter Space Home Escape R" aria-label="Animated route map. Click an intersection to choose a new destination. Arrow keys move the keyboard destination, Enter or Space reroutes, Home selects the origin, Escape restores the current destination, and R replays the initial route.">
    <svg class="d-maps-map-route-draw-svg" viewBox="0 0 360 216" role="img" aria-label="A stylized street grid with a moving route from A to B">
      <g class="d-maps-map-route-draw-streets" aria-hidden="true"></g>
      <path class="d-maps-map-route-draw-old-route" aria-hidden="true"></path>
      <path class="d-maps-map-route-draw-route" aria-hidden="true"></path>
      <g class="d-maps-map-route-draw-trails" aria-hidden="true"></g>
      <circle class="d-maps-map-route-draw-origin" r="4" aria-hidden="true"></circle>
      <circle class="d-maps-map-route-draw-target" r="5" aria-hidden="true"></circle>
      <circle class="d-maps-map-route-draw-candidate" r="7" aria-hidden="true"></circle>
      <circle class="d-maps-map-route-draw-arrival-ring" r="4" aria-hidden="true"></circle>
      <g class="d-maps-map-route-draw-pin" aria-hidden="true"><path d="M0 0L-4-6A6 6 0 1 1 4-6Z"></path><circle cy="-7" r="2"></circle></g>
      <g class="d-maps-map-route-draw-marker" aria-hidden="true"><circle r="4"></circle><path d="M-1.6-2.1L1.7 0-1.6 2.1"></path></g>
    </svg>
    <output class="d-maps-map-route-draw-readout" aria-label="Route distance and estimated arrival" aria-live="off"><span class="d-maps-map-route-draw-distance">0.0 → 3.2 KM</span><span class="d-maps-map-route-draw-eta">ETA 0:04</span></output>
  </div>
  <div class="d-maps-map-route-draw-foot" aria-hidden="true"><span>63 NODES / 113 EDGES</span><span>140 PX/S</span><span>CLICK TO REROUTE</span></div>
  <span class="d-maps-map-route-draw-status" aria-live="polite" aria-atomic="true">Route from A to B ready</span>
</div>`,
  css:`
.d-maps-map-route-draw{position:relative;width:100%;height:320px;box-sizing:border-box;overflow:hidden;contain:layout paint;container-type:inline-size;background:#0a0a0b;color:#ececef;font-family:'JetBrains Mono',ui-monospace,monospace;isolation:isolate}
.d-maps-map-route-draw-head{position:absolute;z-index:4;top:15px;left:16px;right:16px;display:flex;align-items:center;justify-content:space-between;gap:10px;color:#5c5c66;font-size:10px;line-height:1;letter-spacing:.08em;white-space:nowrap}.d-maps-map-route-draw-head span:first-child{color:#9b9ba3}
.d-maps-map-route-draw-stage{position:absolute;inset:40px 10px 38px;display:grid;place-items:center;box-sizing:border-box;overflow:hidden;border:1px solid #232327;border-radius:10px;background:#101012;outline:none;cursor:crosshair;touch-action:pan-y}
.d-maps-map-route-draw-stage::before{content:'';position:absolute;inset:0;z-index:0;pointer-events:none;background:radial-gradient(circle at 50% 46%,rgba(167,139,250,.045),transparent 62%),linear-gradient(rgba(255,255,255,.012) 1px,transparent 1px);background-size:auto,100% 4px;opacity:.72}
.d-maps-map-route-draw-stage:focus-visible{box-shadow:inset 0 0 0 1px #a78bfa}
.d-maps-map-route-draw-svg{position:relative;z-index:1;display:block;width:min(calc(100% - 4px),356px);height:auto;aspect-ratio:5/3;overflow:visible}
.d-maps-map-route-draw-street{stroke:#2e2e34;stroke-width:1;vector-effect:non-scaling-stroke}
.d-maps-map-route-draw-old-route,.d-maps-map-route-draw-route{fill:none;stroke:#a78bfa;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;vector-effect:non-scaling-stroke}.d-maps-map-route-draw-old-route{opacity:0}.d-maps-map-route-draw-route{filter:drop-shadow(0 0 3px rgba(167,139,250,.28))}
.d-maps-map-route-draw-trail{fill:#a78bfa;pointer-events:none}
.d-maps-map-route-draw-origin{fill:#101012;stroke:#9b9ba3;stroke-width:1;vector-effect:non-scaling-stroke}
.d-maps-map-route-draw-target{fill:#101012;stroke:#5c5c66;stroke-width:1;stroke-dasharray:2 2;vector-effect:non-scaling-stroke}
.d-maps-map-route-draw-candidate{fill:none;stroke:#a78bfa;stroke-width:1;stroke-dasharray:2 2;vector-effect:non-scaling-stroke;opacity:0}.d-maps-map-route-draw[data-keyboard-candidate="true"] .d-maps-map-route-draw-candidate{opacity:.75}
.d-maps-map-route-draw-marker circle{fill:#a78bfa;filter:drop-shadow(0 0 4px rgba(167,139,250,.58))}.d-maps-map-route-draw-marker path{fill:none;stroke:#0a0a0b;stroke-width:1;stroke-linecap:round;stroke-linejoin:round;vector-effect:non-scaling-stroke}
.d-maps-map-route-draw-pin{opacity:0}.d-maps-map-route-draw-pin path{fill:#a78bfa}.d-maps-map-route-draw-pin circle{fill:#0a0a0b}.d-maps-map-route-draw-arrival-ring{fill:none;stroke:#4ade80;stroke-width:1;vector-effect:non-scaling-stroke;opacity:0}
.d-maps-map-route-draw-readout{position:absolute;z-index:3;top:9px;right:9px;min-width:96px;box-sizing:border-box;display:grid;gap:4px;padding:6px 7px;border:1px solid #232327;background:rgba(10,10,11,.88);color:#ececef;font:9px/1 'JetBrains Mono',ui-monospace,monospace;letter-spacing:.04em;text-align:right;font-variant-numeric:tabular-nums;pointer-events:none}.d-maps-map-route-draw-eta{color:#9b9ba3;font-size:8px}
.d-maps-map-route-draw-foot{position:absolute;left:16px;right:16px;bottom:13px;display:flex;align-items:center;justify-content:space-between;gap:8px;color:#5c5c66;font-size:9px;line-height:1;letter-spacing:.05em;white-space:nowrap}.d-maps-map-route-draw-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}
@container(max-width:340px){.d-maps-map-route-draw-head{left:12px;right:12px;font-size:9px}.d-maps-map-route-draw-stage{left:6px;right:6px}.d-maps-map-route-draw-foot{left:10px;right:10px;font-size:8px}.d-maps-map-route-draw-readout{right:7px;font-size:8px;min-width:88px}}
@media(prefers-reduced-motion:reduce){.d-maps-map-route-draw *,.d-maps-map-route-draw *::before,.d-maps-map-route-draw *::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}
`,
  js:`const routeStage=root.querySelector('.d-maps-map-route-draw-stage'),svg=root.querySelector('.d-maps-map-route-draw-svg'),streetLayer=root.querySelector('.d-maps-map-route-draw-streets'),oldRoutePath=root.querySelector('.d-maps-map-route-draw-old-route'),routePath=root.querySelector('.d-maps-map-route-draw-route'),trailLayer=root.querySelector('.d-maps-map-route-draw-trails'),originElement=root.querySelector('.d-maps-map-route-draw-origin'),targetElement=root.querySelector('.d-maps-map-route-draw-target'),candidateElement=root.querySelector('.d-maps-map-route-draw-candidate'),arrivalRing=root.querySelector('.d-maps-map-route-draw-arrival-ring'),pinElement=root.querySelector('.d-maps-map-route-draw-pin'),markerElement=root.querySelector('.d-maps-map-route-draw-marker'),distanceElement=root.querySelector('.d-maps-map-route-draw-distance'),etaElement=root.querySelector('.d-maps-map-route-draw-eta'),status=root.querySelector('.d-maps-map-route-draw-status');
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,controller=new AbortController(),listener={signal:controller.signal},passiveListener={signal:controller.signal,passive:true},svgNamespace=svg.namespaceURI;
const seed=42,columns=9,rows=7,nodeCount=63,logicalWidth=360,logicalHeight=216,streetTop=6,streetBottom=210,streetLeft=6,streetRight=354,originId=55,initialDestinationId=26,initialRawNodeIds=[55,46,45,6,15,24,25,26],initialNodeIds=[55,46,45,6,24,26],baseSpeed=140,speedPerMillisecond=.14,turnSpeedRatio=.6,turnDuration=300,turnHalfDistance=16.8,turnDistance=33.6,trailInterval=400,trailLifetime=800,trailRadius=2,trailAlpha=.55,pinLandTime=300,pinBounceTime=380,pinSettleTime=480,arrivalRingDuration=700,oldFadeDuration=300,maxDelta=50,epsilon=1e-9,collinearEpsilon=1e-8,kmInitialTotal=3.2;
const colors={background:'#0a0a0b',panel:'#101012',bg2:'#161619',line0:'#232327',line1:'#2e2e34',txt0:'#ececef',txt1:'#9b9ba3',txt2:'#5c5c66',acc:'#a78bfa',ok:'#4ade80'};
const expectedChecksums={axisChecksum:'75f246bd',nodeChecksum:'4d0ddc79',edgeChecksum:'7be90eb3',streetChecksum:'15237ca9',initialRouteChecksum:'47b762c0',timelineChecksum:'d50433eb',trailChecksum:'583a6236',motionChecksum:'0ef01607',fixtureChecksum:'d4eb08ea'};
function clamp(value,min,max){return Math.max(min,Math.min(max,value))}
function lerp(first,second,amount){return first+(second-first)*amount}
function smoothstep(value){return value*value*(3-2*value)}
function checksumText(value){let hash=2166136261;for(const character of String(value))hash=Math.imul(hash^character.charCodeAt(0),16777619)>>>0;return hash.toString(16).padStart(8,'0')}
function mulberry32(nextSeed){return function(){let value=nextSeed+=0x6D2B79F5;value=Math.imul(value^(value>>>15),value|1);value^=value+Math.imul(value^(value>>>7),value|61);return((value^(value>>>14))>>>0)/4294967296}}
function cubicCoordinate(parameter,first,second){const inverse=1-parameter;return 3*inverse*inverse*parameter*first+3*inverse*parameter*parameter*second+parameter*parameter*parameter}
function easeOutSoft(value){const bounded=clamp(value,0,1);if(bounded===0||bounded===1)return bounded;let low=0,high=1,parameter=.5;for(let index=0;index<40;index++){parameter=(low+high)/2;if(cubicCoordinate(parameter,.22,.36)<bounded)low=parameter;else high=parameter}return cubicCoordinate((low+high)/2,1,1)}
function formatEta(milliseconds){const seconds=Math.ceil(Math.max(0,milliseconds)/1000),minutes=Math.floor(seconds/60),remainder=seconds-minutes*60;return minutes+':'+String(remainder).padStart(2,'0')}
function pathCoordinate(value){return Number.isInteger(value)?String(value):value.toFixed(9)}
const random=mulberry32(seed),xCoordinates=[16];for(let column=1;column<columns-1;column++)xCoordinates.push(16+column*41+(random()-.5)*10);xCoordinates.push(344);const yCoordinates=[18];for(let row=1;row<rows-1;row++)yCoordinates.push(18+row*30+(random()-.5)*8);yCoordinates.push(198);
function nodeId(column,row){return row*columns+column}
const nodes=[];for(let row=0;row<rows;row++)for(let column=0;column<columns;column++)nodes.push({id:nodeId(column,row),row:row,column:column,x:xCoordinates[column],y:yCoordinates[row]});
const diagonalDefinitions=[[45,6],[56,17],[9,53]],edges=[],adjacency=Array.from({length:nodeCount},function(){return[]});
function addEdge(firstId,secondId,kind){const first=nodes[firstId],second=nodes[secondId],length=Math.hypot(second.x-first.x,second.y-first.y),edge={index:edges.length,first:firstId,second:secondId,kind:kind,length:length};edges.push(edge);adjacency[firstId].push({node:secondId,length:length,edgeIndex:edge.index,kind:kind});adjacency[secondId].push({node:firstId,length:length,edgeIndex:edge.index,kind:kind})}
for(let row=0;row<rows;row++)for(let column=0;column<columns;column++){const id=nodeId(column,row);if(column<columns-1)addEdge(id,id+1,'H');if(row<rows-1)addEdge(id,id+columns,'V')}
for(let diagonalIndex=0;diagonalIndex<diagonalDefinitions.length;diagonalIndex++)addEdge(diagonalDefinitions[diagonalIndex][0],diagonalDefinitions[diagonalIndex][1],'D'+diagonalIndex);
for(const neighbors of adjacency)neighbors.sort(function(first,second){return first.node-second.node});
const streets=[];for(let column=0;column<columns;column++)streets.push({index:streets.length,kind:'V',axisIndex:column,firstId:null,secondId:null,x1:xCoordinates[column],y1:streetTop,x2:xCoordinates[column],y2:streetBottom});for(let row=0;row<rows;row++)streets.push({index:streets.length,kind:'H',axisIndex:row,firstId:null,secondId:null,x1:streetLeft,y1:yCoordinates[row],x2:streetRight,y2:yCoordinates[row]});for(let diagonalIndex=0;diagonalIndex<diagonalDefinitions.length;diagonalIndex++){const definition=diagonalDefinitions[diagonalIndex],first=nodes[definition[0]],second=nodes[definition[1]];streets.push({index:streets.length,kind:'D'+diagonalIndex,axisIndex:diagonalIndex,firstId:first.id,secondId:second.id,x1:first.x,y1:first.y,x2:second.x,y2:second.y})}
const streetFragment=document.createDocumentFragment();for(const street of streets){const line=document.createElementNS(svgNamespace,'line');line.setAttribute('class','d-maps-map-route-draw-street');line.setAttribute('data-kind',street.kind);line.setAttribute('data-index',String(street.index));line.setAttribute('x1',street.x1.toFixed(9));line.setAttribute('y1',street.y1.toFixed(9));line.setAttribute('x2',street.x2.toFixed(9));line.setAttribute('y2',street.y2.toFixed(9));streetFragment.appendChild(line)}streetLayer.appendChild(streetFragment);
function simplifyNodeIds(nodeIds){if(nodeIds.length<3)return nodeIds.slice();const simplified=[nodeIds[0]];for(let index=1;index<nodeIds.length-1;index++){const previous=nodes[nodeIds[index-1]],current=nodes[nodeIds[index]],next=nodes[nodeIds[index+1]],firstX=current.x-previous.x,firstY=current.y-previous.y,secondX=next.x-current.x,secondY=next.y-current.y,cross=firstX*secondY-firstY*secondX,dot=firstX*secondX+firstY*secondY;if(Math.abs(cross)>collinearEpsilon||dot<=0)simplified.push(current.id)}simplified.push(nodeIds[nodeIds.length-1]);return simplified}
function dijkstra(destinationId){const distances=Array(nodeCount).fill(Infinity),previous=Array(nodeCount).fill(-1),settled=Array(nodeCount).fill(false);distances[originId]=0;for(let count=0;count<nodeCount;count++){let current=-1;for(let candidate=0;candidate<nodeCount;candidate++){if(settled[candidate])continue;if(current<0||distances[candidate]<distances[current]-epsilon||Math.abs(distances[candidate]-distances[current])<=epsilon&&candidate<current)current=candidate}if(current<0||!Number.isFinite(distances[current]))break;settled[current]=true;if(current===destinationId)break;for(const neighbor of adjacency[current]){const alternative=distances[current]+neighbor.length;if(alternative<distances[neighbor.node]-epsilon||Math.abs(alternative-distances[neighbor.node])<=epsilon&&(previous[neighbor.node]<0||current<previous[neighbor.node])){distances[neighbor.node]=alternative;previous[neighbor.node]=current}}}const path=[];for(let current=destinationId;current>=0;current=previous[current]){path.push(current);if(current===originId)break}path.reverse();return{destinationId:destinationId,distance:distances[destinationId],rawNodeIds:path,nodeIds:simplifyNodeIds(path)}}
function slowDistance(amount){return turnDistance*amount+4.2/Math.PI*Math.sin(Math.PI*2*amount)}
function routeDistanceAt(route,time){if(route.length===0)return 0;const bounded=clamp(time,0,route.duration);let previousDistance=0,previousTime=0;for(const zone of route.zones){if(bounded<zone.startTime)return previousDistance+(bounded-previousTime)*speedPerMillisecond;if(bounded<=zone.endTime){const amount=(bounded-zone.startTime)/turnDuration;return zone.startDistance+slowDistance(amount)}previousDistance=zone.endDistance;previousTime=zone.endTime}return Math.min(route.length,previousDistance+(bounded-previousTime)*speedPerMillisecond)}
function routeSpeedAt(route,time){if(route.length===0||time>=route.duration)return 0;for(const zone of route.zones){if(time>=zone.startTime&&time<=zone.endTime){const amount=(time-zone.startTime)/turnDuration;return baseSpeed*(.8+.2*Math.cos(Math.PI*2*amount))}}return baseSpeed}
function routePositionAt(route,distance){if(route.waypoints.length===1){const only=route.waypoints[0];return{x:only.x,y:only.y,segment:0,segmentProgress:1,heading:0}}const bounded=clamp(distance,0,route.length);for(let index=1;index<route.waypoints.length;index++){const first=route.waypoints[index-1],second=route.waypoints[index],isLast=index===route.waypoints.length-1;if(bounded<second.cumulative-epsilon||isLast){const segmentLength=second.cumulative-first.cumulative,amount=segmentLength?clamp((bounded-first.cumulative)/segmentLength,0,1):1;return{x:lerp(first.x,second.x,amount),y:lerp(first.y,second.y,amount),segment:index-1,segmentProgress:amount,heading:Math.atan2(second.y-first.y,second.x-first.x)*180/Math.PI}}}const last=route.waypoints[route.waypoints.length-1];return{x:last.x,y:last.y,segment:route.waypoints.length-2,segmentProgress:1,heading:0}}
function buildRoute(nodeIds,rawNodeIds,destinationId,revision,routeSource){const simplified=simplifyNodeIds(nodeIds),waypoints=[],pathParts=[];let cumulative=0;for(let index=0;index<simplified.length;index++){const node=nodes[simplified[index]];if(index){const previous=nodes[simplified[index-1]];cumulative+=Math.hypot(node.x-previous.x,node.y-previous.y)}const waypoint={order:index,id:node.id,row:node.row,column:node.column,x:node.x,y:node.y,cumulative:cumulative,turn:index>0&&index<simplified.length-1};waypoints.push(waypoint);pathParts.push((index?'L ':'M ')+pathCoordinate(node.x)+' '+pathCoordinate(node.y))}const zones=[];let previousDistance=0,previousTime=0;for(let index=1;index<waypoints.length-1;index++){const centerDistance=waypoints[index].cumulative,startDistance=centerDistance-turnHalfDistance,endDistance=centerDistance+turnHalfDistance,startTime=previousTime+(startDistance-previousDistance)/speedPerMillisecond,endTime=startTime+turnDuration;zones.push({turn:index,waypointId:waypoints[index].id,centerDistance:centerDistance,startDistance:startDistance,endDistance:endDistance,startTime:startTime,centerTime:startTime+turnDuration/2,endTime:endTime});previousDistance=endDistance;previousTime=endTime}const length=cumulative,duration=length?previousTime+(length-previousDistance)/speedPerMillisecond:0,trailFixtures=[];for(let sequence=1;sequence*trailInterval<duration;sequence++){const time=sequence*trailInterval,distance=routeDistanceAt({length:length,duration:duration,zones:zones},time),position=routePositionAt({length:length,waypoints:waypoints},distance);trailFixtures.push({sequence:sequence,time:time,distance:distance,x:position.x,y:position.y,segment:position.segment,heading:position.heading})}const lastTrailTime=trailFixtures.length?trailFixtures[trailFixtures.length-1].time:0,trailSettleTime=trailFixtures.length?lastTrailTime+trailLifetime:0,settleTime=Math.max(duration+pinSettleTime,duration+arrivalRingDuration,trailSettleTime);return{revision:revision,routeSource:routeSource||'dijkstra',destinationId:destinationId,rawNodeIds:(rawNodeIds||simplified).slice(),nodeIds:simplified.slice(),waypoints:waypoints,zones:zones,path:pathParts.join(' '),length:length,duration:duration,totalKm:length*kmPerPixel,trailFixtures:trailFixtures,settleTime:settleTime}}
const preliminaryInitial={nodeIds:initialNodeIds,waypoints:[],zones:[]};let initialLength=0;for(let index=0;index<initialNodeIds.length;index++){const node=nodes[initialNodeIds[index]];if(index){const previous=nodes[initialNodeIds[index-1]];initialLength+=Math.hypot(node.x-previous.x,node.y-previous.y)}preliminaryInitial.waypoints.push({order:index,id:node.id,row:node.row,column:node.column,x:node.x,y:node.y,cumulative:initialLength,turn:index>0&&index<initialNodeIds.length-1})}const kmPerPixel=kmInitialTotal/initialLength;
const initialRoute=buildRoute(initialNodeIds,initialRawNodeIds,initialDestinationId,0,'canonical-initial'),rerouteFixtureIds=[62,31,9,55],rerouteFixtures=rerouteFixtureIds.map(function(destinationId){const result=dijkstra(destinationId),route=buildRoute(result.nodeIds,result.rawNodeIds,destinationId,0,'dijkstra');return{destinationId:destinationId,rawNodeIds:route.rawNodeIds.slice(),nodeIds:route.nodeIds.slice(),length:route.length,duration:route.duration,totalKm:route.totalKm,turnCount:route.zones.length}}),motionTimes=[0,400,800,1200,1600,2000,2400,2800,3200,3600,initialRoute.duration],motionAnchors=motionTimes.map(function(time){const distance=routeDistanceAt(initialRoute,time),position=routePositionAt(initialRoute,distance);return{time:time,distance:distance,x:position.x,y:position.y,segment:position.segment,heading:position.heading,km:distance*kmPerPixel}});
const axisChecksum=checksumText(xCoordinates.map(function(value){return value.toFixed(9)}).join(',')+'|'+yCoordinates.map(function(value){return value.toFixed(9)}).join(',')),nodeChecksum=checksumText(nodes.map(function(node){return[node.id,node.row,node.column,node.x.toFixed(9),node.y.toFixed(9)].join(',')}).join('|')),edgeChecksum=checksumText(edges.map(function(edge){return[edge.first,edge.second,edge.kind,edge.length.toFixed(9)].join(',')}).join('|')),streetChecksum=checksumText(streets.map(function(street){if(street.kind==='V')return['V',street.axisIndex,street.x1.toFixed(9),street.y1.toFixed(9),street.x2.toFixed(9),street.y2.toFixed(9)].join(',');if(street.kind==='H')return['H',street.axisIndex,street.x1.toFixed(9),street.y1.toFixed(9),street.x2.toFixed(9),street.y2.toFixed(9)].join(',');return['D',street.axisIndex,street.firstId,street.secondId,street.x1.toFixed(9),street.y1.toFixed(9),street.x2.toFixed(9),street.y2.toFixed(9)].join(',')}).join('|')),initialRouteChecksum=checksumText(initialRoute.waypoints.map(function(waypoint){return[waypoint.order,waypoint.id,waypoint.row,waypoint.column,waypoint.x.toFixed(9),waypoint.y.toFixed(9),waypoint.cumulative.toFixed(9),waypoint.turn?1:0].join(',')}).join('|')),timelineChecksum=checksumText(initialRoute.zones.map(function(zone){return[zone.turn,zone.centerDistance.toFixed(9),zone.startDistance.toFixed(9),zone.endDistance.toFixed(9),zone.startTime.toFixed(9),zone.centerTime.toFixed(9),zone.endTime.toFixed(9)].join(',')}).join('|')),trailChecksum=checksumText(initialRoute.trailFixtures.map(function(trail){return[trail.sequence,trail.time,trail.distance.toFixed(9),trail.x.toFixed(9),trail.y.toFixed(9),trail.segment,trail.heading.toFixed(9)].join(',')}).join('|')),motionChecksum=checksumText(motionAnchors.map(function(anchor){return[anchor.time.toFixed(9),anchor.distance.toFixed(9),anchor.x.toFixed(9),anchor.y.toFixed(9),anchor.segment,anchor.heading.toFixed(9)].join(',')}).join('|')),fixtureChecksum=checksumText([axisChecksum,nodeChecksum,edgeChecksum,streetChecksum,initialRouteChecksum,timelineChecksum,trailChecksum,motionChecksum,seed].join('|'));
const actualChecksums={axisChecksum:axisChecksum,nodeChecksum:nodeChecksum,edgeChecksum:edgeChecksum,streetChecksum:streetChecksum,initialRouteChecksum:initialRouteChecksum,timelineChecksum:timelineChecksum,trailChecksum:trailChecksum,motionChecksum:motionChecksum,fixtureChecksum:fixtureChecksum};for(const key of Object.keys(expectedChecksums))if(actualChecksums[key]!==expectedChecksums[key])throw new Error('Map route fixture drift: '+key+' '+actualChecksums[key]);
let currentRoute=initialRoute,oldRoute=null,routeTime=0,drawDistance=0,marker=routePositionAt(initialRoute,0),currentSpeed=baseSpeed,currentDestinationId=initialDestinationId,candidateId=initialDestinationId,keyboardCandidate=false,focused=false,pointerInside=false,source='initial',arrivalAnnounced=false,routeRevision=0;
let running=false,cleaned=false,visible=!('IntersectionObserver'in window),documentVisible=document.visibilityState!=='hidden',frameId=0,lastWall=0,resizeObserver=null,intersectionObserver=null,connectionObserver=null;
let schedulerFrames=0,renderFrames=0,rafRequests=0,rafCancels=0,schedulerStarts=0,schedulerStops=0,pointerDowns=0,clicks=0,ignoredPointers=0,keyboardMoves=0,keyboardCommits=0,keyboardResets=0,focusEvents=0,blurEvents=0,reroutes=0,arrivals=0,trailEmissions=0,resizeEvents=0,visibilityChanges=0,intersectionChanges=0,resets=0,cleanupCalls=0;
function effectiveTime(){return reduced?currentRoute.duration:routeTime}
function nearestNode(x,y){let best=nodes[0],bestDistance=(best.x-x)*(best.x-x)+(best.y-y)*(best.y-y);for(let index=1;index<nodes.length;index++){const node=nodes[index],distance=(node.x-x)*(node.x-x)+(node.y-y)*(node.y-y);if(distance<bestDistance-epsilon||Math.abs(distance-bestDistance)<=epsilon&&node.id<best.id){best=node;bestDistance=distance}}return best.id}
function eventPoint(event){const matrix=svg.getScreenCTM();if(matrix&&typeof DOMPoint==='function'){const transformed=new DOMPoint(event.clientX,event.clientY).matrixTransform(matrix.inverse());return{x:transformed.x,y:transformed.y}}const rect=svg.getBoundingClientRect();return{x:(event.clientX-rect.left)/rect.width*logicalWidth,y:(event.clientY-rect.top)/rect.height*logicalHeight}}
function pinPose(arrivalAge){if(reduced)return{visible:true,offsetY:0,scaleY:1,opacity:1};if(arrivalAge<0)return{visible:false,offsetY:-14,scaleY:1,opacity:0};if(arrivalAge<pinLandTime){const amount=smoothstep(arrivalAge/pinLandTime);return{visible:true,offsetY:lerp(-14,0,amount),scaleY:lerp(1,.8,amount),opacity:amount}}if(arrivalAge<pinBounceTime){const amount=smoothstep((arrivalAge-pinLandTime)/(pinBounceTime-pinLandTime));return{visible:true,offsetY:lerp(0,-3,amount),scaleY:lerp(.8,1.08,amount),opacity:1}}if(arrivalAge<pinSettleTime){const amount=smoothstep((arrivalAge-pinBounceTime)/(pinSettleTime-pinBounceTime));return{visible:true,offsetY:lerp(-3,0,amount),scaleY:lerp(1.08,1,amount),opacity:1}}return{visible:true,offsetY:0,scaleY:1,opacity:1}}
function ringState(arrivalAge){if(reduced)return{visible:true,radius:8,alpha:.8};if(arrivalAge<0||arrivalAge>=arrivalRingDuration-epsilon)return{visible:false,radius:arrivalAge<0?4:18,alpha:0};const amount=arrivalAge/arrivalRingDuration;return{visible:true,radius:lerp(4,18,amount),alpha:1-amount}}
function trailState(time){if(reduced)return[];const active=[],elapsed=Math.min(time,currentRoute.duration);trailEmissions=0;for(const fixture of currentRoute.trailFixtures){if(fixture.time<=elapsed)trailEmissions++;const age=time-fixture.time;if(age<0||age>=trailLifetime)continue;const amount=age/trailLifetime;active.push({sequence:fixture.sequence,time:fixture.time,age:age,distance:fixture.distance,x:fixture.x,y:fixture.y,segment:fixture.segment,heading:fixture.heading,radius:trailRadius,alpha:trailAlpha*(1-amount)})}return active}
function updateTrailElements(items){while(trailLayer.children.length>items.length)trailLayer.lastElementChild.remove();while(trailLayer.children.length<items.length){const circle=document.createElementNS(svgNamespace,'circle');circle.setAttribute('class','d-maps-map-route-draw-trail');trailLayer.appendChild(circle)}for(let index=0;index<items.length;index++){const item=items[index],circle=trailLayer.children[index];circle.setAttribute('cx',item.x.toFixed(9));circle.setAttribute('cy',item.y.toFixed(9));circle.setAttribute('r',item.radius.toFixed(9));circle.setAttribute('opacity',item.alpha.toFixed(9));circle.setAttribute('data-sequence',String(item.sequence))}}
function renderScene(nextSource){source=nextSource||source;const time=effectiveTime(),arrived=time>=currentRoute.duration,arrivalAge=arrived?time-currentRoute.duration:-1;drawDistance=arrived?currentRoute.length:routeDistanceAt(currentRoute,time);marker=routePositionAt(currentRoute,drawDistance);currentSpeed=routeSpeedAt(currentRoute,time);routePath.setAttribute('d',currentRoute.path);routePath.style.strokeDasharray=currentRoute.length.toFixed(9);routePath.style.strokeDashoffset=(currentRoute.length-drawDistance).toFixed(9);markerElement.setAttribute('transform','translate('+marker.x.toFixed(9)+' '+marker.y.toFixed(9)+') rotate('+marker.heading.toFixed(9)+')');const origin=nodes[originId],destination=nodes[currentDestinationId],candidate=nodes[candidateId];originElement.setAttribute('cx',origin.x.toFixed(9));originElement.setAttribute('cy',origin.y.toFixed(9));targetElement.setAttribute('cx',destination.x.toFixed(9));targetElement.setAttribute('cy',destination.y.toFixed(9));candidateElement.setAttribute('cx',candidate.x.toFixed(9));candidateElement.setAttribute('cy',candidate.y.toFixed(9));if(oldRoute){oldRoutePath.setAttribute('d',oldRoute.path);oldRoutePath.style.strokeDasharray=oldRoute.length.toFixed(9);oldRoutePath.style.strokeDashoffset=(oldRoute.length-oldRoute.drawnDistance).toFixed(9);oldRoute.opacity=reduced?.2:1-.8*easeOutSoft(clamp(time/oldFadeDuration,0,1));oldRoutePath.style.opacity=oldRoute.opacity.toFixed(9)}else{oldRoutePath.removeAttribute('d');oldRoutePath.style.opacity='0'}const trails=trailState(time);updateTrailElements(trails);const pose=pinPose(arrivalAge),ring=ringState(arrivalAge);pinElement.setAttribute('transform','translate('+destination.x.toFixed(9)+' '+(destination.y+pose.offsetY).toFixed(9)+') scale(1 '+pose.scaleY.toFixed(9)+')');pinElement.style.opacity=pose.opacity.toFixed(9);arrivalRing.setAttribute('cx',destination.x.toFixed(9));arrivalRing.setAttribute('cy',destination.y.toFixed(9));arrivalRing.setAttribute('r',ring.radius.toFixed(9));arrivalRing.style.opacity=ring.alpha.toFixed(9);const currentKm=Math.min(currentRoute.totalKm,drawDistance*kmPerPixel),totalKm=currentRoute.totalKm,eta=formatEta(currentRoute.duration-time);distanceElement.textContent=currentKm.toFixed(1)+' → '+totalKm.toFixed(1)+' KM';etaElement.textContent='ETA '+eta;if(arrived&&!arrivalAnnounced){arrivalAnnounced=true;arrivals++;status.textContent='Arrived at intersection '+String(currentDestinationId).padStart(2,'0')+', '+totalKm.toFixed(1)+' kilometers'}root.dataset.keyboardCandidate=String(keyboardCandidate&&focused);lastRender={time:time,arrived:arrived,arrivalAge:arrivalAge,currentKm:currentKm,totalKm:totalKm,eta:eta,trails:trails,pose:pose,ring:ring};renderFrames++;expose(nextSource||'render')}
let lastRender={time:0,arrived:false,arrivalAge:-1,currentKm:0,totalKm:kmInitialTotal,eta:'0:04',trails:[],pose:pinPose(-1),ring:ringState(-1)};
function canRun(){return !reduced&&!cleaned&&root.isConnected&&visible&&documentVisible&&routeTime<currentRoute.settleTime}
function requestNextFrame(){if(reduced||cleaned||frameId||!running)return;rafRequests++;frameId=requestAnimationFrame(frame)}
function frame(now){frameId=0;if(!root.isConnected){cleanup();return}if(!canRun()){running=false;lastWall=0;expose('scheduler pause');return}const delta=clamp(now-lastWall,0,maxDelta);lastWall=now;routeTime=Math.min(currentRoute.settleTime,routeTime+delta);schedulerFrames++;renderScene('animation frame');if(canRun())requestNextFrame();else{running=false;lastWall=0;expose('route settled')}}
function startScheduler(nextSource){if(!canRun()||running)return;running=true;lastWall=performance.now();schedulerStarts++;requestNextFrame();expose(nextSource||'scheduler start')}
function stopScheduler(nextSource){if(frameId){cancelAnimationFrame(frameId);rafCancels++;frameId=0}if(running)schedulerStops++;running=false;lastWall=0;expose(nextSource||'scheduler stop')}
function routeTo(destinationId,nextSource){if(cleaned)return;const time=effectiveTime();oldRoute={revision:currentRoute.revision,routeSource:currentRoute.routeSource,path:currentRoute.path,length:currentRoute.length,drawnDistance:Math.min(currentRoute.length,reduced?currentRoute.length:routeDistanceAt(currentRoute,time)),opacity:1,destinationId:currentDestinationId};routeRevision++;if(destinationId===initialDestinationId)currentRoute=buildRoute(initialNodeIds,initialRawNodeIds,destinationId,routeRevision,'canonical-initial');else{const result=dijkstra(destinationId);currentRoute=buildRoute(result.nodeIds,result.rawNodeIds,destinationId,routeRevision,'dijkstra')}currentDestinationId=destinationId;candidateId=destinationId;routeTime=0;arrivalAnnounced=false;reroutes++;source=nextSource||'reroute';status.textContent='Rerouting from A to intersection '+String(destinationId).padStart(2,'0');if(!reduced)lastWall=running?performance.now():0;renderScene(source);if(!reduced)startScheduler('reroute scheduler')}
function resetDemo(){if(cleaned)return;if(frameId){cancelAnimationFrame(frameId);frameId=0}running=false;lastWall=0;currentRoute=initialRoute;oldRoute=null;routeTime=0;drawDistance=0;marker=routePositionAt(initialRoute,0);currentSpeed=baseSpeed;currentDestinationId=initialDestinationId;candidateId=initialDestinationId;keyboardCandidate=false;pointerInside=false;focused=document.activeElement===routeStage;arrivalAnnounced=false;routeRevision=0;schedulerFrames=0;renderFrames=0;rafRequests=0;rafCancels=0;schedulerStarts=0;schedulerStops=0;pointerDowns=0;clicks=0;ignoredPointers=0;keyboardMoves=0;keyboardCommits=0;keyboardResets=0;focusEvents=0;blurEvents=0;reroutes=0;arrivals=0;trailEmissions=0;resizeEvents=0;visibilityChanges=0;intersectionChanges=0;resets++;status.textContent=reduced?'Route from A to B complete':'Route from A to B replaying';renderScene(reduced?'reduced reset':'reset');if(!reduced)startScheduler('reset scheduler')}
function deepFreeze(value){if(value&&typeof value==='object'&&!Object.isFrozen(value)){Object.freeze(value);Object.keys(value).forEach(function(key){deepFreeze(value[key])})}return value}
function copyRoute(route){return{revision:route.revision,routeSource:route.routeSource,destinationId:route.destinationId,rawNodeIds:route.rawNodeIds.slice(),nodeIds:route.nodeIds.slice(),waypoints:route.waypoints.map(function(item){return Object.assign({},item)}),zones:route.zones.map(function(item){return Object.assign({},item)}),path:route.path,length:route.length,duration:route.duration,totalKm:route.totalKm,trailFixtures:route.trailFixtures.map(function(item){return Object.assign({},item)}),settleTime:route.settleTime}}
function inspect(){const rootRect=root.getBoundingClientRect(),stageRect=routeStage.getBoundingClientRect(),svgRect=svg.getBoundingClientRect(),time=effectiveTime(),arrivalAge=time>=currentRoute.duration?time-currentRoute.duration:-1,pose=pinPose(arrivalAge),ring=ringState(arrivalAge),trails=trailState(time);return deepFreeze({config:{seed:seed,randomDraws:12,xRandomDraws:7,yRandomDraws:5,xStart:16,xEnd:344,xStep:41,xJitter:10,yStart:18,yEnd:198,yStep:30,yJitter:8,rng:{algorithm:'mulberry32',drawCount:12,x:{draws:7,start:16,step:41,jitter:10,end:344},y:{draws:5,start:18,step:30,jitter:8,end:198}},columns:columns,rows:rows,nodeCount:nodeCount,edgeCount:edges.length,streetCount:streets.length,diagonalCount:diagonalDefinitions.length,logicalWidth:logicalWidth,logicalHeight:logicalHeight,streetTop:streetTop,streetBottom:streetBottom,streetLeft:streetLeft,streetRight:streetRight,originId:originId,initialDestinationId:initialDestinationId,specialDestinationId:initialDestinationId,specialRouteRule:'destination-26-always-canonical',baseSpeed:baseSpeed,speedPerMillisecond:speedPerMillisecond,turnSpeedRatio:turnSpeedRatio,turnDuration:turnDuration,turnHalfDistance:turnHalfDistance,turnDistance:turnDistance,trailInterval:trailInterval,trailLifetime:trailLifetime,trailRadius:trailRadius,trailAlpha:trailAlpha,pinLandTime:pinLandTime,pinBounceTime:pinBounceTime,pinSettleTime:pinSettleTime,pinStartOffset:-14,pinStartOpacity:0,pinLandScale:.8,pinBounceOffset:-3,pinBounceScale:1.08,pinSettleScale:1,pinKeyframes:[{time:0,offsetY:-14,scaleY:1,opacity:0},{time:pinLandTime,offsetY:0,scaleY:.8,opacity:1},{time:pinBounceTime,offsetY:-3,scaleY:1.08,opacity:1},{time:pinSettleTime,offsetY:0,scaleY:1,opacity:1}],arrivalRingDuration:arrivalRingDuration,ringStartRadius:4,ringEndRadius:18,ringStartAlpha:1,ringEndAlpha:0,reducedRingRadius:8,reducedRingAlpha:.8,arrivalConfig:{pin:[{time:0,offsetY:-14,scaleY:1,opacity:0},{time:300,offsetY:0,scaleY:.8,opacity:1},{time:380,offsetY:-3,scaleY:1.08,opacity:1},{time:480,offsetY:0,scaleY:1,opacity:1}],ring:{duration:700,startRadius:4,endRadius:18,startAlpha:1,endAlpha:0},reducedRing:{radius:8,alpha:.8}},oldFadeDuration:oldFadeDuration,oldFadeEasing:'cubic-bezier(.22,1,.36,1)',maxDelta:maxDelta,kmPerPixel:kmPerPixel,colors:Object.assign({},colors)},fixtures:{xCoordinates:xCoordinates.slice(),yCoordinates:yCoordinates.slice(),nodes:nodes.map(function(item){return Object.assign({},item)}),edges:edges.map(function(item){return Object.assign({},item)}),streets:streets.map(function(item){return Object.assign({},item)}),diagonals:diagonalDefinitions.map(function(pair,index){return{index:index,first:pair[0],second:pair[1]}}),initialRoute:copyRoute(initialRoute),reroutes:rerouteFixtures.map(function(item){return{destinationId:item.destinationId,rawNodeIds:item.rawNodeIds.slice(),nodeIds:item.nodeIds.slice(),length:item.length,duration:item.duration,totalKm:item.totalKm,turnCount:item.turnCount}}),motionAnchors:motionAnchors.map(function(item){return Object.assign({},item)})},state:{routeTime:routeTime,effectiveTime:time,source:source,routeSource:currentRoute.routeSource,currentDestinationId:currentDestinationId,candidateId:candidateId,routeRevision:routeRevision,drawDistance:drawDistance,currentSpeed:currentSpeed,arrived:time>=currentRoute.duration,settled:reduced||routeTime>=currentRoute.settleTime,currentKm:lastRender.currentKm,totalKm:lastRender.totalKm,eta:lastRender.eta},route:{current:copyRoute(currentRoute),old:oldRoute?Object.assign({},oldRoute):null},marker:Object.assign({},marker),trails:{items:trails.map(function(item){return Object.assign({},item)}),emittedCount:trailEmissions,activeCount:trails.length},arrival:{age:arrivalAge,pin:Object.assign({},pose),ring:Object.assign({},ring),announced:arrivalAnnounced},interaction:{focused:focused,pointerInside:pointerInside,keyboardCandidate:keyboardCandidate,currentNode:currentDestinationId,candidateNode:candidateId,distanceText:distanceElement.textContent,etaText:etaElement.textContent,statusText:status.textContent},counters:{schedulerFrames:schedulerFrames,renderFrames:renderFrames,rafRequests:rafRequests,rafCancels:rafCancels,schedulerStarts:schedulerStarts,schedulerStops:schedulerStops,pointerDowns:pointerDowns,clicks:clicks,ignoredPointers:ignoredPointers,keyboardMoves:keyboardMoves,keyboardCommits:keyboardCommits,keyboardResets:keyboardResets,focusEvents:focusEvents,blurEvents:blurEvents,reroutes:reroutes,arrivals:arrivals,trailEmissions:trailEmissions,resizeEvents:resizeEvents,visibilityChanges:visibilityChanges,intersectionChanges:intersectionChanges,resets:resets,cleanupCalls:cleanupCalls},lifecycle:{running:running,reduced:reduced,cleaned:cleaned,visible:visible,documentVisible:documentVisible,frameId:frameId,signalAborted:controller.signal.aborted,hasResizeObserver:!!resizeObserver,hasIntersectionObserver:!!intersectionObserver,hasConnectionObserver:!!connectionObserver},geometry:{rootWidth:rootRect.width,rootHeight:rootRect.height,rootScrollWidth:root.scrollWidth,rootScrollHeight:root.scrollHeight,stageWidth:stageRect.width,stageHeight:stageRect.height,svgWidth:svgRect.width,svgHeight:svgRect.height,viewBox:svg.getAttribute('viewBox')},checksums:Object.assign({},actualChecksums),data:Object.assign({},root.dataset)})}
function expose(nextSource){source=nextSource||source;const time=effectiveTime(),arrived=time>=currentRoute.duration,arrivalAge=arrived?time-currentRoute.duration:-1;Object.assign(root.dataset,{seed:String(seed),columns:String(columns),rows:String(rows),nodeCount:String(nodeCount),edgeCount:String(edges.length),streetCount:String(streets.length),originId:String(originId),destinationId:String(currentDestinationId),candidateId:String(candidateId),routeRevision:String(routeRevision),routeSource:currentRoute.routeSource,specialDestinationId:String(initialDestinationId),routeLength:currentRoute.length.toFixed(9),routeDuration:currentRoute.duration.toFixed(9),routeTime:routeTime.toFixed(3),effectiveTime:time.toFixed(3),drawDistance:drawDistance.toFixed(9),currentSpeed:currentSpeed.toFixed(9),currentKm:lastRender.currentKm.toFixed(9),totalKm:currentRoute.totalKm.toFixed(9),eta:lastRender.eta,markerX:marker.x.toFixed(9),markerY:marker.y.toFixed(9),markerHeading:marker.heading.toFixed(9),markerSegment:String(marker.segment),arrived:String(arrived),arrivalAge:arrivalAge.toFixed(3),trailEmissions:String(trailEmissions),activeTrails:String(lastRender.trails.length),keyboardCandidate:String(keyboardCandidate&&focused),focused:String(focused),pointerInside:String(pointerInside),running:String(running),visible:String(visible),documentVisible:String(documentVisible),reduced:String(reduced),cleaned:String(cleaned),frameId:String(frameId),schedulerFrames:String(schedulerFrames),renderFrames:String(renderFrames),rafRequests:String(rafRequests),reroutes:String(reroutes),resets:String(resets),axisChecksum:axisChecksum,nodeChecksum:nodeChecksum,edgeChecksum:edgeChecksum,streetChecksum:streetChecksum,initialRouteChecksum:initialRouteChecksum,timelineChecksum:timelineChecksum,trailChecksum:trailChecksum,motionChecksum:motionChecksum,fixtureChecksum:fixtureChecksum,source:source})}
routeStage.addEventListener('pointerenter',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer enter ignored');return}pointerInside=true;expose('pointer enter')},passiveListener);
routeStage.addEventListener('pointerleave',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer leave ignored');return}pointerInside=false;expose('pointer leave')},passiveListener);
routeStage.addEventListener('pointerdown',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer down ignored');return}pointerDowns++;if(document.activeElement!==routeStage)routeStage.focus({preventScroll:true})},passiveListener);
routeStage.addEventListener('click',function(event){clicks++;const point=eventPoint(event),destinationId=nearestNode(point.x,point.y);candidateId=destinationId;keyboardCandidate=false;routeTo(destinationId,'pointer reroute')},listener);
routeStage.addEventListener('focus',function(){focused=true;focusEvents++;keyboardCandidate=true;candidateId=currentDestinationId;renderScene('focus')},listener);
routeStage.addEventListener('blur',function(){focused=false;blurEvents++;keyboardCandidate=false;renderScene('blur')},listener);
routeStage.addEventListener('keydown',function(event){const accepted=['ArrowLeft','ArrowRight','ArrowUp','ArrowDown','Enter',' ','Home','Escape','r','R'];if(accepted.indexOf(event.key)===-1)return;event.preventDefault();if(event.key==='r'||event.key==='R'){resetDemo();keyboardResets++;expose('keyboard reset');return}if(event.key==='Enter'||event.key===' '){keyboardCommits++;routeTo(candidateId,'keyboard reroute');return}if(event.key==='Escape'){candidateId=currentDestinationId;keyboardCandidate=true;renderScene('keyboard escape');status.textContent='Destination candidate restored';return}if(event.key==='Home'){candidateId=originId;keyboardCandidate=true;keyboardMoves++;renderScene('keyboard home');status.textContent='Origin selected as destination';return}const current=nodes[candidateId];let row=current.row,column=current.column;if(event.key==='ArrowLeft')column--;if(event.key==='ArrowRight')column++;if(event.key==='ArrowUp')row--;if(event.key==='ArrowDown')row++;row=clamp(row,0,rows-1);column=clamp(column,0,columns-1);candidateId=nodeId(column,row);keyboardCandidate=true;keyboardMoves++;renderScene('keyboard candidate');status.textContent='Destination candidate row '+(row+1)+', column '+(column+1)+'. Press Enter to reroute'},listener);
function onVisibility(){documentVisible=document.visibilityState!=='hidden';visibilityChanges++;if(documentVisible)startScheduler('document visible');else stopScheduler('document hidden')}document.addEventListener('visibilitychange',onVisibility,listener);
function cleanup(){if(cleaned)return;cleanupCalls++;cleaned=true;if(frameId){cancelAnimationFrame(frameId);rafCancels++;frameId=0}running=false;controller.abort();if(resizeObserver)resizeObserver.disconnect();if(intersectionObserver)intersectionObserver.disconnect();if(connectionObserver)connectionObserver.disconnect();expose('cleanup')}
if('ResizeObserver'in window){resizeObserver=new ResizeObserver(function(){if(cleaned)return;resizeEvents++;renderScene('resize')});resizeObserver.observe(svg)}
if('IntersectionObserver'in window){intersectionObserver=new IntersectionObserver(function(entries){if(!entries.length||cleaned)return;visible=entries[0].isIntersecting;intersectionChanges++;if(visible)startScheduler('intersection visible');else stopScheduler('intersection hidden')},{threshold:.05});intersectionObserver.observe(root)}
if('MutationObserver'in window){connectionObserver=new MutationObserver(function(){if(!root.isConnected)cleanup()});connectionObserver.observe(document.documentElement,{childList:true,subtree:true})}
root.__mapRouteDrawInspect=Object.freeze(inspect);root.__mapRouteDrawReset=Object.freeze(resetDemo);renderScene(reduced?'reduced static':'initial');if(!reduced)startScheduler('initial scheduler');`,
  prompt:`Build a self-contained responsive 320px Maps & Geo SVG card using bg0 #0a0a0b, bg1 #101012, bg2 #161619, line0 #232327, line1 #2e2e34, txt0 #ececef, txt1 #9b9ba3, txt2 #5c5c66, accent #a78bfa and success #4ade80. Use one fixed 360 by 216 viewBox and non-scaling strokes. The stage is the sole focusable control and the card must remain free of horizontal and vertical overflow at both 380px and 320px widths.

Construct a deterministic 9-column by 7-row Manhattan graph from one standard mulberry32 stream seeded at 42. Consume exactly twelve random values: first seven interior x coordinates using x = 16 + column times 41 + (random - 0.5) times 10, with fixed outer coordinates 16 and 344; then five interior y coordinates using y = 18 + row times 30 + (random - 0.5) times 8, with fixed outer coordinates 18 and 198. Index exactly 63 nodes row-major as row times 9 plus column. Draw nine vertical streets from y 6 to 210 and seven horizontal streets from x 6 to 354, all as one-pixel line1 strokes. Add three diagonal express avenues joining node 45 to 6, node 56 to 17, and node 9 to 53. Treat their crossings as overpasses and connect only their endpoints. Build exactly 113 undirected Euclidean-weight graph edges.

Keep A fixed at node 55. The initial B is node 26. Its exact canonical raw graph expansion is [55,46,45,6,15,24,25,26], and removing forward-collinear nodes produces the rendered and timed route [55,46,45,6,24,26]. Expose both lists. Draw the simplified route as a two-pixel round accent stroke with dash offset equal to total length minus drawn length. Its canonical length is 503.566209478px. Set the permanent scale to 3.2 divided by that length kilometers per pixel, so its readout progresses from 0.0 to exactly 3.2 KM. Put a diameter-eight accent marker with a dark heading chevron at the exact draw edge.

Travel at 140px per second except for a symmetric 300ms envelope centered on every true turn. For normalized envelope time u, speed is 140 times (0.8 + 0.2 cos(2 pi u)); it begins and ends at 140 and reaches exactly 84px per second, sixty percent, at the turn. Its integrated distance is 33.6u + (4.2 / pi) sin(2 pi u), so it covers 33.6px and each turn lies 16.8px from either edge. The four initial turn-center times are 222.453780943ms, 582.532620366ms, 2713.630983668ms and 3223.086677857ms. Initial arrival is exactly 3836.901496272ms. Interpolate positions and headings from canonical waypoint geometry instead of relying on browser path-length measurements.

At each exact positive multiple of 400ms strictly before arrival, emit a stationary accent trail dot at the marker position. Give each dot constant radius two, lifetime 800ms, and alpha 0.55 times (1 - age / 800). Synchronize a top-right tabular readout formatted current one-decimal kilometers, a right arrow, total one-decimal kilometers, plus ETA using the ceiling of remaining route milliseconds in whole seconds.

On arrival, drop the destination pin through active-time-derived poses: at age zero it is 14px high with scaleY one and opacity zero; at 300ms it lands at zero offset with scaleY 0.8 and opacity one; at 380ms it rebounds to negative 3px with scaleY 1.08; at 480ms it settles at zero and scaleY one. Smoothstep within each interval. Simultaneously pulse a one-pixel success ring from radius 4 to 18 while alpha falls from one to zero over 700ms. Keep the landed pin visible and stop the scheduler after all route, trail, pin and ring work settles.

Clicking converts client coordinates through the inverse SVG transform and chooses the nearest node by squared distance, breaking exact ties by lower node ID. Keep A fixed. Destination 26 is an explicit special route: initial load, reset, same-B replay and every later selection of node 26 must always reuse canonical [55,46,45,6,24,26], never Dijkstra's shorter alternative. For every destination other than 26 compute a new route with Dijkstra: choose the unsettled node by distance then lower ID, visit neighbors in ascending node ID, and on equal relaxation use the lower predecessor. Remove only forward-collinear intermediate nodes before building turn envelopes. Selecting A produces immediate arrival. Preserve only the already-drawn portion of the outgoing route and fade its opacity from one to 0.2 over 300ms with cubic-bezier(0.22,1,0.36,1), while the new route starts at zero. Use the permanent initial kilometer scale for every rerouted total and expose routeSource plus specialDestinationId 26 in inspection and data.

Provide keyboard parity on the sole stage: arrows move a visible destination candidate through the 9 by 7 grid, Enter or Space commits, Home selects A, Escape restores current B, and R restores and replays the initial route. Announce deliberate candidate, reroute and arrival changes through a polite live region without announcing per-frame readout changes.

Drive everything from one requestAnimationFrame scheduler with a 50ms delta cap and no timers. Start only after intersection, pause on document hide or leaving the viewport, and reset the wall clock on resume so active route time never catches up. Use AbortController for every listener, ResizeObserver, IntersectionObserver and MutationObserver detach cleanup. Under reduced motion make zero animation-frame requests, leave authoritative routeTime at zero, show each current route fully drawn with its marker and landed pin at B, use a static success ring of radius eight at 80% opacity, omit trails, and apply click and keyboard reroutes instantly. Disable transitions and animations on elements and pseudo-elements.

Freeze the exact inspection and reset functions, make reset inert after cleanup, and deep-freeze every inspection result. Expose the full seeded axes, all nodes, edges and street geometry, diagonal definitions, initial and live raw/simplified routes, waypoints, turn zones, motion and trail fixtures, draw and marker state, old-route fade, readouts, arrival pose and ring, pointer and keyboard state, counters, scheduler/observer lifecycle, responsive bounds, root data, and nine-decimal FNV-1a checksums for axes, nodes, edges, streets, initial route, timeline, trails, motion and their combined fixture.`
});
