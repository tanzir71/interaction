/* INTRX registry — deterministic dotted route globe */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
  id:'map-dot-globe-arcs',
  title:'Map Dot Globe Arcs',
  cat:'Maps & Geo',
  rootClass:'d-maps-map-dot-globe-arcs',
  tags:['canvas','globe','routes'],
  libs:[],
  desc:'A dense procedural dot globe carries deterministic city-to-city routes. Violet arc heads cross the near hemisphere, arrivals pulse, and hovering or focusing slows the orbit for inspection.',
  seen:'Seen on: GitHub-globe interfaces, point-globe agency heroes, and global network telemetry maps',
  hint:'hover or focus to slow the globe; press R to restart the deterministic route sequence',
  html:`<div class="d-maps-map-dot-globe-arcs" role="region" aria-label="Interactive dot globe routes">
  <div class="d-maps-map-dot-globe-arcs-head" aria-hidden="true"><span>MAP / ROUTES</span><span>32×64 · 16S ORBIT</span></div>
  <div class="d-maps-map-dot-globe-arcs-stage" tabindex="0" aria-keyshortcuts="R" aria-label="Rotating dotted route globe. Hover or focus to slow the orbit. Press R to reset the route sequence.">
    <canvas class="d-maps-map-dot-globe-arcs-canvas" width="360" height="244" role="img" aria-label="A rotating dotted globe with animated city routes"></canvas>
    <span class="d-maps-map-dot-globe-arcs-route" aria-hidden="true"><i></i><b class="d-maps-map-dot-globe-arcs-route-pair">SYD → SIN</b></span>
  </div>
  <div class="d-maps-map-dot-globe-arcs-foot" aria-hidden="true"><span>8 CITIES</span><span>2,048 POINTS</span><span>LIVE ARC</span></div>
  <span class="d-maps-map-dot-globe-arcs-status" aria-live="polite" aria-atomic="true">Route from Sydney to Singapore drawing</span>
</div>`,
  css:`
.d-maps-map-dot-globe-arcs{position:relative;width:100%;height:320px;box-sizing:border-box;overflow:hidden;contain:layout paint;container-type:inline-size;background:#0a0a0b;color:#ececef;font-family:'JetBrains Mono',ui-monospace,monospace;isolation:isolate}
.d-maps-map-dot-globe-arcs-head{position:absolute;z-index:3;top:15px;left:16px;right:16px;display:flex;align-items:center;justify-content:space-between;gap:10px;color:#5c5c66;font-size:10px;line-height:1;letter-spacing:.08em;white-space:nowrap}
.d-maps-map-dot-globe-arcs-head span:first-child{color:#9b9ba3}
.d-maps-map-dot-globe-arcs-stage{position:absolute;inset:36px 8px 34px;display:grid;place-items:center;box-sizing:border-box;overflow:hidden;border:1px solid #232327;border-radius:10px;background:radial-gradient(circle at 50% 49%,#101012 0,#0a0a0b 69%);outline:none;cursor:crosshair;touch-action:pan-y}
.d-maps-map-dot-globe-arcs-stage::before,.d-maps-map-dot-globe-arcs-stage::after{content:'';position:absolute;z-index:1;pointer-events:none}.d-maps-map-dot-globe-arcs-stage::before{left:50%;top:10px;bottom:10px;width:1px;background:linear-gradient(transparent,#232327,transparent);opacity:.42}.d-maps-map-dot-globe-arcs-stage::after{left:10px;right:10px;top:50%;height:1px;background:linear-gradient(90deg,transparent,#232327,transparent);opacity:.3}
.d-maps-map-dot-globe-arcs-stage:focus-visible{box-shadow:inset 0 0 0 1px #a78bfa}
.d-maps-map-dot-globe-arcs-canvas{position:relative;z-index:2;display:block;width:min(100%,360px);height:auto;aspect-ratio:90/61}
.d-maps-map-dot-globe-arcs-route{position:absolute;z-index:4;top:9px;left:9px;display:flex;align-items:center;gap:6px;padding:5px 7px;border:1px solid #232327;background:rgba(10,10,11,.84);color:#9b9ba3;font-size:9px;line-height:1;letter-spacing:.06em;pointer-events:none}
.d-maps-map-dot-globe-arcs-route i{width:5px;height:5px;border-radius:50%;background:#a78bfa;box-shadow:0 0 8px rgba(167,139,250,.62)}
.d-maps-map-dot-globe-arcs-route b{font:inherit;color:#ececef}
.d-maps-map-dot-globe-arcs-foot{position:absolute;left:16px;right:16px;bottom:13px;display:flex;align-items:center;justify-content:space-between;gap:8px;color:#5c5c66;font-size:9px;line-height:1;letter-spacing:.05em;white-space:nowrap}
.d-maps-map-dot-globe-arcs-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}
@container(max-width:340px){.d-maps-map-dot-globe-arcs-head{left:12px;right:12px;font-size:9px}.d-maps-map-dot-globe-arcs-stage{left:6px;right:6px}.d-maps-map-dot-globe-arcs-foot{left:10px;right:10px;font-size:8px}.d-maps-map-dot-globe-arcs-route{font-size:8px}}
@media(prefers-reduced-motion:reduce){.d-maps-map-dot-globe-arcs *,.d-maps-map-dot-globe-arcs *::before,.d-maps-map-dot-globe-arcs *::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}
`,
  js:`const globeStage=root.querySelector('.d-maps-map-dot-globe-arcs-stage'),canvas=root.querySelector('.d-maps-map-dot-globe-arcs-canvas'),context=canvas.getContext('2d',{alpha:false}),routePair=root.querySelector('.d-maps-map-dot-globe-arcs-route-pair'),status=root.querySelector('.d-maps-map-dot-globe-arcs-status');
if(!context)throw new Error('Dot globe canvas unavailable');
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,controller=new AbortController(),listener={signal:controller.signal};
const rings=32,pointsPerRing=64,pointCount=rings*pointsPerRing,logicalWidth=360,logicalHeight=244,centerX=180,centerY=122,sphereRadius=90,dotRadius=1.5,cityRadius=2.5,rotationPeriod=16000,angularVelocity=Math.PI*2/rotationPeriod,initialYaw=.35,speedSlow=.4,speedNormal=1,speedLerp=.05,maxDelta=50,noiseSeed=24681357,noiseThreshold=.55,noiseOctaves=2,pairSeed=42,cityCount=8,launchInterval=2500,drawDuration=900,holdDuration=600,fadeDuration=400,routeLifetime=1900,maxConcurrent=3,arcLift=.3,arcRadiusAtMidpoint=1.3,controlMidpointScale=2.6,controlEndpointScale=-.5,arcSamples=49,sampleSegments=48,tailLength=.36,tailAlphaMin=.08,tailWidth=1.5,headRadius=3,pulseStart=900,pulseEnd=1500,pulseRadiusStart=4,pulseRadiusEnd=14,pulseWidth=1,easeX1=.42,easeY1=0,easeX2=.58,easeY2=1,easeIterations=40;
const colors={background:'#0a0a0b',panel:'#101012',bg2:'#161619',line0:'#232327',line1:'#2e2e34',txt0:'#ececef',txt1:'#9b9ba3',txt2:'#5c5c66',acc:'#a78bfa'};
const cityDefinitions=[['NYC','New York',40.7128,-74.0060],['LON','London',51.5074,-.1278],['TYO','Tokyo',35.6762,139.6503],['SIN','Singapore',1.3521,103.8198],['SYD','Sydney',-33.8688,151.2093],['DXB','Dubai',25.2048,55.2708],['SAO','São Paulo',-23.5505,-46.6333],['CPT','Cape Town',-33.9249,18.4241]];
let activeTime=0,yaw=initialYaw,yawBefore=initialYaw,yawAfter=initialYaw,lastDelta=0,currentSpeed=speedNormal,targetSpeed=speedNormal,speedBefore=speedNormal,speedAfter=speedNormal,speedSource='initial',pointerInside=false,focused=false,source='initial';
let pairState=pairSeed,pairRandomDraws=0,nextPairIndex=0,activeRoutes=[],launchCount=0,observedMax=0,capEnforcements=0,routeExpirations=0;
let schedulerFrames=0,renderFrames=0,projectionBuilds=0,pointerEnters=0,pointerLeaves=0,ignoredPointers=0,focusEvents=0,blurEvents=0,keyboardResets=0,resizeEvents=0,visibilityChanges=0,intersectionChanges=0,schedulerStarts=0,schedulerStops=0,rafRequests=0,rafCancels=0,resets=0,cleanupCalls=0;
let visible=!('IntersectionObserver'in window),documentVisible=document.visibilityState!=='hidden',running=false,cleaned=false,frameId=0,lastWall=0,resizeObserver=null,intersectionObserver=null,connectionObserver=null,dpr=1,cssWidth=0,cssHeight=0,lastProjection=null,lastRouteStates=[];
function clamp(value,min,max){return Math.max(min,Math.min(max,value))}
function lerp(a,b,t){return a+(b-a)*t}
function smooth(value){return value*value*(3-2*value)}
function wrap(value){const turn=Math.PI*2;return((value%turn)+turn)%turn}
function checksumText(value){let hash=2166136261;for(const character of String(value))hash=Math.imul(hash^character.charCodeAt(0),16777619)>>>0;return hash.toString(16).padStart(8,'0')}
function latticeHash(x,y,z){let hash=noiseSeed^Math.imul(x,374761393)^Math.imul(y,668265263)^Math.imul(z,1442695041);hash=Math.imul(hash^(hash>>>13),1274126177);return((hash^(hash>>>16))>>>0)/4294967295}
function valueNoise(x,y,z){const x0=Math.floor(x),y0=Math.floor(y),z0=Math.floor(z),fx=smooth(x-x0),fy=smooth(y-y0),fz=smooth(z-z0),n000=latticeHash(x0,y0,z0),n100=latticeHash(x0+1,y0,z0),n010=latticeHash(x0,y0+1,z0),n110=latticeHash(x0+1,y0+1,z0),n001=latticeHash(x0,y0,z0+1),n101=latticeHash(x0+1,y0,z0+1),n011=latticeHash(x0,y0+1,z0+1),n111=latticeHash(x0+1,y0+1,z0+1),x00=lerp(n000,n100,fx),x10=lerp(n010,n110,fx),x01=lerp(n001,n101,fx),x11=lerp(n011,n111,fx);return lerp(lerp(x00,x10,fy),lerp(x01,x11,fy),fz)}
function continentNoise(x,y,z){const first=valueNoise(x*1.35+4.1,y*1.35-2.7,z*1.35+1.9),second=valueNoise(x*2.7-3.4,y*2.7+5.2,z*2.7-.8);return first*.67+second*.33}
function buildPoints(){const built=[];for(let ring=0;ring<rings;ring++){const latitude=Math.PI/2-(ring+.5)/rings*Math.PI,cosLatitude=Math.cos(latitude),sinLatitude=Math.sin(latitude);for(let column=0;column<pointsPerRing;column++){const longitude=column/pointsPerRing*Math.PI*2-Math.PI,x=cosLatitude*Math.cos(longitude),y=sinLatitude,z=cosLatitude*Math.sin(longitude),noise=continentNoise(x,y,z);built.push({index:ring*pointsPerRing+column,ring:ring,column:column,latitude:latitude,longitude:longitude,x:x,y:y,z:z,noise:noise,land:noise>=noiseThreshold})}}return built}
function buildCities(){return cityDefinitions.map(function(definition,index){const latitude=definition[2]*Math.PI/180,longitude=definition[3]*Math.PI/180,cosLatitude=Math.cos(latitude);return{index:index,id:definition[0],name:definition[1],latitudeDegrees:definition[2],longitudeDegrees:definition[3],latitude:latitude,longitude:longitude,x:cosLatitude*Math.cos(longitude),y:Math.sin(latitude),z:cosLatitude*Math.sin(longitude)}})}
function mulberry32(nextSeed){return function(){let value=nextSeed+=0x6D2B79F5;value=Math.imul(value^(value>>>15),value|1);value^=value+Math.imul(value^(value>>>7),value|61);return((value^(value>>>14))>>>0)/4294967296}}
function buildFixturePairs(){const random=mulberry32(pairSeed),built=[];for(let index=0;index<32;index++){const sourceIndex=Math.floor(random()*cityCount),rawDestination=Math.floor(random()*(cityCount-1)),destinationIndex=rawDestination>=sourceIndex?rawDestination+1:rawDestination;built.push({index:index,source:sourceIndex,destination:destinationIndex,sourceId:cityDefinitions[sourceIndex][0],destinationId:cityDefinitions[destinationIndex][0]})}return built}
function arcPoint(arc,t){const inverse=1-t;return{x:inverse*inverse*arc.start.x+2*inverse*t*arc.control.x+t*t*arc.end.x,y:inverse*inverse*arc.start.y+2*inverse*t*arc.control.y+t*t*arc.end.y,z:inverse*inverse*arc.start.z+2*inverse*t*arc.control.z+t*t*arc.end.z}}
function buildArcFixture(pair){const start=cities[pair.source],end=cities[pair.destination],sumX=start.x+end.x,sumY=start.y+end.y,sumZ=start.z+end.z,length=Math.hypot(sumX,sumY,sumZ),mid={x:sumX/length,y:sumY/length,z:sumZ/length},control={x:controlMidpointScale*mid.x+controlEndpointScale*sumX,y:controlMidpointScale*mid.y+controlEndpointScale*sumY,z:controlMidpointScale*mid.z+controlEndpointScale*sumZ},fixture={pairIndex:pair.index,source:pair.source,destination:pair.destination,sourceId:start.id,destinationId:end.id,start:{x:start.x,y:start.y,z:start.z},end:{x:end.x,y:end.y,z:end.z},surfaceMidpoint:mid,control:control,samples:[]};for(let index=0;index<arcSamples;index++){const t=index/sampleSegments,point=arcPoint(fixture,t);fixture.samples.push({index:index,t:t,x:point.x,y:point.y,z:point.z})}const midpoint=fixture.samples[24];fixture.midpoint={x:midpoint.x,y:midpoint.y,z:midpoint.z,radius:Math.hypot(midpoint.x,midpoint.y,midpoint.z)};return fixture}
const points=buildPoints(),cities=buildCities(),fixturePairs=buildFixturePairs(),arcFixtures=fixturePairs.map(buildArcFixture),landCount=points.filter(function(point){return point.land}).length,oceanCount=pointCount-landCount;
const pointChecksum=checksumText(points.map(function(point){return[point.index,point.ring,point.column,point.latitude.toFixed(9),point.longitude.toFixed(9),point.x.toFixed(9),point.y.toFixed(9),point.z.toFixed(9),point.noise.toFixed(9),point.land?1:0].join(',')}).join('|')),landChecksum=checksumText(points.map(function(point){return point.land?1:0}).join('')),noiseChecksum=checksumText(points.map(function(point){return point.noise.toFixed(9)}).join('|')),cityChecksum=checksumText(cities.map(function(city){return[city.index,city.id,city.latitudeDegrees.toFixed(4),city.longitudeDegrees.toFixed(4),city.latitude.toFixed(9),city.longitude.toFixed(9),city.x.toFixed(9),city.y.toFixed(9),city.z.toFixed(9)].join(',')}).join('|')),pairChecksum=checksumText(fixturePairs.map(function(pair){return[pair.index,pair.source,pair.destination].join(',')}).join('|')),arcChecksum=checksumText(arcFixtures.flatMap(function(arc){return arc.samples.map(function(sample){return[arc.pairIndex,sample.index,sample.t.toFixed(9),sample.x.toFixed(9),sample.y.toFixed(9),sample.z.toFixed(9)].join(',')})}).join('|'));
function cubicCoordinate(t,first,second){const inverse=1-t;return 3*inverse*inverse*t*first+3*inverse*t*t*second+t*t*t}
function easeInOut(value){const bounded=clamp(value,0,1);if(bounded===0||bounded===1)return bounded;let low=0,high=1,parameter=.5;for(let index=0;index<easeIterations;index++){parameter=(low+high)/2;if(cubicCoordinate(parameter,easeX1,easeX2)<bounded)low=parameter;else high=parameter}return cubicCoordinate((low+high)/2,easeY1,easeY2)}
function projectWorld(x,y,z){const cosine=Math.cos(yaw),sine=Math.sin(yaw),viewX=x*cosine+z*sine,viewZ=-x*sine+z*cosine,viewY=y;return{viewX:viewX,viewY:viewY,viewZ:viewZ,screenX:centerX+viewX*sphereRadius,screenY:centerY-viewY*sphereRadius,front:viewZ>0}}
function rebuildProjection(){const visiblePoints=[];let backfaceCount=0,visibleLand=0,visibleOcean=0,minX=Infinity,maxX=-Infinity,minY=Infinity,maxY=-Infinity;for(const point of points){const projected=projectWorld(point.x,point.y,point.z),item={index:point.index,ring:point.ring,column:point.column,worldX:point.x,worldY:point.y,worldZ:point.z,viewX:projected.viewX,viewY:projected.viewY,viewZ:projected.viewZ,screenX:projected.screenX,screenY:projected.screenY,land:point.land,noise:point.noise,color:point.land?colors.txt1:colors.bg2,alpha:point.land?1:.5,radius:dotRadius,front:projected.front};if(!projected.front){backfaceCount++;continue}visiblePoints.push(item);if(point.land)visibleLand++;else visibleOcean++;minX=Math.min(minX,item.screenX);maxX=Math.max(maxX,item.screenX);minY=Math.min(minY,item.screenY);maxY=Math.max(maxY,item.screenY)}const cityItems=cities.map(function(city){const projected=projectWorld(city.x,city.y,city.z);return{index:city.index,id:city.id,name:city.name,worldX:city.x,worldY:city.y,worldZ:city.z,viewX:projected.viewX,viewY:projected.viewY,viewZ:projected.viewZ,screenX:projected.screenX,screenY:projected.screenY,front:projected.front,radius:cityRadius,color:colors.txt0}}),projectionPointChecksum=checksumText(visiblePoints.map(function(point){return[point.index,point.screenX.toFixed(3),point.screenY.toFixed(3),point.viewZ.toFixed(9),point.land?1:0].join(',')}).join('|')),projectionCityChecksum=checksumText(cityItems.map(function(city){return[city.id,city.screenX.toFixed(3),city.screenY.toFixed(3),city.viewZ.toFixed(9),city.front?1:0].join(',')}).join('|'));projectionBuilds++;lastProjection={visiblePoints:visiblePoints,visibleCount:visiblePoints.length,backfaceCount:backfaceCount,visibleLand:visibleLand,visibleOcean:visibleOcean,cityItems:cityItems,frontCityIds:cityItems.filter(function(city){return city.front}).map(function(city){return city.id}),bounds:{minX:minX,maxX:maxX,minY:minY,maxY:maxY},pointChecksum:projectionPointChecksum,cityChecksum:projectionCityChecksum}}
function pairRandom(){pairState+=0x6D2B79F5;let value=pairState;value=Math.imul(value^(value>>>15),value|1);value^=value+Math.imul(value^(value>>>7),value|61);pairRandomDraws++;return((value^(value>>>14))>>>0)/4294967296}
function launchRoute(index,launchTime){const sourceIndex=Math.floor(pairRandom()*cityCount),rawDestination=Math.floor(pairRandom()*(cityCount-1)),destinationIndex=rawDestination>=sourceIndex?rawDestination+1:rawDestination;if(activeRoutes.length>=maxConcurrent){activeRoutes.shift();capEnforcements++}activeRoutes.push({index:index,source:sourceIndex,destination:destinationIndex,launchTime:launchTime,arc:buildArcFixture({index:index,source:sourceIndex,destination:destinationIndex})});launchCount++;nextPairIndex=index+1;observedMax=Math.max(observedMax,activeRoutes.length);routePair.textContent=cities[sourceIndex].id+' → '+cities[destinationIndex].id;status.textContent='Route from '+cities[sourceIndex].name+' to '+cities[destinationIndex].name+' drawing'}
function effectiveRouteAge(route){return reduced&&route.index===0?drawDuration:Math.max(0,activeTime-route.launchTime)}
function routeSnapshot(route){const age=Math.max(0,activeTime-route.launchTime),effectiveAge=effectiveRouteAge(route),phase=effectiveAge<drawDuration?'draw':effectiveAge<drawDuration+holdDuration?'hold':effectiveAge<routeLifetime?'fade':'expired',rawProgress=phase==='draw'?clamp(effectiveAge/drawDuration,0,1):1,easedProgress=phase==='draw'?easeInOut(rawProgress):1,opacity=phase==='fade'?clamp(1-(effectiveAge-drawDuration-holdDuration)/fadeDuration,0,1):phase==='expired'?0:1,tailStart=phase==='draw'?Math.max(0,easedProgress-tailLength):0,headT=easedProgress,worldHead=arcPoint(route.arc,headT),pulseProgress=clamp((effectiveAge-pulseStart)/(pulseEnd-pulseStart),0,1),pulseActive=effectiveAge>=pulseStart&&effectiveAge<pulseEnd,projectedHead=null,drawnSegments=[];const headProjection=projectWorld(worldHead.x,worldHead.y,worldHead.z);return{index:route.index,source:route.source,destination:route.destination,sourceId:cities[route.source].id,destinationId:cities[route.destination].id,launchTime:route.launchTime,age:age,effectiveAge:effectiveAge,phase:phase,rawProgress:rawProgress,easedProgress:easedProgress,opacity:opacity,tailStart:tailStart,headT:headT,worldHead:worldHead,projectedHead:{screenX:headProjection.screenX,screenY:headProjection.screenY,viewX:headProjection.viewX,viewY:headProjection.viewY,viewZ:headProjection.viewZ,front:headProjection.front},headFront:headProjection.front,pulse:{active:pulseActive,progress:pulseProgress,radius:lerp(pulseRadiusStart,pulseRadiusEnd,pulseProgress),alpha:1-pulseProgress,width:pulseWidth},drawnSegments:[]}}
function updateRoutes(){const retained=[];for(const route of activeRoutes){if(activeTime-route.launchTime<routeLifetime)retained.push(route);else routeExpirations++}activeRoutes=retained;while(activeTime>=nextPairIndex*launchInterval)launchRoute(nextPairIndex,nextPairIndex*launchInterval)}
function drawRoute(route){const snapshot=routeSnapshot(route),progress=snapshot.headT,start=snapshot.tailStart,denominator=Math.max(progress-start,1e-9);context.strokeStyle=colors.acc;context.lineWidth=tailWidth;context.lineCap='round';context.lineJoin='round';if(progress>start){for(let segmentIndex=0;segmentIndex<sampleSegments;segmentIndex++){const segmentStart=Math.max(start,segmentIndex/sampleSegments),segmentEnd=Math.min(progress,(segmentIndex+1)/sampleSegments);if(segmentEnd<=segmentStart)continue;const startPoint=arcPoint(route.arc,segmentStart),endPoint=arcPoint(route.arc,segmentEnd),projectedStart=projectWorld(startPoint.x,startPoint.y,startPoint.z),projectedEnd=projectWorld(endPoint.x,endPoint.y,endPoint.z),midT=(segmentStart+segmentEnd)/2,local=clamp((midT-start)/denominator,0,1),alpha=snapshot.opacity*(tailAlphaMin+(1-tailAlphaMin)*local);if(!projectedStart.front||!projectedEnd.front)continue;context.globalAlpha=alpha;context.beginPath();context.moveTo(projectedStart.screenX,projectedStart.screenY);context.lineTo(projectedEnd.screenX,projectedEnd.screenY);context.stroke();snapshot.drawnSegments.push({index:segmentIndex,startT:segmentStart,endT:segmentEnd,midT:midT,alpha:alpha,startViewZ:projectedStart.viewZ,endViewZ:projectedEnd.viewZ,startX:projectedStart.screenX,startY:projectedStart.screenY,endX:projectedEnd.screenX,endY:projectedEnd.screenY})}}
if(snapshot.pulse.active){const destination=cities[route.destination],projectedDestination=projectWorld(destination.x,destination.y,destination.z);if(projectedDestination.front){context.globalAlpha=snapshot.pulse.alpha;context.strokeStyle=colors.acc;context.lineWidth=pulseWidth;context.lineCap='round';context.lineJoin='round';context.beginPath();context.arc(projectedDestination.screenX,projectedDestination.screenY,snapshot.pulse.radius,0,Math.PI*2);context.stroke()}}
if(snapshot.headFront&&snapshot.opacity>0){context.globalAlpha=snapshot.opacity;context.fillStyle=colors.acc;context.beginPath();context.arc(snapshot.projectedHead.screenX,snapshot.projectedHead.screenY,headRadius,0,Math.PI*2);context.fill()}context.globalAlpha=1;return snapshot}
function expose(nextSource){source=nextSource||source;Object.assign(root.dataset,{rings:String(rings),pointsPerRing:String(pointsPerRing),pointCount:String(pointCount),landCount:String(landCount),oceanCount:String(oceanCount),cityCount:String(cityCount),sphereRadius:String(sphereRadius),rotationPeriod:String(rotationPeriod),angularVelocity:angularVelocity.toFixed(12),activeTime:activeTime.toFixed(3),yaw:yaw.toFixed(9),currentSpeed:currentSpeed.toFixed(9),targetSpeed:targetSpeed.toFixed(9),pointerInside:String(pointerInside),focused:String(focused),activeRoutes:String(activeRoutes.length),nextPairIndex:String(nextPairIndex),pairRandomDraws:String(pairRandomDraws),launchCount:String(launchCount),observedMax:String(observedMax),schedulerFrames:String(schedulerFrames),renderFrames:String(renderFrames),projectionBuilds:String(projectionBuilds),running:String(running),visible:String(visible),documentVisible:String(documentVisible),reduced:String(reduced),cleaned:String(cleaned),frameId:String(frameId),dpr:dpr.toFixed(3),pointChecksum:pointChecksum,landChecksum:landChecksum,noiseChecksum:noiseChecksum,cityChecksum:cityChecksum,pairChecksum:pairChecksum,arcChecksum:arcChecksum,projectionChecksum:lastProjection?lastProjection.pointChecksum:'',cityProjectionChecksum:lastProjection?lastProjection.cityChecksum:'',resets:String(resets),source:source})}
function draw(nextSource){context.setTransform(dpr,0,0,dpr,0,0);context.fillStyle=colors.background;context.globalAlpha=1;context.fillRect(0,0,logicalWidth,logicalHeight);context.strokeStyle=colors.line0;context.lineWidth=1;context.beginPath();context.arc(centerX,centerY,sphereRadius,0,Math.PI*2);context.stroke();rebuildProjection();const ordered=lastProjection.visiblePoints.slice().sort(function(first,second){return first.viewZ-second.viewZ});for(const point of ordered){context.globalAlpha=point.alpha;context.fillStyle=point.color;context.beginPath();context.arc(point.screenX,point.screenY,dotRadius,0,Math.PI*2);context.fill()}context.globalAlpha=1;lastRouteStates=activeRoutes.map(drawRoute);for(const city of lastProjection.cityItems){if(!city.front)continue;context.globalAlpha=1;context.fillStyle=colors.txt0;context.beginPath();context.arc(city.screenX,city.screenY,cityRadius,0,Math.PI*2);context.fill()}context.globalAlpha=1;renderFrames++;expose(nextSource||'render')}
function sizeCanvas(nextSource,countResize){const rect=canvas.getBoundingClientRect(),nextDpr=Math.min(2,window.devicePixelRatio||1);cssWidth=rect.width;cssHeight=rect.height;if(canvas.width!==Math.round(logicalWidth*nextDpr)||canvas.height!==Math.round(logicalHeight*nextDpr)){canvas.width=Math.round(logicalWidth*nextDpr);canvas.height=Math.round(logicalHeight*nextDpr)}dpr=nextDpr;if(countResize)resizeEvents++;draw(nextSource||'resize')}
function syncSpeed(nextSource,paintReduced){targetSpeed=pointerInside||focused?speedSlow:speedNormal;speedBefore=currentSpeed;if(reduced)currentSpeed=targetSpeed;speedAfter=currentSpeed;speedSource=nextSource||'speed target';if(reduced&&paintReduced)draw(nextSource||'reduced speed');else expose(nextSource||'speed target')}
function canRun(){return !reduced&&!cleaned&&root.isConnected&&visible&&documentVisible}
function requestNextFrame(){if(reduced||cleaned||frameId)return;rafRequests++;frameId=requestAnimationFrame(frame)}
function frame(now){frameId=0;if(!root.isConnected){cleanup();return}if(!canRun()){running=false;lastWall=0;expose('scheduler pause');return}const delta=clamp(now-lastWall,0,maxDelta);lastWall=now;lastDelta=delta;activeTime+=delta;speedBefore=currentSpeed;targetSpeed=pointerInside||focused?speedSlow:speedNormal;currentSpeed+=(targetSpeed-currentSpeed)*speedLerp;speedAfter=currentSpeed;speedSource='animation frame';yawBefore=yaw;yaw=wrap(yaw+delta*angularVelocity*currentSpeed);yawAfter=yaw;updateRoutes();schedulerFrames++;draw('animation frame');requestNextFrame();expose('animation scheduled')}
function startScheduler(nextSource){if(!canRun()||running)return;running=true;lastWall=performance.now();schedulerStarts++;requestNextFrame();expose(nextSource||'scheduler start')}
function stopScheduler(nextSource){if(frameId){cancelAnimationFrame(frameId);rafCancels++;frameId=0}if(running)schedulerStops++;running=false;lastWall=0;expose(nextSource||'scheduler stop')}
function resetDemo(){if(cleaned)return;activeTime=0;yaw=initialYaw;yawBefore=initialYaw;yawAfter=initialYaw;lastDelta=0;pointerInside=false;focused=document.activeElement===globeStage;currentSpeed=speedNormal;targetSpeed=focused?speedSlow:speedNormal;speedBefore=currentSpeed;if(reduced)currentSpeed=targetSpeed;speedAfter=currentSpeed;speedSource='reset';pairState=pairSeed;pairRandomDraws=0;nextPairIndex=0;activeRoutes=[];launchCount=0;observedMax=0;capEnforcements=0;routeExpirations=0;schedulerFrames=0;renderFrames=0;projectionBuilds=0;pointerEnters=0;pointerLeaves=0;ignoredPointers=0;focusEvents=0;blurEvents=0;keyboardResets=0;resizeEvents=0;visibilityChanges=0;intersectionChanges=0;schedulerStarts=0;schedulerStops=0;rafRequests=0;rafCancels=0;resets++;lastWall=running?performance.now():0;launchRoute(0,0);draw(reduced?'reduced reset':'reset');if(!reduced)startScheduler('reset scheduler');status.textContent='Deterministic route sequence reset'}
function deepFreeze(value){if(value&&typeof value==='object'&&!Object.isFrozen(value)){Object.freeze(value);Object.keys(value).forEach(function(key){deepFreeze(value[key])})}return value}
function copyPoint(point){return{index:point.index,ring:point.ring,column:point.column,latitude:point.latitude,longitude:point.longitude,x:point.x,y:point.y,z:point.z,noise:point.noise,land:point.land}}
function copyCity(city){return{index:city.index,id:city.id,name:city.name,latitudeDegrees:city.latitudeDegrees,longitudeDegrees:city.longitudeDegrees,latitude:city.latitude,longitude:city.longitude,x:city.x,y:city.y,z:city.z}}
function copyArc(arc){return{pairIndex:arc.pairIndex,source:arc.source,destination:arc.destination,sourceId:arc.sourceId,destinationId:arc.destinationId,start:Object.assign({},arc.start),end:Object.assign({},arc.end),surfaceMidpoint:Object.assign({},arc.surfaceMidpoint),control:Object.assign({},arc.control),midpoint:Object.assign({},arc.midpoint),samples:arc.samples.map(function(sample){return Object.assign({},sample)})}}
function inspect(){const pointFixtures=points.map(copyPoint),cityFixtures=cities.map(copyCity),pairFixtures=fixturePairs.map(function(pair){return Object.assign({},pair)}),arcCopies=arcFixtures.map(copyArc),visibleCopies=(lastProjection?lastProjection.visiblePoints:[]).map(function(point){return Object.assign({},point)}),cityProjectionCopies=(lastProjection?lastProjection.cityItems:[]).map(function(city){return Object.assign({},city)}),routeCopies=lastRouteStates.map(function(route){return{index:route.index,source:route.source,destination:route.destination,sourceId:route.sourceId,destinationId:route.destinationId,launchTime:route.launchTime,age:route.age,effectiveAge:route.effectiveAge,phase:route.phase,rawProgress:route.rawProgress,easedProgress:route.easedProgress,opacity:route.opacity,tailStart:route.tailStart,headT:route.headT,worldHead:Object.assign({},route.worldHead),projectedHead:Object.assign({},route.projectedHead),headFront:route.headFront,pulse:Object.assign({},route.pulse),drawnSegments:route.drawnSegments.map(function(segment){return Object.assign({},segment)})}}),rect=canvas.getBoundingClientRect();return deepFreeze({config:{rings:rings,pointsPerRing:pointsPerRing,pointCount:pointCount,logicalWidth:logicalWidth,logicalHeight:logicalHeight,centerX:centerX,centerY:centerY,sphereRadius:sphereRadius,dotRadius:dotRadius,cityRadius:cityRadius,rotationPeriod:rotationPeriod,angularVelocity:angularVelocity,initialYaw:initialYaw,speedSlow:speedSlow,speedNormal:speedNormal,speedLerp:speedLerp,maxDelta:maxDelta,noiseSeed:noiseSeed,noiseThreshold:noiseThreshold,noiseOctaves:noiseOctaves,noiseFrequency:[1.35,2.7],noiseWeights:[.67,.33],pairSeed:pairSeed,cityCount:cityCount,launchInterval:launchInterval,drawDuration:drawDuration,holdDuration:holdDuration,fadeDuration:fadeDuration,routeLifetime:routeLifetime,maxConcurrent:maxConcurrent,arcLift:arcLift,arcRadiusAtMidpoint:arcRadiusAtMidpoint,controlMidpointScale:controlMidpointScale,controlEndpointScale:controlEndpointScale,arcSamples:arcSamples,sampleSegments:sampleSegments,tailLength:tailLength,tailAlphaMin:tailAlphaMin,tailWidth:tailWidth,headRadius:headRadius,pulseStart:pulseStart,pulseEnd:pulseEnd,pulseRadiusStart:pulseRadiusStart,pulseRadiusEnd:pulseRadiusEnd,pulseWidth:pulseWidth,easing:{name:'ease-in-out',x1:easeX1,y1:easeY1,x2:easeX2,y2:easeY2,iterations:easeIterations},colors:Object.assign({},colors)},fixtures:{points:pointFixtures,cities:cityFixtures,pairs:pairFixtures,arcs:arcCopies,pointSamples:[pointFixtures[0],pointFixtures[63],pointFixtures[992],pointFixtures[1023],pointFixtures[1984],pointFixtures[2047]],landCount:landCount,oceanCount:oceanCount},state:{activeTime:activeTime,yaw:yaw,yawBefore:yawBefore,yawAfter:yawAfter,delta:lastDelta,source:source},projection:{visiblePoints:visibleCopies,visibleCount:lastProjection?lastProjection.visibleCount:0,backfaceCount:lastProjection?lastProjection.backfaceCount:0,visibleLand:lastProjection?lastProjection.visibleLand:0,visibleOcean:lastProjection?lastProjection.visibleOcean:0,cityItems:cityProjectionCopies,frontCityIds:lastProjection?lastProjection.frontCityIds.slice():[],bounds:lastProjection?Object.assign({},lastProjection.bounds):null,pointChecksum:lastProjection?lastProjection.pointChecksum:'',cityChecksum:lastProjection?lastProjection.cityChecksum:''},routes:{active:routeCopies,nextPairIndex:nextPairIndex,pairState:pairState,pairRandomDraws:pairRandomDraws,launchCount:launchCount,observedMax:observedMax,capEnforcements:capEnforcements,expirations:routeExpirations},speed:{current:currentSpeed,target:targetSpeed,before:speedBefore,after:speedAfter,lerp:speedLerp,source:speedSource},pointer:{inside:pointerInside,focused:focused},counters:{schedulerFrames:schedulerFrames,renderFrames:renderFrames,projectionBuilds:projectionBuilds,pointerEnters:pointerEnters,pointerLeaves:pointerLeaves,ignoredPointers:ignoredPointers,focusEvents:focusEvents,blurEvents:blurEvents,keyboardResets:keyboardResets,resizeEvents:resizeEvents,visibilityChanges:visibilityChanges,intersectionChanges:intersectionChanges,schedulerStarts:schedulerStarts,schedulerStops:schedulerStops,rafRequests:rafRequests,rafCancels:rafCancels,resets:resets,cleanupCalls:cleanupCalls},lifecycle:{visible:visible,documentVisible:documentVisible,running:running,reduced:reduced,cleaned:cleaned,frameId:frameId,hasResizeObserver:!!resizeObserver,hasIntersectionObserver:!!intersectionObserver,hasConnectionObserver:!!connectionObserver,signalAborted:controller.signal.aborted},canvas:{logicalWidth:logicalWidth,logicalHeight:logicalHeight,backingWidth:canvas.width,backingHeight:canvas.height,cssWidth:rect.width,cssHeight:rect.height,dpr:dpr,centerX:centerX,centerY:centerY,sphereRadius:sphereRadius,limbColor:colors.line0,limbWidth:1},checksums:{pointChecksum:pointChecksum,landChecksum:landChecksum,noiseChecksum:noiseChecksum,cityChecksum:cityChecksum,pairChecksum:pairChecksum,arcChecksum:arcChecksum,projectionChecksum:lastProjection?lastProjection.pointChecksum:'',cityProjectionChecksum:lastProjection?lastProjection.cityChecksum:''},data:Object.assign({},root.dataset)})}
globeStage.addEventListener('pointerenter',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer enter ignored');return}pointerInside=true;pointerEnters++;syncSpeed('pointer enter',true);status.textContent='Globe slowed for route inspection'},listener);
globeStage.addEventListener('pointerleave',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer leave ignored');return}pointerInside=false;pointerLeaves++;syncSpeed('pointer leave',true);status.textContent=focused?'Globe slowed while focused':'Globe rotating at normal speed'},listener);
globeStage.addEventListener('pointercancel',function(event){if(event.isPrimary===false){ignoredPointers++;return}pointerInside=false;syncSpeed('pointer cancel',true)},listener);
globeStage.addEventListener('focus',function(){focused=true;focusEvents++;syncSpeed('focus',true);status.textContent='Globe slowed while focused'},listener);
globeStage.addEventListener('blur',function(){focused=false;blurEvents++;syncSpeed('blur',true);status.textContent=pointerInside?'Globe slowed under pointer':'Globe rotating at normal speed'},listener);
globeStage.addEventListener('keydown',function(event){if(event.key!=='r'&&event.key!=='R')return;event.preventDefault();resetDemo();keyboardResets++;expose('keyboard reset')},listener);
function onVisibility(){documentVisible=document.visibilityState!=='hidden';visibilityChanges++;if(documentVisible)startScheduler('document visible');else stopScheduler('document hidden')}document.addEventListener('visibilitychange',onVisibility,listener);
function cleanup(){if(cleaned)return;cleanupCalls++;cleaned=true;if(frameId){cancelAnimationFrame(frameId);rafCancels++;frameId=0}running=false;controller.abort();if(resizeObserver)resizeObserver.disconnect();if(intersectionObserver)intersectionObserver.disconnect();if(connectionObserver)connectionObserver.disconnect();expose('cleanup')}
if('ResizeObserver'in window){resizeObserver=new ResizeObserver(function(){if(cleaned)return;sizeCanvas('resize',true)});resizeObserver.observe(canvas)}
if('IntersectionObserver'in window){intersectionObserver=new IntersectionObserver(function(entries){if(!entries.length||cleaned)return;visible=entries[0].isIntersecting;intersectionChanges++;if(visible)startScheduler('intersection visible');else stopScheduler('intersection hidden')},{threshold:.05});intersectionObserver.observe(root)}
if('MutationObserver'in window){connectionObserver=new MutationObserver(function(){if(!root.isConnected)cleanup()});connectionObserver.observe(document.documentElement,{childList:true,subtree:true})}
root.__mapDotGlobeArcsInspect=Object.freeze(inspect);root.__mapDotGlobeArcsReset=Object.freeze(resetDemo);launchRoute(0,0);sizeCanvas('initial',false);if(reduced){currentSpeed=targetSpeed;speedBefore=currentSpeed;speedAfter=currentSpeed;draw('reduced static')}else startScheduler('initial scheduler');`,
  prompt:`Build a self-contained responsive 320px Maps & Geo canvas card using bg0 #0a0a0b, bg1 #101012, bg2 #161619, line0 #232327, line1 #2e2e34, txt0 #ececef, txt1 #9b9ba3, txt2 #5c5c66 and acc #a78bfa. Keep one fixed logical canvas at 360 by 244, centered at (180,122), and a globe radius of exactly 90 so a route apex at 1.3 radii plus its 3px head remains inside the canvas. Draw a one-pixel line0 limb. Build exactly 32 latitude rings by 64 longitude columns, 2,048 samples row-major, with every latitude at a ring midpoint and longitude from negative pi toward positive pi. Store unit x = cos(latitude)cos(longitude), y = sin(latitude), z = cos(latitude)sin(longitude). Rotate around y with yaw, project x to centerX + viewX times 90 and y to centerY minus viewY times 90, and cull viewZ less than or equal to zero. Use uniform radius 1.5 dots: land is opaque txt1 and ocean is bg2 at exactly fifty-percent alpha. There is no lighting or axis tilt.

Rebuild the fake continent mask exactly once with the same seeded trilinear 3D value noise as the halftone globe. Use seed 24681357, integer lattice hashing constants 374761393, 668265263, 1442695041 and 1274126177, smooth every fractional coordinate with f squared times (3 minus 2f), and trilinearly interpolate. Sample octave one at (1.35x + 4.1, 1.35y - 2.7, 1.35z + 1.9) and octave two at (2.7x - 3.4, 2.7y + 5.2, 2.7z - 0.8), weighted 0.67 and 0.33. Land is noise greater than or equal to 0.55 and must total 678 points, leaving 1,370 ocean points.

Store eight cities in this exact order and at these exact latitude/longitude degrees: NYC (40.7128,-74.0060), LON (51.5074,-0.1278), TYO (35.6762,139.6503), SIN (1.3521,103.8198), SYD (-33.8688,151.2093), DXB (25.2048,55.2708), SAO (-23.5505,-46.6333), CPT (-33.9249,18.4241). Project them with the same yaw and show only front-facing cities as opaque txt0 circles of radius 2.5.

Use one explicit-state standard mulberry32 stream seeded at 42 solely for runtime pairs. Consume exactly two random values per launch: source = floor(first times 8), raw destination = floor(second times 7), and destination = raw destination greater than or equal to source ? raw destination + 1 : raw destination. Launch route zero synchronously at active time zero; it must be SYD to SIN. Launch subsequent routes at exact active-time multiples of 2,500ms, expire each at age 1,900ms, and enforce an exact cap of three active routes even though these timings naturally produce at most one.

For every pair let a and b be its city unit vectors, m = normalize(a + b), and use control point C = 2.6m - 0.5(a + b). The quadratic is Q(t) = (1 - t) squared a + 2(1 - t)tC + t squared b. Store 49 canonical samples at t = index / 48; Q(0.5) must have radius 1.3, which is a lift of 0.3 globe radii. Draw from age 0 through 900ms using the real CSS ease-in-out cubic bezier (0.42,0,0.58,1), numerically invert its x coordinate, and use the eased value as the head parameter. During drawing the tail starts at max(0, head - 0.36); during hold and fade it spans zero through one. Clip each of 48 sample segments to the active tail. For a clipped segment use midT = (startT + endT) / 2, local = clamp((midT - tailStart) / max(head - tailStart, 1e-9),0,1), and alpha = route opacity times (0.08 + 0.92 times local). Draw only when both segment endpoints have viewZ greater than zero, using a 1.5px round acc stroke. Draw a 3px acc head when front-facing and keep it at the destination after drawing. Hold fully visible from age 900 through 1,500ms, then fade linearly to zero through age 1,900ms. From age 900 inclusive to 1,500 exclusive, draw a one-pixel round acc destination ring whose radius linearly grows from 4 to 14 while alpha falls from 1 to 0.

Begin at yaw 0.35 and rotate exactly once per 16,000ms. Pointer hover anywhere on the stage or stage focus sets target speed to 0.4; otherwise it is 1. On every animation frame apply current speed += (target speed - current speed) times 0.05, cap wall delta to 50ms, advance active route time by the capped delta, and integrate yaw by delta times two pi / 16,000 times current speed. Use one requestAnimationFrame scheduler with no timers. Start only after the card intersects, pause while offscreen or document-hidden, and reset the wall clock so resume never catches up. Use AbortController listeners, ResizeObserver with device-pixel-ratio capped at two, IntersectionObserver, and MutationObserver detach cleanup.

Make the stage the sole focusable control, explain hover/focus slowing in its label, support R reset, and expose a polite route status. Under reduced motion request zero animation frames, keep activeTime at zero and yaw at 0.35, apply speed targets instantly, and represent deterministic route zero at effective age 900 so its complete tail, destination head and initial arrival ring are visible. Disable transitions on elements and pseudo-elements. Freeze the exact inspection and reset functions; make reset inert after cleanup and otherwise restore the pair state, route zero, yaw and runtime counters deterministically. Deep-freeze every inspection snapshot and expose complete point, city, first-32 pair and first-32 arc fixtures, active route phase/head/pulse/drawn-segment details, projection, speed recurrence, pointer/focus state, counters, lifecycle, responsive canvas geometry, root data and deterministic FNV-1a checksums.`
});
