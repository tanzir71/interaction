/* INTRX registry - published noise-displaced topographic paths */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'svg-topographic-drift',title:'SVG Topographic Drift',cat:'SVG & Line',rootClass:'d-topodrift',tags:['SVG','value noise','contours'],libs:[],desc:'Fourteen SVG elevation contours are regenerated from a coherent four-octave value-noise field. Their shared time coordinate drifts slowly, accelerates on hover, sleeps offscreen, and can be frozen without changing the terrain topology.',seen:'Seen on: outdoor identities, geographic storytelling, architecture diagrams, and atmospheric hero backgrounds',hint:'hover to increase survey drift; click or press Space to freeze the terrain',
html:`<div class="d-topodrift" role="group" aria-label="Noise-displaced SVG topographic contours"><div class="d-topodrift-head"><span>RELIEF SURVEY / 14 CONTOURS</span><span>DRIFT <b>0.08</b> <i>RUNNING</i></span></div><div class="d-topodrift-field" tabindex="0" role="button" aria-pressed="false" aria-label="Fourteen animated topographic contour lines. Hover to accelerate; activate to freeze or resume."><svg viewBox="0 0 600 360" preserveAspectRatio="none" aria-hidden="true"><defs><linearGradient id="d-topodrift-stroke" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#4c8d85"/><stop offset=".5" stop-color="#b9d6ad"/><stop offset="1" stop-color="#e7b966"/></linearGradient></defs><g class="d-topodrift-contours"></g><circle class="d-topodrift-station" cx="438" cy="151" r="5"></circle><path class="d-topodrift-cross" d="M420 151H456M438 133V169"></path></svg><div class="d-topodrift-labels" aria-hidden="true"><span>47° 18′ 22″ N</span><span>ELEV / 1240 M</span><span>CONTOUR Δ / 20 M</span></div><span class="d-topodrift-note" aria-hidden="true">4 OCTAVE VALUE NOISE / 32 SAMPLES PER LINE</span></div><div class="d-topodrift-foot"><p aria-live="polite">Terrain contours drifting at survey speed</p><button type="button">Resume drift</button></div></div>`,
css:`.d-topodrift{width:100%;min-height:510px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#f0eee8;background:#0a0a0b;font-family:Roboto Mono,JetBrains Mono,monospace}.d-topodrift-head{display:flex;justify-content:space-between;color:#6e7476;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.11em}.d-topodrift-head b{color:#8ddfc2}.d-topodrift-head i{margin-left:8px;color:#bac1bf;font-style:normal}.d-topodrift-field{position:relative;height:390px;margin-top:13px;overflow:hidden;border:1px solid #303638;background:radial-gradient(circle at 70% 38%,#27413a,#111b1b 45%,#091011 76%);outline:none;cursor:pointer}.d-topodrift-field:focus-visible{box-shadow:inset 0 0 0 2px #8ddfc2}.d-topodrift-field svg{display:block;width:100%;height:100%}.d-topodrift-contours path{fill:none;stroke:url(#d-topodrift-stroke);stroke-width:1.25;vector-effect:non-scaling-stroke;opacity:var(--opacity)}.d-topodrift-contours path:nth-child(3n){stroke-width:2}.d-topodrift-station{fill:#edc56c;stroke:#172726;stroke-width:2;vector-effect:non-scaling-stroke}.d-topodrift-cross{fill:none;stroke:rgba(237,197,108,.5);stroke-width:1;stroke-dasharray:3 4;vector-effect:non-scaling-stroke}.d-topodrift-labels{position:absolute;left:12px;right:12px;top:11px;display:flex;justify-content:space-between;color:rgba(222,231,218,.5);font:700 6px/1 ui-monospace,monospace;letter-spacing:.08em}.d-topodrift-note{position:absolute;left:11px;bottom:9px;color:rgba(215,225,217,.42);font:650 6px/1 ui-monospace,monospace;letter-spacing:.08em}.d-topodrift-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-topodrift-foot p{margin:0;color:#737b79;font:650 9px/1.3 ui-monospace,monospace}.d-topodrift-foot button{min-height:31px;padding:7px 11px;border:1px solid #39413f;border-radius:3px;color:#a5adaa;background:#141917;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-topodrift-foot button:hover,.d-topodrift-foot button:focus-visible{color:#fff;border-color:#707c78;outline:none}@media(max-width:550px){.d-topodrift{padding-inline:6px}.d-topodrift-labels span:nth-child(2){display:none}}`,
js:`const field=root.querySelector('.d-topodrift-field');const group=root.querySelector('.d-topodrift-contours');const driftReadout=root.querySelector('.d-topodrift-head b');const mode=root.querySelector('.d-topodrift-head i');const status=root.querySelector('.d-topodrift-foot p');const resume=root.querySelector('.d-topodrift-foot button');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const paths=[];for(let i=0;i<14;i++){const path=document.createElementNS('http://www.w3.org/2000/svg','path');path.style.setProperty('--opacity',(.26+i/14*.62).toFixed(2));group.appendChild(path);paths.push(path)}let hovered=false,paused=reduce,visible=true,running=false,last=0,time=0,speed=.08;
function hash(n){return((Math.sin(n*127.1)*43758.5453123)%1+1)%1}function noise(x,seed){const i=Math.floor(x),f=x-i,u=f*f*(3-2*f);return(hash(i+seed*131)-.5)*(1-u)+(hash(i+1+seed*131)-.5)*u}function fbm(x,seed,t){let value=0,amplitude=.58,frequency=1;for(let octave=0;octave<4;octave++){value+=noise(x*frequency+t*(.45+octave*.13),seed+octave*17)*amplitude;frequency*=2.03;amplitude*=.5}return value}function pathData(index){const points=[],base=29+index*(302/13),amplitude=11+Math.sin(index*1.8)*3;for(let x=-10;x<=610;x+=20){const broad=fbm(x*.017,index,time),detail=fbm(x*.032,index+31,-time*.63);points.push({x,y:base+(broad*.78+detail*.22)*amplitude})}let d='M'+points[0].x+' '+points[0].y.toFixed(2);for(let i=1;i<points.length-1;i++){const midX=(points[i].x+points[i+1].x)/2,midY=(points[i].y+points[i+1].y)/2;d+='Q'+points[i].x+' '+points[i].y.toFixed(2)+' '+midX.toFixed(2)+' '+midY.toFixed(2)}const end=points[points.length-1];return d+'T'+end.x+' '+end.y.toFixed(2)}function draw(){paths.forEach(function(path,index){path.setAttribute('d',pathData(index))});driftReadout.textContent=speed.toFixed(2)}function targetSpeed(){return hovered?.22:.08}function start(){if(reduce||paused||!visible||running)return;running=true;last=performance.now();requestAnimationFrame(frame)}function frame(now){if(paused||!visible){running=false;return}const dt=Math.min(60,now-last);last=now;speed+=(targetSpeed()-speed)*(1-Math.exp(-dt*.006));time+=speed*dt/1000;draw();requestAnimationFrame(frame)}function toggle(){paused=!paused;field.setAttribute('aria-pressed',paused?'true':'false');mode.textContent=paused?'FROZEN':'RUNNING';status.textContent=paused?'Terrain frozen at time '+time.toFixed(2):'Terrain drift resumed';if(!paused)start()}field.addEventListener('pointerenter',function(){hovered=true;status.textContent='Survey drift accelerating';start()});field.addEventListener('pointerleave',function(){hovered=false;status.textContent=paused?'Terrain remains frozen':'Survey drift easing to idle';start()});field.addEventListener('click',toggle);field.addEventListener('keydown',function(event){if(event.key==='Enter'||event.key===' '){event.preventDefault();toggle()}else if(event.key==='Escape'&&!paused)toggle()});resume.addEventListener('click',function(){if(paused)toggle();else{time=0;status.textContent='Terrain time reset';draw()}resume.focus()});if('IntersectionObserver'in window)new IntersectionObserver(function(entries){visible=entries[0].isIntersecting;if(visible)start();else running=false},{threshold:.05}).observe(root);draw();if(reduce){mode.textContent='STATIC';status.textContent='Static topographic field - reduced motion'}else start();`,
prompt:`Build a self-contained SVG topographic drift with no libraries. Create exactly fourteen SVG path elements. For each contour sample x=-10..610 in 20-unit steps (32 samples including endpoints) around an evenly spaced base elevation. Displace y with a coherent four-octave 1D value-noise field: smooth lattice interpolation f*f*(3-2*f), octave frequency *=2.03, amplitude *=.5, and slightly different time velocity per octave. Blend a broad and detail fbm sample before scaling by each line's amplitude.

Convert samples into smooth Q commands through successive midpoints and a final T endpoint. Maintain one time coordinate. Idle drift target is .08; hover target is .22; ease current speed with alpha=1-exp(-dt*.006) and integrate time += speed*dt/1000. Click or Enter/Space freezes/resumes without resetting topology; Escape freezes. The visible control resumes when frozen or resets terrain time while running.

Run one RAF only while visible and unpaused, using IntersectionObserver. Under prefers-reduced-motion, render one static field without a loop. Synchronize drift rate, running/frozen state, aria-pressed, and live status. Include survey labels, varied contour emphasis, visible focus, scoped CSS, and no dependencies.`});
