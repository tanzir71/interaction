/* INTRX registry - published morphing SVG Lissajous loader */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'svg-lissajous-loader',title:'SVG Lissajous Loader',cat:'SVG & Line',rootClass:'d-lissajous',tags:['SVG','Lissajous','parametric'],libs:[],desc:'A 240-sample parametric curve holds, then morphs among four frequency-ratio figures. A separate dash phase and tracer point circulate continuously on the current equation, producing a loader whose geometry and progress remain visibly connected.',seen:'Seen on: generative loading screens, music tools, experimental studios, and computational-design portfolios',hint:'watch the ratios morph; hover accelerates the clock; click to freeze the current figure',
html:`<div class="d-lissajous" role="group" aria-label="Morphing parametric SVG Lissajous loader"><div class="d-lissajous-head"><span>PARAMETRIC LOADER / 240 SAMPLES</span><span>RATIO <b>3 : 2</b> <i>00%</i></span></div><div class="d-lissajous-field" tabindex="0" role="button" aria-pressed="false" aria-label="Lissajous loader morphing among four figures. Hover to accelerate; activate to freeze."><svg viewBox="0 0 600 360" preserveAspectRatio="xMidYMid meet" aria-hidden="true"><defs><linearGradient id="d-lissajous-gradient" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#78e1bd"/><stop offset=".52" stop-color="#7ca5df"/><stop offset="1" stop-color="#efbd67"/></linearGradient><filter id="d-lissajous-glow" x="-40%" y="-40%" width="180%" height="180%"><feGaussianBlur stdDeviation="5" result="blur"></feGaussianBlur><feMerge><feMergeNode in="blur"></feMergeNode><feMergeNode in="SourceGraphic"></feMergeNode></feMerge></filter></defs><circle class="d-lissajous-orbit" cx="300" cy="180" r="143"></circle><path class="d-lissajous-echo"></path><path class="d-lissajous-curve"></path><circle class="d-lissajous-tracer" r="6"></circle><circle class="d-lissajous-center" cx="300" cy="180" r="3"></circle></svg><div class="d-lissajous-equation" aria-hidden="true"><span>x=A sin(a·t+δ)</span><i></i><span>y=B sin(b·t)</span></div><span class="d-lissajous-note" aria-hidden="true">3.2S HOLD / 1.6S MORPH / 4 FIGURES</span></div><div class="d-lissajous-foot"><p aria-live="polite">Lissajous figure 1 running at ratio 3 to 2</p><button type="button">Resume loader</button></div></div>`,
css:`.d-lissajous{width:100%;min-height:510px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#f0eee8;background:#080b0c;font-family:Inter,system-ui,sans-serif}.d-lissajous-head{display:flex;justify-content:space-between;color:#6e7476;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.11em}.d-lissajous-head b{color:#7fe5c1}.d-lissajous-head i{margin-left:8px;color:#bac1bf;font-style:normal}.d-lissajous-field{position:relative;height:390px;margin-top:13px;overflow:hidden;border:1px solid #2b3938;background:radial-gradient(circle at 50%,#172b34,#0a1114 67%);outline:none;cursor:pointer}.d-lissajous-field:focus-visible{box-shadow:inset 0 0 0 2px #7fe5c1}.d-lissajous-field svg{display:block;width:100%;height:100%}.d-lissajous-orbit{fill:none;stroke:rgba(154,197,190,.12);stroke-width:1;stroke-dasharray:2 7}.d-lissajous-echo{fill:none;stroke:#549b8d;stroke-width:10;opacity:.12;filter:url(#d-lissajous-glow)}.d-lissajous-curve{fill:none;stroke:url(#d-lissajous-gradient);stroke-width:3;stroke-linecap:round;stroke-linejoin:round;stroke-dasharray:12 9;vector-effect:non-scaling-stroke}.d-lissajous-tracer{fill:#f0c76c;stroke:#fff2bf;stroke-width:2;filter:url(#d-lissajous-glow)}.d-lissajous-center{fill:#83dfc0}.d-lissajous-equation{position:absolute;left:12px;right:12px;top:11px;display:flex;align-items:center;gap:8px;color:rgba(210,229,223,.48);font:700 6px/1 ui-monospace,monospace;letter-spacing:.08em}.d-lissajous-equation i{height:1px;flex:1;background:rgba(119,215,183,.16)}.d-lissajous-note{position:absolute;left:11px;bottom:9px;color:rgba(208,226,219,.4);font:650 6px/1 ui-monospace,monospace;letter-spacing:.08em}.d-lissajous-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-lissajous-foot p{margin:0;color:#737b79;font:650 9px/1.3 ui-monospace,monospace}.d-lissajous-foot button{min-height:31px;padding:7px 11px;border:1px solid #39413f;border-radius:3px;color:#a5adaa;background:#141917;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-lissajous-foot button:hover,.d-lissajous-foot button:focus-visible{color:#fff;border-color:#707c78;outline:none}@media(max-width:550px){.d-lissajous{padding-inline:6px}}`,
js:`const field=root.querySelector('.d-lissajous-field');const curve=root.querySelector('.d-lissajous-curve');const echo=root.querySelector('.d-lissajous-echo');const tracer=root.querySelector('.d-lissajous-tracer');const ratio=root.querySelector('.d-lissajous-head b');const percent=root.querySelector('.d-lissajous-head i');const status=root.querySelector('.d-lissajous-foot p');const resume=root.querySelector('.d-lissajous-foot button');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const figures=[{a:3,b:2,delta:1.5708},{a:5,b:4,delta:.7854},{a:2,b:1,delta:1.5708},{a:4,b:3,delta:1.0472}],cycle=4800,hold=3200;let hovered=false,paused=reduce,visible=true,running=false,last=0,time=0,phase=0;
function params(){if(!Number.isFinite(time))time=0;const cycleIndex=Math.floor(time/cycle),local=time-cycleIndex*cycle,index=((cycleIndex%figures.length)+figures.length)%figures.length,next=(index+1)%figures.length,raw=Math.max(0,Math.min(1,(local-hold)/(cycle-hold))),ease=raw*raw*(3-2*raw),from=figures[index],to=figures[next];return{a:from.a+(to.a-from.a)*ease,b:from.b+(to.b-from.b)*ease,delta:from.delta+(to.delta-from.delta)*ease,index,local,ease}}function sample(u,p){return{x:300+132*Math.sin(p.a*u+p.delta),y:180+132*Math.sin(p.b*u)}}function draw(){const p=params(),parts=[];for(let i=0;i<240;i++){const point=sample(i/239*Math.PI*2,p);parts.push((i?'L':'M')+point.x.toFixed(2)+' '+point.y.toFixed(2))}const d=parts.join('');curve.setAttribute('d',d);echo.setAttribute('d',d);curve.style.strokeDashoffset=(-phase).toFixed(2);const head=sample((phase*.012)%(Math.PI*2),p);tracer.setAttribute('cx',head.x.toFixed(2));tracer.setAttribute('cy',head.y.toFixed(2));ratio.textContent=p.a.toFixed(2).replace('.00','')+' : '+p.b.toFixed(2).replace('.00','');percent.textContent=String(Math.round(p.local/cycle*100)).padStart(2,'0')+'%'}function start(){if(reduce||paused||!visible||running)return;running=true;last=performance.now();requestAnimationFrame(frame)}function frame(now){if(paused||!visible){running=false;return}const dt=Math.max(0,Math.min(60,now-last));last=now;const multiplier=hovered?1.65:1;time+=dt*multiplier;phase=(phase+dt*.065*multiplier)%210;draw();requestAnimationFrame(frame)}function toggle(){paused=!paused;field.setAttribute('aria-pressed',paused?'true':'false');status.textContent=paused?'Lissajous loader frozen at '+percent.textContent:'Lissajous loader resumed';if(!paused)start()}field.addEventListener('pointerenter',function(){hovered=true;status.textContent='Parametric clock accelerated';start()});field.addEventListener('pointerleave',function(){hovered=false;status.textContent=paused?'Figure remains frozen':'Parametric clock returned to nominal'});field.addEventListener('click',toggle);field.addEventListener('keydown',function(event){if(event.key==='Enter'||event.key===' '){event.preventDefault();toggle()}else if(event.key==='Escape'&&!paused)toggle()});resume.addEventListener('click',function(){if(paused)toggle();else{time=0;phase=0;status.textContent='Loader reset to figure 1';draw()}resume.focus()});if('IntersectionObserver'in window)new IntersectionObserver(function(entries){visible=entries[0].isIntersecting;if(visible)start();else running=false},{threshold:.05}).observe(root);draw();if(reduce)status.textContent='Static Lissajous figure - reduced motion';else start();`,
prompt:`Build a self-contained morphing SVG Lissajous loader without dependencies. Define four parameter presets: (a,b,delta)=(3,2,π/2),(5,4,π/4),(2,1,π/2),(4,3,π/3). Each 4800ms cycle holds its current preset for 3200ms, then morphs for 1600ms to the next using smoothstep raw²*(3-2raw).

Every frame interpolate a, b, and delta, then sample exactly 240 values u=0..2π. Generate the SVG polyline path from x=300+132*sin(a*u+delta), y=180+132*sin(b*u). Reuse the same path as a soft echo. Independently integrate a 12/9 dash phase and place a tracer at the same parametric equation using a continuously advancing phase. Hover multiplies both clocks by 1.65.

Click or Enter/Space freezes/resumes without changing the current figure; Escape freezes. The visible control resumes when paused or resets to figure 1 while running. Pause offscreen with IntersectionObserver. Under prefers-reduced-motion, render one static figure. Synchronize interpolated ratio, cycle percent, tracer, dash phase, aria-pressed, and live status. Include a glow filter, equation labels, visible focus, scoped CSS, and no dependencies.`});
