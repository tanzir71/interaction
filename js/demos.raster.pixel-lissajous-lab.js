/* INTRX registry — deterministic pixel Lissajous laboratory */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
  id:'pixel-lissajous-lab',
  title:'Pixel Lissajous Lab',
  cat:'Raster & Glitch',
  rootClass:'d-raster-pixel-lissajous-lab',
  tags:['canvas','pixel','lissajous'],
  libs:[],
  desc:'A chunky, persistent-canvas Lissajous curve morphs continuously between integer frequency pairs. Six hundred grid-snapped samples leave a restrained phosphor trail while the phase advances forever.',
  seen:"Seen on: mona_biasia's Lissajous SVG generator, datacurve icon sets, vector oscilloscopes, and pixel instrument panels",
  hint:'drag either stepped frequency slider; use its native arrow, Page, Home, and End keys; press R to reset',
  html:`<div class="d-raster-pixel-lissajous-lab" role="region" aria-label="Interactive pixel Lissajous laboratory">
  <div class="d-raster-pixel-lissajous-lab-head" aria-hidden="true"><span>PIXEL / LISSAJOUS</span><span>600 SAMPLES · 4PX GRID</span></div>
  <div class="d-raster-pixel-lissajous-lab-stage" tabindex="0" aria-keyshortcuts="R" aria-label="Pixel Lissajous curve. Adjust the frequency sliders below or press R to reset.">
    <canvas class="d-raster-pixel-lissajous-lab-canvas" width="360" height="196" role="img" aria-label="A persistent pixel Lissajous curve"></canvas>
  </div>
  <form class="d-raster-pixel-lissajous-lab-controls" aria-label="Lissajous frequency controls">
    <label class="d-raster-pixel-lissajous-lab-control"><span>A</span><input class="d-raster-pixel-lissajous-lab-slider d-raster-pixel-lissajous-lab-slider--a" type="range" min="1" max="9" step="1" value="3" aria-keyshortcuts="R" aria-label="Lissajous frequency a"></label>
    <label class="d-raster-pixel-lissajous-lab-control"><span>B</span><input class="d-raster-pixel-lissajous-lab-slider d-raster-pixel-lissajous-lab-slider--b" type="range" min="1" max="9" step="1" value="4" aria-keyshortcuts="R" aria-label="Lissajous frequency b"></label>
    <output class="d-raster-pixel-lissajous-lab-readout" aria-live="off" aria-label="Current Lissajous parameters">a:3 b:4 φ:1.72</output>
  </form>
  <div class="d-raster-pixel-lissajous-lab-foot" aria-hidden="true"><span>X = SIN(A·T + φ)</span><span>Y = SIN(B·T)</span></div>
  <span class="d-raster-pixel-lissajous-lab-status" aria-live="polite" aria-atomic="true">Lissajous curve running</span>
</div>`,
  css:`
.d-raster-pixel-lissajous-lab{position:relative;width:100%;height:320px;box-sizing:border-box;overflow:hidden;contain:layout paint;container-type:inline-size;background:#0a0a0b;color:#ececef;font-family:'JetBrains Mono',monospace;isolation:isolate}
.d-raster-pixel-lissajous-lab-head{position:absolute;z-index:2;top:13px;left:16px;right:16px;display:flex;align-items:center;justify-content:space-between;gap:10px;color:#5c5c66;font-size:10px;line-height:1;letter-spacing:.08em;white-space:nowrap}
.d-raster-pixel-lissajous-lab-head span:first-child{color:#9b9ba3}
.d-raster-pixel-lissajous-lab-stage{position:absolute;inset:32px 8px 91px;display:grid;place-items:center;box-sizing:border-box;overflow:hidden;border:1px solid #232327;border-radius:8px;background:#0a0a0b;outline:none;cursor:default}
.d-raster-pixel-lissajous-lab-stage:focus-visible{box-shadow:inset 0 0 0 1px #fa7319}
.d-raster-pixel-lissajous-lab-stage::before,.d-raster-pixel-lissajous-lab-stage::after{content:'';position:absolute;z-index:0;pointer-events:none;background:#232327;opacity:.34}
.d-raster-pixel-lissajous-lab-stage::before{left:50%;top:0;bottom:0;width:1px}
.d-raster-pixel-lissajous-lab-stage::after{left:0;right:0;top:50%;height:1px}
.d-raster-pixel-lissajous-lab-canvas{position:relative;z-index:1;display:block;width:min(100%,360px);height:auto;aspect-ratio:90/49;image-rendering:pixelated;image-rendering:crisp-edges}
.d-raster-pixel-lissajous-lab-controls{position:absolute;left:16px;right:16px;bottom:38px;display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr) auto;align-items:center;gap:13px;margin:0;padding:0;border:0}
.d-raster-pixel-lissajous-lab-control{display:grid;grid-template-columns:10px minmax(0,1fr);align-items:center;gap:7px;min-width:0;color:#9b9ba3;font-size:10px;line-height:1}
.d-raster-pixel-lissajous-lab-slider{width:100%;height:16px;margin:0;padding:0;appearance:none;-webkit-appearance:none;background:transparent;cursor:ew-resize;touch-action:pan-y}
.d-raster-pixel-lissajous-lab-slider:focus{outline:none}
.d-raster-pixel-lissajous-lab-slider:focus-visible{filter:drop-shadow(0 0 3px rgba(250,115,25,.65))}
.d-raster-pixel-lissajous-lab-slider::-webkit-slider-runnable-track{height:1px;background:#232327;border:0}
.d-raster-pixel-lissajous-lab-slider::-webkit-slider-thumb{width:12px;height:12px;margin-top:-5.5px;border:0;border-radius:0;background:#fa7319;appearance:none;-webkit-appearance:none}
.d-raster-pixel-lissajous-lab-slider::-moz-range-track{height:1px;background:#232327;border:0}
.d-raster-pixel-lissajous-lab-slider::-moz-range-progress{height:1px;background:#232327}
.d-raster-pixel-lissajous-lab-slider::-moz-range-thumb{width:12px;height:12px;border:0;border-radius:0;background:#fa7319}
.d-raster-pixel-lissajous-lab-readout{min-width:108px;color:#5c5c66;font:10px/1 'JetBrains Mono',monospace;text-align:right;white-space:nowrap}
.d-raster-pixel-lissajous-lab-foot{position:absolute;left:16px;right:16px;bottom:13px;display:flex;align-items:center;justify-content:space-between;gap:8px;color:#5c5c66;font-size:9px;line-height:1;letter-spacing:.035em;white-space:nowrap}
.d-raster-pixel-lissajous-lab-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}
@container(max-width:340px){.d-raster-pixel-lissajous-lab-head{left:12px;right:12px;font-size:9px}.d-raster-pixel-lissajous-lab-controls{left:12px;right:12px;gap:8px}.d-raster-pixel-lissajous-lab-readout{min-width:99px}.d-raster-pixel-lissajous-lab-foot{left:12px;right:12px;font-size:8px}}
@media(prefers-reduced-motion:reduce){.d-raster-pixel-lissajous-lab *{animation:none!important;transition:none!important}}
`,
  js:`const lissajousStage=root.querySelector('.d-raster-pixel-lissajous-lab-stage'),canvas=root.querySelector('.d-raster-pixel-lissajous-lab-canvas'),context=canvas.getContext('2d',{alpha:false}),controls=root.querySelector('.d-raster-pixel-lissajous-lab-controls'),sliderA=root.querySelector('.d-raster-pixel-lissajous-lab-slider--a'),sliderB=root.querySelector('.d-raster-pixel-lissajous-lab-slider--b'),readout=root.querySelector('.d-raster-pixel-lissajous-lab-readout'),status=root.querySelector('.d-raster-pixel-lissajous-lab-status');
if(!context)throw new Error('Pixel Lissajous canvas unavailable');
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,controller=new AbortController(),listener={signal:controller.signal};
const logicalWidth=360,logicalHeight=196,gridSize=4,gridColumns=logicalWidth/gridSize,gridRows=logicalHeight/gridSize,sampleCount=600,headIndex=599,recentStart=499,recentCount=100,txt1Start=350,txt1Count=149,txt2Start=200,txt2Count=150,goneCount=200,initialPhi=1.72,phiStep=.008,tweenDuration=800,tweenBezier=[.65,0,.35,1],persistenceFactor=.96,fadeAlpha=.04,defaultA=3,defaultB=4,frequencyMin=1,frequencyMax=9,frequencyStep=1,centerX=180,centerY=98,xAmplitude=152,yAmplitude=76,maxDelta=50;
const colors={background:'#0a0a0b',line0:'#232327',txt1:'#9b9ba3',txt2:'#5c5c66',acc:'#fa7319',head:'#ffffff'};
const parameters=Array.from({length:sampleCount},function(_,index){return{index:index,t:index/headIndex*Math.PI*2,age:headIndex-index,tone:index<headIndex?(index<txt2Start?'gone':index<txt1Start?'txt2':index<recentStart?'txt1':'recent'):'head'}});
function checksumText(value){let hash=2166136261;for(const character of String(value))hash=Math.imul(hash^character.charCodeAt(0),16777619)>>>0;return hash.toString(16).padStart(8,'0')}
const fixtureChecksum=checksumText(parameters.map(function(item){return[item.index,item.t.toFixed(9),item.age,item.tone].join(',')}).join('|'));
let activeTime=0,frameCount=0,phi=initialPhi,currentA=defaultA,currentB=defaultB,targetA=defaultA,targetB=defaultB,tweenStartA=defaultA,tweenStartB=defaultB,tweenElapsed=0,tweenActive=false,tweenRawProgress=1,tweenEasedProgress=1,source='initial';
let schedulerFrames=0,renderFrames=0,geometryBuilds=0,tweenStarts=0,tweenRetargets=0,tweenCompletes=0,inputEvents=0,changeEvents=0,resizeEvents=0,visibilityChanges=0,intersectionChanges=0,schedulerStarts=0,schedulerStops=0,resets=0,cleanupCalls=0,clearFrames=0,fadeFrames=0;
let visible=!('IntersectionObserver'in window),documentVisible=document.visibilityState!=='hidden',running=false,cleaned=false,frameId=0,lastWall=0,resizeObserver=null,intersectionObserver=null,connectionObserver=null,dpr=1,cssWidth=0,cssHeight=0,lastMode='full-clear',lastSamples=[],lastBounds=null,lastToneCounts=null,lastGeometryChecksum='',trailChecksum='00000000';
function clamp(value,min,max){return Math.max(min,Math.min(max,value))}
function cubicCoordinate(t,p1,p2){const inverse=1-t;return 3*inverse*inverse*t*p1+3*inverse*t*t*p2+t*t*t}
function easeInOut(progress){const wanted=clamp(progress,0,1);let low=0,high=1,parameter=.5;for(let iteration=0;iteration<48;iteration++){parameter=(low+high)/2;if(cubicCoordinate(parameter,tweenBezier[0],tweenBezier[2])<wanted)low=parameter;else high=parameter}parameter=(low+high)/2;return cubicCoordinate(parameter,tweenBezier[1],tweenBezier[3])}
function toneColor(tone){return tone==='head'?colors.head:tone==='recent'?colors.acc:tone==='txt1'?colors.txt1:tone==='txt2'?colors.txt2:null}
function buildGeometry(){const samples=[];let logicalMinX=Infinity,logicalMaxX=-Infinity,logicalMinY=Infinity,logicalMaxY=-Infinity,pixelMinX=Infinity,pixelMaxX=-Infinity,pixelMinY=Infinity,pixelMaxY=-Infinity;const toneCounts={gone:0,txt2:0,txt1:0,recent:0,head:0,drawn:0};for(const parameter of parameters){const rawX=Math.sin(currentA*parameter.t+phi),rawY=Math.sin(currentB*parameter.t),logicalX=centerX+xAmplitude*rawX,logicalY=centerY+yAmplitude*rawY,pixelX=clamp(Math.round((logicalX-gridSize/2)/gridSize)*gridSize,0,logicalWidth-gridSize),pixelY=clamp(Math.round((logicalY-gridSize/2)/gridSize)*gridSize,0,logicalHeight-gridSize),color=toneColor(parameter.tone),drawn=!!color;const sample={index:parameter.index,t:parameter.t,age:parameter.age,rawX:rawX,rawY:rawY,logicalX:logicalX,logicalY:logicalY,pixelX:pixelX,pixelY:pixelY,width:gridSize,height:gridSize,tone:parameter.tone,color:color,drawn:drawn};samples.push(sample);toneCounts[parameter.tone]++;if(drawn)toneCounts.drawn++;logicalMinX=Math.min(logicalMinX,logicalX);logicalMaxX=Math.max(logicalMaxX,logicalX);logicalMinY=Math.min(logicalMinY,logicalY);logicalMaxY=Math.max(logicalMaxY,logicalY);pixelMinX=Math.min(pixelMinX,pixelX);pixelMaxX=Math.max(pixelMaxX,pixelX);pixelMinY=Math.min(pixelMinY,pixelY);pixelMaxY=Math.max(pixelMaxY,pixelY)}lastSamples=samples;lastToneCounts=toneCounts;lastBounds={logicalMinX:logicalMinX,logicalMaxX:logicalMaxX,logicalMinY:logicalMinY,logicalMaxY:logicalMaxY,pixelMinX:pixelMinX,pixelMaxX:pixelMaxX,pixelMinY:pixelMinY,pixelMaxY:pixelMaxY};lastGeometryChecksum=checksumText(samples.map(function(sample){return[sample.index,sample.t.toFixed(9),sample.rawX.toFixed(9),sample.rawY.toFixed(9),sample.logicalX.toFixed(6),sample.logicalY.toFixed(6),sample.pixelX,sample.pixelY,sample.age,sample.tone,sample.color||'none'].join(',')}).join('|'));geometryBuilds++}
function updateReadout(){readout.textContent='a:'+targetA+' b:'+targetB+' φ:'+phi.toFixed(2)}
function expose(nextSource){source=nextSource||source;Object.assign(root.dataset,{logicalWidth:String(logicalWidth),logicalHeight:String(logicalHeight),gridSize:String(gridSize),sampleCount:String(sampleCount),headIndex:String(headIndex),goneCount:String(goneCount),txt2Count:String(txt2Count),txt1Count:String(txt1Count),recentCount:String(recentCount),frameCount:String(frameCount),phi:phi.toFixed(6),phiStep:String(phiStep),currentA:currentA.toFixed(9),currentB:currentB.toFixed(9),targetA:String(targetA),targetB:String(targetB),tweenElapsed:tweenElapsed.toFixed(3),tweenActive:String(tweenActive),tweenRawProgress:tweenRawProgress.toFixed(9),tweenEasedProgress:tweenEasedProgress.toFixed(9),schedulerFrames:String(schedulerFrames),renderFrames:String(renderFrames),geometryBuilds:String(geometryBuilds),clearFrames:String(clearFrames),fadeFrames:String(fadeFrames),lastMode:lastMode,running:String(running),visible:String(visible),documentVisible:String(documentVisible),reduced:String(reduced),cleaned:String(cleaned),frameId:String(frameId),dpr:dpr.toFixed(3),fixtureChecksum:fixtureChecksum,geometryChecksum:lastGeometryChecksum,trailChecksum:trailChecksum,resets:String(resets),source:source});updateReadout()}
function draw(nextSource,fullClear){context.setTransform(dpr,0,0,dpr,0,0);context.imageSmoothingEnabled=false;if(fullClear){context.globalAlpha=1;context.fillStyle=colors.background;context.fillRect(0,0,logicalWidth,logicalHeight);lastMode='full-clear';clearFrames++}else{context.globalAlpha=fadeAlpha;context.fillStyle=colors.background;context.fillRect(0,0,logicalWidth,logicalHeight);context.globalAlpha=1;lastMode='fade';fadeFrames++}buildGeometry();for(const sample of lastSamples){if(!sample.drawn)continue;context.fillStyle=sample.color;context.fillRect(sample.pixelX,sample.pixelY,gridSize,gridSize)}context.globalAlpha=1;context.imageSmoothingEnabled=false;renderFrames++;trailChecksum=checksumText(trailChecksum+'|'+lastMode+'|'+frameCount+'|'+lastGeometryChecksum);expose(nextSource||'render')}
function sizeCanvas(nextSource,countResize){const rect=canvas.getBoundingClientRect(),nextDpr=Math.min(2,window.devicePixelRatio||1);cssWidth=rect.width;cssHeight=rect.height;if(canvas.width!==Math.round(logicalWidth*nextDpr)||canvas.height!==Math.round(logicalHeight*nextDpr)){canvas.width=Math.round(logicalWidth*nextDpr);canvas.height=Math.round(logicalHeight*nextDpr)}dpr=nextDpr;context.imageSmoothingEnabled=false;if(countResize)resizeEvents++;draw(nextSource||'resize observer',true)}
function canRun(){return !reduced&&!cleaned&&root.isConnected&&visible&&documentVisible}
function stepTween(delta){if(!tweenActive)return;tweenElapsed=clamp(tweenElapsed+delta,0,tweenDuration);tweenRawProgress=tweenElapsed/tweenDuration;tweenEasedProgress=easeInOut(tweenRawProgress);currentA=tweenStartA+(targetA-tweenStartA)*tweenEasedProgress;currentB=tweenStartB+(targetB-tweenStartB)*tweenEasedProgress;if(tweenRawProgress>=1){currentA=targetA;currentB=targetB;tweenActive=false;tweenCompletes++}}
function frame(now){frameId=0;if(!root.isConnected){cleanup();return}if(!canRun()){running=false;lastWall=0;expose('scheduler pause');return}const delta=clamp(now-lastWall,0,maxDelta);lastWall=now;activeTime+=delta;stepTween(delta);frameCount++;phi=initialPhi+frameCount*phiStep;schedulerFrames++;draw('animation frame',false);frameId=requestAnimationFrame(frame);expose('animation scheduled')}
function startScheduler(nextSource){if(!canRun()||running)return;running=true;lastWall=performance.now();schedulerStarts++;frameId=requestAnimationFrame(frame);expose(nextSource||'scheduler start')}
function stopScheduler(nextSource){if(frameId)cancelAnimationFrame(frameId);frameId=0;if(running)schedulerStops++;running=false;lastWall=0;expose(nextSource||'scheduler stop')}
function retarget(nextA,nextB,nextSource){const changed=nextA!==targetA||nextB!==targetB;if(!changed){expose(nextSource||'input unchanged');return}if(tweenActive)tweenRetargets++;targetA=nextA;targetB=nextB;if(reduced){currentA=targetA;currentB=targetB;tweenStartA=currentA;tweenStartB=currentB;tweenElapsed=tweenDuration;tweenRawProgress=1;tweenEasedProgress=1;tweenActive=false;draw('reduced input',true)}else{tweenStartA=currentA;tweenStartB=currentB;tweenElapsed=0;tweenRawProgress=0;tweenEasedProgress=0;tweenActive=currentA!==targetA||currentB!==targetB;tweenStarts++;if(running)lastWall=performance.now();expose(nextSource||'input retarget')}status.textContent='Frequency pair '+targetA+' by '+targetB}
function handleInput(nextSource){inputEvents++;retarget(clamp(Math.round(Number(sliderA.value)),frequencyMin,frequencyMax),clamp(Math.round(Number(sliderB.value)),frequencyMin,frequencyMax),nextSource||'slider input')}
function resetDemo(){if(cleaned)return;const resetNumber=resets+1;stopScheduler('reset stop');activeTime=0;frameCount=0;phi=initialPhi;currentA=defaultA;currentB=defaultB;targetA=defaultA;targetB=defaultB;tweenStartA=defaultA;tweenStartB=defaultB;tweenElapsed=0;tweenActive=false;tweenRawProgress=1;tweenEasedProgress=1;schedulerFrames=0;renderFrames=0;geometryBuilds=0;tweenStarts=0;tweenRetargets=0;tweenCompletes=0;inputEvents=0;changeEvents=0;resizeEvents=0;visibilityChanges=0;intersectionChanges=0;schedulerStarts=0;schedulerStops=0;clearFrames=0;fadeFrames=0;resets=resetNumber;trailChecksum='00000000';sliderA.value=String(defaultA);sliderB.value=String(defaultB);draw('reset',true);status.textContent='Lissajous curve reset';if(!reduced)startScheduler('reset scheduler')}
function deepFreeze(value){if(value&&typeof value==='object'&&!Object.isFrozen(value)){Object.freeze(value);Object.keys(value).forEach(function(key){deepFreeze(value[key])})}return value}
function inspect(){const sampleCopies=lastSamples.map(function(sample){return Object.assign({},sample)}),rect=canvas.getBoundingClientRect(),olderCount=goneCount+txt2Count+txt1Count;return deepFreeze({config:{logicalWidth:logicalWidth,logicalHeight:logicalHeight,gridSize:gridSize,gridColumns:gridColumns,gridRows:gridRows,sampleCount:sampleCount,headIndex:headIndex,recentStart:recentStart,recentCount:recentCount,txt1Start:txt1Start,txt1Count:txt1Count,txt2Start:txt2Start,txt2Count:txt2Count,goneCount:goneCount,olderCount:olderCount,initialPhi:initialPhi,phiStep:phiStep,tweenDuration:tweenDuration,tweenBezier:tweenBezier.slice(),persistenceFactor:persistenceFactor,fadeAlpha:fadeAlpha,defaultA:defaultA,defaultB:defaultB,frequencyMin:frequencyMin,frequencyMax:frequencyMax,frequencyStep:frequencyStep,centerX:centerX,centerY:centerY,xAmplitude:xAmplitude,yAmplitude:yAmplitude,maxDelta:maxDelta,colors:Object.assign({},colors)},fixtures:{parameters:parameters.map(function(parameter){return Object.assign({},parameter)}),fixtureChecksum:fixtureChecksum},state:{activeTime:activeTime,frameCount:frameCount,phi:phi,currentA:currentA,currentB:currentB,targetA:targetA,targetB:targetB,tweenStartA:tweenStartA,tweenStartB:tweenStartB,tweenElapsed:tweenElapsed,tweenActive:tweenActive,tweenRawProgress:tweenRawProgress,tweenEasedProgress:tweenEasedProgress,source:source},points:{samples:sampleCopies,head:sampleCopies[headIndex],recentCount:recentCount,olderCount:olderCount,goneCount:goneCount,toneCounts:Object.assign({},lastToneCounts),bounds:Object.assign({},lastBounds),geometryChecksum:lastGeometryChecksum},trail:{persistenceFactor:persistenceFactor,fadeAlpha:fadeAlpha,clearFrames:clearFrames,fadeFrames:fadeFrames,lastMode:lastMode,drawn:lastToneCounts.drawn,gone:lastToneCounts.gone,drawnPerFrame:lastToneCounts.drawn,gonePerFrame:lastToneCounts.gone,trailChecksum:trailChecksum},input:{a:Number(sliderA.value),b:Number(sliderB.value),aMin:Number(sliderA.min),aMax:Number(sliderA.max),aStep:Number(sliderA.step),bMin:Number(sliderB.min),bMax:Number(sliderB.max),bStep:Number(sliderB.step),readout:readout.textContent},counters:{schedulerFrames:schedulerFrames,renderFrames:renderFrames,geometryBuilds:geometryBuilds,tweenStarts:tweenStarts,tweenRetargets:tweenRetargets,tweenCompletes:tweenCompletes,inputEvents:inputEvents,changeEvents:changeEvents,resizeEvents:resizeEvents,visibilityChanges:visibilityChanges,intersectionChanges:intersectionChanges,schedulerStarts:schedulerStarts,schedulerStops:schedulerStops,resets:resets,cleanupCalls:cleanupCalls},lifecycle:{visible:visible,documentVisible:documentVisible,running:running,reduced:reduced,cleaned:cleaned,frameId:frameId},canvas:{logicalWidth:logicalWidth,logicalHeight:logicalHeight,backingWidth:canvas.width,backingHeight:canvas.height,cssWidth:rect.width,cssHeight:rect.height,dpr:dpr,imageSmoothingEnabled:context.imageSmoothingEnabled},checksums:{fixtureChecksum:fixtureChecksum,geometryChecksum:lastGeometryChecksum,trailChecksum:trailChecksum},data:Object.assign({},root.dataset)})}
sliderA.addEventListener('input',function(){handleInput('slider a input')},listener);sliderB.addEventListener('input',function(){handleInput('slider b input')},listener);
sliderA.addEventListener('change',function(){changeEvents++;status.textContent='Frequency a set to '+sliderA.value;expose('slider a change')},listener);sliderB.addEventListener('change',function(){changeEvents++;status.textContent='Frequency b set to '+sliderB.value;expose('slider b change')},listener);
controls.addEventListener('submit',function(event){event.preventDefault()},listener);
function resetKey(event){if(event.key!=='r'&&event.key!=='R')return;event.preventDefault();resetDemo()}
lissajousStage.addEventListener('keydown',resetKey,listener);sliderA.addEventListener('keydown',resetKey,listener);sliderB.addEventListener('keydown',resetKey,listener);
document.addEventListener('visibilitychange',function(){documentVisible=document.visibilityState!=='hidden';visibilityChanges++;if(documentVisible)startScheduler('document visible');else stopScheduler('document hidden');if(reduced)expose('document visibility')},listener);
if('ResizeObserver'in window){resizeObserver=new ResizeObserver(function(){if(cleaned)return;sizeCanvas('resize observer',true)});resizeObserver.observe(canvas)}
if('IntersectionObserver'in window){intersectionObserver=new IntersectionObserver(function(entries){if(cleaned)return;const entry=entries[entries.length-1];visible=!!entry&&entry.isIntersecting;intersectionChanges++;if(visible)startScheduler('intersection visible');else stopScheduler('intersection hidden');if(reduced)expose('intersection')},{threshold:.01});intersectionObserver.observe(root)}
function cleanup(){if(cleaned)return;cleanupCalls++;cleaned=true;stopScheduler('cleanup');controller.abort();if(resizeObserver)resizeObserver.disconnect();if(intersectionObserver)intersectionObserver.disconnect();if(connectionObserver)connectionObserver.disconnect();expose('cleanup')}
if('MutationObserver'in window){connectionObserver=new MutationObserver(function(){if(!root.isConnected)cleanup()});connectionObserver.observe(document.documentElement,{childList:true,subtree:true})}
root.__pixelLissajousLabInspect=inspect;root.__pixelLissajousLabReset=resetDemo;
sizeCanvas('initial',false);if(reduced){visible=true;status.textContent='Static Lissajous curve ready';draw('reduced ready',true)}else startScheduler('initial scheduler');`,
  prompt:`
Build a self-contained chunky pixel Lissajous laboratory in a 320px-tall responsive card. Use a fixed 360×196 logical canvas with a DPR backing capped at 2, image smoothing disabled, and a strict 4px grid. Every visible curve mark must be an actual axis-aligned fillRect(x, y, 4, 4) whose x and y origins are integer multiples of 4; do not stroke paths, draw circles, or rely on anti-aliasing.

Reconstruct exactly 600 samples indexed 0–599 with t = index / 599 × 2π. For each sample use x = sin(currentA × t + phi) and y = sin(currentB × t), logicalX = 180 + 152x, logicalY = 98 + 76y, pixelX = clamp(round((logicalX − 2) / 4) × 4, 0, 356), and pixelY = clamp(round((logicalY − 2) / 4) × 4, 0, 192). Draw ascending by index so newer marks win at collisions. Make the age partition explicit and invariant: samples 0–199 are gone and calculated but not drawn; 200–349 are 150 txt2 pixels in #5c5c66; 350–498 are 149 txt1 pixels in #9b9ba3; 499–598 are exactly 100 recent acc pixels in #fa7319; and sample 599 is the single head pixel in #ffffff. Thus all 600 samples remain reconstructable and exactly 400 are drawn per frame.

Keep the canvas persistent. Before every active animation draw, fill the whole canvas with bg0 #0a0a0b at global alpha 0.04, multiplying the retained signal by exactly 0.96 toward bg0, then draw the current 400 visible 4×4 marks. Use an opaque bg0 full clear only for initial sizing, resize reconstruction, deterministic reset, and reduced-motion state changes. Start phi at exactly 1.72 so the initial readout is exactly a:3 b:4 φ:1.72; on every active requestAnimationFrame increment an integer frameCount once and set the unbounded phase to phi = 1.72 + frameCount × 0.008. Hidden or offscreen time must not advance phi.

Add two real range inputs a and b, each min 1, max 9, step 1, initially 3 and 4. Style each track as a 1px line0 #232327 line and each thumb as a square 12px acc #fa7319 block. Preserve native pointer and keyboard range semantics including arrows, Page Up/Down, Home, and End. Show the target integers and live phase in a 10px txt2 #5c5c66 output with the exact format a:3 b:4 φ:1.72. R on the stage or either slider performs deterministic reset.

In normal motion, a slider change must never jump the curve. Capture the current interpolated a and b floats as tweenStartA and tweenStartB, set the new integer targets, and interpolate both over exactly 800ms using cubic-bezier(0.65, 0, 0.35, 1). Solve the bezier x coordinate for linear elapsed progress before evaluating y; retargeting an in-flight morph must begin at the current floats with zero discontinuity. Cap scheduler delta at 50ms. Under prefers-reduced-motion, create no animation loop, keep a true static 600-sample curve, and apply integer slider state instantly with an opaque reconstruction.

Use one requestAnimationFrame scheduler with document visibility and IntersectionObserver pausing, no hidden-time catch-up, ResizeObserver DPR handling, AbortController-backed listeners, detach cleanup, and deterministic reset. Expose frozen reconstruction-grade inspection data including every sample's index, t, age, raw sine coordinates, logical coordinates, snapped pixel coordinates, 4×4 size, tone, exact color, and drawn flag; constants and exact age-boundary counts; current and target tween state; slider attributes and readout; full-clear/fade counters; scheduling/lifecycle counters; canvas dimensions and smoothing state; and deterministic fixture, geometry, and rolling persistent-trail checksums.`
});
