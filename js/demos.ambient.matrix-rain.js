/* INTRX registry - glyph rain with pointer-cleared aperture */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'matrix-rain',title:'Matrix Rain',cat:'Ambient',rootClass:'d-matrix-demo',tags:['canvas','glyphs','cursor'],libs:[],desc:'Seeded glyph columns fall at independent rates with bright heads and fading tails. The cursor opens a soft circular aperture by suppressing glyph opacity inside its radius.',seen:'Seen on: cyber-security pages, developer tools, terminal themes, games, and digital installations',hint:'inspect the seeded columns, move through the rain to clear a hole, switch glyph sets, pause, and reset',html:`<div class="d-matrix-demo"><header><span>AMBIENT / GLYPH PRECIPITATION</span><span class="d-matrix-readout">RUNNING / LATIN</span></header><div class="d-matrix-stage"><canvas aria-label="Falling glyph columns"></canvas><p>BRIGHT HEADS / 11-GLYPH TAIL / POINTER APERTURE</p></div><footer><button class="d-matrix-inspect" type="button">Inspect seed</button><button class="d-matrix-glyphs" type="button">Use kana</button><button class="d-matrix-toggle" type="button">Pause rain</button><button class="d-matrix-reset" type="button">Reset columns</button><span class="d-matrix-status" aria-live="polite">Glyph rain running</span></footer></div>`,css:`.d-matrix-demo{width:100%;min-height:555px;box-sizing:border-box;padding:20px clamp(5px,4vw,28px);background:#07110b;color:#dffff0;font-family:Inter,system-ui,sans-serif}.d-matrix-demo>header{display:flex;justify-content:space-between;color:#456c54;font:700 8px/1 ui-monospace,monospace;letter-spacing:.14em}.d-matrix-readout{color:#75ff9f}.d-matrix-stage{position:relative;height:430px;margin-top:13px;overflow:hidden;border:1px solid #245a35;background:#020805}.d-matrix-stage canvas{display:block;width:100%;height:100%;cursor:none}.d-matrix-stage p{position:absolute;right:11px;bottom:9px;margin:0;color:#326043;font:700 7px/1 ui-monospace,monospace;pointer-events:none}.d-matrix-demo footer{display:flex;justify-content:center;gap:5px;margin-top:10px}.d-matrix-demo button{min-height:32px;padding:7px 9px;border:1px solid #245a35;background:#0b1c11;color:#62a779;font:700 7px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-matrix-demo button:hover,.d-matrix-demo button:focus-visible{background:#75ff9f;color:#061009;outline:none}.d-matrix-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}`,js:`const canvas=root.querySelector('canvas'),ctx=canvas.getContext('2d'),readout=root.querySelector('.d-matrix-readout'),status=root.querySelector('.d-matrix-status'),inspect=root.querySelector('.d-matrix-inspect'),glyphButton=root.querySelector('.d-matrix-glyphs'),toggle=root.querySelector('.d-matrix-toggle'),reset=root.querySelector('.d-matrix-reset'),sets=['01ZXCVBNMASDFGHJKLQWERTY','アイウエオカキクケコサシスセソ'],reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;let columns=[],setIndex=0,pointer=null,w=0,h=0,frames=0,running=false,toggles=0,raf=0;function random(i,s){const v=Math.sin((i+4)*611.7+s*47.3)*25171.9;return v-Math.floor(v)}function seed(){const count=Math.max(12,Math.floor(w/18));columns=Array.from({length:count},(_,i)=>({x:i*18+9,y:random(i,1)*h,speed:.65+random(i,2)*1.45,offset:Math.floor(random(i,3)*sets[setIndex].length)}))}function checksum(){return columns.reduce((sum,c,i)=>sum+c.y*(i+1),0).toFixed(2)}function paint(source){ctx.fillStyle='#020805';ctx.fillRect(0,0,w,h);ctx.font='12px ui-monospace,monospace';ctx.textAlign='center';const glyphs=sets[setIndex];for(const c of columns)for(let tail=0;tail<11;tail++){const y=(c.y-tail*17+h+187)%h,dx=pointer?c.x-pointer.x:999,dy=pointer?y-pointer.y:999,distance=Math.hypot(dx,dy),hole=pointer?Math.min(1,Math.max(0,(distance-34)/32)):1,alpha=(1-tail/11)*hole;if(alpha<.03)continue;ctx.fillStyle=tail===0?'rgba(220,255,232,'+alpha.toFixed(2)+')':'rgba(69,255,123,'+(alpha*.72).toFixed(2)+')';ctx.fillText(glyphs[(c.offset+tail+Math.floor(frames/8))%glyphs.length],c.x,y)}root.dataset.running=String(running);root.dataset.frames=String(frames);root.dataset.set=setIndex?'kana':'latin';root.dataset.pointer=pointer?pointer.x.toFixed(1)+','+pointer.y.toFixed(1):'none';root.dataset.columns=String(columns.length);root.dataset.checksum=checksum();root.dataset.toggles=String(toggles);root.dataset.source=source;readout.textContent=(running?'RUNNING':'PAUSED')+' / '+(setIndex?'KANA':'LATIN')}function step(){for(const c of columns){c.y+=c.speed;if(c.y>h+187)c.y-=h+187}}function loop(){if(!running)return;step();frames++;paint('frame');raf=requestAnimationFrame(loop)}function setRunning(next,source){cancelAnimationFrame(raf);running=next&&!reduced;toggles++;paint(source);toggle.textContent=running?'Pause rain':'Run rain';if(running)raf=requestAnimationFrame(loop)}canvas.addEventListener('pointermove',event=>{const r=canvas.getBoundingClientRect();pointer={x:event.clientX-r.left,y:event.clientY-r.top};paint('pointer')});canvas.addEventListener('pointerleave',()=>{pointer=null;paint('leave')});glyphButton.addEventListener('click',()=>{setIndex=1-setIndex;seed();paint('glyphs');glyphButton.textContent=setIndex?'Use latin':'Use kana';status.textContent=setIndex?'Kana glyph set active':'Latin glyph set active'});inspect.addEventListener('click',()=>{cancelAnimationFrame(raf);running=false;frames=0;pointer=null;setIndex=0;seed();paint('inspect');toggle.textContent='Run rain';glyphButton.textContent='Use kana';status.textContent='Seeded columns frozen'});toggle.addEventListener('click',()=>{setRunning(!running,'toggle');status.textContent=running?'Glyph rain running':'Glyph rain paused'});reset.addEventListener('click',()=>{cancelAnimationFrame(raf);running=false;frames=0;pointer=null;setIndex=0;toggles++;seed();paint('reset');toggle.textContent='Run rain';glyphButton.textContent='Use kana';status.textContent='Glyph rain reset to seed 611'});new ResizeObserver(()=>{const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);w=r.width;h=r.height;canvas.width=Math.round(w*d);canvas.height=Math.round(h*d);ctx.setTransform(d,0,0,d,0,0);seed();paint('resize')}).observe(canvas);paint('initial');if(!reduced)setRunning(true,'auto');`,prompt:`Render seeded canvas glyph rain in 18px columns. Each column has an independent speed, a bright head, and an 11-glyph fading tail. Support Latin and kana symbol sets. Pointer movement must create a soft 34–66px aperture by attenuating glyph alpha based on distance, then remove it on leave. Add inspect, glyph switching, run/pause, exact reset, reduced motion, and running/frames/set/pointer/columns/checksum/toggles/source telemetry.`});
