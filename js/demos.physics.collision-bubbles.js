/* INTRX registry - elastic collision bubbles with pointer repeller */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'collision-bubbles',title:'Collision Bubbles',cat:'Physics',rootClass:'d-collisions',tags:['elastic collision','repeller','simulation'],libs:[],desc:'Twelve buoyant circles exchange momentum through equal-mass elastic collisions inside a bounded field. The pointer acts as a soft moving body, separating overlaps and injecting velocity without teleporting the bubbles.',seen:'Seen on: playful filters, service clouds, interactive data clusters, and portfolio navigation',hint:'move through the cluster to repel bubbles, then burst the field to remix momentum',
html:`<div class="d-collisions"><header><span>ELASTIC FIELD / 12 BODIES</span><span class="d-collisions-hits">COLLISIONS 0000</span></header><div class="d-collisions-stage" tabindex="0" role="application" aria-label="Twelve floating elastic collision bubbles with pointer repeller"><div class="d-collisions-bubble" style="--r:31px;--tone:#d9ec66">FORM</div><div class="d-collisions-bubble" style="--r:24px;--tone:#e79b72">01</div><div class="d-collisions-bubble" style="--r:38px;--tone:#76bfd7">SPACE</div><div class="d-collisions-bubble" style="--r:27px;--tone:#d893ce">04</div><div class="d-collisions-bubble" style="--r:34px;--tone:#7bcda4">FLOW</div><div class="d-collisions-bubble" style="--r:22px;--tone:#dec16f">06</div><div class="d-collisions-bubble" style="--r:29px;--tone:#96a7e3">LINE</div><div class="d-collisions-bubble" style="--r:36px;--tone:#da8796">TOUCH</div><div class="d-collisions-bubble" style="--r:25px;--tone:#8dc1aa">09</div><div class="d-collisions-bubble" style="--r:32px;--tone:#c9a2da">TIME</div><div class="d-collisions-bubble" style="--r:23px;--tone:#e0aa7d">11</div><div class="d-collisions-bubble" style="--r:35px;--tone:#8bd5c8">SYSTEM</div><div class="d-collisions-repeller" aria-hidden="true"><i></i></div><span class="d-collisions-guide">MOVE TO REPEL / ARROWS MOVE FIELD BODY</span></div><footer><button type="button" class="d-collisions-burst">Burst field</button><button type="button" class="d-collisions-pause" aria-pressed="false">Pause</button><p class="d-collisions-status" aria-live="polite">Elastic collision field running</p></footer></div>`,
css:`.d-collisions{width:100%;min-height:555px;box-sizing:border-box;padding:20px clamp(5px,4vw,28px);color:#edf0e9;background:#0a0a0b;font-family:Roboto Mono,JetBrains Mono,monospace}.d-collisions>header{display:flex;justify-content:space-between;color:#70766d;font:700 8px/1 ui-monospace,monospace;letter-spacing:.14em}.d-collisions-hits{color:#d9ec66}.d-collisions-stage{position:relative;height:430px;margin-top:13px;overflow:hidden;border:1px solid #31362e;background:radial-gradient(circle at 50%,#283024,#151813 70%);outline:none;cursor:none;touch-action:pan-y}.d-collisions-stage:focus-visible{box-shadow:inset 0 0 0 2px #d9ec66}.d-collisions-bubble{position:absolute;left:0;top:0;width:calc(var(--r)*2);height:calc(var(--r)*2);margin:calc(var(--r)*-1);display:grid;place-items:center;box-sizing:border-box;border:1px solid color-mix(in srgb,var(--tone),#333 45%);border-radius:50%;color:var(--tone);background:radial-gradient(circle at 34% 27%,color-mix(in srgb,var(--tone),transparent 65%),rgba(25,29,23,.92) 63%);box-shadow:inset 0 0 14px color-mix(in srgb,var(--tone),transparent 82%),0 5px 10px rgba(0,0,0,.24);font:700 7px/1 ui-monospace,monospace;letter-spacing:.08em;will-change:transform;user-select:none}.d-collisions-repeller{position:absolute;left:50%;top:50%;width:72px;height:72px;transform:translate(-50%,-50%);border:1px dashed rgba(217,236,102,.4);border-radius:50%;pointer-events:none;opacity:0;transition:opacity .2s}.d-collisions-stage:hover .d-collisions-repeller,.d-collisions-stage:focus-within .d-collisions-repeller{opacity:1}.d-collisions-repeller i{position:absolute;left:50%;top:50%;width:5px;height:5px;transform:translate(-50%,-50%);border-radius:50%;background:#d9ec66;box-shadow:0 0 12px #d9ec66}.d-collisions-guide{position:absolute;left:11px;bottom:10px;color:#62685f;font:700 6px/1 ui-monospace,monospace;letter-spacing:.12em}.d-collisions footer{display:flex;justify-content:center;gap:6px;margin-top:10px}.d-collisions footer button{min-height:32px;padding:7px 11px;border:1px solid #373c33;color:#858c80;background:#171a15;font:700 7px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-collisions footer button:hover,.d-collisions footer button:focus-visible,.d-collisions footer button[aria-pressed=true]{color:#15170f;background:#d9ec66;outline:none}.d-collisions-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}@media(max-width:540px){.d-collisions{padding-inline:4px}.d-collisions-stage{height:400px}}@media(prefers-reduced-motion:reduce){.d-collisions-bubble{transition:transform .1s}}`,
js:`const field=root.querySelector('.d-collisions-stage');const elements=[...root.querySelectorAll('.d-collisions-bubble')];const repeller=root.querySelector('.d-collisions-repeller');const hitsLabel=root.querySelector('.d-collisions-hits');const burstButton=root.querySelector('.d-collisions-burst');const pauseButton=root.querySelector('.d-collisions-pause');const status=root.querySelector('.d-collisions-status');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;let bodies=[],pointer={x:0,y:0,active:false,vx:0,vy:0,lastX:0,lastY:0,lastT:0},collisions=0,paused=reduce,visible=true,running=false,last=0;function bounds(){return{w:field.clientWidth,h:field.clientHeight}}function init(){const d=bounds();bodies=elements.map((el,i)=>{const r=parseFloat(el.style.getPropertyValue('--r'))||28,angle=i*2.399;return{el,r,x:d.w/2+Math.cos(angle)*(70+i*8)%Math.max(80,d.w*.35),y:d.h/2+Math.sin(angle)*(55+i*6)%Math.max(70,d.h*.3),vx:Math.cos(angle*1.7)*(25+i*2),vy:Math.sin(angle*1.3)*(22+i*1.5)}});collisions=0;draw();start()}function resolve(){for(let i=0;i<bodies.length;i++)for(let j=i+1;j<bodies.length;j++){const a=bodies[i],b=bodies[j],dx=b.x-a.x,dy=b.y-a.y,distance=Math.max(.001,Math.hypot(dx,dy)),minimum=a.r+b.r;if(distance>=minimum)continue;collisions++;const nx=dx/distance,ny=dy/distance,overlap=minimum-distance;a.x-=nx*overlap*.5;a.y-=ny*overlap*.5;b.x+=nx*overlap*.5;b.y+=ny*overlap*.5;const relative=(b.vx-a.vx)*nx+(b.vy-a.vy)*ny;if(relative<0){a.vx+=relative*nx;a.vy+=relative*ny;b.vx-=relative*nx;b.vy-=relative*ny}}}function repel(){if(!pointer.active)return;bodies.forEach(body=>{const dx=body.x-pointer.x,dy=body.y-pointer.y,distance=Math.max(.001,Math.hypot(dx,dy)),minimum=body.r+36;if(distance>=minimum)return;const nx=dx/distance,ny=dy/distance,push=(minimum-distance)*.22;body.x+=nx*(minimum-distance);body.y+=ny*(minimum-distance);body.vx+=nx*push+pointer.vx*.06;body.vy+=ny*push+pointer.vy*.06})}function draw(){bodies.forEach(body=>body.el.style.transform='translate('+body.x.toFixed(2)+'px,'+body.y.toFixed(2)+'px)');hitsLabel.textContent='COLLISIONS '+String(collisions).padStart(4,'0');root.dataset.collisions=String(collisions)}function frame(now){if(paused||!visible){running=false;draw();return}const dt=Math.min(.028,Math.max(.008,(now-last)/1000));last=now;const d=bounds();bodies.forEach(body=>{body.x+=body.vx*dt;body.y+=body.vy*dt;if(body.x<body.r){body.x=body.r;body.vx=Math.abs(body.vx)}else if(body.x>d.w-body.r){body.x=d.w-body.r;body.vx=-Math.abs(body.vx)}if(body.y<body.r){body.y=body.r;body.vy=Math.abs(body.vy)}else if(body.y>d.h-body.r){body.y=d.h-body.r;body.vy=-Math.abs(body.vy)}body.vx*=Math.pow(.9995,dt*60);body.vy*=Math.pow(.9995,dt*60)});resolve();repel();draw();requestAnimationFrame(frame)}function start(){if(running||paused||!visible)return;running=true;last=performance.now();requestAnimationFrame(frame)}function movePointer(x,y,now){const dt=Math.max(8,now-pointer.lastT);pointer.vx=(x-pointer.lastX)/dt*1000;pointer.vy=(y-pointer.lastY)/dt*1000;pointer.x=x;pointer.y=y;pointer.lastX=x;pointer.lastY=y;pointer.lastT=now;pointer.active=true;repeller.style.left=x+'px';repeller.style.top=y+'px';start()}field.addEventListener('pointermove',event=>{const box=field.getBoundingClientRect();movePointer(event.clientX-box.left,event.clientY-box.top,performance.now())});field.addEventListener('pointerleave',()=>pointer.active=false);field.addEventListener('keydown',event=>{const step=26;if(!pointer.active){const d=bounds();pointer.x=d.w/2;pointer.y=d.h/2;pointer.active=true}if(event.key==='ArrowLeft'){event.preventDefault();movePointer(pointer.x-step,pointer.y,performance.now())}else if(event.key==='ArrowRight'){event.preventDefault();movePointer(pointer.x+step,pointer.y,performance.now())}else if(event.key==='ArrowUp'){event.preventDefault();movePointer(pointer.x,pointer.y-step,performance.now())}else if(event.key==='ArrowDown'){event.preventDefault();movePointer(pointer.x,pointer.y+step,performance.now())}else if(event.key==='Escape'){pointer.active=false}});function burst(){const d=bounds();bodies.forEach(body=>{const dx=body.x-d.w/2,dy=body.y-d.h/2,length=Math.max(1,Math.hypot(dx,dy));body.vx=dx/length*(130+Math.random()*120);body.vy=dy/length*(130+Math.random()*120)});status.textContent='Radial momentum burst applied';start()}burstButton.addEventListener('click',burst);pauseButton.addEventListener('click',()=>{paused=!paused;pauseButton.setAttribute('aria-pressed',paused?'true':'false');pauseButton.textContent=paused?'Resume':'Pause';status.textContent=paused?'Collision field paused':'Elastic collision field running';if(!paused)start()});if('ResizeObserver'in window)new ResizeObserver(()=>{const d=bounds();bodies.forEach(body=>{body.x=Math.max(body.r,Math.min(d.w-body.r,body.x));body.y=Math.max(body.r,Math.min(d.h-body.r,body.y))});draw()}).observe(field);if('IntersectionObserver'in window)new IntersectionObserver(entries=>{visible=entries[0].isIntersecting;if(visible)start();else running=false},{threshold:.05}).observe(root);init();`,
prompt:`Build a self-contained twelve-bubble elastic collision field. Each circle stores radius, position, and velocity. Integrate at dt clamped 0.008–0.028s, bounce perfectly from container walls, and resolve each overlapping pair by equal separation along the normal followed by an equal-mass elastic normal velocity exchange when bodies are closing.

Treat the pointer as a moving radius-36 repeller. For overlaps, move the bubble fully outside the pointer and add normal push plus 6% of sampled pointer px/s velocity. Arrow keys move the repeller by 26px. A Burst control replaces velocities with radial impulses of 130–250px/s; Pause/Resume freezes the loop.

Track cumulative collision count, expose it as data, resize safely, pause offscreen, provide reduced-motion pause, synchronize status/ARIA, show the repeller, use visible focus and touch-action pan-y, scoped CSS, and no dependencies.`});
