/* INTRX registry — published WebGL noise-flow pixel river */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'webgl-pixel-river',title:'WebGL Pixel River',cat:'Image & WebGL',rootClass:'d-pixelriver',tags:['WebGL','noise flow','pixel shader'],libs:[],desc:'A finite-difference noise gradient turns UV space into a flowing current before the result is quantized into a crisp pixel field. Hover, focus, or a touch latch deepens the shader displacement without changing the flow topology.',seen:'Seen on: generative identities, music visuals, digital-art covers, and immersive portfolio fields',hint:'hover or focus to strengthen the current; click to latch intensity for touch',
html:`<div class="d-pixelriver" role="group" aria-label="Interactive WebGL pixel river"><div class="d-pixelriver-head"><span>FLOW FIELD / CURL NOISE</span><span>INTENSITY <b>0.22</b> · CURRENT <i>IDLE</i></span></div><div class="d-pixelriver-field"><canvas tabindex="0" role="button" aria-pressed="false" aria-label="Flowing WebGL pixel river. Hover or focus to increase flow; activate to latch high intensity."></canvas><div class="d-pixelriver-vector" aria-hidden="true"><span>UV</span><i></i><b>∿</b></div><div class="d-pixelriver-scale" aria-hidden="true"><span>150 × 90 CELLS</span><i></i><span>NOISE GRADIENT / ε .012</span></div><p class="d-pixelriver-fallback">WebGL unavailable — showing static river field.</p></div><div class="d-pixelriver-foot"><p aria-live="polite">Pixel river flowing at idle intensity</p><button type="button">Release current</button></div></div>`,
css:`.d-pixelriver{width:100%;min-height:510px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#eef2f2;background:#080a0d;font-family:Inter,system-ui,sans-serif}.d-pixelriver-head{display:flex;justify-content:space-between;color:#687179;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.12em}.d-pixelriver-head b{color:#6fe0ff}.d-pixelriver-head i{color:#d1dbe0;font-style:normal}.d-pixelriver-field{position:relative;height:390px;margin-top:13px;overflow:hidden;border:1px solid #222a33;background:linear-gradient(145deg,#112c3a,#10131d 60%,#080b10)}.d-pixelriver canvas{display:block;width:100%;height:100%;outline:none;cursor:pointer}.d-pixelriver canvas:focus-visible{box-shadow:inset 0 0 0 2px #8ee8ff}.d-pixelriver-vector{position:absolute;left:14px;top:13px;display:flex;align-items:center;gap:8px;color:rgba(224,241,247,.55);font:650 7px/1 ui-monospace,monospace;pointer-events:none}.d-pixelriver-vector i{width:54px;height:1px;background:#6aa8b9}.d-pixelriver-vector b{color:#7ae2fc;font-size:20px}.d-pixelriver-scale{position:absolute;left:14px;right:14px;bottom:11px;display:flex;align-items:center;gap:9px;color:rgba(222,237,242,.46);font:650 6px/1 ui-monospace,monospace;letter-spacing:.08em;pointer-events:none}.d-pixelriver-scale i{height:1px;flex:1;background:rgba(124,173,187,.35)}.d-pixelriver-fallback{display:none;position:absolute;inset:0;place-items:center;margin:0;color:#a3c0ca;font:650 9px/1 ui-monospace,monospace}.d-pixelriver.no-webgl .d-pixelriver-fallback{display:grid}.d-pixelriver.no-webgl canvas{opacity:0}.d-pixelriver-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-pixelriver-foot p{margin:0;color:#717c84;font:650 9px/1.3 ui-monospace,monospace}.d-pixelriver-foot button{min-height:31px;padding:7px 11px;border:1px solid #303a43;border-radius:3px;color:#a2acb3;background:#11171c;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-pixelriver-foot button:hover,.d-pixelriver-foot button:focus-visible{color:#fff;border-color:#667681;outline:none}@media(max-width:550px){.d-pixelriver{padding-inline:6px}.d-pixelriver-scale span:last-child{display:none}}`,
js:`const canvas=root.querySelector('canvas');const intensityReadout=root.querySelector('.d-pixelriver-head b');const currentReadout=root.querySelector('.d-pixelriver-head i');const status=root.querySelector('.d-pixelriver-foot p');const release=root.querySelector('.d-pixelriver-foot button');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const gl=canvas.getContext('webgl',{alpha:false,antialias:false});if(!gl){root.classList.add('no-webgl');status.textContent='WebGL unavailable';return}const vertexSource='attribute vec2 a_position;void main(){gl_Position=vec4(a_position,0.0,1.0);}';const fragmentSource='precision mediump float;uniform vec2 u_resolution;uniform float u_time;uniform float u_intensity;float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453123);}float noise(vec2 p){vec2 i=floor(p),f=fract(p);f=f*f*(3.0-2.0*f);return mix(mix(hash(i),hash(i+vec2(1.0,0.0)),f.x),mix(hash(i+vec2(0.0,1.0)),hash(i+vec2(1.0)),f.x),f.y);}float fbm(vec2 p){float value=0.0,amp=0.5;for(int i=0;i<4;i++){value+=amp*noise(p);p=p*2.03+17.7;amp*=0.5;}return value;}void main(){vec2 uv=gl_FragCoord.xy/u_resolution;float aspect=u_resolution.x/u_resolution.y;vec2 q=vec2(uv.x*aspect,uv.y);float t=u_time*0.12;vec2 domain=q*2.8+vec2(-t,t*0.38);float eps=0.012;float n=fbm(domain);float nx=fbm(domain+vec2(eps,0.0));float ny=fbm(domain+vec2(0.0,eps));vec2 flow=vec2((ny-n)/eps,-(nx-n)/eps);flow=normalize(flow+vec2(0.001));vec2 moved=uv+flow*0.028*u_intensity;moved.y+=sin(moved.x*8.0+u_time*0.7)*0.009*u_intensity;vec2 cells=vec2(150.0,90.0);vec2 pixelUV=(floor(moved*cells)+0.5)/cells;float river=fbm(vec2(pixelUV.x*aspect,pixelUV.y)*4.4+vec2(-t*1.7,t));float filament=sin((pixelUV.x*aspect+pixelUV.y)*42.0+river*7.0-u_time)*0.5+0.5;vec3 navy=vec3(0.018,0.045,0.105);vec3 blue=vec3(0.03,0.45,0.72);vec3 cyan=vec3(0.2,0.92,0.9);vec3 amber=vec3(1.0,0.56,0.2);vec3 color=mix(navy,blue,smoothstep(0.22,0.78,river));color=mix(color,cyan,pow(filament,5.0)*0.72);color=mix(color,amber,pow(1.0-filament,13.0)*0.28*u_intensity);vec2 grid=abs(fract(moved*cells)-0.5);float edge=smoothstep(0.47,0.5,max(grid.x,grid.y));color*=1.0-edge*0.2;gl_FragColor=vec4(color,1.0);}';function compile(type,source){const shader=gl.createShader(type);gl.shaderSource(shader,source);gl.compileShader(shader);if(!gl.getShaderParameter(shader,gl.COMPILE_STATUS))throw new Error(gl.getShaderInfoLog(shader));return shader}let program;try{program=gl.createProgram();gl.attachShader(program,compile(gl.VERTEX_SHADER,vertexSource));gl.attachShader(program,compile(gl.FRAGMENT_SHADER,fragmentSource));gl.linkProgram(program);if(!gl.getProgramParameter(program,gl.LINK_STATUS))throw new Error(gl.getProgramInfoLog(program))}catch(error){root.classList.add('no-webgl');status.textContent='WebGL shader unavailable';return}gl.useProgram(program);const buffer=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buffer);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),gl.STATIC_DRAW);const position=gl.getAttribLocation(program,'a_position');gl.enableVertexAttribArray(position);gl.vertexAttribPointer(position,2,gl.FLOAT,false,0,0);const resolutionUniform=gl.getUniformLocation(program,'u_resolution'),timeUniform=gl.getUniformLocation(program,'u_time'),intensityUniform=gl.getUniformLocation(program,'u_intensity');let hovered=false,focused=false,latched=false,intensity=.22,visible=true,running=false,last=0,time=0;
function target(){return hovered||focused||latched?1:.22}function resize(){const rect=canvas.getBoundingClientRect(),dpr=Math.min(2,window.devicePixelRatio||1),width=Math.max(1,Math.round(rect.width*dpr)),height=Math.max(1,Math.round(rect.height*dpr));if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height;gl.viewport(0,0,width,height)}draw()}function draw(){gl.uniform2f(resolutionUniform,canvas.width,canvas.height);gl.uniform1f(timeUniform,time);gl.uniform1f(intensityUniform,intensity);gl.drawArrays(gl.TRIANGLES,0,3);intensityReadout.textContent=intensity.toFixed(2)}function start(){if(reduce||!visible||running)return;running=true;last=performance.now();requestAnimationFrame(frame)}function frame(now){if(!visible){running=false;return}const dt=Math.min(60,now-last);last=now;time+=dt/1000;const alpha=1-Math.exp(-dt*.008);intensity+=(target()-intensity)*alpha;draw();requestAnimationFrame(frame)}function sync(announce){canvas.setAttribute('aria-pressed',latched?'true':'false');currentReadout.textContent=target()>.5?'STRONG':'IDLE';if(announce)status.textContent=latched?'Strong current latched':'Current released to hover control';if(reduce){intensity=target();time=0;draw()}else start()}canvas.addEventListener('pointerenter',function(){hovered=true;status.textContent='Flow intensity rising';sync(false)});canvas.addEventListener('pointerleave',function(){hovered=false;status.textContent='Flow intensity easing to idle';sync(false)});canvas.addEventListener('focus',function(){focused=true;sync(false)});canvas.addEventListener('blur',function(){focused=false;sync(false)});canvas.addEventListener('click',function(){latched=!latched;sync(true)});canvas.addEventListener('keydown',function(event){if(event.key==='Escape'){latched=false;hovered=false;sync(true);canvas.blur()}});release.addEventListener('click',function(){latched=false;hovered=false;focused=false;sync(true);release.focus()});if('ResizeObserver'in window)new ResizeObserver(resize).observe(canvas);if('IntersectionObserver'in window)new IntersectionObserver(function(entries){visible=entries[0].isIntersecting;if(visible)start()},{threshold:.05}).observe(root);resize();if(reduce){intensity=.35;draw();status.textContent='Static flow field — reduced motion'}else start();`,
prompt:`Build a self-contained WebGL 1.0 “pixel river” with no textures or libraries. Render a fullscreen triangle. In the fragment shader, implement value noise plus four-octave fbm. At each UV, sample fbm at a domain point and at +epsilon x/y (epsilon .012), then form a normalized curl-like vector vec2((ny-n)/eps, -(nx-n)/eps). Displace UV by flow*0.028*u_intensity.

Quantize displaced UV to a 150×90 cell grid before generating a layered navy/blue/cyan/amber procedural river texture. Add subtle cell edges. Animate the noise domain with u_time. Hover/focus target intensity is 1.0; idle is .22. Ease the JS intensity uniform using alpha=1-exp(-dt*.008), allowing smooth reversal. Click latches strong intensity for touch; Escape/Release clears it.

Run one RAF loop only while visible, using IntersectionObserver and a running guard. Resize canvas and viewport DPR-aware. Handle WebGL/shader failure visibly. Under prefers-reduced-motion, draw one static representative frame without a loop. Synchronize intensity, current state, aria-pressed latch, and restrained live status. Include visible canvas focus, flow annotations, scoped CSS, and no dependencies.`});
