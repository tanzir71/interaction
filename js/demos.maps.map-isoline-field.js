/* INTRX registry - deterministic living isoline field */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({
  id:'map-isoline-field',
  title:'Map Isoline Field',
  cat:'Maps & Geo',
  rootClass:'d-maps-map-isoline-field',
  tags:['canvas','marching-squares','noise'],
  libs:[],
  desc:'A deterministic two-octave elevation field becomes seven live contour bands. A softly eased heat source bends the topology while cached index labels remain steady inside real line gaps.',
  seen:'Seen on: Atlas of Inequality by Esteban Moro and the topo-drift genre',
  hint:'move to raise terrain; arrows move heat; Space toggles; Escape relaxes; R resets',
  html:`<div class="d-maps-map-isoline-field" role="region" aria-label="Interactive living contour map">
  <div class="d-maps-map-isoline-field-head" aria-hidden="true"><span>MAP / ISOLINES</span><span class="d-maps-map-isoline-field-readout">ISO 0.51 / Z 0.0000</span></div>
  <div class="d-maps-map-isoline-field-stage" role="application" tabindex="0" aria-keyshortcuts="ArrowLeft ArrowRight ArrowUp ArrowDown Home Enter Space Escape R" aria-label="Living contour field. Move the pointer to add heat, or use arrow keys to move the keyboard heat source. Space toggles heat, Escape relaxes it, Home centers it, and R resets.">
    <canvas class="d-maps-map-isoline-field-canvas" width="360" height="216" role="img" aria-label="Seven deterministic elevation contours bending around an interactive heat source"></canvas>
  </div>
  <div class="d-maps-map-isoline-field-foot" aria-hidden="true"><span>64 X 40 / SEED 42</span><span>2 OCT / 7 LEVELS</span></div>
  <span class="d-maps-map-isoline-field-status" aria-live="polite" aria-atomic="true">Living contour field ready</span>
</div>`,
  css:`
.d-maps-map-isoline-field{position:relative;width:100%;height:320px;box-sizing:border-box;overflow:hidden;contain:layout paint;container-type:inline-size;background:#0a0a0b;color:#ececef;font-family:'JetBrains Mono',ui-monospace,monospace;isolation:isolate;touch-action:pan-y}
.d-maps-map-isoline-field-head{position:absolute;z-index:3;top:14px;left:16px;right:16px;display:flex;align-items:center;justify-content:space-between;gap:12px;color:#9b9ba3;font-size:10px;line-height:1;letter-spacing:.08em;white-space:nowrap}
.d-maps-map-isoline-field-readout{color:#5c5c66;font-variant-numeric:tabular-nums}
.d-maps-map-isoline-field-stage{position:absolute;top:50%;left:50%;width:min(calc(100% - 28px),360px);aspect-ratio:5/3;box-sizing:border-box;overflow:hidden;border:0;border-radius:10px;background:#101012;box-shadow:inset 0 0 0 1px #232327;outline:none;transform:translate(-50%,-50%);cursor:crosshair;touch-action:pan-y}
.d-maps-map-isoline-field-stage:focus-visible{box-shadow:inset 0 0 0 1px #fa7319}
.d-maps-map-isoline-field-canvas{display:block;width:100%;height:100%;background:#101012}
.d-maps-map-isoline-field-foot{position:absolute;z-index:3;left:16px;right:16px;bottom:14px;display:flex;align-items:center;justify-content:space-between;gap:8px;color:#5c5c66;font-size:9px;line-height:1;letter-spacing:.05em;white-space:nowrap}
.d-maps-map-isoline-field-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap}
@container(max-width:340px){.d-maps-map-isoline-field-head{left:12px;right:12px;font-size:9px}.d-maps-map-isoline-field-stage{width:calc(100% - 20px)}.d-maps-map-isoline-field-foot{left:12px;right:12px;font-size:8px}}
@media(prefers-reduced-motion:reduce){.d-maps-map-isoline-field *,.d-maps-map-isoline-field *::before,.d-maps-map-isoline-field *::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}
`,
  js:`const isolineStage=root.querySelector('.d-maps-map-isoline-field-stage'),canvas=root.querySelector('.d-maps-map-isoline-field-canvas'),context=canvas.getContext('2d',{alpha:false}),readout=root.querySelector('.d-maps-map-isoline-field-readout'),status=root.querySelector('.d-maps-map-isoline-field-status');
const gridWidth=64,gridHeight=40,sampleCount=gridWidth*gridHeight,cellColumns=gridWidth-1,cellRows=gridHeight-1,cellCount=cellColumns*cellRows,logicalWidth=360,logicalHeight=216,seed=42,randomDraws=255,noiseOctaves=2,noiseWeights=[.68,.32],noiseDomains=[{xScale:3.2,xOffset:7.25,yScale:2.4,yOffset:11.5,zScale:1,zOffset:3.75},{xScale:6.4,xOffset:19.5,yScale:4.8,yOffset:5.75,zScale:2,zOffset:13.25}],zDrift=.0015,levels=[.24,.33,.42,.51,.60,.69,.78],majorIndices=[2,5],indexLevelIndex=3,indexLevel=levels[indexLevelIndex],maxDelta=50;
const heatAmplitude=.35,heatRadius=90,heatSigma=45,heatSigmaDenominator=2*heatSigma*heatSigma,heatPositionLerp=.07,heatStrengthInLerp=.07,heatStrengthOutLerp=.03,keyboardStep=18,gridSpacing=24,gridLineWidth=.5,gridAlpha=.34;
const labelCount=3,labelRefreshInterval=30,labelTargets=[[72,54],[180,162],[288,54]],labelMinimumX=24,labelMaximumX=336,labelMinimumY=16,labelMaximumY=200,labelMinimumLength=2,labelSeparation=56,labelGapWidth=26,labelGapHeight=12,labelText='510';
const ambiguityScale=64,ambiguityTieRule='Q>=-epsilon:T-R+B-L;Q<-epsilon:T-L+R-B',edgeDirections={T:'TL-to-TR',R:'TR-to-BR',B:'BL-to-BR',L:'TL-to-BL'},casePairs=[[],[['T','L']],[['T','R']],[['L','R']],[['R','B']],null,[['T','B']],[['L','B']],[['B','L']],[['T','B']],null,[['R','B']],[['L','R']],[['T','R']],[['T','L']],[]];
const colors={background:'#0a0a0b',panel:'#101012',bg2:'#161619',line0:'#232327',line1:'#2e2e34',txt0:'#ececef',txt1:'#9b9ba3',txt2:'#5c5c66',acc:'#fa7319'};
const expectedChecksums={permutation:'69d3c6d3',field:'03c514b3',segments:'15f625a2',cases:'b953fc3f',labels:'b3ca629a',fixture:'bc6dc640'},expectedRange={minimum:.1163531916314953,maximum:.860727652694485},expectedSamples=[.5506409313725491,.5230479688533255,.6788721429754901],expectedSegmentCounts=[69,98,118,135,169,156,40],expectedLabelSources=[43,95,12],driftFrames=[1,10,30,60,120],expectedDriftHashes=['7e1e7d15','96fc9ad9','c9d8875e','faf3a93b','7d6e18ed'],expectedDriftCounts=[[69,98,118,135,169,156,40],[70,100,118,134,173,158,40],[70,112,120,134,185,153,36],[69,116,120,144,179,172,26],[88,116,119,145,173,178,13]];
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,controller=new AbortController(),listener={signal:controller.signal},passiveListener={signal:controller.signal,passive:true};
function mulberry32(nextSeed){return function(){let value=nextSeed+=0x6D2B79F5;value=Math.imul(value^(value>>>15),value|1);value^=value+Math.imul(value^(value>>>7),value|61);return((value^(value>>>14))>>>0)/4294967296}}
function clamp(value,minimum,maximum){return Math.max(minimum,Math.min(maximum,value))}
function normalizeZero(value){return Object.is(value,-0)?0:value}
function smooth(value){return value*value*(3-2*value)}
function lerp(first,second,amount){return first+(second-first)*amount}
function checksumText(value){let hash=2166136261;for(const character of String(value))hash=Math.imul(hash^character.charCodeAt(0),16777619)>>>0;return hash.toString(16).padStart(8,'0')}
const randomSource=mulberry32(seed);let randomDrawCount=0;function nextRandom(){randomDrawCount++;return randomSource()}const permutationBase=Array.from({length:256},function(_,index){return index});
for(let index=255;index>0;index--){const swapIndex=Math.floor(nextRandom()*(index+1)),held=permutationBase[index];permutationBase[index]=permutationBase[swapIndex];permutationBase[swapIndex]=held}
const permutation=permutationBase.concat(permutationBase),permutationChecksum=checksumText(permutationBase.join(','));
function latticeValue(ix,iy,iz){return permutationBase[(permutationBase[(permutationBase[ix&255]+iy)&255]+iz)&255]/255}
function valueNoise3D(x,y,z){const x0=Math.floor(x),y0=Math.floor(y),z0=Math.floor(z),xf=x-x0,yf=y-y0,zf=z-z0,ux=smooth(xf),uy=smooth(yf),uz=smooth(zf),n000=latticeValue(x0,y0,z0),n100=latticeValue(x0+1,y0,z0),n010=latticeValue(x0,y0+1,z0),n110=latticeValue(x0+1,y0+1,z0),n001=latticeValue(x0,y0,z0+1),n101=latticeValue(x0+1,y0,z0+1),n011=latticeValue(x0,y0+1,z0+1),n111=latticeValue(x0+1,y0+1,z0+1),x00=lerp(n000,n100,ux),x10=lerp(n010,n110,ux),x01=lerp(n001,n101,ux),x11=lerp(n011,n111,ux),y0v=lerp(x00,x10,uy),y1v=lerp(x01,x11,uy);return lerp(y0v,y1v,uz)}
function baseFieldValue(column,row,z){const nx=column/(gridWidth-1),ny=row/(gridHeight-1),first=noiseDomains[0],second=noiseDomains[1],a=valueNoise3D(nx*first.xScale+first.xOffset,ny*first.yScale+first.yOffset,z*first.zScale+first.zOffset),b=valueNoise3D(nx*second.xScale+second.xOffset,ny*second.yScale+second.yOffset,z*second.zScale+second.zOffset);return a*noiseWeights[0]+b*noiseWeights[1]}
function heatKernel(distance){return distance<=heatRadius?heatAmplitude*Math.exp(-(distance*distance)/heatSigmaDenominator):0}
function buildField(z,heatX,heatY,heatStrength){const values=new Array(sampleCount),stepX=logicalWidth/(gridWidth-1),stepY=logicalHeight/(gridHeight-1);let minimum=Infinity,maximum=-Infinity;for(let row=0;row<gridHeight;row++){const y=row*stepY;for(let column=0;column<gridWidth;column++){const index=row*gridWidth+column,x=column*stepX,dx=x-heatX,dy=y-heatY,distance=Math.sqrt(dx*dx+dy*dy),base=baseFieldValue(column,row,z),bump=distance<=heatRadius?heatAmplitude*heatStrength*Math.exp(-(distance*distance)/heatSigmaDenominator):0,value=base+bump;values[index]=value;if(value<minimum)minimum=value;if(value>maximum)maximum=value}}return{values:values,minimum:minimum,maximum:maximum,z:z,heatX:heatX,heatY:heatY,heatStrength:heatStrength}}
function edgePoint(edge,column,row,level,tl,tr,br,bl){let s0,s1,x0,y0,x1,y1;if(edge==='T'){s0=tl-level;s1=tr-level;x0=column;y0=row;x1=column+1;y1=row}else if(edge==='R'){s0=tr-level;s1=br-level;x0=column+1;y0=row;x1=column+1;y1=row+1}else if(edge==='B'){s0=bl-level;s1=br-level;x0=column;y0=row+1;x1=column+1;y1=row+1}else{s0=tl-level;s1=bl-level;x0=column;y0=row;x1=column;y1=row+1}const denominator=s0-s1;let amount=Math.abs(denominator)<=1e-12?.5:s0/denominator;if(!Number.isFinite(amount))amount=.5;amount=clamp(amount,0,1);if(amount<=1e-12)amount=0;else if(amount>=1-1e-12)amount=1;const gx=normalizeZero(lerp(x0,x1,amount)),gy=normalizeZero(lerp(y0,y1,amount));return{x:normalizeZero(gx*logicalWidth/(gridWidth-1)),y:normalizeZero(gy*logicalHeight/(gridHeight-1)),t:amount,edge:edge}}
function ambiguityDecision(tl,tr,br,bl,level){const a=tl-level,b=tr-level,c=br-level,d=bl-level,q=a*c-b*d,epsilon=ambiguityScale*Number.EPSILON*Math.max(1,Math.abs(a*c),Math.abs(b*d)),positive=q>=-epsilon;return{q:q,epsilon:epsilon,branch:positive?'positive-or-tie':'negative',pairs:positive?[["T","R"],["B","L"]]:[["T","L"],["R","B"]]}}
function segmentKey(first,second){const a=first.x.toFixed(9)+','+first.y.toFixed(9),b=second.x.toFixed(9)+','+second.y.toFixed(9);return a<b?a+'|'+b:b+'|'+a}
function extractContours(field){const levelItems=[],allSegments=[],segmentCounts=[];let totalSegments=0,ambiguousCells=0,positiveBranches=0,negativeBranches=0,tieBranches=0,rejectedSegments=0,duplicateSegments=0;for(let levelIndex=0;levelIndex<levels.length;levelIndex++){const level=levels[levelIndex],segments=[],seen=new Set();for(let row=0;row<cellRows;row++)for(let column=0;column<cellColumns;column++){const cellIndex=row*cellColumns+column,tl=field.values[row*gridWidth+column],tr=field.values[row*gridWidth+column+1],br=field.values[(row+1)*gridWidth+column+1],bl=field.values[(row+1)*gridWidth+column],bits=(tl>=level?1:0)|(tr>=level?2:0)|(br>=level?4:0)|(bl>=level?8:0);let pairs=casePairs[bits],ambiguity=null;if(bits===5||bits===10){ambiguity=ambiguityDecision(tl,tr,br,bl,level);pairs=ambiguity.pairs;ambiguousCells++;if(ambiguity.branch==='negative')negativeBranches++;else{positiveBranches++;if(Math.abs(ambiguity.q)<=ambiguity.epsilon)tieBranches++}}for(let pairIndex=0;pairIndex<pairs.length;pairIndex++){const pair=pairs[pairIndex],first=edgePoint(pair[0],column,row,level,tl,tr,br,bl),second=edgePoint(pair[1],column,row,level,tl,tr,br,bl),dx=second.x-first.x,dy=second.y-first.y,lengthSquared=dx*dx+dy*dy,length=Math.sqrt(lengthSquared);if(!Number.isFinite(first.x)||!Number.isFinite(first.y)||!Number.isFinite(second.x)||!Number.isFinite(second.y)||!Number.isFinite(lengthSquared)||lengthSquared<=1e-18){rejectedSegments++;continue}const key=segmentKey(first,second);if(seen.has(key)){duplicateSegments++;continue}seen.add(key);const sourceIndex=segments.length,item={sourceIndex:sourceIndex,levelIndex:levelIndex,level:level,cellIndex:cellIndex,row:row,column:column,bits:bits,edges:pair[0]+'-'+pair[1],x0:first.x,y0:first.y,x1:second.x,y1:second.y,length:length,midX:(first.x+second.x)/2,midY:(first.y+second.y)/2,ambiguity:ambiguity?{q:ambiguity.q,epsilon:ambiguity.epsilon,branch:ambiguity.branch}:null};segments.push(item);allSegments.push(item)}}const style=levelIndex===indexLevelIndex?{role:'index',color:colors.acc,width:1.5}:majorIndices.indexOf(levelIndex)>=0?{role:'major',color:colors.txt2,width:1}:{role:'minor',color:colors.line1,width:1};segmentCounts.push(segments.length);totalSegments+=segments.length;levelItems.push({levelIndex:levelIndex,level:level,style:style,segmentCount:segments.length,segments:segments})}return{levels:levelItems,allSegments:allSegments,segmentCounts:segmentCounts,totalSegments:totalSegments,ambiguousCells:ambiguousCells,positiveBranches:positiveBranches,negativeBranches:negativeBranches,tieBranches:tieBranches,rejectedSegments:rejectedSegments,duplicateSegments:duplicateSegments}}
function chooseLabels(indexSegments,refreshFrame){const selected=[];for(let targetIndex=0;targetIndex<labelTargets.length;targetIndex++){const target=labelTargets[targetIndex],candidates=[];for(let index=0;index<indexSegments.length;index++){const segment=indexSegments[index];if(segment.midX<labelMinimumX||segment.midX>labelMaximumX||segment.midY<labelMinimumY||segment.midY>labelMaximumY||segment.length<labelMinimumLength)continue;let separated=true;for(let priorIndex=0;priorIndex<selected.length;priorIndex++){const dx=segment.midX-selected[priorIndex].x,dy=segment.midY-selected[priorIndex].y;if(Math.sqrt(dx*dx+dy*dy)<labelSeparation){separated=false;break}}if(!separated)continue;const dy=segment.y1-segment.y0,score=(segment.midX-target[0])*(segment.midX-target[0])+(segment.midY-target[1])*(segment.midY-target[1])+400*Math.pow(Math.abs(dy)/segment.length,2);candidates.push({segment:segment,score:score})}candidates.sort(function(first,second){return first.score-second.score||first.segment.sourceIndex-second.segment.sourceIndex});if(!candidates.length)continue;const chosen=candidates[0],segment=chosen.segment;selected.push({index:selected.length,sourceIndex:segment.sourceIndex,levelIndex:indexLevelIndex,text:labelText,x:segment.midX,y:segment.midY,length:segment.length,score:chosen.score,angle:Math.atan2(segment.y1-segment.y0,segment.x1-segment.x0),gap:{x:segment.midX-labelGapWidth/2,y:segment.midY-labelGapHeight/2,width:labelGapWidth,height:labelGapHeight},refreshFrame:refreshFrame})}return selected}
function serializeField(field){return field.values.map(function(value,index){return index+','+value.toFixed(12)}).join('|')}
function serializeSegments(contours){return contours.allSegments.map(function(item){return[item.levelIndex,item.cellIndex,item.bits,item.edges,item.x0.toFixed(9),item.y0.toFixed(9),item.x1.toFixed(9),item.y1.toFixed(9)].join(',')}).join('|')}
function serializeLabels(items){return items.map(function(item){return[item.sourceIndex,item.x.toFixed(9),item.y.toFixed(9),item.length.toFixed(9),item.score.toFixed(9)].join(',')}).join('|')}
function buildSyntheticFixtures(){const caseSources=[],caseItems=[];for(let bits=0;bits<16;bits++){const values=[bits&1?1:-1,bits&2?1:-1,bits&4?1:-1,bits&8?1:-1],decision=bits===5||bits===10?ambiguityDecision(values[0],values[1],values[2],values[3],0):null,pairs=decision?decision.pairs:casePairs[bits],source=bits+':'+pairs.map(function(pair){return pair.join('-')}).join('+');caseSources.push(source);caseItems.push({bits:bits,values:values,pairs:pairs.map(function(pair){return pair.slice()}),source:source,q:decision?decision.q:null,epsilon:decision?decision.epsilon:null,branch:decision?decision.branch:'fixed'})}const ambiguityInputs=[{name:'case5-positive',values:[2,-1,2,-1]},{name:'case5-negative',values:[1,-2,1,-2]},{name:'case5-tie',values:[1,-1,1,-1]},{name:'case10-positive',values:[-2,1,-2,1]},{name:'case10-negative',values:[-1,2,-1,2]},{name:'case10-tie',values:[-1,1,-1,1]}],ambiguities=ambiguityInputs.map(function(item){const decision=ambiguityDecision(item.values[0],item.values[1],item.values[2],item.values[3],0);return{name:item.name,values:item.values.slice(),q:decision.q,epsilon:decision.epsilon,branch:decision.branch,pairs:decision.pairs.map(function(pair){return pair.slice()})}}),complements=[];for(let index=0;index<3;index++){const first=ambiguities[index],second=ambiguities[index+3];complements.push({first:first.name,second:second.name,sameBranch:first.branch===second.branch,samePairs:JSON.stringify(first.pairs)===JSON.stringify(second.pairs)})}const leftValues=[.8,.6,-.4,-.7],rightValues=[.6,-.2,.5,-.4],leftPoint=edgePoint('R',0,0,0,leftValues[0],leftValues[1],leftValues[2],leftValues[3]),rightPoint=edgePoint('L',1,0,0,rightValues[0],rightValues[1],rightValues[2],rightValues[3]);return{caseItems:caseItems,caseSource:caseSources.join('|'),caseChecksum:checksumText(caseSources.join('|')),ambiguities:ambiguities,complements:complements,seam:{left:leftPoint,right:rightPoint,shared:Math.abs(leftPoint.x-rightPoint.x)<1e-12&&Math.abs(leftPoint.y-rightPoint.y)<1e-12}}}
const initialField=buildField(0,logicalWidth/2,logicalHeight/2,0),initialContours=extractContours(initialField),initialLabels=chooseLabels(initialContours.levels[indexLevelIndex].segments,0),syntheticFixtures=buildSyntheticFixtures(),fieldChecksum=checksumText(serializeField(initialField)),segmentChecksum=checksumText(serializeSegments(initialContours)),caseChecksum=syntheticFixtures.caseChecksum,labelChecksum=checksumText(serializeLabels(initialLabels)),fixtureChecksum=checksumText([permutationChecksum,fieldChecksum,segmentChecksum,caseChecksum,labelChecksum].join('|'));
const initialValues=initialField.values,initialSampleValues=[initialValues[0],initialValues[19*gridWidth+31],initialValues[39*gridWidth+63]],initialChecks={permutation:permutationChecksum===expectedChecksums.permutation,field:fieldChecksum===expectedChecksums.field,segments:segmentChecksum===expectedChecksums.segments,cases:caseChecksum===expectedChecksums.cases,labels:labelChecksum===expectedChecksums.labels,fixture:fixtureChecksum===expectedChecksums.fixture,minimum:Math.abs(initialField.minimum-expectedRange.minimum)<1e-15,maximum:Math.abs(initialField.maximum-expectedRange.maximum)<1e-15,samples:initialSampleValues.every(function(value,index){return Math.abs(value-expectedSamples[index])<1e-15}),segmentCounts:initialContours.segmentCounts.join(',')===expectedSegmentCounts.join(','),labelSources:initialLabels.map(function(item){return item.sourceIndex}).join(',')===expectedLabelSources.join(','),randomDraws:randomDrawCount===randomDraws&&randomDrawCount===255,synthetics:syntheticFixtures.complements.every(function(item){return item.sameBranch&&item.samePairs})&&syntheticFixtures.seam.shared};
if(Object.keys(initialChecks).some(function(key){return !initialChecks[key]}))throw new Error('map-isoline-field deterministic fixture mismatch: '+Object.keys(initialChecks).filter(function(key){return !initialChecks[key]}).join(','));
const driftFixtures=driftFrames.map(function(frame,index){const field=buildField(frame*zDrift,logicalWidth/2,logicalHeight/2,0),contours=extractContours(field),hash=checksumText(serializeField(field)),valid=hash===expectedDriftHashes[index]&&contours.segmentCounts.join(',')===expectedDriftCounts[index].join(',');if(!valid)throw new Error('map-isoline-field drift fixture mismatch at frame '+frame);return{frame:frame,z:frame*zDrift,fieldChecksum:hash,segmentCounts:contours.segmentCounts.slice()}});
let frameSerial=0,z=0,lastDelta=0,heatX=logicalWidth/2,heatY=logicalHeight/2,heatTargetX=logicalWidth/2,heatTargetY=logicalHeight/2,heatStrength=0,heatTargetStrength=0,pointerInside=false,keyboardActive=false,focused=false,inputMode='idle',source='initial';
let visible=!('IntersectionObserver'in window),documentVisible=document.visibilityState!=='hidden',running=false,cleaned=false,frameId=0,lastWall=0,dpr=1,cssWidth=0,cssHeight=0,currentField=initialField,currentContours=initialContours,currentLabels=initialLabels,currentFieldHash=fieldChecksum,currentSegmentHash=segmentChecksum,currentLabelHash=labelChecksum,lastLabelRefreshFrame=0,resizeObserver=null,intersectionObserver=null,connectionObserver=null;
let schedulerFrames=0,renderFrames=0,fieldBuilds=1,contourBuilds=1,labelRefreshes=1,pointerMoves=0,pointerEnters=0,pointerLeaves=0,pointerDowns=0,ignoredPointers=0,keyboardMoves=0,keyboardToggles=0,keyboardEscapes=0,keyboardHomes=0,keyboardResets=0,focusEvents=0,blurEvents=0,resizeEvents=0,visibilityChanges=0,intersectionChanges=0,schedulerStarts=0,schedulerStops=0,rafRequests=0,rafCancels=0,resets=0,cleanupCalls=0;
function copySegment(item){return{sourceIndex:item.sourceIndex,levelIndex:item.levelIndex,level:item.level,cellIndex:item.cellIndex,row:item.row,column:item.column,bits:item.bits,edges:item.edges,x0:item.x0,y0:item.y0,x1:item.x1,y1:item.y1,length:item.length,midX:item.midX,midY:item.midY,ambiguity:item.ambiguity?Object.assign({},item.ambiguity):null}}
function copyLabel(item){return{index:item.index,sourceIndex:item.sourceIndex,levelIndex:item.levelIndex,text:item.text,x:item.x,y:item.y,length:item.length,score:item.score,angle:item.angle,gap:Object.assign({},item.gap),refreshFrame:item.refreshFrame}}
function drawGrid(){context.strokeStyle=colors.line0;context.lineWidth=gridLineWidth;context.globalAlpha=gridAlpha;context.beginPath();for(let x=gridSpacing;x<logicalWidth;x+=gridSpacing){context.moveTo(x,0);context.lineTo(x,logicalHeight)}for(let y=gridSpacing;y<logicalHeight;y+=gridSpacing){context.moveTo(0,y);context.lineTo(logicalWidth,y)}context.stroke();context.globalAlpha=1}
function drawContours(){for(let levelIndex=0;levelIndex<currentContours.levels.length;levelIndex++){const levelItem=currentContours.levels[levelIndex];context.strokeStyle=levelItem.style.color;context.lineWidth=levelItem.style.width;context.globalAlpha=1;context.beginPath();for(let index=0;index<levelItem.segments.length;index++){const segment=levelItem.segments[index];context.moveTo(segment.x0,segment.y0);context.lineTo(segment.x1,segment.y1)}context.stroke()}}
function drawLabels(){context.textAlign='center';context.textBaseline='middle';context.font='8px JetBrains Mono, monospace';for(let index=0;index<currentLabels.length;index++){const label=currentLabels[index];context.fillStyle=colors.panel;context.fillRect(label.gap.x,label.gap.y,label.gap.width,label.gap.height);context.fillStyle=colors.txt2;context.fillText(label.text,label.x,label.y+.5)}}
function drawCanvas(){context.setTransform(dpr,0,0,dpr,0,0);context.globalAlpha=1;context.fillStyle=colors.panel;context.fillRect(0,0,logicalWidth,logicalHeight);drawGrid();drawContours();drawLabels();context.globalAlpha=1}
function currentFieldChecksum(){return currentFieldHash}
function currentSegmentChecksum(){return currentSegmentHash}
function currentLabelChecksum(){return currentLabelHash}
function expose(nextSource){source=nextSource||source;Object.assign(root.dataset,{gridWidth:String(gridWidth),gridHeight:String(gridHeight),sampleCount:String(sampleCount),cellCount:String(cellCount),seed:String(seed),randomDraws:String(randomDrawCount),noiseOctaves:String(noiseOctaves),zDrift:zDrift.toFixed(4),frameSerial:String(frameSerial),z:z.toFixed(9),lastDelta:lastDelta.toFixed(3),isoLevels:levels.join(','),indexLevelIndex:String(indexLevelIndex),indexLevel:indexLevel.toFixed(2),segmentCounts:currentContours.segmentCounts.join(','),totalSegments:String(currentContours.totalSegments),heatX:heatX.toFixed(6),heatY:heatY.toFixed(6),heatTargetX:heatTargetX.toFixed(6),heatTargetY:heatTargetY.toFixed(6),heatStrength:heatStrength.toFixed(9),heatTargetStrength:heatTargetStrength.toFixed(9),pointerInside:String(pointerInside),keyboardActive:String(keyboardActive),focused:String(focused),inputMode:inputMode,labelCount:String(currentLabels.length),labelRefreshes:String(labelRefreshes),lastLabelRefreshFrame:String(lastLabelRefreshFrame),schedulerFrames:String(schedulerFrames),renderFrames:String(renderFrames),fieldBuilds:String(fieldBuilds),contourBuilds:String(contourBuilds),running:String(running),visible:String(visible),documentVisible:String(documentVisible),reduced:String(reduced),cleaned:String(cleaned),frameId:String(frameId),dpr:dpr.toFixed(3),permutationChecksum:permutationChecksum,fieldChecksum:currentFieldChecksum(),segmentChecksum:currentSegmentChecksum(),labelChecksum:currentLabelChecksum(),fixtureChecksum:fixtureChecksum,resets:String(resets),source:source})}
function rebuildScene(refreshLabels,nextSource,deferExpose){currentField=buildField(z,heatX,heatY,heatStrength);fieldBuilds++;currentContours=extractContours(currentField);contourBuilds++;currentFieldHash=checksumText(serializeField(currentField));currentSegmentHash=checksumText(serializeSegments(currentContours));if(refreshLabels){currentLabels=chooseLabels(currentContours.levels[indexLevelIndex].segments,frameSerial);currentLabelHash=checksumText(serializeLabels(currentLabels));lastLabelRefreshFrame=frameSerial;labelRefreshes++}drawCanvas();renderFrames++;readout.textContent='ISO '+indexLevel.toFixed(2)+' / Z '+z.toFixed(4);if(!deferExpose)expose(nextSource||'render')}
function sizeCanvas(nextSource,countResize){const rect=canvas.getBoundingClientRect(),nextDpr=Math.min(2,window.devicePixelRatio||1),nextWidth=Math.round(logicalWidth*nextDpr),nextHeight=Math.round(logicalHeight*nextDpr);cssWidth=rect.width;cssHeight=rect.height;if(canvas.width!==nextWidth||canvas.height!==nextHeight){canvas.width=nextWidth;canvas.height=nextHeight}dpr=nextDpr;if(countResize)resizeEvents++;drawCanvas();renderFrames++;expose(nextSource||'resize')}
function canRun(){return!reduced&&!cleaned&&root.isConnected&&visible&&documentVisible}
function requestNextFrame(){if(reduced||cleaned||frameId||!running)return;rafRequests++;frameId=requestAnimationFrame(frame)}
function updateHeatFrame(){heatX+=(heatTargetX-heatX)*heatPositionLerp;heatY+=(heatTargetY-heatY)*heatPositionLerp;if(heatTargetStrength>heatStrength)heatStrength+=(heatTargetStrength-heatStrength)*heatStrengthInLerp;else heatStrength+=(heatTargetStrength-heatStrength)*heatStrengthOutLerp;if(Math.abs(heatStrength-heatTargetStrength)<1e-12)heatStrength=heatTargetStrength}
function frame(now){frameId=0;if(!root.isConnected){cleanup();return}if(!canRun()){running=false;lastWall=0;lastDelta=0;expose('scheduler pause');return}lastDelta=clamp(now-lastWall,0,maxDelta);lastWall=now;frameSerial++;z=frameSerial*zDrift;updateHeatFrame();schedulerFrames++;rebuildScene(frameSerial%labelRefreshInterval===0,'animation frame',true);requestNextFrame();expose('animation scheduled')}
function startScheduler(nextSource){if(!canRun()||running){expose(nextSource||'scheduler idle');return}running=true;lastWall=performance.now();schedulerStarts++;requestNextFrame();expose(nextSource||'scheduler start')}
function stopScheduler(nextSource){if(frameId){cancelAnimationFrame(frameId);rafCancels++;frameId=0}if(running)schedulerStops++;running=false;lastWall=0;expose(nextSource||'scheduler stop')}
function applyReducedState(nextSource){if(!reduced)return;heatX=heatTargetX;heatY=heatTargetY;heatStrength=heatTargetStrength;z=0;rebuildScene(true,nextSource||'reduced state')}
function updatePointer(event,nextSource){const rect=canvas.getBoundingClientRect(),localX=clamp((event.clientX-rect.left)/Math.max(rect.width,1)*logicalWidth,0,logicalWidth),localY=clamp((event.clientY-rect.top)/Math.max(rect.height,1)*logicalHeight,0,logicalHeight);heatTargetX=localX;heatTargetY=localY;heatTargetStrength=1;pointerInside=true;keyboardActive=false;inputMode='pointer';if(reduced)applyReducedState(nextSource);else expose(nextSource)}
function relaxHeat(nextSource){heatTargetStrength=keyboardActive?1:0;if(!keyboardActive)inputMode='idle';if(reduced)applyReducedState(nextSource);else expose(nextSource)}
function resetActivityCounters(){schedulerFrames=0;renderFrames=0;fieldBuilds=0;contourBuilds=0;labelRefreshes=0;pointerMoves=0;pointerEnters=0;pointerLeaves=0;pointerDowns=0;ignoredPointers=0;keyboardMoves=0;keyboardToggles=0;keyboardEscapes=0;keyboardHomes=0;keyboardResets=0;focusEvents=0;blurEvents=0;resizeEvents=0;visibilityChanges=0;intersectionChanges=0;schedulerStarts=0;schedulerStops=0;rafRequests=0;rafCancels=0}
function resetDemo(){if(cleaned)return;stopScheduler('reset stop');const nextReset=resets+1;frameSerial=0;z=0;lastDelta=0;heatX=logicalWidth/2;heatY=logicalHeight/2;heatTargetX=logicalWidth/2;heatTargetY=logicalHeight/2;heatStrength=0;heatTargetStrength=0;pointerInside=false;keyboardActive=false;focused=document.activeElement===isolineStage;inputMode='idle';currentLabels=[];lastLabelRefreshFrame=0;resetActivityCounters();resets=nextReset;rebuildScene(true,reduced?'reduced reset':'reset');status.textContent='Living contour field reset to seed 42';if(!reduced)startScheduler('reset scheduler')}
function deepFreeze(value){if(value&&typeof value==='object'&&!Object.isFrozen(value)){Object.freeze(value);Object.keys(value).forEach(function(key){deepFreeze(value[key])})}return value}
function inspect(){const rootRect=root.getBoundingClientRect(),stageRect=isolineStage.getBoundingClientRect(),canvasRect=canvas.getBoundingClientRect(),levelSnapshots=currentContours.levels.map(function(item){return{levelIndex:item.levelIndex,level:item.level,style:Object.assign({},item.style),segmentCount:item.segmentCount,segments:item.segments.map(copySegment)}});return deepFreeze({config:{gridWidth:gridWidth,gridHeight:gridHeight,sampleCount:sampleCount,cellColumns:cellColumns,cellRows:cellRows,cellCount:cellCount,logicalWidth:logicalWidth,logicalHeight:logicalHeight,layout:{rootHeight:320,stageMaximumWidth:360,cssAspect:'5/3'},backgroundGrid:{spacing:gridSpacing,lineWidth:gridLineWidth,alpha:gridAlpha,color:colors.line0},seed:seed,rng:{algorithm:'mulberry32',purpose:'Fisher-Yates permutation only',draws:randomDrawCount,expectedDraws:randomDraws,loop:'i=255 down to 1',choice:'floor(nextRandom()*(i+1))'},randomDraws:randomDrawCount,noiseOctaves:noiseOctaves,noiseWeights:noiseWeights.slice(),noiseDomains:noiseDomains.map(function(item){return Object.assign({},item)}),latticeFormula:'p[(p[(p[ix&255]+iy)&255]+iz)&255]/255',fadeFormula:'t*t*(3-2*t)',interpolationOrder:'x-then-y-then-z',normalization:'weighted-sum-no-clamp',zFormula:'frameSerial*0.0015',zDrift:zDrift,levels:levels.slice(),majorIndices:majorIndices.slice(),indexLevelIndex:indexLevelIndex,indexLevel:indexLevel,lineStyles:{minor:{color:colors.line1,width:1},major:{color:colors.txt2,width:1},index:{color:colors.acc,width:1.5}},marching:{bits:{TL:1,TR:2,BR:4,BL:8},edgeDirections:Object.assign({},edgeDirections),interpolation:'abs(s0-s1)<=1e-12?0.5:s0/(s0-s1)',casePairs:casePairs.map(function(pairs){return pairs?pairs.map(function(pair){return pair.slice()}):null}),ambiguityFormula:'Q=(TL-iso)*(BR-iso)-(TR-iso)*(BL-iso)',ambiguityEpsilon:'64*Number.EPSILON*max(1,abs(a*c),abs(b*d))',ambiguityTieRule:ambiguityTieRule,denominatorEpsilon:1e-12,snapEpsilon:1e-12,snapInclusive:true,duplicatePrecision:9,rejectionLengthSquared:1e-18},heat:{amplitude:heatAmplitude,radius:heatRadius,sigma:heatSigma,formula:'base+0.35*strength*(d<=90?exp(-(d*d)/(2*45*45)):0)',positionLerp:heatPositionLerp,strengthInLerp:heatStrengthInLerp,strengthOutLerp:heatStrengthOutLerp,leavePosition:'retain-last'},labels:{count:labelCount,text:labelText,refreshInterval:labelRefreshInterval,targets:labelTargets.map(function(item){return item.slice()}),bounds:{minimumX:labelMinimumX,maximumX:labelMaximumX,minimumY:labelMinimumY,maximumY:labelMaximumY},minimumSegmentLength:labelMinimumLength,separation:labelSeparation,gapWidth:labelGapWidth,gapHeight:labelGapHeight,score:'distanceSquared+400*(abs(dy)/length)^2',tieBreak:'score-ascending-sourceIndex-ascending'},keyboardStep:keyboardStep,maxDelta:maxDelta,frameDeltaUsage:'telemetry-only-z-is-frame-locked',colors:Object.assign({},colors),serializers:{permutation:'p.join comma',field:'index,value.toFixed(12) joined pipe',segments:'levelIndex,cellIndex,bits,edges,x0.9,y0.9,x1.9,y1.9 joined pipe',cases:'mask:edge-pairs joined pipe',labels:'sourceIndex,x9,y9,length9,score9 joined pipe'}},fixtures:{randomDrawCount:randomDrawCount,permutationBase:permutationBase.slice(),permutation:permutation.slice(),fieldZ0:initialField.values.slice(),fieldRange:{minimum:initialField.minimum,maximum:initialField.maximum},fieldSamples:[{index:0,column:0,row:0,value:initialSampleValues[0]},{index:19*gridWidth+31,column:31,row:19,value:initialSampleValues[1]},{index:sampleCount-1,column:63,row:39,value:initialSampleValues[2]}],segmentCounts:initialContours.segmentCounts.slice(),segmentSamples:initialContours.levels.map(function(item){return{levelIndex:item.levelIndex,first:item.segments.length?copySegment(item.segments[0]):null,middle:item.segments.length?copySegment(item.segments[Math.floor(item.segments.length/2)]):null,last:item.segments.length?copySegment(item.segments[item.segments.length-1]):null}}),labels:initialLabels.map(copyLabel),synthetic:JSON.parse(JSON.stringify(syntheticFixtures)),drift:driftFixtures.map(function(item){return{frame:item.frame,z:item.z,fieldChecksum:item.fieldChecksum,segmentCounts:item.segmentCounts.slice()}}),kernel:[0,22.5,45,67.5,90,90.000001].map(function(distance){return{distance:distance,value:heatKernel(distance)}})},state:{frameSerial:frameSerial,z:z,delta:lastDelta,source:source,fieldMinimum:currentField.minimum,fieldMaximum:currentField.maximum},field:{values:currentField.values.slice(),minimum:currentField.minimum,maximum:currentField.maximum},contours:{levels:levelSnapshots,segmentCounts:currentContours.segmentCounts.slice(),totalSegments:currentContours.totalSegments,ambiguousCells:currentContours.ambiguousCells,positiveBranches:currentContours.positiveBranches,negativeBranches:currentContours.negativeBranches,tieBranches:currentContours.tieBranches,rejectedSegments:currentContours.rejectedSegments,duplicateSegments:currentContours.duplicateSegments},labels:{items:currentLabels.map(copyLabel),count:currentLabels.length,refreshInterval:labelRefreshInterval,lastRefreshFrame:lastLabelRefreshFrame,allInGaps:currentLabels.every(function(item){return item.gap.width===labelGapWidth&&item.gap.height===labelGapHeight})},interaction:{pointerInside:pointerInside,keyboardActive:keyboardActive,focused:focused,inputMode:inputMode,heat:{x:heatX,y:heatY,targetX:heatTargetX,targetY:heatTargetY,strength:heatStrength,targetStrength:heatTargetStrength},statusText:status.textContent,readoutText:readout.textContent},counters:{schedulerFrames:schedulerFrames,renderFrames:renderFrames,fieldBuilds:fieldBuilds,contourBuilds:contourBuilds,labelRefreshes:labelRefreshes,pointerMoves:pointerMoves,pointerEnters:pointerEnters,pointerLeaves:pointerLeaves,pointerDowns:pointerDowns,ignoredPointers:ignoredPointers,keyboardMoves:keyboardMoves,keyboardToggles:keyboardToggles,keyboardEscapes:keyboardEscapes,keyboardHomes:keyboardHomes,keyboardResets:keyboardResets,focusEvents:focusEvents,blurEvents:blurEvents,resizeEvents:resizeEvents,visibilityChanges:visibilityChanges,intersectionChanges:intersectionChanges,schedulerStarts:schedulerStarts,schedulerStops:schedulerStops,rafRequests:rafRequests,rafCancels:rafCancels,resets:resets,cleanupCalls:cleanupCalls},lifecycle:{running:running,reduced:reduced,cleaned:cleaned,visible:visible,documentVisible:documentVisible,frameId:frameId,signalAborted:controller.signal.aborted,hasResizeObserver:!!resizeObserver,hasIntersectionObserver:!!intersectionObserver,hasConnectionObserver:!!connectionObserver},geometry:{rootWidth:rootRect.width,rootHeight:rootRect.height,rootScrollWidth:root.scrollWidth,rootScrollHeight:root.scrollHeight,stageWidth:stageRect.width,stageHeight:stageRect.height,canvasWidth:canvasRect.width,canvasHeight:canvasRect.height,backingWidth:canvas.width,backingHeight:canvas.height,logicalWidth:logicalWidth,logicalHeight:logicalHeight,dpr:dpr,cssWidth:cssWidth,cssHeight:cssHeight},checksums:{permutationChecksum:permutationChecksum,fieldChecksum:currentFieldChecksum(),segmentChecksum:currentSegmentChecksum(),labelChecksum:currentLabelChecksum(),fixtureChecksum:fixtureChecksum,initial:Object.assign({},expectedChecksums),checks:Object.assign({},initialChecks)},data:Object.assign({},root.dataset)})}
isolineStage.addEventListener('pointerenter',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer enter ignored');return}pointerEnters++;pointerInside=true;updatePointer(event,'pointer enter');status.textContent='Heat source active under pointer'},passiveListener);
isolineStage.addEventListener('pointermove',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer move ignored');return}pointerMoves++;updatePointer(event,'pointer move');status.textContent='Heat source at '+Math.round(heatTargetX)+', '+Math.round(heatTargetY)},passiveListener);
isolineStage.addEventListener('pointerdown',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer down ignored');return}pointerDowns++;updatePointer(event,'pointer down');status.textContent='Heat source placed at '+Math.round(heatTargetX)+', '+Math.round(heatTargetY)},listener);
isolineStage.addEventListener('pointerleave',function(event){if(event.isPrimary===false){ignoredPointers++;expose('pointer leave ignored');return}pointerLeaves++;pointerInside=false;keyboardActive=false;heatTargetStrength=0;inputMode='idle';relaxHeat('pointer leave');status.textContent='Heat source relaxing over two seconds'},passiveListener);
isolineStage.addEventListener('pointercancel',function(event){if(event.isPrimary===false){ignoredPointers++;return}pointerInside=false;keyboardActive=false;heatTargetStrength=0;inputMode='idle';relaxHeat('pointer cancel')},passiveListener);
isolineStage.addEventListener('focus',function(){focused=true;focusEvents++;status.textContent='Contour field focused; use arrow keys to move heat';expose('focus')},listener);
isolineStage.addEventListener('blur',function(){focused=false;blurEvents++;if(!pointerInside){keyboardActive=false;heatTargetStrength=0;inputMode='idle';relaxHeat('blur')}else expose('blur');status.textContent=pointerInside?'Heat source active under pointer':'Contour field idle'},listener);
isolineStage.addEventListener('keydown',function(event){const key=event.key;if(key==='r'||key==='R'){event.preventDefault();resetDemo();keyboardResets++;expose('keyboard reset');return}if(key==='Escape'){event.preventDefault();keyboardEscapes++;keyboardActive=false;pointerInside=false;heatTargetStrength=0;inputMode='idle';relaxHeat('keyboard escape');status.textContent='Heat source relaxing';return}if(key==='Home'){event.preventDefault();keyboardHomes++;keyboardActive=true;pointerInside=false;inputMode='keyboard';heatTargetX=logicalWidth/2;heatTargetY=logicalHeight/2;heatTargetStrength=1;if(reduced)applyReducedState('keyboard home');else expose('keyboard home');status.textContent='Keyboard heat centered';return}if(key==='Enter'||key===' '){event.preventDefault();keyboardToggles++;keyboardActive=!keyboardActive;pointerInside=false;inputMode=keyboardActive?'keyboard':'idle';heatTargetStrength=keyboardActive?1:0;if(reduced)applyReducedState('keyboard toggle');else expose('keyboard toggle');status.textContent=keyboardActive?'Keyboard heat active':'Keyboard heat relaxing';return}let dx=0,dy=0;if(key==='ArrowLeft')dx=-keyboardStep;else if(key==='ArrowRight')dx=keyboardStep;else if(key==='ArrowUp')dy=-keyboardStep;else if(key==='ArrowDown')dy=keyboardStep;else return;event.preventDefault();keyboardMoves++;keyboardActive=true;pointerInside=false;inputMode='keyboard';heatTargetX=clamp(heatTargetX+dx,0,logicalWidth);heatTargetY=clamp(heatTargetY+dy,0,logicalHeight);heatTargetStrength=1;if(reduced)applyReducedState('keyboard move');else expose('keyboard move');status.textContent='Keyboard heat at '+Math.round(heatTargetX)+', '+Math.round(heatTargetY)},listener);
function onVisibility(){documentVisible=document.visibilityState!=='hidden';visibilityChanges++;expose('document visibility state');if(documentVisible)startScheduler('document visible');else stopScheduler('document hidden')}document.addEventListener('visibilitychange',onVisibility,listener);
function cleanup(){if(cleaned)return;cleanupCalls++;cleaned=true;stopScheduler('cleanup');controller.abort();if(resizeObserver)resizeObserver.disconnect();if(intersectionObserver)intersectionObserver.disconnect();if(connectionObserver)connectionObserver.disconnect();expose('cleanup')}
if('ResizeObserver'in window){resizeObserver=new ResizeObserver(function(){if(cleaned)return;sizeCanvas('resize',true)});resizeObserver.observe(canvas)}
if('IntersectionObserver'in window){intersectionObserver=new IntersectionObserver(function(entries){if(!entries.length||cleaned)return;visible=entries[0].isIntersecting;intersectionChanges++;expose('intersection state');if(visible)startScheduler('intersection visible');else stopScheduler('intersection hidden')},{threshold:.05});intersectionObserver.observe(root)}
if('MutationObserver'in window){connectionObserver=new MutationObserver(function(){if(!root.isConnected)cleanup()});connectionObserver.observe(document.documentElement,{childList:true,subtree:true})}
root.__mapIsolineFieldInspect=Object.freeze(inspect);root.__mapIsolineFieldReset=Object.freeze(resetDemo);sizeCanvas('initial',false);expose(reduced?'reduced static':'initial');if(!reduced)startScheduler('initial scheduler')`,
  prompt:`Build a self-contained responsive 320px Maps and Geo canvas card using only bg0 #0a0a0b, bg1 #101012, bg2 #161619, line0 #232327, line1 #2e2e34, txt0 #ececef, txt1 #9b9ba3, txt2 #5c5c66 and acc #fa7319. Use one 360 by 216 logical canvas with a 64 by 40 row-major scalar sample grid, therefore 63 by 39 marching cells. Preserve the canvas at an exact 5:3 CSS aspect ratio, center it, and cap its CSS width at 360px. Beneath contours draw a deterministic 24px line0 reference grid at 0.5px and 34% alpha. Keep the stage as the sole focusable element and give the nonfocusable canvas role img with a concise label. Add a compact header readout, a footer showing 64 X 40, seed 42, two octaves and seven levels, plus a visually hidden polite status. Every HTML and CSS class must use one unique demo prefix.

Create a standard mulberry32 stream seeded at 42 and use it only to Fisher-Yates shuffle integers zero through 255. Iterate i from 255 down through 1, choose j as floor(nextRandom times (i plus 1)), consume exactly 255 values, and duplicate the permutation only for inspection. Sample lattice value with p[(p[(p[ix masked 255] plus iy) masked 255] plus iz) masked 255] divided by 255. Smooth fractions with t squared times (3 minus 2t), interpolate x then y then z, and use exactly two octaves. For sample column x and row y define nx=x/63 and ny=y/39. Octave zero samples nx times 3.2 plus 7.25, ny times 2.4 plus 11.5, z plus 3.75. Octave one samples nx times 6.4 plus 19.5, ny times 4.8 plus 5.75, z times 2 plus 13.25. Weight them 0.68 and 0.32 without normalization or clamping. Logical sample coordinates span exactly x zero through 360 and y zero through 216.

Set z exactly to active frame serial times 0.0015, so reset is frame zero at z zero and the first active frame is z 0.0015. Extract levels 0.24, 0.33, 0.42, 0.51, 0.60, 0.69 and 0.78 every rendered frame. Render level indices 2 and 5 as one-pixel txt2 majors, level index 3 as the sole 1.5-pixel acc index contour, and all others as one-pixel line1 minors. Marching bits are TL 1, TR 2, BR 4 and BL 8. Directed edges are top TL to TR, right TR to BR, bottom BL to BR and left TL to BL. For each crossing set s0 and s1 to endpoint value minus iso; when abs(s0-s1) is at most 1e-12 use t=0.5, otherwise use t=s0/(s0-s1). Clamp zero through one, snap t less than or equal to 1e-12 to zero and t greater than or equal to 1 minus 1e-12 to one, and normalize negative zero. Canonicalize duplicate keys with endpoint coordinates fixed to nine decimals. Reject nonfinite segments and any segment whose squared length is at most 1e-18. For cases 5 and 10 compute Q=(TL-iso)(BR-iso)-(TR-iso)(BL-iso) and epsilon=64 times Number.EPSILON times max(1,abs(first product),abs(second product)). When Q is at least negative epsilon choose T-R plus B-L; otherwise choose T-L plus R-B. Use this same explicit tie branch for both cases and include synthetic all-case, positive, negative, tie, complement and shared-seam fixtures.

The pointer adds an unclamped finite-support heat source: base plus 0.35 times strength times exp(-(distance squared)/(2 times 45 squared)) when distance is at most 90 logical pixels, otherwise zero. Retain its last center on leave. Ease x and y toward their targets by 0.07 per active frame, ease strength toward one by 0.07 while active, and toward zero by 0.03 after leave so relaxation reads as about two seconds. Pointer movement, keyboard arrows in 18-pixel steps, Home center, Enter or Space toggle, Escape relax and R reset must update accessible state; under reduced motion they redraw synchronously.

Cache exactly three labels for the 0.51 contour and recompute at init, reset, every active frame divisible by 30, and synchronous reduced-motion heat changes. Consider canonical row-major index segments whose midpoint x is 24 through 336, y is 16 through 200 and length is at least 2. In sequence target 72,54 then 180,162 then 288,54; exclude a candidate within 56 pixels of an earlier pick. Score squared target distance plus 400 times (abs(dy)/length) squared, sorting by score then source index. Center fixed text 510 in 8px txt2 over a 26 by 12 bg1 rectangle so the index line has a real visible gap.

Use one requestAnimationFrame scheduler, no timers, no WAAPI, no dependencies, no network, no assets and no runtime randomness. Track wall delta as clamp(now minus lastWall, zero, 50ms) for telemetry while keeping z strictly frame-serial based. Cap device pixel ratio at two, observe resize, intersection and document visibility, pause without advancing frame serial or catching up, and clean up through AbortController plus a MutationObserver. Reduced motion requests zero animation frames, keeps frame zero and z zero, draws the deterministic static field, and applies interaction state changes immediately. Freeze inspect and reset hooks, make reset inert after cleanup, and deep-freeze every inspection snapshot. Expose complete configuration, the permutation and z-zero field fixtures, synthetic ambiguity fixtures, drift samples, heat kernel values, live field and contour snapshots, exact label gaps, counters, lifecycle, responsive geometry, root data and FNV-1a checksums. FNV-1a uses initial 2166136261, Math.imul by 16777619 and eight lowercase hex digits; lock permutation 69d3c6d3, field 03c514b3, segments 15f625a2, cases b953fc3f, labels b3ca629a and combined bc6dc640.`
});
