/* INTRX registry - wandering fireflies that gather at an idle pointer */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'fireflies-night',title:'Fireflies Night',cat:'Ambient',rootClass:'d-firefly-demo',tags:['canvas','particles','idle cursor'],libs:[],desc:'Seeded fireflies wander with individual phase noise and pulsing light. A stationary pointer becomes an attractor only after an idle delay, so exploration stays loose before the swarm gathers.',seen:'Seen on: nature brands, wellness stories, festival pages, bedtime products, and quiet portfolio scenes',hint:'inspect the swarm, park the pointer until gather mode begins, summon the center, pause, and reset',html:`<div class="d-firefly-demo"><header><span>AMBIENT / DELAYED ATTRACTOR</span><span class="d-firefly-readout">WANDER / 34 LIGHTS</span></header><div class="d-firefly-stage"><canvas aria-label="Wandering fireflies at night"></canvas><div class="d-firefly-moon"></div><p>650MS IDLE → SOFT GATHER / MOVE → RELEASE</p></div><footer><button class="d-firefly-inspect" type="button">Inspect swarm</button><button class="d-firefly-summon" type="button">Summon center</button><button class="d-firefly-toggle" type="button">Pause night</button><button class="d-firefly-reset" type="button">Reset swarm</button><span class="d-firefly-status" aria-live="polite">Fireflies wandering</span></footer></div>`,css:`.d-firefly-demo{width:100%;min-height:555px;box-sizing:border-box;padding:20px clamp(5px,4vw,28px);background:#081218;color:#e8f4d4;font-family:Inter,system-ui,sans-serif}.d-firefly-demo>header{display:flex;justify-content:space-between;color:#526a70;font:700 8px/1 ui-monospace,monospace;letter-spacing:.14em}.d-firefly-readout{color:#d9f58c}.d-firefly-stage{position:relative;height:430px;margin-top:13px;overflow:hidden;border:1px solid #29434b;background:radial-gradient(circle at 75% 22%,#17333a,#071219 55%,#050b10)}.d-firefly-stage canvas{display:block;width:100%;height:100%}.d-firefly-moon{position:absolute;right:12%;top:13%;width:62px;height:62px;border-radius:50%;background:#dff7d71f;box-shadow:0 0 55px #dff7d721;pointer-events:none}.d-firefly-stage p{position:absolute;left:11px;bottom:9px;margin:0;color:#527068;font:700 7px/1 ui-monospace,monospace}.d-firefly-demo footer{display:flex;justify-content:center;gap:5px;margin-top:10px}.d-firefly-demo button{min-height:32px;padding:7px 9px;border:1px solid #294a4f;background:#102128;color:#789397;font:700 7px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-firefly-demo button:hover,.d-firefly-demo button:focus-visible{background:#d9f58c;color:#0b1513;outline:none}.d-firefly-status{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}`,js:`const canvas=root.querySelector('canvas'),ctx=canvas.getContext('2d'),readout=root.querySelector('.d-firefly-readout'),status=root.querySelector('.d-firefly-status'),inspect=root.querySelector('.d-firefly-inspect'),summon=root.querySelector('.d-firefly-summon'),toggle=root.querySelector('.d-firefly-toggle'),reset=root.querySelector('.d-firefly-reset'),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;let flies=[],pointer=null,idleSince=0,gather=false,w=0,h=0,frames=0,running=false,toggles=0,raf=0;function random(i,s){const v=Math.sin((i+3)*487.31+s*91.2)*31873.7;return v-Math.floor(v)}function seed(){flies=Array.from({length:34},(_,i)=>({x:random(i,1)*w,y:random(i,2)*h,vx:0,vy:0,phase:random(i,3)*6.283,size:1.2+random(i,4)*1.8}))}function checksum(){return flies.reduce((sum,f,i)=>sum+f.x*(i+1)+f.y*(35-i),0).toFixed(2)}function paint(source){ctx.clearRect(0,0,w,h);for(const f of flies){const glow=.32+.68*(.5+.5*Math.sin(f.phase));ctx.fillStyle='rgba(221,255,134,'+(glow*.2).toFixed(2)+')';ctx.beginPath();ctx.arc(f.x,f.y,f.size*5,0,Math.PI*2);ctx.fill();ctx.fillStyle='rgba(238,255,174,'+glow.toFixed(2)+')';ctx.beginPath();ctx.arc(f.x,f.y,f.size,0,Math.PI*2);ctx.fill()}if(pointer&&gather){ctx.strokeStyle='rgba(217,245,140,.24)';ctx.beginPath();ctx.arc(pointer.x,pointer.y,24,0,Math.PI*2);ctx.stroke()}root.dataset.running=String(running);root.dataset.frames=String(frames);root.dataset.mode=gather?'gather':'wander';root.dataset.pointer=pointer?pointer.x.toFixed(1)+','+pointer.y.toFixed(1):'none';root.dataset.checksum=checksum();root.dataset.toggles=String(toggles);root.dataset.source=source;readout.textContent=(gather?'GATHER':'WANDER')+' / 34 LIGHTS'}function step(now){gather=Boolean(pointer&&now-idleSince>=650);for(let i=0;i<flies.length;i++){const f=flies[i];f.phase+=.025+random(i,5)*.012;f.vx+=Math.sin(f.phase*1.7+i)*.012;f.vy+=Math.cos(f.phase*1.31-i)*.012;if(gather){const dx=pointer.x-f.x,dy=pointer.y-f.y,dist=Math.hypot(dx,dy)+1,force=Math.min(.035,dist*.00009);f.vx+=dx/dist*force;f.vy+=dy/dist*force}f.vx*=.985;f.vy*=.985;const cap=gather?1.25:.72,speed=Math.hypot(f.vx,f.vy);if(speed>cap){f.vx=f.vx/speed*cap;f.vy=f.vy/speed*cap}f.x=(f.x+f.vx+w)%w;f.y=(f.y+f.vy+h)%h}}function loop(now){if(!running)return;step(now);frames++;paint('frame');raf=requestAnimationFrame(loop)}function setRunning(next,source){cancelAnimationFrame(raf);running=next&&!reduced;toggles++;paint(source);toggle.textContent=running?'Pause night':'Run night';if(running)raf=requestAnimationFrame(loop)}canvas.addEventListener('pointermove',event=>{const r=canvas.getBoundingClientRect();pointer={x:event.clientX-r.left,y:event.clientY-r.top};idleSince=performance.now();gather=false;paint('pointer')});canvas.addEventListener('pointerleave',()=>{pointer=null;gather=false;paint('leave')});summon.addEventListener('click',()=>{pointer={x:w/2,y:h/2};idleSince=performance.now()-700;gather=true;paint('summon');status.textContent='Fireflies summoned to center'});inspect.addEventListener('click',()=>{cancelAnimationFrame(raf);running=false;frames=0;pointer=null;gather=false;seed();paint('inspect');toggle.textContent='Run night';status.textContent='Seeded swarm frozen'});toggle.addEventListener('click',()=>{setRunning(!running,'toggle');status.textContent=running?'Fireflies wandering':'Night paused'});reset.addEventListener('click',()=>{cancelAnimationFrame(raf);running=false;frames=0;pointer=null;gather=false;toggles++;seed();paint('reset');toggle.textContent='Run night';status.textContent='Fireflies reset to seed 487'});new ResizeObserver(()=>{const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);w=r.width;h=r.height;canvas.width=Math.round(w*d);canvas.height=Math.round(h*d);ctx.setTransform(d,0,0,d,0,0);seed();paint('resize')}).observe(canvas);paint('initial');if(!reduced)setRunning(true,'auto');`,prompt:`Create 34 deterministic canvas fireflies with phase-based wandering velocity, damping, wrapping edges, and pulsing core/glow radii. Pointer movement records a target and resets an idle timer; only after 650ms stationary should the flies gently accelerate toward it. Leaving releases them. Include center summon, inspect, run/pause, exact reset, reduced motion, and running/frames/mode/pointer/checksum/toggles/source telemetry.`});
