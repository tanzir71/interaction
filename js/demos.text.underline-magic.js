/* INTRX registry — published shared hopping underline */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'text-underline-magic',title:'Text Underline Magic',cat:'Text & Type',rootClass:'d-magicline',tags:['underline','navigation','shared indicator'],libs:[],desc:'One measured underline draws beneath a navigation label, then hops between targets on a small arc. Mid-flight retargeting begins from its current geometry, so fast pointer movement remains continuous.',seen:'Seen on: portfolio navigation, editorial tabs, product headers, and compact project filters',hint:'move across the links or use arrow keys — one underline follows every target',
html:`<div class="d-magicline" role="group" aria-label="Navigation with shared animated underline"><div class="d-magicline-head"><span>SHARED INDICATOR / NAV 04</span><span>TARGET <b>WORK</b> · X <i>000</i></span></div><div class="d-magicline-field"><p class="d-magicline-brand">NORTH/02</p><nav aria-label="Demo sections"><button type="button" data-index="0" aria-pressed="true">Work</button><button type="button" data-index="1" aria-pressed="false">Studio</button><button type="button" data-index="2" aria-pressed="false">Notes</button><button type="button" data-index="3" aria-pressed="false">Contact</button><span class="d-magicline-indicator" aria-hidden="true"></span></nav><div class="d-magicline-copy" aria-hidden="true"><span>ONE ELEMENT</span><strong>Draw once.<br>Move everywhere.</strong></div><div class="d-magicline-ruler" aria-hidden="true"><span>0</span><i></i><span>MEASURED LINK RECTANGLES</span><i></i><span>W</span></div></div><div class="d-magicline-foot"><p aria-live="polite">Work selected</p><button type="button" class="d-magicline-reset">Return to Work</button></div></div>`,
css:`.d-magicline{width:100%;min-height:500px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#f0f0ec;background:#0a0a0b;font-family:Roboto Mono,JetBrains Mono,monospace}.d-magicline-head{display:flex;justify-content:space-between;color:#70737a;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.12em}.d-magicline-head b{color:#ffce62}.d-magicline-head i{color:#d8dce1;font-style:normal}.d-magicline-field{position:relative;height:380px;margin-top:13px;overflow:hidden;border:1px solid #292b31;background:linear-gradient(145deg,#17181c,#0e0f13 65%)}.d-magicline-brand{position:absolute;left:24px;top:22px;margin:0;color:#f0f0ec;font:800 11px/1 ui-monospace,monospace;letter-spacing:.08em}.d-magicline nav{position:absolute;z-index:4;right:24px;top:13px;display:flex;align-items:center;gap:clamp(12px,3vw,32px);padding:9px 0 12px}.d-magicline nav button{padding:5px 0;border:0;color:#73777f;background:transparent;font:650 10px/1 Roboto Mono,system-ui,sans-serif;cursor:pointer}.d-magicline nav button:hover,.d-magicline nav button:focus-visible,.d-magicline nav button.is-active{color:#f4f2ea;outline:none}.d-magicline nav button:focus-visible{box-shadow:0 0 0 1px #ffce62}.d-magicline-indicator{position:absolute;left:0;bottom:4px;width:0;height:2px;background:#ffce62;box-shadow:0 0 8px rgba(255,206,98,.35);transform-origin:center;will-change:transform,width}.d-magicline-copy{position:absolute;left:clamp(25px,6vw,70px);bottom:70px}.d-magicline-copy span{display:block;margin-bottom:15px;color:#9a7c42;font:700 7px/1 ui-monospace,monospace;letter-spacing:.14em}.d-magicline-copy strong{font:500 clamp(35px,6vw,65px)/.92 Georgia,serif;letter-spacing:-.045em}.d-magicline-ruler{position:absolute;left:24px;right:24px;bottom:24px;display:flex;align-items:center;gap:8px;color:#555960;font:650 6px/1 ui-monospace,monospace}.d-magicline-ruler i{height:1px;flex:1;background:#2e3036}.d-magicline-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-magicline-foot p{margin:0;color:#797d84;font:650 9px/1.3 ui-monospace,monospace}.d-magicline-foot button{min-height:31px;padding:7px 11px;border:1px solid #393c43;border-radius:3px;color:#a8abb1;background:#15171b;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-magicline-foot button:hover,.d-magicline-foot button:focus-visible{color:#fff;border-color:#757a83;outline:none}@media(max-width:600px){.d-magicline{padding-inline:6px}.d-magicline-brand{display:none}.d-magicline nav{left:16px;right:16px;justify-content:space-between;gap:8px}.d-magicline-copy{left:20px}.d-magicline-copy strong{font-size:39px}}@media(prefers-reduced-motion:reduce){.d-magicline-indicator{will-change:auto}}`,
js:`const nav=root.querySelector('nav');const links=Array.from(nav.querySelectorAll('button'));const indicator=root.querySelector('.d-magicline-indicator');const targetReadout=root.querySelector('.d-magicline-head b');const xReadout=root.querySelector('.d-magicline-head i');const status=root.querySelector('.d-magicline-foot p');const reset=root.querySelector('.d-magicline-reset');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const DURATION=480;let hovered=-1,focused=-1,latched=0,current={x:0,width:0,y:0},from={x:0,width:0},to={x:0,width:0},started=0,running=false;
function selected(){return hovered>=0?hovered:focused>=0?focused:latched}function geometry(index){const navRect=nav.getBoundingClientRect(),rect=links[index].getBoundingClientRect();return{x:rect.left-navRect.left,width:rect.width}}function render(){indicator.style.width=current.width.toFixed(2)+'px';indicator.style.transform='translate3d('+current.x.toFixed(2)+'px,'+current.y.toFixed(2)+'px,0) scaleY('+(1+Math.abs(current.y)*.035).toFixed(3)+')';xReadout.textContent=String(Math.round(current.x)).padStart(3,'0')}function moveTo(index,instant){to=geometry(index);from={x:current.x,width:current.width};targetReadout.textContent=links[index].textContent.toUpperCase();links.forEach(function(link,i){link.classList.toggle('is-active',i===index);link.setAttribute('aria-pressed',latched===i?'true':'false')});if(reduce||instant){current={x:to.x,width:to.width,y:0};from={x:to.x,width:to.width};started=performance.now()-DURATION;render();return}started=performance.now();if(!running){running=true;requestAnimationFrame(frame)}}function frame(now){const progress=Math.min(1,(now-started)/DURATION),eased=1-Math.pow(1-progress,3);current.x=from.x+(to.x-from.x)*eased;current.width=from.width+(to.width-from.width)*eased;current.y=-Math.sin(progress*Math.PI)*8;render();if(progress<1)requestAnimationFrame(frame);else{current={x:to.x,width:to.width,y:0};render();running=false}}function update(){moveTo(selected(),false)}links.forEach(function(link,index){link.addEventListener('pointerenter',function(){hovered=index;update()});link.addEventListener('pointerleave',function(){hovered=-1;update()});link.addEventListener('focus',function(){focused=index;update()});link.addEventListener('blur',function(){focused=-1;update()});link.addEventListener('click',function(){latched=index;status.textContent=link.textContent+' selected';update()});link.addEventListener('keydown',function(event){if(event.key==='ArrowRight'||event.key==='ArrowLeft'){event.preventDefault();links[(index+(event.key==='ArrowRight'?1:-1)+links.length)%links.length].focus()}else if(event.key==='Escape'){hovered=-1;focused=-1;latched=0;moveTo(0,false);link.blur()}})});reset.addEventListener('click',function(){hovered=-1;focused=-1;latched=0;status.textContent='Work selected';moveTo(0,false);reset.focus()});if('ResizeObserver'in window)new ResizeObserver(function(){moveTo(selected(),true)}).observe(nav);requestAnimationFrame(function(){moveTo(0,true)});`,
prompt:`Build a self-contained four-link navigation with exactly one shared underline indicator. Use semantic buttons for Work, Studio, Notes, and Contact. Measure the selected link with getBoundingClientRect relative to the nav rectangle; never hard-code x positions or widths.

For each target change, capture the indicator’s current rendered x/width as the new from state, then animate to the measured target over 480ms. Use easeOutCubic for x and width, plus y=-sin(progress*pi)*8px for a hop arc. Scale its height slightly from absolute y. This allows rapid mid-flight retargeting without snapping or multiple indicators.

Active intent prioritizes hovered, then focused, then latched link. Click updates the persistent selection, Arrow Left/Right moves focus cyclically, Escape and Return to Work select the first link. aria-pressed represents only the persistent selection. ResizeObserver must snap geometry to the current target after layout changes. Under prefers-reduced-motion, move instantly. Include visible focus, live selection status, responsive nav spacing, scoped CSS, and no dependencies.`});
