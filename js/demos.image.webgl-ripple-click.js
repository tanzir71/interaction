/* INTRX registry — published WebGL multi-origin click ripples */
window.INTRX=window.INTRX||{demos:[],register(d){this.demos.push(d)}};
INTRX.register({id:'webgl-ripple-click',title:'WebGL Ripple Click',cat:'Image & WebGL',rootClass:'d-ripplegl',tags:['WebGL','fragment shader','uniform arrays'],libs:[],desc:'Up to five click origins travel into a fragment shader as uniform arrays. Expanding signed wavefronts displace a procedural mineral texture, overlap additively, fade by age, and release the render loop when finished.',seen:'Seen on: image reveals, immersive landing pages, album artwork, and interactive campaign canvases',hint:'click several points or press Enter — up to five shader ripples overlap',
html:`<div class="d-ripplegl" role="group" aria-label="Interactive WebGL ripple texture"><div class="d-ripplegl-head"><span>FRAGMENT FIELD / 05 ORIGINS</span><span>ACTIVE <b>0</b> · SPAWNED <i>00</i></span></div><div class="d-ripplegl-field"><canvas tabindex="0" role="button" aria-label="Procedural WebGL texture. Click or press Enter to spawn a distortion ripple."></canvas><div class="d-ripplegl-cross" aria-hidden="true"><i></i><i></i></div><div class="d-ripplegl-coords" aria-hidden="true">UV <b>0.500 / 0.500</b></div><p class="d-ripplegl-fallback">WebGL unavailable — showing static field.</p><span class="d-ripplegl-label" aria-hidden="true">SIGNED WAVE DISPLACEMENT / AGE DECAY 3.2S</span></div><div class="d-ripplegl-foot"><p aria-live="polite">Ripple field ready</p><button type="button">Clear ripples</button></div></div>`,
css:`.d-ripplegl{width:100%;min-height:510px;box-sizing:border-box;padding:20px clamp(8px,4vw,34px) 16px;color:#edf2f1;background:#080b0c;font-family:Roboto Mono,JetBrains Mono,monospace}.d-ripplegl-head{display:flex;justify-content:space-between;color:#687276;font:700 9px/1.2 ui-monospace,monospace;letter-spacing:.12em}.d-ripplegl-head b{color:#78ead0}.d-ripplegl-head i{color:#d3dcda;font-style:normal}.d-ripplegl-field{position:relative;height:390px;margin-top:13px;overflow:hidden;border:1px solid #222c2e;background:radial-gradient(circle at 45% 45%,#254e4b,#111b1d 58%,#091013)}.d-ripplegl canvas{display:block;width:100%;height:100%;outline:none;cursor:crosshair}.d-ripplegl canvas:focus-visible{box-shadow:inset 0 0 0 2px #9bf4df}.d-ripplegl-cross{position:absolute;left:50%;top:50%;pointer-events:none;opacity:.35}.d-ripplegl-cross i{position:absolute;background:#b7e6db}.d-ripplegl-cross i:first-child{left:-16px;top:0;width:32px;height:1px}.d-ripplegl-cross i:last-child{left:0;top:-16px;width:1px;height:32px}.d-ripplegl-coords{position:absolute;right:13px;top:12px;color:rgba(225,242,238,.55);font:650 7px/1 ui-monospace,monospace;letter-spacing:.09em;pointer-events:none}.d-ripplegl-coords b{color:#d5ece7}.d-ripplegl-label{position:absolute;left:13px;bottom:11px;color:rgba(216,235,231,.48);font:650 6px/1 ui-monospace,monospace;letter-spacing:.1em;pointer-events:none}.d-ripplegl-fallback{display:none;position:absolute;inset:0;place-items:center;margin:0;color:#a7c2bc;font:650 9px/1 ui-monospace,monospace}.d-ripplegl.no-webgl .d-ripplegl-fallback{display:grid}.d-ripplegl.no-webgl canvas{opacity:0}.d-ripplegl-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:12px}.d-ripplegl-foot p{margin:0;color:#727e80;font:650 9px/1.3 ui-monospace,monospace}.d-ripplegl-foot button{min-height:31px;padding:7px 11px;border:1px solid #313d3f;border-radius:3px;color:#a2adaf;background:#111719;font:700 8px/1 ui-monospace,monospace;text-transform:uppercase;cursor:pointer}.d-ripplegl-foot button:hover,.d-ripplegl-foot button:focus-visible{color:#fff;border-color:#68797b;outline:none}@media(max-width:550px){.d-ripplegl{padding-inline:6px}}`,
js:`const canvas=root.querySelector('canvas');const activeReadout=root.querySelector('.d-ripplegl-head b');const spawnedReadout=root.querySelector('.d-ripplegl-head i');const coordinates=root.querySelector('.d-ripplegl-coords b');const status=root.querySelector('.d-ripplegl-foot p');const clearButton=root.querySelector('.d-ripplegl-foot button');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const gl=canvas.getContext('webgl',{alpha:false,antialias:false});if(!gl){root.classList.add('no-webgl');status.textContent='WebGL unavailable';return}const MAX_RIPPLES=5,LIFETIME=3.2;const vertexSource='attribute vec2 a_position;void main(){gl_Position=vec4(a_position,0.0,1.0);}';const fragmentSource='precision highp float;uniform vec2 u_resolution;uniform float u_time;uniform vec2 u_origins[5];uniform float u_ages[5];uniform int u_count;float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453);}void main(){vec2 uv=gl_FragCoord.xy/u_resolution;float aspect=u_resolution.x/u_resolution.y;vec2 displacement=vec2(0.0);float glow=0.0;for(int i=0;i<5;i++){if(i>=u_count)continue;float age=u_ages[i];vec2 delta=uv-u_origins[i];delta.x*=aspect;float dist=max(length(delta),0.0001);float radius=age*0.28;float envelope=exp(-abs(dist-radius)*34.0)*exp(-age*0.58);float wave=sin((dist-radius)*78.0)*envelope;vec2 direction=delta/dist;direction.x/=aspect;displacement+=direction*wave*0.018;glow+=envelope*0.34;}vec2 p=uv+displacement;float bands=sin(p.x*17.0+p.y*9.0+sin(p.y*14.0)*1.6)*0.5+0.5;float veins=sin((p.x-p.y)*36.0+sin(p.x*12.0)*2.0)*0.5+0.5;float grain=hash(floor(p*u_resolution*0.32));vec3 deep=vec3(0.025,0.105,0.12);vec3 jade=vec3(0.12,0.55,0.46);vec3 sand=vec3(0.89,0.72,0.42);vec3 color=mix(deep,jade,bands);color=mix(color,sand,pow(veins,8.0)*0.48);color+=(grain-0.5)*0.055;color+=vec3(0.32,0.9,0.76)*glow;float vignette=smoothstep(0.82,0.2,length(uv-0.5));gl_FragColor=vec4(color*(0.68+0.32*vignette),1.0);}';function shader(type,source){const item=gl.createShader(type);gl.shaderSource(item,source);gl.compileShader(item);if(!gl.getShaderParameter(item,gl.COMPILE_STATUS))throw new Error(gl.getShaderInfoLog(item));return item}let program;try{program=gl.createProgram();gl.attachShader(program,shader(gl.VERTEX_SHADER,vertexSource));gl.attachShader(program,shader(gl.FRAGMENT_SHADER,fragmentSource));gl.linkProgram(program);if(!gl.getProgramParameter(program,gl.LINK_STATUS))throw new Error(gl.getProgramInfoLog(program))}catch(error){root.classList.add('no-webgl');status.textContent='WebGL shader unavailable';return}gl.useProgram(program);const buffer=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buffer);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),gl.STATIC_DRAW);const position=gl.getAttribLocation(program,'a_position');gl.enableVertexAttribArray(position);gl.vertexAttribPointer(position,2,gl.FLOAT,false,0,0);const resolutionUniform=gl.getUniformLocation(program,'u_resolution'),timeUniform=gl.getUniformLocation(program,'u_time'),originsUniform=gl.getUniformLocation(program,'u_origins[0]'),agesUniform=gl.getUniformLocation(program,'u_ages[0]'),countUniform=gl.getUniformLocation(program,'u_count');let ripples=[],spawned=0,running=false,visible=true,startTime=performance.now(),dpr=1;
function resize(){const rect=canvas.getBoundingClientRect();dpr=Math.min(2,window.devicePixelRatio||1);const width=Math.max(1,Math.round(rect.width*dpr)),height=Math.max(1,Math.round(rect.height*dpr));if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height;gl.viewport(0,0,width,height)}draw(performance.now())}function draw(now){const active=reduce?ripples:ripples.filter(function(ripple){return(now-ripple.born)/1000<LIFETIME});if(!reduce)ripples=active;const origins=new Float32Array(MAX_RIPPLES*2),ages=new Float32Array(MAX_RIPPLES);active.forEach(function(ripple,index){origins[index*2]=ripple.x;origins[index*2+1]=ripple.y;ages[index]=reduce?.62:(now-ripple.born)/1000});gl.uniform2f(resolutionUniform,canvas.width,canvas.height);gl.uniform1f(timeUniform,(now-startTime)/1000);gl.uniform2fv(originsUniform,origins);gl.uniform1fv(agesUniform,ages);gl.uniform1i(countUniform,active.length);gl.drawArrays(gl.TRIANGLES,0,3);activeReadout.textContent=String(active.length);if(!reduce&&active.length&&visible){requestAnimationFrame(draw)}else running=false}function start(){if(reduce||!visible||running)return;running=true;requestAnimationFrame(draw)}function spawn(x,y){const rect=canvas.getBoundingClientRect(),u=Math.max(0,Math.min(1,(x-rect.left)/rect.width)),v=Math.max(0,Math.min(1,1-(y-rect.top)/rect.height));if(ripples.length>=MAX_RIPPLES)ripples.shift();ripples.push({x:u,y:v,born:performance.now()});spawned++;spawnedReadout.textContent=String(spawned).padStart(2,'0');coordinates.textContent=u.toFixed(3)+' / '+v.toFixed(3);status.textContent='Ripple '+spawned+' spawned at UV '+u.toFixed(2)+', '+v.toFixed(2);if(reduce)draw(performance.now());else start()}canvas.addEventListener('pointerdown',function(event){spawn(event.clientX,event.clientY)});canvas.addEventListener('keydown',function(event){if(event.key==='Enter'||event.key===' '){event.preventDefault();const rect=canvas.getBoundingClientRect();spawn(rect.left+rect.width*.5,rect.top+rect.height*.5)}});clearButton.addEventListener('click',function(){ripples=[];draw(performance.now());status.textContent='Ripple field cleared';canvas.focus()});if('ResizeObserver'in window)new ResizeObserver(resize).observe(canvas);if('IntersectionObserver'in window)new IntersectionObserver(function(entries){visible=entries[0].isIntersecting;if(visible&&ripples.length)start()},{threshold:.05}).observe(root);resize();`,
prompt:`Build a self-contained WebGL 1.0 click-ripple texture with no libraries or external images. Render a fullscreen triangle. The fragment shader must declare uniform vec2 u_origins[5], uniform float u_ages[5], and uniform int u_count. Maintain at most five JS ripple objects with UV origin and born time; remove them after 3.2 seconds.

In the fixed five-iteration shader loop, aspect-correct delta.x, compute radius=age*0.28, a narrow exponential ring envelope, and a signed sine wave around that radius. Accumulate direction*wave displacement and ring glow, then evaluate a procedural mineral texture from the displaced UV. Upload origin and age arrays with uniform2fv/uniform1fv every frame.

Click maps client coordinates into clamped UV with flipped y; Enter/Space spawns at center. Resize DPR-aware canvas and viewport with ResizeObserver. Run RAF only while visible active ripples exist; IntersectionObserver resumes it if needed. Handle shader/WebGL failure with a static fallback. Under prefers-reduced-motion, draw a representative static-age ripple without looping. Include visible canvas focus, UV/active/spawn telemetry, clear control, scoped CSS, and no dependencies.`});
